[
  {
    "title": "ABS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ABS--Absolute-value--predicated--?lang=en",
    "description": "Absolute value (predicated) This instruction calculates the absolute value of the signed integer in each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "ABS<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenvarelement : integer = SInt(operand[e*:esize]);\n        element = Abs(element);\n        result[e*:esize] = element[esize-1:0];\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "ADCLB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADCLB--Add-with-carry-long--bottom--?lang=en",
    "description": "Add with carry long (bottom) This instruction adds the even-numbered elements of the first source vector and the 1-bit carry from the least-significant bit of the odd-numbered elements of the second source vector to the even-numbered elements of the destination and accumulator vector. The 1-bit carry output is placed in the corresponding odd-numbered element of the destination vector.",
    "syntax": [
      "ADCLB<Zda>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letpairs : integer =VLDIV(esize *2);letoperand : bits(VL) = Z{}(n);letcarries : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);forp =0to pairs-1doletelement1 : bits(esize) = result[(2*p +0)*:esize];letelement2 : bits(esize) = operand[(2*p +0)*:esize];letcarry_in : bit = carries[(2*p +1)*:esize][0];let(res, nzcv) =AddWithCarry{esize}(element1, element2, carry_in);letcarry_out : bit = nzcv[1];\n\n    result[(2*p +0)*:esize] = res;\n    result[(2*p +1)*:esize] = ZeroExtend{esize}(carry_out);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ADCLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADCLT--Add-with-carry-long--top--?lang=en",
    "description": "Add with carry long (top) This instruction adds the odd-numbered elements of the first source vector and the 1-bit carry from the least-significant bit of the odd-numbered elements of the second source vector to the even-numbered elements of the destination and accumulator vector. The 1-bit carry output is placed in the corresponding odd-numbered element of the destination vector.",
    "syntax": [
      "ADCLT<Zda>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letpairs : integer =VLDIV(esize *2);letoperand : bits(VL) = Z{}(n);letcarries : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);forp =0to pairs-1doletelement1 : bits(esize) = result[(2*p +0)*:esize];letelement2 : bits(esize) = operand[(2*p +1)*:esize];letcarry_in : bit = carries[(2*p +1)*:esize][0];let(res, nzcv) =AddWithCarry{esize}(element1, element2, carry_in);letcarry_out : bit = nzcv[1];\n\n    result[(2*p +0)*:esize] = res;\n    result[(2*p +1)*:esize] = ZeroExtend{esize}(carry_out);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ADD (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADD--immediate---Add-immediate--unpredicated--?lang=en",
    "description": "Add immediate (unpredicated) This instruction adds an unsigned immediate to each element of the source vector, and destructively places the results in the corresponding elements of the source vector. This instruction is unpredicated. The immediate is an unsigned value in the range 0 to 255, and for element widths of 16 bits or higher it may also be a positive multiple of 256 in the range 256 to 65280. The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\".",
    "syntax": [
      "ADD<Zdn>.<T>,<Zdn>.<T>, #<imm>{,<shift>}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];\n    result[e*:esize] = element1 + imm;\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ADD (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADD--vectors--predicated---Add--predicated--?lang=en",
    "description": "Add (predicated) This instruction adds active elements of the first source vector to the corresponding elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "ADD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = element1 + element2;elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ADD (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADD--vectors--unpredicated---Add--unpredicated--?lang=en",
    "description": "Add (unpredicated) This instruction adds all elements of the first source vector to the corresponding elements of the second source vector, and places the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "ADD<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[e*:esize] = element1 + element2;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ADDHNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADDHNB--Add-narrow-high-part--bottom--?lang=en",
    "description": "Add narrow high part (bottom) This instruction adds each vector element of the first source vector to the corresponding vector element of the second source vector, and places the most significant half of the result in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero. This instruction is unpredicated.",
    "syntax": [
      "ADDHNB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letres : integer = (element1 + element2) >> halfesize;\n    result[(2*e +0)*:halfesize] = res[halfesize-1:0];\n    result[(2*e +1)*:halfesize] = Zeros{halfesize};\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ADDHNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADDHNT--Add-narrow-high-part--top--?lang=en",
    "description": "Add narrow high part (top) This instruction adds each vector element of the first source vector to the corresponding vector element of the second source vector, and places the most significant half of the result in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged. This instruction is unpredicated.",
    "syntax": [
      "ADDHNT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(d);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letres : integer = (element1 + element2) >> halfesize;\n    result[(2*e +1)*:halfesize] = res[halfesize-1:0];\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ADDP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADDP--Add-pairwise-?lang=en",
    "description": "Add pairwise This instruction adds pairs of adjacent elements within each source vector, and destructively places the interleaved results in the first source vector.",
    "syntax": [
      "ADDP<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);varelement1 : integer;varelement2 : integer;fore =0to elements-1doif!ActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = operand1[e*:esize];elseifIsEven(e) then\n            element1= UInt(operand1[(e +0)*:esize]);\n            element2 = UInt(operand1[(e +1)*:esize]);elseelement1 = UInt(operand2[(e -1)*:esize]);\n            element2 = UInt(operand2[(e +0)*:esize]);\n        end;letres : integer = element1 + element2;\n        result[e*:esize] = res[esize-1:0];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ADDPL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADDPL--Add-multiple-of-predicate-register-size-to-scalar-register-?lang=en",
    "description": "Add multiple of predicate register size to scalar register This instruction adds the current predicate register size in bytes multiplied by an immediate in the range -32 to 31 to the 64-bit source general-purpose register or current stack pointer and places the result in the 64-bit destination general-purpose register or current stack pointer.",
    "syntax": [
      "ADDPL<Xd|SP>,<Xn|SP>, #<imm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letoperand1 : bits(64) =ifn ==31thenSP{64}()elseX{64}(n);letresult : bits(64) = operand1 + (imm * (PL DIV8));ifd ==31thenSP{64}() = result;elseX{64}(d) = result;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ADDPT (predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADDPT--predicated---Add-checked-pointer-vectors--predicated--?lang=en",
    "description": "Add checked pointer vectors (predicated) This instruction adds, with pointer check, Active elements of the first source vector to the corresponding elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "ADDPT<Zdn>.D,<Pg>/M,<Zdn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_CPA",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV64;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask,64) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(64) = operand1[e*:64];letelement2 : bits(64) = operand2[e*:64];ifActivePredicateElement{PL}(mask, e,64) thenletres : bits(64) = element1 + element2;\n        result[e*:64] =PointerAddCheck(res, element1);elseresult[e*:64] = operand1[e*:64];\n    end;Z{VL}(dn) = result;\nend;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ADDPT (unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADDPT--unpredicated---Add-checked-pointer-vectors--unpredicated--?lang=en",
    "description": "Add checked pointer vectors (unpredicated) This instruction adds, with pointer check, all elements of the first source vector to the corresponding elements of the second source vector and places the results in the corresponding elements of the destination vector. This instruction is unpredicated. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "ADDPT<Zd>.D,<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_CPA",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV64;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(64) = operand1[e*:64];letelement2 : bits(64) = operand2[e*:64];letres : bits(64) = element1 + element2;\n    result[e*:64] =PointerAddCheck(res, element1);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "ADDQP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADDQP--Add-pairwise-within-quadword-vector-segments--unpredicated--?lang=en",
    "description": "Add pairwise within quadword vector segments (unpredicated) This instruction adds pairs of adjacent elements within each concatenated pair of 128-bit segments of the first and second source vectors, and places the results in the corresponding 128-bit segment of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "ADDQP<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letelempersegment : integer =128DIV esize;letoperand1 :bits(VL)= Z{}(n);letoperand2 :bits(VL)= Z{}(m);varresult :bits(VL);fors =0to segments-1do\n    let seg1 :bits(128)= operand1[s*:128];let seg2 :bits(128)= operand2[s*:128];let concat :bits(256)= seg2::seg1;for e =0to elempersegment-1do\n        let element1 :bits(esize)= concat[(2*e +0)*:esize];let element2 :bits(esize)= concat[(2*e +1)*:esize];result[(s*elempersegment + e)*:esize] = element1 + element2;end;end;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ADDQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADDQV--Add-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Add reduction of quadword vector segments This instruction adds the elements of the same position from each 128-bit source vector segment, and places each result into the corresponding element of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as zero.",
    "syntax": [
      "ADDQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer = VL DIV128;letelempersegment : integer =128DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(128) = Zeros{};varstmp : bits(128) = Zeros{};vardtmp : integer;fore =0to elempersegment-1dodtmp =0;fors =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) then\n            stmp = operand[s*:128];\n            dtmp = dtmp + UInt(stmp[e*:esize]);\n        end;\n    end;\n    result[e*:esize] = dtmp[esize-1:0];\nend;V{128}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ADDSUBP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADDSUBP--Add-subtract-pairwise-?lang=en",
    "description": "Add subtract pairwise This instruction adds pairs of adjacent elements within the first source vector and subtracts pairs of adjacent elements within the second source vector, and places the interleaved results in the destination vector. This instruction is unpredicated.",
    "syntax": [
      "ADDSUBP<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letpairs : integer =VLDIV(esize *2);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to pairs-1doletelt1_a : integer = UInt(operand1[(2*e +0)*:esize]);letelt1_b : integer = UInt(operand1[(2*e +1)*:esize]);letelt2_a : integer = UInt(operand2[(2*e +0)*:esize]);letelt2_b : integer = UInt(operand2[(2*e +1)*:esize]);letres1 : integer = elt1_a + elt1_b;letres2 : integer = elt2_a - elt2_b;\n    result[(2*e +0)*:esize] = res1[esize-1:0];\n    result[(2*e +1)*:esize] = res2[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ADDVL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADDVL--Add-multiple-of-vector-register-size-to-scalar-register-?lang=en",
    "description": "Add multiple of vector register size to scalar register This instruction adds the current vector register size in bytes multiplied by an immediate in the range -32 to 31 to the 64-bit source general-purpose register or current stack pointer, and places the result in the 64-bit destination general-purpose register or current stack pointer.",
    "syntax": [
      "ADDVL<Xd|SP>,<Xn|SP>, #<imm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letoperand1 :bits(64)=ifn ==31thenSP{64}()elseX{64}(n);letresult :bits(64)= operand1 + (imm * (VL DIV8));ifd ==31thenSP{64}() = result;elseX{64}(d) = result;end;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ADR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ADR--Calculate-vector-address-?lang=en",
    "description": "Calculate vector address This instruction optionally sign-extends or zero-extends the least significant 32 bits of each element from a vector of offsets or indices in the second source vector, and scales each index by 2, 4, or 8. The scaled indices are then added to a vector of base addresses from the first source vector, and the resulting addresses are placed in the destination vector. This instruction is unpredicated. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:Packed offsets,Unpacked 32-bit signed offsetsandUnpacked 32-bit unsigned offsets",
    "syntax": [
      "ADR<Zd>.D, [<Zn>.D,<Zm>.D, UXTW{<amount>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letbase: bits(VL) = Z{}(n);letoffs : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletaddr : bits(esize) =base[e*:esize];letoffselt : bits(osize) = offs[e*:esize][osize-1:0];letoffset : integer =ifunsigned thenUInt(offselt) elseSInt(offselt);\n    result[e*:esize] = addr + (offset * mbytes);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "AESD (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/AESD--indexed---Multi-vector-AES-single-round-decryption-?lang=en",
    "description": "Multi-vector AES single round decryption This instruction reads a 16-byte state array from each 128-bit segment of the two or four first source vectors, together with a round key from the indexed 128-bit segment of the corresponding 512-bit portion of the second source vector. Each state array undergoes a single round of the AddRoundKey(), InvShiftRows(), and InvSubBytes() transformations in accordance with the AES standard. Each updated state array is destructively placed in the corresponding segment of the two or four first source vectors. When the vector length is less than 512 bits, the most significant bits of the index are ignored to select the indexed 128-bit segment of the second source vector. This instruction is unpredicated. This instruction is legal when executed in Streaming SVE mode if both FEAT_SSVE_AES and FEAT_SVE_AES2 are implemented. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "AESD {<Zdn1>.B-<Zdn4>.B }, {<Zdn1>.B-<Zdn4>.B },<Zm>.Q[<index>]"
    ],
    "features": [
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_AES) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();ifVL ==128then index =0; end;ifVL ==256then index = index MOD2; end;letsegments : integer = VL DIV128;letoperand2 : bits(VL) = Z{}(m);varresults : array [[4]]ofbits(VL);forr =0to nreg-1doletoperand1 : bits(VL) = Z{}(dn + r);fors =0to segments-1doletkeyindex : integer = (s - (s MOD4)) + index;letres : bits(128) = operand1[s*:128] XOR operand2[keyindex*:128];\n        results[[r]][s*:128] =AESInvSubBytes(AESInvShiftRows(res));\n\n    end;\nend;forr =0to nreg-1doZ{VL}(dn + r) = results[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "AESD (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/AESD--vectors---AES-single-round-decryption-?lang=en",
    "description": "AES single round decryption This instruction reads a 16-byte state array from each 128-bit segment of the first source vector, together with a round key from the corresponding 128-bit segment of the second source vector. Each state array undergoes a single round of the AddRoundKey(), InvShiftRows(), and InvSubBytes() transformations in accordance with the AES standard. Each updated state array is destructively placed in the corresponding segment of the first source vector. This instruction is unpredicated. ID_AA64ZFR0_EL1.AES indicates whether this instruction is implemented. This instruction is legal when executed in Streaming SVE mode if one of the following is true: Both FEAT_SSVE_AES and FEAT_SVE_AES are implemented.FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "AESD<Zdn>.B,<Zdn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_AES) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);\n\nresult = operand1 XOR operand2;fors =0to segments-1doresult[s*:128] =AESInvSubBytes(AESInvShiftRows(result[s*:128]));\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "AESDIMC",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/AESDIMC--Multi-vector-AES-single-round-decryption-and-inverse-mix-columns-?lang=en",
    "description": "Multi-vector AES single round decryption and inverse mix columns This instruction reads a 16-byte state array from each 128-bit segment of the two or four first source vectors, together with a round key from the indexed 128-bit segment of the corresponding 512-bit portion of the second source vector. Each state array undergoes a single round of the AddRoundKey(), InvShiftRows(), InvSubBytes(), and InvMixColumns() transformations in accordance with the AES standard. Each updated state array is destructively placed in the corresponding segment of the two or four first source vectors. When the vector length is less than 512 bits, the most significant bits of the index are ignored to select the indexed 128-bit segment of the second source vector. This instruction is unpredicated. This instruction is legal when executed in Streaming SVE mode if both FEAT_SSVE_AES and FEAT_SVE_AES2 are implemented. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "AESDIMC {<Zdn1>.B-<Zdn4>.B }, {<Zdn1>.B-<Zdn4>.B },<Zm>.Q[<index>]"
    ],
    "features": [
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_AES) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();ifVL ==128then index =0; end;ifVL ==256then index = index MOD2; end;letsegments : integer = VL DIV128;letoperand2 : bits(VL) = Z{}(m);varresults : array [[4]]ofbits(VL);forr =0to nreg-1doletoperand1 : bits(VL) = Z{}(dn + r);fors =0to segments-1doletkeyindex : integer = (s - (s MOD4)) + index;letres : bits(128) = operand1[s*:128] XOR operand2[keyindex*:128];\n        results[[r]][s*:128] =AESInvMixColumns(AESInvSubBytes(AESInvShiftRows(res)));\n\n    end;\nend;forr =0to nreg-1doZ{VL}(dn + r) = results[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "AESE (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/AESE--indexed---Multi-vector-AES-single-round-encryption-?lang=en",
    "description": "Multi-vector AES single round encryption This instruction reads a 16-byte state array from each 128-bit segment of the two or four first source vectors, together with a round key from the indexed 128-bit segment of the corresponding 512-bit portion of the second source vector. Each state array undergoes a single round of the AddRoundKey(), ShiftRows(), and SubBytes() transformations in accordance with the AES standard. Each updated state array is destructively placed in the corresponding segment of the two or four first source vectors. When the vector length is less than 512 bits, the most significant bits of the index are ignored to select the indexed 128-bit segment of the second source vector. This instruction is unpredicated. This instruction is legal when executed in Streaming SVE mode if both FEAT_SSVE_AES and FEAT_SVE_AES2 are implemented. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "AESE {<Zdn1>.B-<Zdn4>.B }, {<Zdn1>.B-<Zdn4>.B },<Zm>.Q[<index>]"
    ],
    "features": [
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_AES) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();ifVL ==128then index =0; end;ifVL ==256then index = index MOD2; end;letsegments : integer = VL DIV128;letoperand2 : bits(VL) = Z{}(m);varresults : array [[4]]ofbits(VL);forr =0to nreg-1doletoperand1 : bits(VL) = Z{}(dn + r);fors =0to segments-1doletkeyindex : integer = (s - (s MOD4)) + index;letres : bits(128) = operand1[s*:128] XOR operand2[keyindex*:128];\n        results[[r]][s*:128] =AESSubBytes(AESShiftRows(res));\n\n    end;\nend;forr =0to nreg-1doZ{VL}(dn + r) = results[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "AESE (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/AESE--vectors---AES-single-round-encryption-?lang=en",
    "description": "AES single round encryption This instruction reads a 16-byte state array from each 128-bit segment of the first source vector together with a round key from the corresponding 128-bit segment of the second source vector. Each state array undergoes a single round of the AddRoundKey(), ShiftRows(), and SubBytes() transformations in accordance with the AES standard. Each updated state array is destructively placed in the corresponding segment of the first source vector. This instruction is unpredicated. ID_AA64ZFR0_EL1.AES indicates whether this instruction is implemented. This instruction is legal when executed in Streaming SVE mode if one of the following is true: Both FEAT_SSVE_AES and FEAT_SVE_AES are implemented.FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "AESE<Zdn>.B,<Zdn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_AES) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);\n\nresult = operand1 XOR operand2;fors =0to segments-1doresult[s*:128] =AESSubBytes(AESShiftRows(result[s*:128]));\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "AESEMC",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/AESEMC--Multi-vector-AES-single-round-encryption-and-mix-columns-?lang=en",
    "description": "Multi-vector AES single round encryption and mix columns This instruction reads a 16-byte state array from each 128-bit segment of the two or four first source vectors, together with a round key from the indexed 128-bit segment of the corresponding 512-bit portion of the second source vector. Each state array undergoes a single round of the AddRoundKey(), ShiftRows(), SubBytes(), and MixColumns() transformations in accordance with the AES standard. Each updated state array is destructively placed in the corresponding segment of the two or four first source vectors. When the vector length is less than 512 bits, the most significant bits of the index are ignored to select the indexed 128-bit segment of the second source vector. This instruction is unpredicated. This instruction is legal when executed in Streaming SVE mode if both FEAT_SSVE_AES and FEAT_SVE_AES2 are implemented. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "AESEMC {<Zdn1>.B-<Zdn4>.B }, {<Zdn1>.B-<Zdn4>.B },<Zm>.Q[<index>]"
    ],
    "features": [
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_AES) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();ifVL ==128then index =0; end;ifVL ==256then index = index MOD2; end;letsegments : integer = VL DIV128;letoperand2 : bits(VL) = Z{}(m);varresults : array [[4]]ofbits(VL);forr =0to nreg-1doletoperand1 : bits(VL) = Z{}(dn + r);fors =0to segments-1doletkeyindex : integer = (s - (s MOD4)) + index;letres : bits(128) = operand1[s*:128] XOR operand2[keyindex*:128];\n        results[[r]][s*:128] =AESMixColumns(AESSubBytes(AESShiftRows(res)));\n\n    end;\nend;forr =0to nreg-1doZ{VL}(dn + r) = results[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "AESIMC",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/AESIMC--AES-inverse-mix-columns-?lang=en",
    "description": "AES inverse mix columns This instruction reads a 16-byte state array from each 128-bit segment of the source register, and performs a single round of the InvMixColumns() transformation on each state array in accordance with the AES standard. Each updated state array is destructively placed in the corresponding segment of the first source vector. This instruction is unpredicated. ID_AA64ZFR0_EL1.AES indicates whether this instruction is implemented. This instruction is legal when executed in Streaming SVE mode if one of the following is true: Both FEAT_SSVE_AES and FEAT_SVE_AES are implemented.FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "AESIMC<Zdn>.B,<Zdn>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_AES) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letoperand : bits(VL) = Z{}(dn);varresult : bits(VL);fors =0to segments-1doresult[s*:128] =AESInvMixColumns(operand[s*:128]);\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "AESMC",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/AESMC--AES-mix-columns-?lang=en",
    "description": "AES mix columns This instruction reads a 16-byte state array from each 128-bit segment of the source register, and performs a single round of the MixColumns() transformation on each state array in accordance with the AES standard. Each updated state array is destructively placed in the corresponding segment of the first source vector. This instruction is unpredicated. ID_AA64ZFR0_EL1.AES indicates whether this instruction is implemented. This instruction is legal when executed in Streaming SVE mode if one of the following is true: Both FEAT_SSVE_AES and FEAT_SVE_AES are implemented.FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "AESMC<Zdn>.B,<Zdn>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_AES) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letoperand : bits(VL) = Z{}(dn);varresult : bits(VL);fors =0to segments-1doresult[s*:128] =AESMixColumns(operand[s*:128]);\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "AND (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/AND--immediate---Bitwise-AND-with-immediate--unpredicated--?lang=en",
    "description": "Bitwise AND with immediate (unpredicated) This instruction performs a bitwise AND between an immediate and each 64-bit element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate is a 64-bit value consisting of a single run of ones or zeros repeating every 2, 4, 8, 16, 32 or 64 bits. This instruction is unpredicated. This instruction is used by the pseudo-instructionBIC (immediate).",
    "syntax": [
      "AND<Zdn>.<T>,<Zdn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV64;letoperand :bits(VL)= Z{}(dn);varresult :bits(VL);fore =0to elements-1do\n    let element1 :bits(64)= operand[e*:64];result[e*:64] = element1ANDimm;end;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "AND (predicates)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/AND--predicates---Bitwise-AND-predicates-?lang=en",
    "description": "Bitwise AND predicates This instruction performs a bitwise AND between the active elements of the first source predicate and the corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. This instruction is used by the aliasMOV (predicate, zeroing).",
    "syntax": [
      "AND<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(element1 AND element2);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "AND (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/AND--vectors--predicated---Bitwise-AND--predicated--?lang=en",
    "description": "Bitwise AND (predicated) This instruction performs a bitwise AND between the active elements of the first source vector and the corresponding elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "AND<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)=ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult :bits(VL);fore =0to elements-1do\n    let element1 :bits(esize)= operand1[e*:esize];let element2 :bits(esize)= operand2[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = element1ANDelement2;elseresult[e*:esize] = operand1[e*:esize];end;end;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "AND (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/AND--vectors--unpredicated---Bitwise-AND--unpredicated--?lang=en",
    "description": "Bitwise AND (unpredicated) This instruction performs a bitwise AND between the elements of the first source vector and the corresponding elements of the second source vector, and places the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "AND<Zd>.D,<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letoperand1 :bits(VL)= Z{}(n);letoperand2 :bits(VL)= Z{}(m);Z{VL}(d) = operand1ANDoperand2;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ANDQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ANDQV--Bitwise-AND-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Bitwise AND reduction of quadword vector segments This instruction performs a bitwise AND between elements in the same position from each 128-bit source vector segment, and places each result into the corresponding element number of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as all ones.",
    "syntax": [
      "ANDQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer = VL DIV128;letelempersegment : integer =128DIV esize;letmask :bits(PL)= P{}(g);letoperand :bits(VL)=ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult :bits(128)= Zeros{};varstmp:bits(128)= Zeros{};vardtmp :bits(esize);fore =0to elempersegment-1do\n    dtmp = Ones{esize};for s =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) thenstmp= operand[s*:128];dtmp = dtmpANDstmp[e*:esize];end;end;result[e*:esize] = dtmp[esize-1:0];end;V{128}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ANDS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ANDS--Bitwise-AND-predicates--setting-the-condition-flags-?lang=en",
    "description": "Bitwise AND predicates, setting the condition flags This instruction performs a bitwise AND between the active elements of the first source predicate and the corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This instruction is used by the aliasMOVS (predicated).",
    "syntax": [
      "ANDS<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(element1 AND element2);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nIf FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "ANDV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ANDV--Bitwise-AND-reduction-to-scalar-?lang=en",
    "description": "Bitwise AND reduction to scalar This instruction performs a bitwise AND between the elements of the source vector, and places the result in the SIMD&FP scalar destination register. Inactive elements in the source vector are treated as all ones.",
    "syntax": [
      "ANDV<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(esize) = Ones{esize};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result = result AND operand[e*:esize];\n\n    end;\nend;V{esize}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ASR (immediate, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ASR--immediate--predicated---Arithmetic-shift-right-by-immediate--predicated--?lang=en",
    "description": "Arithmetic shift right by immediate (predicated) This instruction shifts right by immediate, preserving the sign bit, each active element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "ASR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letPL : integer{} = VL DIV8;letoperand1 :bits(VL)= Z{}(dn);letmask :bits(PL)= P{}(g);varresult :bits(VL);fore =0to elements-1do\n    let element1 :bits(esize)= operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =ASR(element1,shift);elseresult[e*:esize] = operand1[e*:esize];end;end;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ASR (immediate, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ASR--immediate--unpredicated---Arithmetic-shift-right-by-immediate--unpredicated--?lang=en",
    "description": "Arithmetic shift right by immediate (unpredicated) This instruction shifts right by immediate, preserving the sign bit, each element of the source vector, and places the results in the corresponding elements of the destination vector. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "ASR<Zd>.<T>,<Zn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];\n    result[e*:esize] = ASR(element1, shift);\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ASR (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ASR--vectors---Arithmetic-shift-right-by-vector--predicated--?lang=en",
    "description": "Arithmetic shift right by vector (predicated) This instruction shifts right, preserving the sign bit, active elements of the first source vector by the corresponding elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. The shift amount operand is a vector of unsigned elements in which all bits are significant, and not used modulo the element size. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "ASR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];letshift : integer = Min(UInt(element2), esize);\n        result[e*:esize] = ASR(element1, shift);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ASR (wide elements, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ASR--wide-elements--predicated---Arithmetic-shift-right-by-64-bit-wide-elements--predicated--?lang=en",
    "description": "Arithmetic shift right by 64-bit wide elements (predicated) This instruction shifts right, preserving the sign bit, active elements of the first source vector by the corresponding overlapping 64-bit elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. The shift amount is a vector of unsigned 64-bit doubleword elements in which all bits are significant, and not used modulo the destination element size. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "ASR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(64) = operand2[((e * esize) DIVRM64)*:64];letshift : integer = Min(UInt(element2), esize);\n        result[e*:esize] = ASR(element1, shift);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ASR (wide elements, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ASR--wide-elements--unpredicated---Arithmetic-shift-right-by-64-bit-wide-elements--unpredicated--?lang=en",
    "description": "Arithmetic shift right by 64-bit wide elements (unpredicated) This instruction shifts right, preserving the sign bit, all elements of the first source vector by the corresponding overlapping 64-bit elements of the second source vector and places the first in the corresponding elements of the destination vector. The shift amount is a vector of unsigned 64-bit doubleword elements in which all bits are significant, and not used modulo the destination element size. This instruction is unpredicated.",
    "syntax": [
      "ASR<Zd>.<T>,<Zn>.<T>,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(64) = operand2[((e * esize) DIVRM64)*:64];letshift : integer = Min(UInt(element2), esize);\n    result[e*:esize] = ASR(element1, shift);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ASRD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ASRD--Arithmetic-shift-right-for-divide-by-immediate--predicated--?lang=en",
    "description": "Arithmetic shift right for divide by immediate (predicated) This instruction shifts right by immediate, preserving the sign bit, each active element of the source vector, and destructively places the results in the corresponding elements of the source vector. The result rounds toward zero as in a signed division. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "ASRD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenvarelement1 : integer = SInt(operand1[e*:esize]);ifelement1 <0then\n            element1 = element1 + ((1<< shift) -1);\n        end;\n        result[e*:esize] = (element1 >> shift)[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ASRR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ASRR--Reversed-arithmetic-shift-right-by-vector--predicated--?lang=en",
    "description": "Reversed arithmetic shift right by vector (predicated) This instruction reverse shifts right, preserving the sign bit, active elements of the second source vector by the corresponding elements of the first source vector, and destructively places the results in the corresponding elements of the first source vector. The shift amount operand is a vector of unsigned elements in which all bits are significant, and not used modulo the element size. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "ASRR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];letshift : integer = Min(UInt(element1), esize);\n        result[e*:esize] = ASR(element2, shift);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BCAX",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BCAX--Bitwise-clear-and-exclusive-OR-?lang=en",
    "description": "Bitwise clear and exclusive-OR This instruction performs a bitwise AND between elements of the second source vector and the inverted corresponding elements of the third source vector, then an exclusive-OR between the results and the corresponding elements of the first source vector. The final results are destructively placed in the corresponding elements of the destination and first source vector. This instruction is unpredicated.",
    "syntax": [
      "BCAX<Zdn>.D,<Zdn>.D,<Zm>.D,<Zk>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)= Z{}(m);letoperand3 :bits(VL)= Z{}(k);Z{VL}(dn) = operand1 XOR (operand2ANDNOT(operand3));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BDEP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BDEP--Scatter-lower-bits-into-positions-selected-by-bitmask-?lang=en",
    "description": "Scatter lower bits into positions selected by bitmask This instruction scatters the lowest-numbered contiguous bits within each element of the first source vector to the bit positions indicated by non-zero bits in the corresponding mask element of the second source vector, preserving their order, and sets the bits corresponding to a zero mask bit to zero. This instruction is unpredicated. ID_AA64ZFR0_EL1.BitPerm indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled, or FEAT_SSVE_BitPerm is implemented.",
    "syntax": [
      "BDEP<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_BitPerm) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letdata : bits(VL) = Z{}(n);letmask : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements -1doresult[e*:esize] =BitDeposit{esize}(data[e*:esize], mask[e*:esize]);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "BEXT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BEXT--Gather-lower-bits-from-positions-selected-by-bitmask-?lang=en",
    "description": "Gather lower bits from positions selected by bitmask This instruction gathers bits in each element of the first source vector from the bit positions indicated by non-zero bits in the corresponding mask element of the second source vector to the lowest-numbered contiguous bits of the corresponding destination element, preserving their order, and sets the remaining higher-numbered bits to zero. This instruction is unpredicated. ID_AA64ZFR0_EL1.BitPerm indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled, or FEAT_SSVE_BitPerm is implemented.",
    "syntax": [
      "BEXT<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_BitPerm) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letdata : bits(VL) = Z{}(n);letmask : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements -1doresult[e*:esize] =BitExtract{esize}(data[e*:esize], mask[e*:esize]);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "BF1CVT, BF2CVT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BF1CVT--BF2CVT--8-bit-floating-point-convert-to-BFloat16-?lang=en",
    "description": "8-bit floating-point convert to BFloat16 This instruction converts each even-numbered 8-bit floating-point element of the source vector to BFloat16 while downscaling the value, and places the results in the overlapping 16-bit elements of the destination vector. BF1CVT scales the values by 2-UInt(FPMR.LSCALE[5:0]). BF2CVT scales the values by 2-UInt(FPMR.LSCALE2[5:0]). The 8-bit floating-point encoding format for BF1CVT is selected by FPMR.F8S1. The 8-bit floating-point encoding format for BF2CVT is selected by FPMR.F8S2. This instruction is unpredicated. It has encodings from 2 classes:BF1CVTandBF2CVT",
    "syntax": [
      "BF2CVT<Zd>.H,<Zn>.B"
    ],
    "features": [
      "FEAT_FP8",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize) = operand[e*:esize];letres : bits(d_esize) =FP8ConvertBF(element[s_esize-1:0], issrc2, FPCR(), FPMR());\n    result[e*:esize] = ZeroExtend{esize}(res);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "BF1CVTLT, BF2CVTLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BF1CVTLT--BF2CVTLT--8-bit-floating-point-convert-to-BFloat16--top--?lang=en",
    "description": "8-bit floating-point convert to BFloat16 (top) This instruction converts each odd-numbered 8-bit floating-point element of the source vector to BFloat16 while downscaling the value, and places the results in the overlapping 16-bit elements of the destination vector. BF1CVTLT scales the values by 2-UInt(FPMR.LSCALE[5:0]). BF2CVTLT scales the values by 2-UInt(FPMR.LSCALE2[5:0]). The 8-bit floating-point encoding format for BF1CVTLT is selected by FPMR.F8S1. The 8-bit floating-point encoding format for BF2CVTLT is selected by FPMR.F8S2. This instruction is unpredicated. It has encodings from 2 classes:BF1CVTLTandBF2CVTLT",
    "syntax": [
      "BF2CVTLT<Zd>.H,<Zn>.B"
    ],
    "features": [
      "FEAT_FP8",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize DIV2) = operand[(2*e +1)*:(esize DIV2)];\n    result[e*:esize] =FP8ConvertBF(element, issrc2, FPCR(), FPMR());\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "BFADD (predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFADD--predicated---BFloat16-add--predicated--?lang=en",
    "description": "BFloat16 add (predicated) This instruction adds active BFloat16 elements of the first source vector to the corresponding elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFADD<Zdn>.H,<Pg>/M,<Zdn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV16;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask,16) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];ifActivePredicateElement{PL}(mask, e,16) thenletelement2 : bits(16) = operand2[e*:16];\n        result[e*:16] =BFAdd{16}(element1, element2, FPCR());elseresult[e*:16] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFADD (unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFADD--unpredicated---BFloat16-add--unpredicated--?lang=en",
    "description": "BFloat16 add (unpredicated) This instruction adds all BFloat16 elements of the first source vector to the corresponding elements of the second source vector and places the results in the corresponding elements of the destination vector. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. This instruction is unpredicated. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFADD<Zd>.H,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];letelement2 : bits(16) = operand2[e*:16];\n    result[e*:16] =BFAdd{16}(element1, element2, FPCR());\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "BFCLAMP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFCLAMP--BFloat16-clamp-to-minimum-maximum-number-?lang=en",
    "description": "BFloat16 clamp to minimum/maximum number This instruction clamps each BFloat16 element in the destination vector to between the BFloat16 minimum value in the corresponding element of the first source vector and the BFloat16 maximum value in the corresponding element of the second source vector and destructively places the clamped results in the corresponding elements of the destination vector. Regardless of the value of FPCR.AH, the behavior is as follows for each minimum number and maximum number operation: Negative zero compares less than positive zero.If one value is numeric and the other is a quiet NaN, the result is the numeric value.When FPCR.DN is 0, if either value is a signaling NaN or if both values are NaNs, the result is a quiet NaN.When FPCR.DN is 1, if either value is a signaling NaN or if both values are NaNs, the result is Default NaN. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. This instruction is unpredicated. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFCLAMP<Zd>.H,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;varresult : bits(VL);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(d);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];letelement2 : bits(16) = operand2[e*:16];letelement3 : bits(16) = operand3[e*:16];letmaxelement : bits(16) = BFMaxNum{}(element1, element3, FPCR());\n    result[e*:16] =BFMinNum{16}(maxelement, element2, FPCR());\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFCVT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFCVT--Single-precision-convert-to-BFloat16--predicated--?lang=en",
    "description": "Single-precision convert to BFloat16 (predicated) This instruction converts to BFloat16 from single-precision in each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. Since the result type is smaller than the input type, the results are zero-extended to fill each destination element. ID_AA64ZFR0_EL1.BF16 indicates whether this instruction is implemented. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "BFCVT<Zd>.H,<Pg>/Z,<Zn>.S"
    ],
    "features": [
      "FEAT_BF16",
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV32;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask,32) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e,32) thenletelement : bits(32) = operand[e*:32];\n        result[(2*e)*:16] =FPConvertBF(element, FPCR());\n        result[(2*e+1)*:16] = Zeros{16};\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "BFCVTN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFCVTN--BFloat16-convert-to-interleaved-8-bit-floating-point-?lang=en",
    "description": "BFloat16 convert to interleaved 8-bit floating-point This instruction converts each BFloat16 element of the pair of source vectors to 8-bit floating-point while scaling the value by 2SInt(FPMR.NSCALE), and places the two-way interleaved results in the corresponding 8-bit elements of the destination vector. The 8-bit floating-point encoding format is selected by FPMR.F8D. This instruction is unpredicated.",
    "syntax": [
      "BFCVTN<Zd>.B, {<Zn1>.H-<Zn2>.H }"
    ],
    "features": [
      "FEAT_FP8",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;varresult : bits(VL);letoperand1 : bits(VL) = Z{}(n+0);letoperand2 : bits(VL) = Z{}(n+1);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];letelement2 : bits(16) = operand2[e*:16];\n    result[(2*e +0)*:8] =BFConvertFP8(element1, FPCR(), FPMR());\n    result[(2*e +1)*:8] =BFConvertFP8(element2, FPCR(), FPMR());\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "BFCVTNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFCVTNT--Single-precision-convert-to-BFloat16--top--predicated--?lang=en",
    "description": "Single-precision convert to BFloat16 (top, predicated) This instruction converts each active floating-point element of the source vector from single-precision to BFloat16 floating-point, and places the results in the odd-numbered 16-bit elements of the destination vector, leaving the even-numbered elements unchanged. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. ID_AA64ZFR0_EL1.BF16 indicates whether this instruction is implemented. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "BFCVTNT<Zd>.H,<Pg>/Z,<Zn>.S"
    ],
    "features": [
      "FEAT_BF16",
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV32;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask,32) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) = Z{}(d);fore =0to elements-1doifActivePredicateElement{PL}(mask, e,32) thenletelement : bits(32) = operand[e*:32];\n        result[(2*e+1)*:16] =FPConvertBF(element, FPCR());\n    elsif !merging then\n        result[(2*e+1)*:16] = Zeros{16};\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "BFDOT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFDOT--indexed---BFloat16-dot-product-by-indexed-element-to-single-precision-?lang=en",
    "description": "BFloat16 dot product by indexed element to single-precision This instruction delimits the source vectors into pairs of BFloat16 elements. The BFloat16 pairs within the second source vector are specified using an immediate index that selects the same BFloat16 pair position within each 128-bit vector segment. The index range is from 0 to 3. If FEAT_EBF16 is not implemented or FPCR.EBF is 0, this instruction: Performs an unfused sum-of-products of each pair of adjacent BFloat16 elements in the first source vector with the specified pair of elements in the second vector. The intermediate single-precision products are rounded before they are summed, and the intermediate sum is rounded before accumulation into the single-precision destination element that overlaps with the corresponding pair of BFloat16 elements in the first source vector.Uses the non-IEEE 754 Round-to-Odd rounding mode, which forces bit 0 of an inexact result to 1, and rounds an overflow to an appropriately signed Infinity.Flushes denormalized inputs and results to zero, as if FPCR.{FZ, FIZ} is {1, 1}.Honors FPCR.AH when generating a default NaN result, otherwise disables alternative floating point behaviors, as if FPCR.AH is 0. If FEAT_EBF16 is implemented and FPCR.EBF is 1, this instruction: Performs a fused sum-of-products of each pair of adjacent BFloat16 elements in the first source vector with the specified pair of elements in the second vector. The intermediate single-precision products are not rounded before they are summed, but the intermediate sum is rounded before accumulation into the single-precision destination element that overlaps with the corresponding pair of BFloat16 elements in the first source vector.Follows all other floating-point behaviors that apply to single-precision arithmetic, as governed by FPCR.RMode, FPCR.FZ, FPCR.AH, and FPCR.FIZ. Irrespective of FEAT_EBF16 and FPCR.EBF, this instruction: Does not modify the cumulative FPSR exception bits (IDC, IXC, UFC, OFC, DZC, and IOC).Disables trapped floating-point exceptions, as if the FPCR trap enable bits (IDE, IXE, UFE, OFE, DZE, and IOE) are all zero.Generates only the default NaN, as if FPCR.DN is 1. This instruction is unpredicated. ID_AA64ZFR0_EL1.BF16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFDOT<Zda>.S,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_BF16",
      "FEAT_EBF16",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;leteltspersegment : integer =128DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelt1_a : bits(16) = operand1[(2* e +0)*:16];letelt1_b : bits(16) = operand1[(2* e +1)*:16];letelt2_a : bits(16) = operand2[(2* s +0)*:16];letelt2_b : bits(16) = operand2[(2* s +1)*:16];varsum : bits(32) = operand3[e*:32];\n\n    sum =BFDotAdd(sum, elt1_a, elt1_b, elt2_a, elt2_b, FPCR());\n    result[e*:32] = sum;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFDOT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFDOT--vectors---BFloat16-dot-product-to-single-precision-?lang=en",
    "description": "BFloat16 dot product to single-precision This instruction delimits the source vectors into pairs of BFloat16 elements. If FEAT_EBF16 is not implemented or FPCR.EBF is 0, this instruction: Performs an unfused sum-of-products of each pair of adjacent BFloat16 elements in the source vectors. The intermediate single-precision products are rounded before they are summed, and the intermediate sum is rounded before accumulation into the single-precision destination element that overlaps with the corresponding pair of BFloat16 elements in the source vectors.Uses the non-IEEE 754 Round-to-Odd rounding mode, which forces bit 0 of an inexact result to 1, and rounds an overflow to an appropriately signed Infinity.Flushes denormalized inputs and results to zero, as if FPCR.{FZ, FIZ} is {1, 1}.Honors FPCR.AH when generating a default NaN result, otherwise disables alternative floating point behaviors, as if FPCR.AH is 0. If FEAT_EBF16 is implemented and FPCR.EBF is 1, this instruction: Performs a fused sum-of-products of each pair of adjacent BFloat16 elements in the source vectors. The intermediate single-precision products are not rounded before they are summed, but the intermediate sum is rounded before accumulation into the single-precision destination element that overlaps with the corresponding pair of BFloat16 elements in the source vectors.Follows all other floating-point behaviors that apply to single-precision arithmetic, as governed by FPCR.RMode, FPCR.FZ, FPCR.AH, and FPCR.FIZ. Irrespective of FEAT_EBF16 and FPCR.EBF, this instruction: Does not modify the cumulative FPSR exception bits (IDC, IXC, UFC, OFC, DZC, and IOC).Disables trapped floating-point exceptions, as if the FPCR trap enable bits (IDE, IXE, UFE, OFE, DZE, and IOE) are all zero.Generates only the default NaN, as if FPCR.DN is 1. This instruction is unpredicated. ID_AA64ZFR0_EL1.BF16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFDOT<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_BF16",
      "FEAT_EBF16",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelt1_a : bits(16) = operand1[(2* e +0)*:16];letelt1_b : bits(16) = operand1[(2* e +1)*:16];letelt2_a : bits(16) = operand2[(2* e +0)*:16];letelt2_b : bits(16) = operand2[(2* e +1)*:16];varsum : bits(32) = operand3[e*:32];\n\n    sum =BFDotAdd(sum, elt1_a, elt1_b, elt2_a, elt2_b, FPCR());\n    result[e*:32] = sum;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMAX",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMAX--BFloat16-maximum--predicated--?lang=en",
    "description": "BFloat16 maximum (predicated) This instruction determines the maximum of active BFloat16 elements of the first source vector and the corresponding BFloat16 elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. When FPCR.AH is 0, the behavior is as follows: Negative zero compares less than positive zero.When FPCR.DN is 0, if either element is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if either element is a NaN, the result is Default NaN. When FPCR.AH is 1, the behavior is as follows: If both elements are zeros, regardless of the sign of either zero, the result is the second element.If either element is a NaN, regardless of the value of FPCR.DN, the result is the second element. Inactive elements in the destination vector register remain unmodified. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMAX<Zdn>.H,<Pg>/M,<Zdn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV16;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask,16) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];ifActivePredicateElement{PL}(mask, e,16) thenletelement2 : bits(16) = operand2[e*:16];\n        result[e*:16] =BFMax{16}(element1, element2, FPCR());elseresult[e*:16] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMAXNM",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMAXNM--BFloat16-maximum-number--predicated--?lang=en",
    "description": "BFloat16 maximum number (predicated) This instruction determines the maximum number value of active BFloat16 elements of the first source vector and the corresponding BFloat16 elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Regardless of the value of FPCR.AH, the behavior is as follows: Negative zero compares less than positive zero.If one element is numeric and the other is a quiet NaN, the result is the numeric value.When FPCR.DN is 0, if either element is a signaling NaN or if both elements are NaNs, the result is a quiet NaN.When FPCR.DN is 1, if either element is a signaling NaN or if both elements are NaNs, the result is Default NaN. Inactive elements in the destination vector register remain unmodified. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMAXNM<Zdn>.H,<Pg>/M,<Zdn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV16;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask,16) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];ifActivePredicateElement{PL}(mask, e,16) thenletelement2 : bits(16) = operand2[e*:16];\n        result[e*:16] =BFMaxNum{16}(element1, element2, FPCR());elseresult[e*:16] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMIN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMIN--BFloat16-minimum--predicated--?lang=en",
    "description": "BFloat16 minimum (predicated) This instruction determines the minimum of active BFloat16 elements of the first source vector and the corresponding BFloat16 elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. When FPCR.AH is 0, the behavior is as follows: Negative zero compares less than positive zero.When FPCR.DN is 0, if either element is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if either element is a NaN, the result is Default NaN. When FPCR.AH is 1, the behavior is as follows: If both elements are zeros, regardless of the sign of either zero, the result is the second element.If either element is a NaN, regardless of the value of FPCR.DN, the result is the second element. Inactive elements in the destination vector register remain unmodified. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMIN<Zdn>.H,<Pg>/M,<Zdn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV16;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask,16) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];ifActivePredicateElement{PL}(mask, e,16) thenletelement2 : bits(16) = operand2[e*:16];\n        result[e*:16] =BFMin{16}(element1, element2, FPCR());elseresult[e*:16] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMINNM",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMINNM--BFloat16-minimum-number--predicated--?lang=en",
    "description": "BFloat16 minimum number (predicated) This instruction determines the minimum number value of active BFloat16 elements of the first source vector and the corresponding BFloat16 elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Regardless of the value of FPCR.AH, the behavior is as follows: Negative zero compares less than positive zero.If one element is numeric and the other is a quiet NaN, the result is the numeric value.When FPCR.DN is 0, if either element is a signaling NaN or if both elements are NaNs, the result is a quiet NaN.When FPCR.DN is 1, if either element is a signaling NaN or if both elements are NaNs, the result is Default NaN. Inactive elements in the destination vector register remain unmodified. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMINNM<Zdn>.H,<Pg>/M,<Zdn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV16;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask,16) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];ifActivePredicateElement{PL}(mask, e,16) thenletelement2 : bits(16) = operand2[e*:16];\n        result[e*:16] =BFMinNum{16}(element1, element2, FPCR());elseresult[e*:16] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMLA (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMLA--indexed---BFloat16-fused-multiply-add-by-indexed-element-?lang=en",
    "description": "BFloat16 fused multiply-add by indexed element This instruction multiplies all BFloat16 elements within each 128-bit segment of the first source vector by the specified element in the corresponding second source vector segment. The products are then destructively added without intermediate rounding to the corresponding elements of the addend and destination vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to 7. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. This instruction is unpredicated. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMLA<Zda>.H,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;leteltspersegment : integer =128DIV16;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelem2 : bits(16) = op2[s*:16];letelem1 : bits(16) =ifop1_neg thenBFNeg(op1[e*:16]) else op1[e*:16];letelem3 : bits(16) =ifop3_neg thenBFNeg(result[e*:16]) else result[e*:16];\n    result[e*:16] =BFMulAdd(elem3, elem1, elem2, FPCR());\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMLA (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMLA--vectors---BFloat16-fused-multiply-add-?lang=en",
    "description": "BFloat16 fused multiply-add This instruction multiplies the active BFloat16 elements of the first source vector by the corresponding BFloat16 elements of the second source vector. The results are then added to elements of the third source (addend) vector without intermediate rounding and destructively placed in the destination and third source (addend) vector. Inactive elements in the destination vector register remain unmodified. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMLA<Zda>.H,<Pg>/M,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV16;letmask : bits(PL) = P{}(g);letop1 : bits(VL) =ifAnyActiveElement{PL}(mask,16) thenZ{VL}(n)elseZeros{VL};letop2 : bits(VL) =ifAnyActiveElement{PL}(mask,16) thenZ{VL}(m)elseZeros{VL};letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e,16) thenletelem1 : bits(16) =ifop1_neg thenBFNeg(op1[e*:16]) else op1[e*:16];letelem2 : bits(16) = op2[e*:16];letelem3 : bits(16) =ifop3_neg thenBFNeg(op3[e*:16]) else op3[e*:16];\n\n        result[e*:16] =BFMulAdd(elem3, elem1, elem2, FPCR());elseresult[e*:16] = op3[e*:16];\n\n    end;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMLALB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMLALB--indexed---BFloat16-multiply-add-by-indexed-element-to-single-precision--bottom--?lang=en",
    "description": "BFloat16 multiply-add by indexed element to single-precision (bottom) This instruction widens the even-numbered BFloat16 elements in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to single-precision format and then destructively multiplies and adds these values without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding BFloat16 elements in the first source vector. This instruction is unpredicated. ID_AA64ZFR0_EL1.BF16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMLALB<Zda>.S,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_BF16",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;leteltspersegment : integer =128DIV32;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =2* segmentbase + index;letelem1 : bits(16) = (ifop1_neg thenBFNeg(op1[(2*e +0)*:16])\n                               else op1[(2*e +0)*:16]);letelem2 : bits(16) = op2[s*:16];letelem3 : bits(32) = op3[e*:32];\n    result[e*:32] =BFMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMLALB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMLALB--vectors---BFloat16-multiply-add-to-single-precision--bottom--?lang=en",
    "description": "BFloat16 multiply-add to single-precision (bottom) This instruction widens the even-numbered BFloat16 elements in the first source vector and the corresponding elements in the second source vector to single-precision format and then destructively multiplies and adds these values without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding BFloat16 elements in the source vectors. This instruction is unpredicated. ID_AA64ZFR0_EL1.BF16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMLALB<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_BF16",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelem1 : bits(16) = (ifop1_neg thenBFNeg(op1[(2*e +0)*:16])\n                               else op1[(2*e +0)*:16]);letelem2 : bits(16) = op2[(2*e +0)*:16];letelem3 : bits(32) = op3[e*:32];\n    result[e*:32] =BFMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMLALT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMLALT--indexed---BFloat16-multiply-add-by-indexed-element-to-single-precision--top--?lang=en",
    "description": "BFloat16 multiply-add by indexed element to single-precision (top) This instruction widens the odd-numbered BFloat16 elements in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to single-precision format and then destructively multiplies and adds these values without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding BFloat16 elements in the first source vector. This instruction is unpredicated. ID_AA64ZFR0_EL1.BF16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMLALT<Zda>.S,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_BF16",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;leteltspersegment : integer =128DIV32;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =2* segmentbase + index;letelem1 : bits(16) = (ifop1_neg thenBFNeg(op1[(2*e +1)*:16])\n                               else op1[(2*e +1)*:16]);letelem2 : bits(16) = op2[s*:16];letelem3 : bits(32) = op3[e*:32];\n    result[e*:32] =BFMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMLALT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMLALT--vectors---BFloat16-multiply-add-to-single-precision--top--?lang=en",
    "description": "BFloat16 multiply-add to single-precision (top) This instruction widens the odd-numbered BFloat16 elements in the first source vector and the corresponding elements in the second source vector to single-precision format and then destructively multiplies and adds these values without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding BFloat16 elements in the source vectors. This instruction is unpredicated. ID_AA64ZFR0_EL1.BF16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMLALT<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_BF16",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelem1 : bits(16) = (ifop1_neg thenBFNeg(op1[(2*e +1)*:16])\n                               else op1[(2*e +1)*:16]);letelem2 : bits(16) = op2[(2*e +1)*:16];letelem3 : bits(32) = op3[e*:32];\n    result[e*:32] =BFMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMLS (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMLS--indexed---BFloat16-fused-multiply-subtract-by-indexed-element-?lang=en",
    "description": "BFloat16 fused multiply-subtract by indexed element This instruction multiplies all BFloat16 elements within each 128-bit segment of the first source vector by the specified element in the corresponding second source vector segment. The products are then destructively subtracted without intermediate rounding from the corresponding elements of the addend and destination vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to 7. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. This instruction is unpredicated. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMLS<Zda>.H,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;leteltspersegment : integer =128DIV16;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelem2 : bits(16) = op2[s*:16];letelem1 : bits(16) =ifop1_neg thenBFNeg(op1[e*:16]) else op1[e*:16];letelem3 : bits(16) =ifop3_neg thenBFNeg(result[e*:16]) else result[e*:16];\n    result[e*:16] =BFMulAdd(elem3, elem1, elem2, FPCR());\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMLS (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMLS--vectors---BFloat16-fused-multiply-subtract-?lang=en",
    "description": "BFloat16 fused multiply-subtract This instruction multiplies the active BFloat16 elements of the first source vector by the corresponding BFloat16 elements of the second source vector. The results are then subtracted from elements of the third source (addend) vector without intermediate rounding and destructively placed in the destination and third source (addend) vector. Inactive elements in the destination vector register remain unmodified. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMLS<Zda>.H,<Pg>/M,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV16;letmask : bits(PL) = P{}(g);letop1 : bits(VL) =ifAnyActiveElement{PL}(mask,16) thenZ{VL}(n)elseZeros{VL};letop2 : bits(VL) =ifAnyActiveElement{PL}(mask,16) thenZ{VL}(m)elseZeros{VL};letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e,16) thenletelem1 : bits(16) =ifop1_neg thenBFNeg(op1[e*:16]) else op1[e*:16];letelem2 : bits(16) = op2[e*:16];letelem3 : bits(16) =ifop3_neg thenBFNeg(op3[e*:16]) else op3[e*:16];\n\n        result[e*:16] =BFMulAdd(elem3, elem1, elem2, FPCR());elseresult[e*:16] = op3[e*:16];\n\n    end;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMLSLB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMLSLB--indexed---BFloat16-multiply-subtract-by-indexed-element-from-single-precision--bottom--?lang=en",
    "description": "BFloat16 multiply-subtract by indexed element from single-precision (bottom) This instruction widens the even-numbered BFloat16 elements in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to single-precision format and then destructively multiplies and subtracts these values without intermediate rounding from the single-precision elements of the destination vector that overlap with the corresponding BFloat16 elements in the first source vector. This instruction is unpredicated.",
    "syntax": [
      "BFMLSLB<Zda>.S,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;leteltspersegment : integer =128DIV32;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =2* segmentbase + index;letelem1 : bits(16) = (ifop1_neg thenBFNeg(op1[(2*e +0)*:16])\n                               else op1[(2*e +0)*:16]);letelem2 : bits(16) = op2[s*:16];letelem3 : bits(32) = op3[e*:32];\n    result[e*:32] =BFMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMLSLB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMLSLB--vectors---BFloat16-multiply-subtract-from-single-precision--bottom--?lang=en",
    "description": "BFloat16 multiply-subtract from single-precision (bottom) This instruction widens the even-numbered BFloat16 elements in the first source vector and the corresponding elements in the second source vector to single-precision format and then destructively multiplies and subtracts these values without intermediate rounding from the single-precision elements of the destination vector that overlap with the corresponding BFloat16 elements in the source vectors. This instruction is unpredicated.",
    "syntax": [
      "BFMLSLB<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelem1 : bits(16) = (ifop1_neg thenBFNeg(op1[(2*e +0)*:16])\n                               else op1[(2*e +0)*:16]);letelem2 : bits(16) = op2[(2*e +0)*:16];letelem3 : bits(32) = op3[e*:32];\n    result[e*:32] =BFMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMLSLT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMLSLT--indexed---BFloat16-multiply-subtract-by-indexed-element-from-single-precision--top--?lang=en",
    "description": "BFloat16 multiply-subtract by indexed element from single-precision (top) This instruction widens the odd-numbered BFloat16 elements in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to single-precision format and then destructively multiplies and subtracts these values without intermediate rounding from the single-precision elements of the destination vector that overlap with the corresponding BFloat16 elements in the first source vector. This instruction is unpredicated.",
    "syntax": [
      "BFMLSLT<Zda>.S,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;leteltspersegment : integer =128DIV32;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =2* segmentbase + index;letelem1 : bits(16) = (ifop1_neg thenBFNeg(op1[(2*e +1)*:16])\n                               else op1[(2*e +1)*:16]);letelem2 : bits(16) = op2[s*:16];letelem3 : bits(32) = op3[e*:32];\n    result[e*:32] =BFMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMLSLT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMLSLT--vectors---BFloat16-multiply-subtract-from-single-precision--top--?lang=en",
    "description": "BFloat16 multiply-subtract from single-precision (top) This instruction widens the odd-numbered BFloat16 elements in the first source vector and the corresponding elements in the second source vector to single-precision format and then destructively multiplies and subtracts these values without intermediate rounding from the single-precision elements of the destination vector that overlap with the corresponding BFloat16 elements in the source vectors. This instruction is unpredicated.",
    "syntax": [
      "BFMLSLT<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelem1 : bits(16) = (ifop1_neg thenBFNeg(op1[(2*e +1)*:16])\n                               else op1[(2*e +1)*:16]);letelem2 : bits(16) = op2[(2*e +1)*:16];letelem3 : bits(32) = op3[e*:32];\n    result[e*:32] =BFMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMMLA (non-widening)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMMLA--non-widening---BFloat16-matrix-multiply-accumulate-?lang=en",
    "description": "BFloat16 matrix multiply-accumulate This instruction multiplies the 2x2 matrix of BFloat16 values in each 64-bit segment of the first source vector by the 2x2 matrix of BFloat16 values in the corresponding 64-bit segment of the second source vector. The intermediate products are rounded before they are summed, and the intermediate sum is rounded before accumulation into the 2x2 BFloat16 matrix held in the corresponding 64-bit segment of the addend and destination vector. This is equivalent to performing a 2-way dot product per destination element. This instruction is unpredicated. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "BFMMLA<Zda>.H,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer =  VL DIV64;letoperand1 :bits(VL)= Z{}(n);letoperand2 :bits(VL)= Z{}(m);letoperand3 :bits(VL)= Z{}(da);varresult :bits(VL);varop1, op2 :bits(64);varres,addend:bits(64);fors =0to segments-1do\n    op1    = operand1[s*:(64)];op2    = operand2[s*:(64)];addend= operand3[s*:(64)];res    =BFMatMulAdd(addend,op1, op2,  FPCR());result[s*:(64)] = res;end;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMMLA (widening)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMMLA--widening---BFloat16-matrix-multiply-accumulate-to-single-precision-?lang=en",
    "description": "BFloat16 matrix multiply-accumulate to single-precision This instruction multiplies the 2×4 matrix of BFloat16 values held in each 128-bit segment of the first source vector by the 4×2 matrix of BFloat16 values in the corresponding segment of the second source vector. If FEAT_EBF16 is not implemented or FPCR.EBF is 0, this instruction: Calculates the intermediate single-precision results as a sum of two unfused sum-of-products using two pairs of adjacent BFloat16 values from each of the source vectors, and rounds the results before accumulation into the 2×2 matrix of single-precision results in the corresponding segment of the destination vector. This is equivalent to accumulating a 4-way dot product per destination element.Uses the non-IEEE 754 Round-to-Odd rounding mode, which forces bit 0 of an inexact result to 1, and rounds an overflow to an appropriately signed Infinity.Flushes denormalized inputs and results to zero, as if FPCR.{FZ, FIZ} is {1, 1}.Honors FPCR.AH when generating a default NaN result, otherwise disables alternative floating point behaviors, as if FPCR.AH is 0. If FEAT_EBF16 is implemented and FPCR.EBF is 1, this instruction: Calculates the intermediate single-precision results as a sum of two fused sum-of-products using two pairs of adjacent BFloat16 values from each of the source vectors, and rounds the results before accumulation into the 2×2 matrix of single-precision results in the destination vector. This is equivalent to accumulating a 4-way dot product per destination element.Follows all other floating-point behaviors that apply to single-precision arithmetic, as governed by FPCR.RMode, FPCR.FZ, FPCR.AH, and FPCR.FIZ. Irrespective of FEAT_EBF16 and FPCR.EBF, this instruction: Does not modify the cumulative FPSR exception bits (IDC, IXC, UFC, OFC, DZC, and IOC).Disables trapped floating-point exceptions, as if the FPCR trap enable bits (IDE, IXE, UFE, OFE, DZE, and IOE) are all zero.Generates only the default NaN, as if FPCR.DN is 1. This instruction is unpredicated and vector length agnostic. ID_AA64ZFR0_EL1.BF16 indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "BFMMLA<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_BF16",
      "FEAT_EBF16",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer =  VL DIV128;letoperand1 :bits(VL)= Z{}(n);letoperand2 :bits(VL)= Z{}(m);letoperand3 :bits(VL)= Z{}(da);varresult :bits(VL);varop1, op2 :bits(128);varres,addend:bits(128);fors =0to segments-1do\n    op1    = operand1[s*:(128)];op2    = operand2[s*:(128)];addend= operand3[s*:(128)];res    =BFMatMulAddH(addend,op1, op2,  FPCR());result[s*:(128)] = res;end;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMUL (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMUL--indexed---BFloat16-multiply-by-indexed-element-?lang=en",
    "description": "BFloat16 multiply by indexed element This instruction multiplies all BFloat16 elements within each 128-bit segment of the first source vector by the specified element in the corresponding second source vector segment and places the results in the corresponding elements of the destination vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to 7. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. This instruction is unpredicated. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMUL<Zd>.H,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;leteltspersegment : integer =128DIV16;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelem2 : bits(16) = op2[s*:16];letelem1 : bits(16) = op1[e*:16];\n    result[e*:16] =BFMul(elem1, elem2, FPCR());\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "BFMUL (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMUL--vectors--predicated---BFloat16-multiply--predicated--?lang=en",
    "description": "BFloat16 multiply (predicated) This instruction multiplies active BFloat16 elements of the first source vector by the corresponding BFloat16 elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMUL<Zdn>.H,<Pg>/M,<Zdn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV16;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask,16) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];ifActivePredicateElement{PL}(mask, e,16) thenletelement2 : bits(16) = operand2[e*:16];\n        result[e*:16] =BFMul(element1, element2, FPCR());elseresult[e*:16] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFMUL (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFMUL--vectors--unpredicated---BFloat16-multiply--unpredicated--?lang=en",
    "description": "BFloat16 multiply (unpredicated) This instruction multiplies all BFloat16 elements of the first source vector by the corresponding BFloat16 elements of the second source vector, and places the results in the corresponding elements of the destination vector. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. This instruction is unpredicated. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFMUL<Zd>.H,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];letelement2 : bits(16) = operand2[e*:16];\n    result[e*:16] =BFMul(element1, element2, FPCR());\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "BFSCALE",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFSCALE--BFloat16-adjust-exponent--predicated--?lang=en",
    "description": "BFloat16 adjust exponent (predicated) This instruction multiplies the active BFloat16 elements of the first source vector by 2.0 to the power of the signed integer values in the corresponding elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified. This instruction follows SVE2 non-widening BFloat16 numerical behaviors.",
    "syntax": [
      "BFSCALE<Zdn>.H,<Pg>/M,<Zdn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV16;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask,16) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:(16)];ifActivePredicateElement{PL}(mask, e,16) thenletelement2 : integer = SInt(operand2[e*:(16)]);\n        result[e*:(16)] =BFScale(element1, element2, FPCR());elseresult[e*:(16)] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFSUB (predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFSUB--predicated---BFloat16-subtract--predicated--?lang=en",
    "description": "BFloat16 subtract (predicated) This instruction subtracts active BFloat16 elements of the second source vector from corresponding BFloat16 elements of the first source vector and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFSUB<Zdn>.H,<Pg>/M,<Zdn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV16;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask,16) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];ifActivePredicateElement{PL}(mask, e,16) thenletelement2 : bits(16) = operand2[e*:16];\n        result[e*:16] =BFSub(element1, element2, FPCR());elseresult[e*:16] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BFSUB (unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BFSUB--unpredicated---BFloat16-subtract--unpredicated--?lang=en",
    "description": "BFloat16 subtract (unpredicated) This instruction subtracts all BFloat16 elements of the second source vector from corresponding BFloat16 elements of the first source vector and places the results in the corresponding elements of the destination vector. This instruction follows SVE2 non-widening BFloat16 numerical behaviors. This instruction is unpredicated. ID_AA64ZFR0_EL1.B16B16 indicates whether this instruction is implemented.",
    "syntax": [
      "BFSUB<Zd>.H,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];letelement2 : bits(16) = operand2[e*:16];\n    result[e*:16] =BFSub(element1, element2, FPCR());\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "BGRP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BGRP--Group-bits-to-right-or-left-as-selected-by-bitmask-?lang=en",
    "description": "Group bits to right or left as selected by bitmask This instruction separates bits in each element of the first source vector by gathering from the bit positions indicated by non-zero bits in the corresponding mask element of the second source vector to the lowest-numbered contiguous bits of the corresponding destination element, and from positions indicated by zero bits to the highest-numbered bits of the destination element, preserving the bit order within each group. This instruction is unpredicated. ID_AA64ZFR0_EL1.BitPerm indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled, or FEAT_SSVE_BitPerm is implemented.",
    "syntax": [
      "BGRP<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_BitPerm) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letdata : bits(VL) = Z{}(n);letmask : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements -1doresult[e*:esize] =BitGroup{esize}(data[e*:esize], mask[e*:esize]);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "BIC (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BIC--immediate---Bitwise-clear-bits-using-immediate--unpredicated---an-alias-of-AND--immediate--?lang=en",
    "description": "Bitwise clear bits using immediate (unpredicated) This instruction performs a bitwise clear bits using an immediate with each 64-bit element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate is a 64-bit value consisting of a single run of ones or zeros repeating every 2, 4, 8, 16, 32 or 64 bits. This instruction is unpredicated. This is a pseudo-instruction ofAND (immediate). This means: The encodings in this description are named to match the encodings ofAND (immediate).The assembler syntax is used only for assembly, and is not used on disassembly.The description ofAND (immediate)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "BIC<Zdn>.<T>,<Zdn>.<T>, #<const>\nis equivalent to\nAND<Zdn>.<T>,<Zdn>.<T>, #(-<const>- 1)"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofAND (immediate)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "BIC (predicates)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BIC--predicates---Bitwise-clear-predicates-?lang=en",
    "description": "Bitwise clear predicates This instruction performs a bitwise AND between the active elements of the first source predicate and the inverted corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags.",
    "syntax": [
      "BIC<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(element1AND(NOT element2));elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "BIC (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BIC--vectors--predicated---Bitwise-clear--predicated--?lang=en",
    "description": "Bitwise clear (predicated) This instruction performs a bitwise AND between the active elements of the first source vector and the inverted corresponding elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "BIC<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =element1AND(NOT element2);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BIC (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BIC--vectors--unpredicated---Bitwise-clear--unpredicated--?lang=en",
    "description": "Bitwise clear (unpredicated) This instruction performs a bitwise AND between the elements of the first source vector and the inverted corresponding elements of the second source vector, and places the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "BIC<Zd>.D,<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);Z{VL}(d) =operand1AND(NOT operand2);",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "BICS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BICS--Bitwise-clear-predicates--setting-the-condition-flags-?lang=en",
    "description": "Bitwise clear predicates, setting the condition flags This instruction performs a bitwise AND between the active elements of the first source predicate and the inverted corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "BICS<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(element1AND(NOT element2));elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nIf FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "BRKA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BRKA--Break-after-first-true-condition-?lang=en",
    "description": "Break after first true condition This instruction sets the destination predicate elements up to and including the first active and true source element to true, then sets subsequent elements to false. Inactive elements in the destination predicate register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. This instruction does not set the condition flags.",
    "syntax": [
      "BRKA<Pd>.B,<Pg>/<ZM>,<Pn>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(PL) = P{}(n);letoperand2 : bits(PL) =ifmerging thenP{PL}(d)elseZeros{PL};varbreak: boolean = FALSE;varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement : boolean =ActivePredicateElement{PL}(operand, e, esize);ifActivePredicateElement{PL}(mask, e, esize) thenletpbit : bit =if!breakthen'1'else'0';\n        result[e*:psize] = ZeroExtend{psize}(pbit);break=break|| element;\n    elsif merging thenletpbit : bit =PredicateElement{PL}(operand2, e, esize);\n        result[e*:psize] = ZeroExtend{psize}(pbit);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "BRKAS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BRKAS--Break-after-first-true-condition--setting-the-condition-flags-?lang=en",
    "description": "Break after first true condition, setting the condition flags This instruction sets the destination predicate elements up to and including the first active and true source element to true, then sets subsequent elements to false. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "BRKAS<Pd>.B,<Pg>/Z,<Pn>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(PL) = P{}(n);letoperand2 : bits(PL) =ifmerging thenP{PL}(d)elseZeros{PL};varbreak: boolean = FALSE;varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement : boolean =ActivePredicateElement{PL}(operand, e, esize);ifActivePredicateElement{PL}(mask, e, esize) thenletpbit : bit =if!breakthen'1'else'0';\n        result[e*:psize] = ZeroExtend{psize}(pbit);break=break|| element;\n    elsif merging thenletpbit : bit =PredicateElement{PL}(operand2, e, esize);\n        result[e*:psize] = ZeroExtend{psize}(pbit);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "BRKB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BRKB--Break-before-first-true-condition-?lang=en",
    "description": "Break before first true condition This instruction sets the destination predicate elements up to but not including the first active and true source element to true, then sets subsequent elements to false. Inactive elements in the destination predicate register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. This instruction does not set the condition flags.",
    "syntax": [
      "BRKB<Pd>.B,<Pg>/<ZM>,<Pn>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(PL) = P{}(n);letoperand2 : bits(PL) =ifmerging thenP{PL}(d)elseZeros{PL};varbreak: boolean = FALSE;varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement : boolean =ActivePredicateElement{PL}(operand, e, esize);ifActivePredicateElement{PL}(mask, e, esize) thenbreak=break|| element;letpbit : bit =if!breakthen'1'else'0';\n        result[e*:psize] = ZeroExtend{psize}(pbit);\n    elsif merging thenletpbit : bit =PredicateElement{PL}(operand2, e, esize);\n        result[e*:psize] = ZeroExtend{psize}(pbit);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "BRKBS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BRKBS--Break-before-first-true-condition--setting-the-condition-flags-?lang=en",
    "description": "Break before first true condition, setting the condition flags This instruction sets the destination predicate elements up to but not including the first active and true source element to true, then sets subsequent elements to false. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "BRKBS<Pd>.B,<Pg>/Z,<Pn>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(PL) = P{}(n);letoperand2 : bits(PL) =ifmerging thenP{PL}(d)elseZeros{PL};varbreak: boolean = FALSE;varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement : boolean =ActivePredicateElement{PL}(operand, e, esize);ifActivePredicateElement{PL}(mask, e, esize) thenbreak=break|| element;letpbit : bit =if!breakthen'1'else'0';\n        result[e*:psize] = ZeroExtend{psize}(pbit);\n    elsif merging thenletpbit : bit =PredicateElement{PL}(operand2, e, esize);\n        result[e*:psize] = ZeroExtend{psize}(pbit);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "BRKN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BRKN--Propagate-break-to-next-partition-?lang=en",
    "description": "Propagate break to next partition This instruction propagates break to next partition. If the Last active element of the first source predicate is false, then the destination predicate is set to all-false. Otherwise, the destination and second source predicates are left unchanged. This instruction does not set the condition flags.",
    "syntax": [
      "BRKN<Pdm>.B,<Pg>/Z,<Pn>.B,<Pdm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(dm);varresult : bits(PL);ifLastActive{PL}(mask, operand1,8) =='1'then\n    result = operand2;elseresult = Zeros{PL};\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(Ones{PL}, result,8);\nend;P{PL}(dm) = result;",
    "operational_info": ""
  },
  {
    "title": "BRKNS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BRKNS--Propagate-break-to-next-partition--setting-the-condition-flags-?lang=en",
    "description": "Propagate break to next partition, setting the condition flags This instruction propagates break to next partition and sets the condition flags. If the Last active element of the first source predicate is false, then the destination predicate is set to all-false. Otherwise, the destination and second source predicates are left unchanged. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "BRKNS<Pdm>.B,<Pg>/Z,<Pn>.B,<Pdm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(dm);varresult : bits(PL);ifLastActive{PL}(mask, operand1,8) =='1'then\n    result = operand2;elseresult = Zeros{PL};\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(Ones{PL}, result,8);\nend;P{PL}(dm) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "BRKPA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BRKPA--Break-after-first-true-condition--propagating-from-previous-partition-?lang=en",
    "description": "Break after first true condition, propagating from previous partition This instruction breaks after first true condition, propagating from previous partition. If the Last active element of the first source predicate is false, then the destination predicate is set to all-false. Otherwise, the destination predicate elements up to and including the first active and true source element are set to true, then the subsequent elements are set to false. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags.",
    "syntax": [
      "BRKPA<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);varlast : boolean = (LastActive{PL}(mask, operand1,8) =='1');fore =0to elements-1doifActivePredicateElement{PL}(mask, e,8) thenletpbit : bit =iflast then'1'else'0';\n        result[e*:1] = ZeroExtend{1}(pbit);\n        last = last && (!ActivePredicateElement{PL}(operand2, e,8));elseresult[e*:1] = ZeroExtend{1}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "BRKPAS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BRKPAS--Break-after-first-true-condition--propagating-from-previous-partition-and-setting-the-condition-flags-?lang=en",
    "description": "Break after first true condition, propagating from previous partition and setting the condition flags This instruction breaks after first true condition, propagating from previous partition, and sets the condition flags. If the Last active element of the first source predicate is false, then the destination predicate is set to all-false. Otherwise, the destination predicate elements up to and including the first active and true source element are set to true, then the subsequent elements are set to false. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "BRKPAS<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);varlast : boolean = (LastActive{PL}(mask, operand1,8) =='1');fore =0to elements-1doifActivePredicateElement{PL}(mask, e,8) thenletpbit : bit =iflast then'1'else'0';\n        result[e*:1] = ZeroExtend{1}(pbit);\n        last = last && (!ActivePredicateElement{PL}(operand2, e,8));elseresult[e*:1] = ZeroExtend{1}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "BRKPB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BRKPB--Break-before-first-true-condition--propagating-from-previous-partition-?lang=en",
    "description": "Break before first true condition, propagating from previous partition This instruction breaks before first true condition, propagating from previous partition. If the Last active element of the first source predicate is false, then the destination predicate is set to all-false. Otherwise, the destination predicate elements up to but not including the first active and true source element are set to true, then the subsequent elements are set to false. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags.",
    "syntax": [
      "BRKPB<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);varlast : boolean = (LastActive{PL}(mask, operand1,8) =='1');fore =0to elements-1doifActivePredicateElement{PL}(mask, e,8) then\n        last = last && (!ActivePredicateElement{PL}(operand2, e,8));letpbit : bit =iflast then'1'else'0';\n        result[e*:1] = ZeroExtend{1}(pbit);elseresult[e*:1] = ZeroExtend{1}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "BRKPBS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BRKPBS--Break-before-first-true-condition--propagating-from-previous-partition-and-setting-the-condition-flags-?lang=en",
    "description": "Break before first true condition, propagating from previous partition and setting the condition flags This instruction breaks before first true condition, propagating from previous partition, and sets the condition flags. If the Last active element of the first source predicate is false, then the destination predicate is set to all-false. Otherwise, the destination predicate elements up to but not including the first active and true source element are set to true, then the subsequent elements are set to false. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "BRKPBS<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);varlast : boolean = (LastActive{PL}(mask, operand1,8) =='1');fore =0to elements-1doifActivePredicateElement{PL}(mask, e,8) then\n        last = last && (!ActivePredicateElement{PL}(operand2, e,8));letpbit : bit =iflast then'1'else'0';\n        result[e*:1] = ZeroExtend{1}(pbit);elseresult[e*:1] = ZeroExtend{1}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "BSL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BSL--Bitwise-select-?lang=en",
    "description": "Bitwise select This instruction selects bits from the first source vector where the corresponding bit in the third source vector is '1', and from the second source vector where the corresponding bit in the third source vector is '0'. The result is destructively placed in the destination and first source vector. This instruction is unpredicated.",
    "syntax": [
      "BSL<Zdn>.D,<Zdn>.D,<Zm>.D,<Zk>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)= Z{}(m);letoperand3 :bits(VL)= Z{}(k);Z{VL}(dn) = (operand1ANDoperand3) OR (operand2ANDNOT(operand3));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BSL1N",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BSL1N--Bitwise-select-with-first-input-inverted-?lang=en",
    "description": "Bitwise select with first input inverted This instruction selects bits from the inverted first source vector where the corresponding bit in the third source vector is '1', and from the second source vector where the corresponding bit in the third source vector is '0'. The result is destructively placed in the destination and first source vector. This instruction is unpredicated.",
    "syntax": [
      "BSL1N<Zdn>.D,<Zdn>.D,<Zm>.D,<Zk>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)= Z{}(m);letoperand3 :bits(VL)= Z{}(k);Z{VL}(dn) = (NOT(operand1)ANDoperand3) OR (operand2ANDNOT(operand3));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "BSL2N",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/BSL2N--Bitwise-select-with-second-input-inverted-?lang=en",
    "description": "Bitwise select with second input inverted This instruction selects bits from the first source vector where the corresponding bit in the third source vector is '1', and from the inverted second source vector where the corresponding bit in the third source vector is '0'. The result is destructively placed in the destination and first source vector. This instruction is unpredicated.",
    "syntax": [
      "BSL2N<Zdn>.D,<Zdn>.D,<Zm>.D,<Zk>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)= Z{}(m);letoperand3 :bits(VL)= Z{}(k);Z{VL}(dn) = (operand1ANDoperand3) OR (NOT(operand2)ANDNOT(operand3));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "CADD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CADD--Complex-integer-add-?lang=en",
    "description": "Complex integer add This instruction adds the real and imaginary components of the integral complex numbers from the first source vector to the complex numbers from the second source vector that have first been rotated by 90 or 270 degrees in the direction from the positive real axis towards the positive imaginary axis, when considered in polar representation, equivalent to multiplying the complex numbers in the second source vector by ±j beforehand. The results are destructively placed in the corresponding elements of the first source vector. This instruction is unpredicated. Each complex number is represented in a vector register as an even/odd pair of elements with the real part in the even-numbered element and the imaginary part in the odd-numbered element.",
    "syntax": [
      "CADD<Zdn>.<T>,<Zdn>.<T>,<Zm>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letpairs : integer =VLDIV(2* esize);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);forp =0to pairs-1dovaracc_r : integer  = SInt(operand1[(2* p +0)*:esize]);varacc_i : integer  = SInt(operand1[(2* p +1)*:esize]);letelt2_r : integer = SInt(operand2[(2* p +0)*:esize]);letelt2_i : integer = SInt(operand2[(2* p +1)*:esize]);ifsub_i then\n        acc_r = acc_r - elt2_i;\n        acc_i = acc_i + elt2_r;\n    end;ifsub_r then\n        acc_r = acc_r + elt2_i;\n        acc_i = acc_i - elt2_r;\n    end;\n\n    result[(2* p +0)*:esize] = acc_r[esize-1:0];\n    result[(2* p +1)*:esize] = acc_i[esize-1:0];\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "CDOT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CDOT--indexed---Complex-integer-dot-product-by-indexed-element-?lang=en",
    "description": "Complex integer dot product by indexed element This instruction delimits the source vectors into pairs of 8-bit or 16-bit signed integer complex numbers. Within each pair, the complex numbers in the first source vector are multiplied by the corresponding complex numbers in the second source vector and the resulting wide real or wide imaginary part of the product is accumulated into a 32-bit or 64-bit destination vector element that overlaps all four of the elements that comprise a pair of complex number values in the first source vector. As a result each instruction implicitly deinterleaves the real and imaginary components of their complex number inputs, so that the destination vector accumulates 4×wide real sums or 4×wide imaginary sums. The complex numbers in the second source vector are rotated by 0, 90, 180 or 270 degrees in the direction from the positive real axis towards the positive imaginary axis, when considered in polar representation, by performing the following transformations prior to the dot product operations: If the rotation is #0, the imaginary parts of the complex numbers in the second source vector are negated. The destination vector therefore accumulates the real parts of a complex dot product.If the rotation is #90, the real and imaginary parts of the complex numbers the second source vector are swapped. The destination vector therefore accumulates the imaginary parts of a complex dot product.If the rotation is #180, there is no transformation. The destination vector therefore accumulates the real parts of a complex conjugate dot product.If the rotation is #270, the real parts of the complex numbers in the second source vector are negated and then swapped with the imaginary parts. The destination vector therefore accumulates the imaginary parts of a complex conjugate dot product. The indexed form of these instructions select a single pair of complex numbers within each 128-bit segment of the second source vector as the multiplier for all pairs of complex numbers within the corresponding 128-bit segment of the first source vector. The complex number pairs within the second source vector are specified using an immediate index that selects the same complex number pair position within each 128-bit vector segment. The index range is from 0 to one less than the number of complex number pairs per 128-bit segment. Each complex number is represented in a vector register as an even/odd pair of elements with the real part in the even-numbered element and the imaginary part in the odd-numbered element. It has encodings from 2 classes:8-bit to 32-bitand16-bit to 64-bit",
    "syntax": [
      "CDOT<Zda>.D,<Zn>.H,<Zm>.H[<imm>],<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;varres : bits(esize) = operand3[e*:esize];fori =0to1doletelt1_r : integer = SInt(operand1[(4* e +2* i +0)*:(esize DIV4)]);letelt1_i : integer = SInt(operand1[(4* e +2* i +1)*:(esize DIV4)]);letelt2_a : integer = SInt(operand2[(4* s +2* i + sel_a)*:(esize DIV4)]);letelt2_b : integer = SInt(operand2[(4* s +2* i + sel_b)*:(esize DIV4)]);ifsub_i then\n            res = (res + (elt1_r * elt2_a)) - (elt1_i * elt2_b);elseres = res + (elt1_r * elt2_a) + (elt1_i * elt2_b);\n        end;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "CDOT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CDOT--vectors---Complex-integer-dot-product-?lang=en",
    "description": "Complex integer dot product This instruction delimits the source vectors into pairs of 8-bit or 16-bit signed integer complex numbers. Within each pair, the complex numbers in the first source vector are multiplied by the corresponding complex numbers in the second source vector and the resulting wide real or wide imaginary part of the product is accumulated into a 32-bit or 64-bit destination vector element that overlaps all four of the elements that comprise a pair of complex number values in the first source vector. As a result each instruction implicitly deinterleaves the real and imaginary components of their complex number inputs, so that the destination vector accumulates 4×wide real sums or 4×wide imaginary sums. The complex numbers in the second source vector are rotated by 0, 90, 180 or 270 degrees in the direction from the positive real axis towards the positive imaginary axis, when considered in polar representation, by performing the following transformations prior to the dot product operations: If the rotation is #0, the imaginary parts of the complex numbers in the second source vector are negated. The destination vector therefore accumulates the real parts of a complex dot product.If the rotation is #90, the real and imaginary parts of the complex numbers the second source vector are swapped. The destination vector therefore accumulates the imaginary parts of a complex dot product.If the rotation is #180, there is no transformation. The destination vector therefore accumulates the real parts of a complex conjugate dot product.If the rotation is #270, the real parts of the complex numbers in the second source vector are negated and then swapped with the imaginary parts. The destination vector therefore accumulates the imaginary parts of a complex conjugate dot product. Each complex number is represented in a vector register as an even/odd pair of elements with the real part in the even-numbered element and the imaginary part in the odd-numbered element.",
    "syntax": [
      "CDOT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1dovarres : bits(esize) = operand3[e*:esize];fori =0to1doletelt1_r : integer = SInt(operand1[(4* e +2* i +0)*:(esize DIV4)]);letelt1_i : integer = SInt(operand1[(4* e +2* i +1)*:(esize DIV4)]);letelt2_a : integer = SInt(operand2[(4* e +2* i + sel_a)*:(esize DIV4)]);letelt2_b : integer = SInt(operand2[(4* e +2* i + sel_b)*:(esize DIV4)]);ifsub_i then\n            res = (res + (elt1_r * elt2_a)) - (elt1_i * elt2_b);elseres = res + (elt1_r * elt2_a) + (elt1_i * elt2_b);\n        end;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "CLASTA (SIMD&FP scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CLASTA--SIMD-FP-scalar---Conditionally-extract-element-after-last-to-SIMD-FP-scalar-register-?lang=en",
    "description": "Conditionally extract element after last to SIMD&FP scalar register This instruction extracts and zero-extends the element after the Last active element, or if the Last active element is the final element it extracts element zero, from the source vector register, and destructively places the zero-extended value in the destination and first source SIMD & floating-point scalar register. If there are no Active elements, the least significant element-size bits of the destination and first source SIMD & floating-point scalar register are destructively zero-extended.",
    "syntax": [
      "CLASTA<V><dn>,<Pg>,<V><dn>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(esize) = V{}(dn);letoperand2 : bits(VL) = Z{}(m);varresult : bits(esize);varlast : integer =LastActiveElement{PL}(mask, esize);iflast <0then\n    result = ZeroExtend{esize}(operand1);elseif!isBefore then\n        last = last +1;iflast >= elements then last =0; end;\n    end;\n    result = operand2[last*:esize];\n\nend;V{esize}(dn) = result;",
    "operational_info": ""
  },
  {
    "title": "CLASTA (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CLASTA--scalar---Conditionally-extract-element-after-last-to-general-purpose-register-?lang=en",
    "description": "Conditionally extract element after last to general-purpose register This instruction extracts and zero-extends the element after the Last active element, or if the Last active element is the final element it extracts element zero, from the source vector register, and destructively places the zero-extended value in the destination and first source general-purpose register. If there are no Active elements, the least significant element-size bits of the destination and first source general-purpose register are destructively zero-extended.",
    "syntax": [
      "CLASTA<R><dn>,<Pg>,<R><dn>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(esize) = X{}(dn);letoperand2 : bits(VL) = Z{}(m);varresult : bits(csize);varlast : integer =LastActiveElement{PL}(mask, esize);iflast <0then\n    result = ZeroExtend{csize}(operand1);elseif!isBefore then\n        last = last +1;iflast >= elements then last =0; end;\n    end;\n    result = ZeroExtend{csize}(operand2[last*:esize]);\n\nend;X{csize}(dn) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "CLASTA (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CLASTA--vectors---Conditionally-extract-element-after-last-to-vector-register-?lang=en",
    "description": "Conditionally extract element after last to vector register This instruction extracts the element after the Last active element, or if the Last active element is the final element it extracts element zero, from the second source vector register, and then replicates the extracted element to destructively fill the destination and first source vector. If there are no Active elements, the destination and source vector are unmodified.",
    "syntax": [
      "CLASTA<Zdn>.<T>,<Pg>,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);varlast : integer =LastActiveElement{PL}(mask, esize);iflast <0then\n    result = operand1;elseif!isBefore then\n        last = last +1;iflast >= elements then last =0; end;\n    end;fore =0to elements-1doresult[e*:esize] = operand2[last*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "CLASTB (SIMD&FP scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CLASTB--SIMD-FP-scalar---Conditionally-extract-last-element-to-SIMD-FP-scalar-register-?lang=en",
    "description": "Conditionally extract last element to SIMD&FP scalar register This instruction extracts and zero-extends the Last active element from the source vector register, and destructively places the zero-extended value in the destination and first source SIMD & floating-point scalar register. If there are no Active elements, the least significant element-size bits of the destination and first source SIMD & floating-point scalar register are destructively zero-extended.",
    "syntax": [
      "CLASTB<V><dn>,<Pg>,<V><dn>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(esize) = V{}(dn);letoperand2 : bits(VL) = Z{}(m);varresult : bits(esize);varlast : integer =LastActiveElement{PL}(mask, esize);iflast <0then\n    result = ZeroExtend{esize}(operand1);elseif!isBefore then\n        last = last +1;iflast >= elements then last =0; end;\n    end;\n    result = operand2[last*:esize];\n\nend;V{esize}(dn) = result;",
    "operational_info": ""
  },
  {
    "title": "CLASTB (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CLASTB--scalar---Conditionally-extract-last-element-to-general-purpose-register-?lang=en",
    "description": "Conditionally extract last element to general-purpose register This instruction extracts and zero-extends the Last active element from the source vector register, and destructively places the zero-extended value in the destination and first source general-purpose register. If there are no Active elements, the least significant element-size bits of the destination and first source general-purpose register are destructively zero-extended.",
    "syntax": [
      "CLASTB<R><dn>,<Pg>,<R><dn>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(esize) = X{}(dn);letoperand2 : bits(VL) = Z{}(m);varresult : bits(csize);varlast : integer =LastActiveElement{PL}(mask, esize);iflast <0then\n    result = ZeroExtend{csize}(operand1);elseif!isBefore then\n        last = last +1;iflast >= elements then last =0; end;\n    end;\n    result = ZeroExtend{csize}(operand2[last*:esize]);\n\nend;X{csize}(dn) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "CLASTB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CLASTB--vectors---Conditionally-extract-last-element-to-vector-register-?lang=en",
    "description": "Conditionally extract last element to vector register This instruction extracts the Last active element from the second source vector register, and then replicates the extracted element to destructively fill the destination and first source vector. If there are no Active elements, the destination and source vector are unmodified.",
    "syntax": [
      "CLASTB<Zdn>.<T>,<Pg>,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);varlast : integer =LastActiveElement{PL}(mask, esize);iflast <0then\n    result = operand1;elseif!isBefore then\n        last = last +1;iflast >= elements then last =0; end;\n    end;fore =0to elements-1doresult[e*:esize] = operand2[last*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "CLS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CLS--Count-leading-sign-bits--predicated--?lang=en",
    "description": "Count leading sign bits (predicated) This instruction determines the number of consecutive sign bits, starting from the most significant bit in each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "CLS<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] = CountLeadingSignBits(element)[esize-1:0];\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "CLZ",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CLZ--Count-leading-zero-bits--predicated--?lang=en",
    "description": "Count leading zero bits (predicated) This instruction determines the number of consecutive binary zero bits, starting from the most significant bit in each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "CLZ<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] = CountLeadingZeroBits(element)[esize-1:0];\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "CMLA (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CMLA--indexed---Complex-integer-multiply-add-by-indexed-element-?lang=en",
    "description": "Complex integer multiply-add by indexed element This instruction multiplies the duplicated real components for rotations 0 and 180, or imaginary components for rotations 90 and 270, of the integral numbers in each 128-bit segment of the first source vector by the specified complex number in the corresponding the second source vector segment rotated by 0, 90, 180 or 270 degrees in the direction from the positive real axis towards the positive imaginary axis, when considered in polar representation. This instruction then adds the products to the corresponding components of the complex numbers in the addend vector, and destructively places the results in the corresponding elements of the addend vector. This instruction is unpredicated. These transformations permit the creation of a variety of multiply-add and multiply-subtract operations on complex numbers by combining two of these instructions with the same vector operands but with rotations that are 90 degrees apart. Each complex number is represented in a vector register as an even/odd pair of elements with the real part in the even-numbered element and the imaginary part in the odd-numbered element. It has encodings from 2 classes:16-bitand32-bit",
    "syntax": [
      "CMLA<Zda>.S,<Zn>.S,<Zm>.S[<imm>],<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letpairs : integer =VLDIV(2* esize);letpairspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);forp =0to pairs-1doletsegmentbase : integer = p - (p MOD pairspersegment);lets : integer = segmentbase + index;letelt1_a : integer = SInt(operand1[(2* p + sel_a)*:esize]);letelt2_a : integer = SInt(operand2[(2* s + sel_a)*:esize]);letelt2_b : integer = SInt(operand2[(2* s + sel_b)*:esize]);letelt3_r : bits(esize) = operand3[(2* p +0)*:esize];letelt3_i : bits(esize) = operand3[(2* p +1)*:esize];letproduct_r : integer =  elt1_a * elt2_a;letproduct_i : integer =  elt1_a * elt2_b;ifsub_r then\n        result[(2* p +0)*:esize] = elt3_r - product_r;elseresult[(2* p +0)*:esize] = elt3_r + product_r;\n    end;ifsub_i then\n        result[(2* p +1)*:esize] = elt3_i - product_i;elseresult[(2* p +1)*:esize] = elt3_i + product_i;\n    end;\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "CMLA (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CMLA--vectors---Complex-integer-multiply-add-?lang=en",
    "description": "Complex integer multiply-add This instruction multiplies the duplicated real components for rotations 0 and 180, or imaginary components for rotations 90 and 270, of the integral numbers in the first source vector by the corresponding complex number in the second source vector rotated by 0, 90, 180 or 270 degrees in the direction from the positive real axis towards the positive imaginary axis, when considered in polar representation. This instruction then adds the products to the corresponding components of the complex numbers in the addend vector, and destructively places the results in the corresponding elements of the addend vector. This instruction is unpredicated. These transformations permit the creation of a variety of multiply-add and multiply-subtract operations on complex numbers by combining two of these instructions with the same vector operands but with rotations that are 90 degrees apart. Each complex number is represented in a vector register as an even/odd pair of elements with the real part in the even-numbered element and the imaginary part in the odd-numbered element.",
    "syntax": [
      "CMLA<Zda>.<T>,<Zn>.<T>,<Zm>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letpairs : integer =VLDIV(2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);forp =0to pairs-1doletelt1_a : integer = SInt(operand1[(2* p + sel_a)*:esize]);letelt2_a : integer = SInt(operand2[(2* p + sel_a)*:esize]);letelt2_b : integer = SInt(operand2[(2* p + sel_b)*:esize]);letelt3_r : bits(esize) = operand3[(2* p +0)*:esize];letelt3_i : bits(esize) = operand3[(2* p +1)*:esize];letproduct_r : integer =  elt1_a * elt2_a;letproduct_i : integer =  elt1_a * elt2_b;ifsub_r then\n        result[(2* p +0)*:esize] = elt3_r - product_r;elseresult[(2* p +0)*:esize] = elt3_r + product_r;\n    end;ifsub_i then\n        result[(2* p +1)*:esize] = elt3_i - product_i;elseresult[(2* p +1)*:esize] = elt3_i + product_i;\n    end;\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "CMP<cc> (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CMP-cc---immediate---Compare-vector-to-immediate-?lang=en",
    "description": "Compare vector to immediate This instruction compares active integer elements in the source vector with an immediate, and places the boolean results of the specified comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. <cc>ComparisonEQequalGEsigned greater than or equalGTsigned greater thanHIunsigned higher thanHSunsigned higher than or sameLEsigned less than or equalLOunsigned lower thanLSunsigned lower than or sameLTsigned less thanNEnot equal It has encodings from 10 classes:Equal,Greater than,Greater than or equal,Higher,Higher or same,Less than,Less than or equal,Lower,Lower or sameandNot equal",
    "syntax": [
      "CMPNE<Pd>.<T>,<Pg>/Z,<Zn>.<T>, #<imm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);ifActivePredicateElement{PL}(mask, e, esize) thenvarcond : boolean;casecmp_op ofwhenCmp_EQ=> cond = element1 == imm;whenCmp_NE=> cond = element1 != imm;whenCmp_GE=> cond = element1 >= imm;whenCmp_LT=> cond = element1 <  imm;whenCmp_GT=> cond = element1 >  imm;whenCmp_LE=> cond = element1 <= imm;\n        end;letpbit : bit =ifcond then'1'else'0';\n        result[e*:psize] = ZeroExtend{psize}(pbit);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nIf FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the predicate register or NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "CMP<cc> (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CMP-cc---vectors---Compare-vectors-?lang=en",
    "description": "Compare vectors This instruction compares active integer elements in the first source vector with the corresponding elements in the second source vector, and places the boolean results of the specified comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. <cc>ComparisonEQequalGEsigned greater than or equalGTsigned greater thanHIunsigned higher thanHSunsigned higher than or sameNEnot equal This instruction is used by the pseudo-instructionsCMPLE (vectors),CMPLO (vectors),CMPLS (vectors), andCMPLT (vectors). It has encodings from 6 classes:Equal,Greater than,Greater than or equal,Higher,Higher or sameandNot equal",
    "syntax": [
      "CMPNE<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);ifActivePredicateElement{PL}(mask, e, esize) thenvarcond : boolean;letop2elt : bits(esize) = operand2[e*:esize];letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);casecmp_op ofwhenCmp_EQ=> cond = element1 == element2;whenCmp_NE=> cond = element1 != element2;whenCmp_GE=> cond = element1 >= element2;whenCmp_LT=> cond = element1 <  element2;whenCmp_GT=> cond = element1 >  element2;whenCmp_LE=> cond = element1 <= element2;\n        end;letpbit : bit =ifcond then'1'else'0';\n        result[e*:psize] = ZeroExtend{psize}(pbit);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nIf FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the predicate register or NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "CMP<cc> (wide elements)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CMP-cc---wide-elements---Compare-vector-to-64-bit-wide-elements-?lang=en",
    "description": "Compare vector to 64-bit wide elements This instruction compares active integer elements in the first source vector with overlapping 64-bit doubleword elements in the second source vector, and places the boolean results of the specified comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. <cc>ComparisonEQequalGEsigned greater than or equalGTsigned greater thanHIunsigned higher thanHSunsigned higher than or sameLEsigned less than or equalLOunsigned lower thanLSunsigned lower than or sameLTsigned less thanNEnot equal It has encodings from 10 classes:Equal,Greater than,Greater than or equal,Higher,Higher or same,Less than,Less than or equal,Lower,Lower or sameandNot equal",
    "syntax": [
      "CMPNE<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);ifActivePredicateElement{PL}(mask, e, esize) thenvarcond : boolean;letop2elt : bits(64) = operand2[((e * esize) DIVRM64)*:64];letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);casecmp_op ofwhenCmp_EQ=> cond = element1 == element2;whenCmp_NE=> cond = element1 != element2;whenCmp_GE=> cond = element1 >= element2;whenCmp_LT=> cond = element1 <  element2;whenCmp_GT=> cond = element1 >  element2;whenCmp_LE=> cond = element1 <= element2;\n        end;letpbit : bit =ifcond then'1'else'0';\n        result[e*:psize] = ZeroExtend{psize}(pbit);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nIf FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the predicate register or NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "CMPLE (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CMPLE--vectors---Compare-signed-less-than-or-equal-to-vector--setting-the-condition-flags--an-alias-of-CMP-cc---vectors--?lang=en",
    "description": "Compare signed less than or equal to vector, setting the condition flags This instruction compares active signed integer elements in the first source vector being less than or equal to the corresponding signed elements in the second source vector, and places the boolean results of the comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This is a pseudo-instruction ofCMP<cc> (vectors). This means: The encodings in this description are named to match the encodings ofCMP<cc> (vectors).The assembler syntax is used only for assembly, and is not used on disassembly.The description ofCMP<cc> (vectors)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "CMPLE<Pd>.<T>,<Pg>/Z,<Zm>.<T>,<Zn>.<T>\nis equivalent to\nCMPGE<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofCMP<cc> (vectors)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "CMPLO (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CMPLO--vectors---Compare-unsigned-lower-than-vector--setting-the-condition-flags--an-alias-of-CMP-cc---vectors--?lang=en",
    "description": "Compare unsigned lower than vector, setting the condition flags This instruction compares active unsigned integer elements in the first source vector being lower than the corresponding unsigned elements in the second source vector, and places the boolean results of the comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This is a pseudo-instruction ofCMP<cc> (vectors). This means: The encodings in this description are named to match the encodings ofCMP<cc> (vectors).The assembler syntax is used only for assembly, and is not used on disassembly.The description ofCMP<cc> (vectors)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "CMPLO<Pd>.<T>,<Pg>/Z,<Zm>.<T>,<Zn>.<T>\nis equivalent to\nCMPHI<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofCMP<cc> (vectors)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "CMPLS (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CMPLS--vectors---Compare-unsigned-lower-than-or-same-as-vector--setting-the-condition-flags--an-alias-of-CMP-cc---vectors--?lang=en",
    "description": "Compare unsigned lower than or same as vector, setting the condition flags This instruction compares active unsigned integer elements in the first source vector being lower than or the same as the corresponding unsigned elements in the second source vector, and places the boolean results of the comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This is a pseudo-instruction ofCMP<cc> (vectors). This means: The encodings in this description are named to match the encodings ofCMP<cc> (vectors).The assembler syntax is used only for assembly, and is not used on disassembly.The description ofCMP<cc> (vectors)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "CMPLS<Pd>.<T>,<Pg>/Z,<Zm>.<T>,<Zn>.<T>\nis equivalent to\nCMPHS<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofCMP<cc> (vectors)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "CMPLT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CMPLT--vectors---Compare-signed-less-than-vector--setting-the-condition-flags--an-alias-of-CMP-cc---vectors--?lang=en",
    "description": "Compare signed less than vector, setting the condition flags This instruction compares active signed integer elements in the first source vector being less than the corresponding signed elements in the second source vector, and places the boolean results of the comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This is a pseudo-instruction ofCMP<cc> (vectors). This means: The encodings in this description are named to match the encodings ofCMP<cc> (vectors).The assembler syntax is used only for assembly, and is not used on disassembly.The description ofCMP<cc> (vectors)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "CMPLT<Pd>.<T>,<Pg>/Z,<Zm>.<T>,<Zn>.<T>\nis equivalent to\nCMPGT<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofCMP<cc> (vectors)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "CNOT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CNOT--Logically-invert-boolean-condition--predicated--?lang=en",
    "description": "Logically invert boolean condition (predicated) This instruction logically inverts the boolean value in each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. Boolean TRUE is any non-zero value in a source, and one in a result element. Boolean FALSE is always zero. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "CNOT<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] = ZeroExtend{esize}(IsZeroBit{esize}(element));\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "CNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CNT--Count-non-zero-bits--predicated--?lang=en",
    "description": "Count non-zero bits (predicated) This instruction determines the number of non-zero bits in each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "CNT<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] = BitCount(element)[esize-1:0];\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "CNTB, CNTD, CNTH, CNTW",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CNTB--CNTD--CNTH--CNTW--Set-scalar-to-multiple-of-predicate-constraint-element-count-?lang=en",
    "description": "Set scalar to multiple of predicate constraint element count This instruction determines the number of active elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then places the result in the scalar destination. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 4 classes:Byte,Doubleword,HalfwordandWord",
    "syntax": [
      "CNTW<Xd>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);X{64}(d) = (count * imm)[63:0];",
    "operational_info": ""
  },
  {
    "title": "CNTP (predicate as counter)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CNTP--predicate-as-counter---Set-scalar-to-count-from-predicate-as-counter-?lang=en",
    "description": "Set scalar to count from predicate-as-counter This instruction determines the number of true elements in the source predicate, up to the total number of elements in two or four vectors, and places the scalar result in the destination general-purpose register.",
    "syntax": [
      "CNTP<Xd>,<PNn>.<T>,<vl>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letpred : bits(PL) = P{}(n);letmask : bits(PL*4) = CounterToPredicate{}(pred[15:0]);varsum : bits(64) = Zeros{};letmaxelements : integer = elements * width;fore =0to maxelements-1doifActivePredicateElement{PL*4}(mask, e, esize) then\n        sum = sum +1;\n    end;\nend;X{64}(d) = sum;",
    "operational_info": ""
  },
  {
    "title": "CNTP (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CNTP--predicate---Set-scalar-to-count-of-true-predicate-elements-?lang=en",
    "description": "Set scalar to count of true predicate elements This instruction determines the number of active and true elements in the source predicate and places the scalar result in the destination general-purpose register. Inactive predicate elements are not counted.",
    "syntax": [
      "CNTP<Xd>,<Pg>,<Pn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(PL) = P{}(n);varsum : bits(64) = Zeros{};fore =0to elements-1doif(ActivePredicateElement{PL}(mask, e, esize)\n         &&ActivePredicateElement{PL}(operand, e, esize)) then\n        sum = sum +1;\n    end;\nend;X{64}(d) = sum;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "COMPACT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/COMPACT--Copy-Active-vector-elements-to-lower-numbered-elements-?lang=en",
    "description": "Copy Active vector elements to lower-numbered elements This instruction copies Active elements from the source vector to consecutive elements of the destination vector, in increasing order of element number, and sets any remaining elements to zero. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled, or FEAT_SME2p2 is implemented. It has encodings from 2 classes:Byte and halfwordandWord and doubleword",
    "syntax": [
      "COMPACT<Zd>.<T>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2p2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) = Zeros{};varx : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand1[e*:esize];\n        result[x*:esize] = element;\n        x = x +1;\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "CPY (SIMD&FP scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CPY--SIMD-FP-scalar---Copy-SIMD-FP-scalar-register-to-vector-elements--predicated--?lang=en",
    "description": "Copy SIMD&FP scalar register to vector elements (predicated) This instruction copies the SIMD & floating-point scalar source register to each Active element in the destination vector. Inactive elements in the destination vector register remain unmodified. This instruction is used by the aliasMOV (SIMD&FP scalar, predicated).",
    "syntax": [
      "CPY<Zd>.<T>,<Pg>/M,<V><n>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(esize) = (ifAnyActiveElement{PL}(mask, esize) thenV{esize}(n)elseZeros{esize});varresult : bits(VL) = Z{}(d);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = operand1;\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "CPY (immediate, merging)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CPY--immediate--merging---Copy-signed-integer-immediate-to-vector-elements--merging--?lang=en",
    "description": "Copy signed integer immediate to vector elements (merging) This instruction copies a signed integer immediate to each Active element in the destination vector. Inactive elements in the destination vector register remain unmodified. The immediate operand is a signed value in the range -128 to +127, and for element widths of 16 bits or higher it may also be a signed multiple of 256 in the range -32768 to +32512 (excluding 0). The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\". This instruction is used by the aliasMOV (immediate, merging). This instruction is used by the pseudo-instructionFMOV (zero, predicated).",
    "syntax": [
      "CPY<Zd>.<T>,<Pg>/M, #<imm>{,<shift>}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letdest : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = imm[esize-1:0];elseresult[e*:esize] = dest[e*:esize];\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "CPY (immediate, zeroing)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CPY--immediate--zeroing---Copy-signed-integer-immediate-to-vector-elements--zeroing--?lang=en",
    "description": "Copy signed integer immediate to vector elements (zeroing) This instruction copies a signed integer immediate to each Active element in the destination vector. Inactive elements in the destination vector register are set to zero. The immediate operand is a signed value in the range -128 to +127, and for element widths of 16 bits or higher it may also be a signed multiple of 256 in the range -32768 to +32512 (excluding 0). The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\". This instruction is used by the aliasMOV (immediate, zeroing).",
    "syntax": [
      "CPY<Zd>.<T>,<Pg>/Z, #<imm>{,<shift>}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letdest : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = imm[esize-1:0];elseresult[e*:esize] = dest[e*:esize];\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "CPY (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CPY--scalar---Copy-general-purpose-register-to-vector-elements--predicated--?lang=en",
    "description": "Copy general-purpose register to vector elements (predicated) This instruction copies the general-purpose scalar source register to each Active element in the destination vector. Inactive elements in the destination vector register remain unmodified. This instruction is used by the aliasMOV (scalar, predicated).",
    "syntax": [
      "CPY<Zd>.<T>,<Pg>/M,<R><n|SP>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varresult :bits(VL)= Z{}(d);ifAnyActiveElement{PL}(mask, esize) then\n    var operand1 :bits(64);ifn ==31then\n        operand1 =SP{64}();elseoperand1 =X{64}(n);end;for e =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n            result[e*:esize] = operand1[esize-1:0];end;end;end;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "CTERMEQ, CTERMNE",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/CTERMEQ--CTERMNE--Compare-and-terminate-loop-?lang=en",
    "description": "Compare and terminate loop This instruction detects termination conditions in serialized vector loops by testing whether the comparison between the scalar source operands holds true. If the comparison is not successful, the instruction tests the state of the !Last condition flag (C), which indicates whether the previous flag-setting predicate instruction selected the last element of the vector partition. The Z and C condition flags are preserved by this instruction. The N and V condition flags are set as a pair to generate one of the following conditions for a subsequent conditional instruction: ConditionNVMeaningGE00Continue loop (compare failed and last element not selected)LT01Terminate loop (last element selected)LT10Terminate loop (compare succeeded)GE11Never generated The scalar source operands are 32-bit or 64-bit general-purpose registers of the same size. It has encodings from 2 classes:EqualandNot equal",
    "syntax": [
      "CTERMNE<R><n>,<R><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letoperand1 : bits(esize) = X{}(n);letoperand2 : bits(esize) = X{}(m);letelement1 : integer = UInt(operand1);letelement2 : integer = UInt(operand2);varterm : boolean;casecmp_op ofwhenCmp_EQ=> term = element1 == element2;whenCmp_NE=> term = element1 != element2;\nend;ifterm thenPSTATE.N ='1';PSTATE.V ='0';elsePSTATE.N ='0';PSTATE.V = (NOTPSTATE.C);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "DECB, DECD, DECH, DECW (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/DECB--DECD--DECH--DECW--scalar---Decrement-scalar-by-multiple-of-predicate-constraint-element-count-?lang=en",
    "description": "Decrement scalar by multiple of predicate constraint element count This instruction determines the number of active elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement the scalar destination. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 4 classes:Byte,Doubleword,HalfwordandWord",
    "syntax": [
      "DECW<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letVL : integer{} =CurrentVL();letoperand1 :bits(64)= X{}(dn);X{64}(dn) = operand1 - (count * imm);",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "DECD, DECH, DECW (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/DECD--DECH--DECW--vector---Decrement-vector-by-multiple-of-predicate-constraint-element-count-?lang=en",
    "description": "Decrement vector by multiple of predicate constraint element count This instruction determines the number of active elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement all destination vector elements. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 3 classes:Doubleword,HalfwordandWord",
    "syntax": [
      "DECW<Zdn>.S{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doresult[e*:esize] = operand1[e*:esize] - (count * imm);\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "DECP (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/DECP--scalar---Decrement-scalar-by-count-of-true-predicate-elements-?lang=en",
    "description": "Decrement scalar by count of true predicate elements This instruction determines the number of true elements in the source predicate and then uses the result to decrement the scalar destination.",
    "syntax": [
      "DECP<Xdn>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(64) = X{}(dn);letoperand2 : bits(PL) = P{}(m);varcount : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(operand2, e, esize) then\n        count = count +1;\n    end;\nend;X{64}(dn) = operand1 - count;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "DECP (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/DECP--vector---Decrement-vector-by-count-of-true-predicate-elements-?lang=en",
    "description": "Decrement vector by count of true predicate elements This instruction determines the number of true elements in the source predicate and then uses the result to decrement all destination vector elements. The predicate size specifier may be omitted in assembler source code, but this is deprecated and will be prohibited in a future release of the architecture.",
    "syntax": [
      "DECP<Zdn>.<T>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(PL) = P{}(m);varresult : bits(VL);varcount : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(operand2, e, esize) then\n        count = count +1;\n    end;\nend;fore =0to elements-1doresult[e*:esize] = operand1[e*:esize] - count;\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "DUP (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/DUP--immediate---Broadcast-signed-immediate-to-vector-elements--unpredicated--?lang=en",
    "description": "Broadcast signed immediate to vector elements (unpredicated) This instruction unconditionally broadcasts the signed integer immediate into each element of the destination vector. This instruction is unpredicated. The immediate operand is a signed value in the range -128 to +127, and for element widths of 16 bits or higher it may also be a signed multiple of 256 in the range -32768 to +32512 (excluding 0). The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\". This instruction is used by the aliasMOV (immediate, unpredicated). This instruction is used by the pseudo-instructionFMOV (zero, unpredicated).",
    "syntax": [
      "DUP<Zd>.<T>, #<imm>{,<shift>}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letresult : bits(VL) = Replicate{}(imm[esize-1:0]);Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "DUP (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/DUP--indexed---Broadcast-indexed-element-to-vector--unpredicated--?lang=en",
    "description": "Broadcast indexed element to vector (unpredicated) This instruction unconditionally broadcasts the indexed source vector element into each element of the destination vector. This instruction is unpredicated. The immediate element index is in the range of 0 to 63 (bytes), 31 (halfwords), 15 (words), 7 (doublewords) or 3 (quadwords). Selecting an element beyond the accessible vector length causes the destination vector to be set to zero. This instruction is used by the aliasMOV (SIMD&FP scalar, unpredicated).",
    "syntax": [
      "DUP<Zd>.<T>,<Zn>.<T>[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) =ifindex < elements thenZ{VL}(n)elseZeros{VL};varresult : bits(VL);varelement : bits(esize);ifindex >= elements then\n    element = Zeros{esize};elseelement = operand1[index*:esize];\nend;\nresult = Replicate{VL}(element);Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "DUP (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/DUP--scalar---Broadcast-general-purpose-register-to-vector-elements--unpredicated--?lang=en",
    "description": "Broadcast general-purpose register to vector elements (unpredicated) This instruction unconditionally broadcasts the general-purpose scalar source register into each element of the destination vector. This instruction is unpredicated. This instruction is used by the aliasMOV (scalar, unpredicated).",
    "syntax": [
      "DUP<Zd>.<T>,<R><n|SP>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;varoperand : bits(64);ifn ==31then\n    operand =SP{64}();elseoperand =X{64}(n);\nend;varresult : bits(VL);fore =0to elements-1doresult[e*:esize] = operand[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "DUPM",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/DUPM--Broadcast-logical-bitmask-immediate-to-vector--unpredicated--?lang=en",
    "description": "Broadcast logical bitmask immediate to vector (unpredicated) This instruction unconditionally broadcasts the logical bitmask immediate into each element of the destination vector. This instruction is unpredicated. The immediate is a 64-bit value consisting of a single run of ones or zeros repeating every 2, 4, 8, 16, 32 or 64 bits. This instruction is used by the aliasMOV.",
    "syntax": [
      "DUPM<Zd>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letresult : bits(VL) = Replicate{}(imm);Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "DUPQ",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/DUPQ--Broadcast-indexed-element-within-each-quadword-vector-segment--unpredicated--?lang=en",
    "description": "Broadcast indexed element within each quadword vector segment (unpredicated) This instruction unconditionally broadcasts the indexed element within each 128-bit source vector segment to all elements of the corresponding destination vector segment. This instruction is unpredicated. The immediate element index is in the range of 0 to 15 (bytes), 7 (halfwords), 3 (words) or 1 (doublewords).",
    "syntax": [
      "DUPQ<Zd>.<T>,<Zn>.<T>[<imm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letelements : integer =128DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);varelement : bits(esize);fors =0to segments-1doelement = operand[(s * elements + index)*:esize];\n    result[s*:128] = Replicate{128}(element);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "EON",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EON--Bitwise-exclusive-OR-with-inverted-immediate--unpredicated---an-alias-of-EOR--immediate--?lang=en",
    "description": "Bitwise exclusive-OR with inverted immediate (unpredicated) This instruction performs a bitwise exclusive-OR between an inverted immediate and each 64-bit element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate is a 64-bit value consisting of a single run of ones or zeros repeating every 2, 4, 8, 16, 32 or 64 bits. This instruction is unpredicated. This is a pseudo-instruction ofEOR (immediate). This means: The encodings in this description are named to match the encodings ofEOR (immediate).The assembler syntax is used only for assembly, and is not used on disassembly.The description ofEOR (immediate)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "EON<Zdn>.<T>,<Zdn>.<T>, #<const>\nis equivalent to\nEOR<Zdn>.<T>,<Zdn>.<T>, #(-<const>- 1)"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofEOR (immediate)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "EOR (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EOR--immediate---Bitwise-exclusive-OR-with-immediate--unpredicated--?lang=en",
    "description": "Bitwise exclusive-OR with immediate (unpredicated) This instruction performs a bitwise exclusive-OR between an immediate and each 64-bit element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate is a 64-bit value consisting of a single run of ones or zeros repeating every 2, 4, 8, 16, 32 or 64 bits. This instruction is unpredicated. This instruction is used by the pseudo-instructionEON.",
    "syntax": [
      "EOR<Zdn>.<T>,<Zdn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV64;letoperand : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(64) = operand[e*:64];\n    result[e*:64] = element1 XOR imm;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "EOR (predicates)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EOR--predicates---Bitwise-exclusive-OR-predicates-?lang=en",
    "description": "Bitwise exclusive-OR predicates This instruction performs a bitwise exclusive-OR between the active elements of the first source predicate and the corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. This instruction is used by the aliasNOT (predicate).",
    "syntax": [
      "EOR<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(element1 XOR element2);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "EOR (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EOR--vectors--predicated---Bitwise-exclusive-OR--predicated--?lang=en",
    "description": "Bitwise exclusive-OR (predicated) This instruction performs a bitwise exclusive-OR between the active elements of the first source vector and the corresponding elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "EOR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = element1 XOR element2;elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "EOR (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EOR--vectors--unpredicated---Bitwise-exclusive-OR--unpredicated--?lang=en",
    "description": "Bitwise exclusive-OR (unpredicated) This instruction performs a bitwise exclusive-OR between the elements of the first source vector and the corresponding elements of the second source vector, and places the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "EOR<Zd>.D,<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);Z{VL}(d) = operand1 XOR operand2;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "EOR3",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EOR3--Bitwise-exclusive-OR-between-three-vectors-?lang=en",
    "description": "Bitwise exclusive-OR between three vectors This instruction performs a bitwise exclusive-OR between the corresponding elements of the three source vectors, and destructively places the results in the corresponding elements of the destination and first source vector. This instruction is unpredicated.",
    "syntax": [
      "EOR3<Zdn>.D,<Zdn>.D,<Zm>.D,<Zk>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)= Z{}(m);letoperand3 :bits(VL)= Z{}(k);Z{VL}(dn) = operand1 XOR operand2 XOR operand3;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "EORBT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EORBT--Interleaving-exclusive-OR--bottom--top--?lang=en",
    "description": "Interleaving exclusive-OR (bottom, top) This instruction performs an interleaving exclusive-OR between the even-numbered elements of the first source vector register and the odd-numbered elements of the second source vector register, placing the result in the even-numbered elements of the destination vector, leaving the odd-numbered elements unchanged. This instruction is unpredicated.",
    "syntax": [
      "EORBT<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement1 : bits(esize) = operand1[(2*e + sel1)*:esize];letelement2 : bits(esize) = operand2[(2*e + sel2)*:esize];\n    result[(2*e + sel1)*:esize] = element1 XOR element2;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "EORQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EORQV--Bitwise-exclusive-OR-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Bitwise exclusive-OR reduction of quadword vector segments This instruction performs a bitwise exclusive-OR between elements in the same position from each 128-bit source vector segment, and places each result into the corresponding element number of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as all zeros.",
    "syntax": [
      "EORQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer = VL DIV128;letelempersegment : integer =128DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(128) = Zeros{};varstmp : bits(128) = Zeros{};vardtmp : bits(esize);fore =0to elempersegment-1dodtmp = Zeros{esize};fors =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) then\n            stmp = operand[s*:128];\n            dtmp = dtmp XOR stmp[e*:esize];\n        end;\n    end;\n    result[e*:esize] = dtmp[esize-1:0];\nend;V{128}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "EORS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EORS--Bitwise-exclusive-OR-predicates--setting-the-condition-flags-?lang=en",
    "description": "Bitwise exclusive-OR predicates, setting the condition flags This instruction performs a bitwise exclusive-OR between the active elements of the first source predicate and the corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This instruction is used by the aliasNOTS.",
    "syntax": [
      "EORS<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(element1 XOR element2);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nIf FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "EORTB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EORTB--Interleaving-exclusive-OR--top--bottom--?lang=en",
    "description": "Interleaving exclusive-OR (top, bottom) This instruction performs an interleaving exclusive-OR between the odd-numbered elements of the first source vector register and the even-numbered elements of the second source vector register, placing the result in the odd-numbered elements of the destination vector, leaving the even-numbered elements unchanged. This instruction is unpredicated.",
    "syntax": [
      "EORTB<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement1 : bits(esize) = operand1[(2*e + sel1)*:esize];letelement2 : bits(esize) = operand2[(2*e + sel2)*:esize];\n    result[(2*e + sel1)*:esize] = element1 XOR element2;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "EORV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EORV--Bitwise-exclusive-OR-reduction-to-scalar-?lang=en",
    "description": "Bitwise exclusive-OR reduction to scalar This instruction performs a bitwise exclusive-OR between the elements of the source vector, and places the result in the SIMD&FP scalar destination register. Inactive elements in the source vector are treated as zero.",
    "syntax": [
      "EORV<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(esize) = Zeros{esize};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result = result XOR operand[e*:esize];\n\n    end;\nend;V{esize}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "EXPAND",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EXPAND--Copy-lower-numbered-vector-elements-to-Active-elements-?lang=en",
    "description": "Copy lower-numbered vector elements to Active elements This instruction copies consecutive elements from the source vector to Active elements of the destination vector, in increasing order of element number. Inactive elements in the destination vector register are set to zero. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled, or FEAT_SME2p2 is implemented.",
    "syntax": [
      "EXPAND<Zd>.<T>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2p2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL);varx : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = operand1[x*:esize];\n        x = x +1;elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "EXT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EXT--Extract-vector-from-pair-of-vectors-?lang=en",
    "description": "Extract vector from pair of vectors This instruction copies the indexed byte up to the last byte of the first source vector to the bottom of the result vector, then fills the remainder of the result starting from the first byte of the second source vector. The result is destructively placed in the destination and first source vector, or constructively placed in the destination vector. This instruction is unpredicated. An index that is greater than or equal to the vector length in bytes is treated as zero, resulting in the first source vector being copied to the result unchanged. It has encodings from 2 classes:ConstructiveandDestructive",
    "syntax": [
      "EXT<Zdn>.B,<Zdn>.B,<Zm>.B, #<imm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 :bits(VL)= Z{}(s1);letoperand2 :bits(VL)= Z{}(s2);varresult :bits(VL);letconcat :bits(VL*2)= operand2 :: operand1;ifposition >= VL then\n    result = concat[VL-1:0];elseresult = concat[(position+VL)-1:position];end;Z{VL}(dst) = result;",
    "operational_info": "For the \"Constructive\" variant:\nThis instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nFor the \"Destructive\" variant:\nThis instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThe destructive variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the destructive variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "EXTQ",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/EXTQ--Extract-vector-segment-from-each-pair-of-quadword-vector-segments-?lang=en",
    "description": "Extract vector segment from each pair of quadword vector segments This instruction copies, for each 128-bit vector segment of the result, the indexed byte up to and including the last byte of the corresponding first source vector segment to the bottom of the result segment, and then fills the remainder of the result segment starting from the first byte of the corresponding second source vector segment. The result segments are destructively placed in the corresponding first source vector segment. This instruction is unpredicated.",
    "syntax": [
      "EXTQ<Zdn>.B,<Zdn>.B,<Zm>.B, #<imm>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)= Z{}(m);varresult :bits(VL);fors =0to segments-1do\n    let concat :bits(256)= operand2[s*:128] :: operand1[s*:128];result[s*:128] = concat[position+127:position];end;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FABD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FABD--Floating-point-absolute-difference--predicated--?lang=en",
    "description": "Floating-point absolute difference (predicated) This instruction calculates the absolute difference of active floating-point elements of the first source vector and the corresponding floating-point elements of the second source vector, and destructively places the result in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FABD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPAbs{esize}(FPSub{esize}(element1, element2, FPCR()), FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FABS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FABS--Floating-point-absolute-value--predicated--?lang=en",
    "description": "Floating-point absolute value (predicated) This instruction takes the absolute value of each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. This clears the sign bit and cannot signal a floating-point exception. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "FABS<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =FPAbs{esize}(element, FPCR());\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FAC<cc>",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FAC-cc---Floating-point-absolute-compare-?lang=en",
    "description": "Floating-point absolute compare This instruction compares active absolute values of floating-point elements in the first source vector with the corresponding absolute values of elements in the second source vector, and places the boolean results of the specified comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. <cc>ComparisonGEgreater than or equalGTgreater thanLEless than or equalLTless than This instruction is used by the pseudo-instructionsFACLE, andFACLT. It has encodings from 2 classes:Greater thanandGreater than or equal",
    "syntax": [
      "FACGE<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : bits(esize) = FPAbs{}(operand1[e*:esize], FPCR());letelement2 : bits(esize) = FPAbs{}(operand2[e*:esize], FPCR());varres : boolean;casecmp_op ofwhenCmp_GE=> res =FPCompareGE{esize}(element1, element2, FPCR());whenCmp_GT=> res =FPCompareGT{esize}(element1, element2, FPCR());\n        end;letpbit : bit =ifres then'1'else'0';\n        result[e*:psize] = ZeroExtend{psize}(pbit);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;P{PL}(d) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the predicate register written by this instruction might be significantly delayed."
  },
  {
    "title": "FACLE",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FACLE--Floating-point-absolute-compare-less-than-or-equal--an-alias-of-FAC-cc--?lang=en",
    "description": "Floating-point absolute compare less than or equal This instruction compares active absolute values of floating-point elements in the first source vector being less than or equal to the corresponding absolute values of elements in the second source vector, and places the boolean results of the comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. This is a pseudo-instruction ofFAC<cc>. This means: The encodings in this description are named to match the encodings ofFAC<cc>.The assembler syntax is used only for assembly, and is not used on disassembly.The description ofFAC<cc>gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "FACLE<Pd>.<T>,<Pg>/Z,<Zm>.<T>,<Zn>.<T>\nis equivalent to\nFACGE<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofFAC<cc>gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "FACLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FACLT--Floating-point-absolute-compare-less-than--an-alias-of-FAC-cc--?lang=en",
    "description": "Floating-point absolute compare less than This instruction compares active absolute values of floating-point elements in the first source vector being less than the corresponding absolute values of elements in the second source vector, and places the boolean results of the comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. This is a pseudo-instruction ofFAC<cc>. This means: The encodings in this description are named to match the encodings ofFAC<cc>.The assembler syntax is used only for assembly, and is not used on disassembly.The description ofFAC<cc>gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "FACLT<Pd>.<T>,<Pg>/Z,<Zm>.<T>,<Zn>.<T>\nis equivalent to\nFACGT<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofFAC<cc>gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "FADD (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FADD--immediate---Floating-point-add-immediate--predicated--?lang=en",
    "description": "Floating-point add immediate (predicated) This instruction adds an immediate to each active floating-point element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate may take the value +0.5 or +1.0 only. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FADD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =FPAdd{esize}(element1, imm, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FADD (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FADD--vectors--predicated---Floating-point-add--predicated--?lang=en",
    "description": "Floating-point add (predicated) This instruction adds active floating-point elements of the first source vector to the corresponding floating-point elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FADD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPAdd{esize}(element1, element2, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FADD (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FADD--vectors--unpredicated---Floating-point-add--unpredicated--?lang=en",
    "description": "Floating-point add (unpredicated) This instruction adds all floating-point elements of the first source vector to the corresponding elements of the second source vector, and places the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "FADD<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[e*:esize] =FPAdd{esize}(element1, element2, FPCR());\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FADDA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FADDA--Floating-point-add-strictly-ordered-reduction--accumulating-in-scalar-?lang=en",
    "description": "Floating-point add strictly-ordered reduction, accumulating in scalar This instruction adds the floating-point values in a SIMD&FP scalar source and all active lanes of the vector source and destructively places the result in the SIMD&FP scalar source register. Vector elements are processed strictly in order from low to high, with the scalar source providing the initial value. Inactive elements in the source vector are ignored. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "FADDA<V><dn>,<Pg>,<V><dn>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(esize) = V{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(esize) = operand1;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand2[e*:esize];\n        result =FPAdd{esize}(result, element, FPCR());\n    end;\nend;V{esize}(dn) = result;",
    "operational_info": ""
  },
  {
    "title": "FADDP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FADDP--Floating-point-add-pairwise-?lang=en",
    "description": "Floating-point add pairwise This instruction adds pairs of adjacent floating-point elements within each source vector, and interleaves the results from corresponding lanes. The interleaved result values are destructively placed in the first source vector.",
    "syntax": [
      "FADDP<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)=ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult :bits(VL)= Z{}(dn);varelement1 :bits(esize);varelement2 :bits(esize);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenifIsEven(e) then\n            element1 = operand1[(e +0)*:esize];element2 = operand1[(e +1)*:esize];elseelement1 = operand2[(e -1)*:esize];element2 = operand2[(e +0)*:esize];end;result[e*:esize] =FPAdd{esize}(element1, element2, FPCR());end;end;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FADDQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FADDQV--Floating-point-add-recursive-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Floating-point add recursive reduction of quadword vector segments This instruction performs a floating-point addition of the same element numbers from each 128-bit source vector segment using a recursive pairwise reduction, and places each result into the corresponding element number of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as +0.0.",
    "syntax": [
      "FADDQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer{} = VL DIV128;letelempersegment : integer =128DIV esize;letsegbits : integer{} = segments*esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letidentity : bits(esize) =FPZero{esize}('0');varresult : bits(128) = Zeros{};fore =0to elempersegment-1dovarstmp : bits(segbits);fors =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) then\n            stmp[s*:esize] = operand[(s * elempersegment + e)*:esize];elsestmp[s*:esize] = identity;\n        end;\n    end;\n    result[e*:esize] =FPReduce{esize, segbits}(ReduceOp_FADD, stmp, FPCR());\nend;V{128}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FADDV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FADDV--Floating-point-add-recursive-reduction-to-scalar-?lang=en",
    "description": "Floating-point add recursive reduction to scalar This instruction adds horizontally the floating-point values over all lanes of a vector using a recursive pairwise reduction, and places the result in the SIMD&FP scalar destination register. Inactive elements in the source vector are treated as +0.0.",
    "syntax": [
      "FADDV<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letidentity : bits(esize) =FPZero{esize}('0');V{esize}(d) =FPReducePredicated{esize, VL, PL}(ReduceOp_FADD, operand, mask,\n                                                                identity, FPCR());",
    "operational_info": ""
  },
  {
    "title": "FAMAX",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FAMAX--Floating-point-absolute-maximum--predicated--?lang=en",
    "description": "Floating-point absolute maximum (predicated) This instruction determines the maximum absolute value from active floating-point elements of the first source vector and the corresponding floating-point elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Regardless of the value of FPCR.AH, the behavior is as follows: When FPCR.DN is 0, if either element is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if either element is a NaN, the result is the Default NaN, with the sign bit set to 0.Denormalized inputs and results are never flushed to zero, as if FPCR.{FZ, FZ16, FIZ} are all 0.Denormalized inputs never generate an Input Denormal floating-point exception. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FAMAX<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_FAMINMAX",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPAbsMax{esize}(element1, element2, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FAMIN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FAMIN--Floating-point-absolute-minimum--predicated--?lang=en",
    "description": "Floating-point absolute minimum (predicated) This instruction determines the minimum absolute value from active floating-point elements of the first source vector and the corresponding floating-point elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Regardless of the value of FPCR.AH, the behavior is as follows: When FPCR.DN is 0, if either element is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if either element is a NaN, the result is the Default NaN, with the sign bit set to 0.Denormalized inputs and results are never flushed to zero, as if FPCR.{FZ, FZ16, FIZ} are all 0.Denormalized inputs never generate an Input Denormal floating-point exception. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FAMIN<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_FAMINMAX",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPAbsMin{esize}(element1, element2, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FCADD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCADD--Floating-point-complex-add--predicated--?lang=en",
    "description": "Floating-point complex add (predicated) This instruction adds the real and imaginary components of the active floating-point complex numbers from the first source vector to the complex numbers from the second source vector that have first been rotated by 90 or 270 degrees in the direction from the positive real axis towards the positive imaginary axis, when considered in polar representation, equivalent to multiplying the complex numbers in the second source vector by ±j beforehand. The results are destructively placed in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified. Each complex number is represented in a vector register as an even/odd pair of elements with the real part in the even-numbered element and the imaginary part in the odd-numbered element.",
    "syntax": [
      "FCADD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letpairs : integer = VL DIV (2* esize);letmask :bits(PL)= P{}(g);letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)=ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult :bits(VL);forp =0to pairs-1do\n    var acc_r :bits(esize)= operand1[(2* p +0)*:esize];var acc_i :bits(esize)= operand1[(2* p +1)*:esize];ifActivePredicateElement{PL}(mask,2* p +0, esize) then\n        var elt2_i :bits(esize)= operand2[(2* p +1)*:esize];ifsub_ithen elt2_i =FPNeg{esize}(elt2_i, FPCR()); end;acc_r =FPAdd{esize}(acc_r, elt2_i, FPCR());end;ifActivePredicateElement{PL}(mask,2* p +1, esize) then\n        var elt2_r :bits(esize)= operand2[(2* p +0)*:esize];ifsub_rthen elt2_r =FPNeg{esize}(elt2_r, FPCR()); end;acc_i =FPAdd{esize}(acc_i, elt2_r, FPCR());end;result[(2* p +0)*:esize] = acc_r;result[(2* p +1)*:esize] = acc_i;end;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FCLAMP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCLAMP--Floating-point-clamp-to-minimum-maximum-number-?lang=en",
    "description": "Floating-point clamp to minimum/maximum number This instruction clamps each floating-point element in the destination vector to between the floating-point minimum value in the corresponding element of the first source vector and the floating-point maximum value in the corresponding element of the second source vector and destructively places the clamped results in the corresponding elements of the destination vector. Regardless of the value of FPCR.AH, the behavior is as follows for each minimum number and maximum number operation: Negative zero compares less than positive zero.If one value is numeric and the other is a quiet NaN, the result is the numeric value.When FPCR.DN is 0, if either value is a signaling NaN or if both values are NaNs, the result is a quiet NaN.When FPCR.DN is 1, if either value is a signaling NaN or if both values are NaNs, the result is Default NaN. This instruction is unpredicated.",
    "syntax": [
      "FCLAMP<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;varresult : bits(VL);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(d);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];letelement3 : bits(esize) = operand3[e*:esize];letmaxelement : bits(esize) = FPMaxNum{}(element1, element3, FPCR());\n    result[e*:esize] =FPMinNum{esize}(maxelement, element2, FPCR());\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FCM<cc> (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCM-cc---vectors---Floating-point-compare-?lang=en",
    "description": "Floating-point compare This instruction compares active floating-point elements in the first source vector with the corresponding elements in the second source vector, and places the boolean results of the specified comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. <cc>ComparisonEQequalGEgreater than or equalGTgreater thanNEnot equalUOunordered This instruction is used by the pseudo-instructionsFCMLE (vectors), andFCMLT (vectors). It has encodings from 5 classes:Equal,Greater than,Greater than or equal,Not equalandUnordered",
    "syntax": [
      "FCMUO<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];varres : boolean;casecmp_op ofwhenCmp_EQ=> res =FPCompareEQ{esize}(element1, element2, FPCR());whenCmp_GE=> res =FPCompareGE{esize}(element1, element2, FPCR());whenCmp_GT=> res =FPCompareGT{esize}(element1, element2, FPCR());whenCmp_UN=> res =FPCompareUN{esize}(element1, element2, FPCR());whenCmp_NE=> res =FPCompareNE{esize}(element1, element2, FPCR());whenCmp_LT=> res =FPCompareGT{esize}(element2, element1, FPCR());whenCmp_LE=> res =FPCompareGE{esize}(element2, element1, FPCR());\n        end;letpbit : bit =ifres then'1'else'0';\n        result[e*:psize] = ZeroExtend{psize}(pbit);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;P{PL}(d) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the predicate register written by this instruction might be significantly delayed."
  },
  {
    "title": "FCM<cc> (zero)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCM-cc---zero---Floating-point-compare-with-zero-?lang=en",
    "description": "Floating-point compare with zero This instruction compares active floating-point elements in the source vector with zero, and places the boolean results of the specified comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. <cc>ComparisonEQequalGEgreater than or equalGTgreater thanLEless than or equalLTless thanNEnot equalUOunordered It has encodings from 6 classes:Equal,Greater than,Greater than or equal,Less than,Less than or equalandNot equal",
    "syntax": [
      "FCMNE<Pd>.<T>,<Pg>/Z,<Zn>.<T>, #0.0"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];varres : boolean;caseop ofwhenCmp_EQ=> res =FPCompareEQ{esize}(element,0[esize-1:0], FPCR());whenCmp_GE=> res =FPCompareGE{esize}(element,0[esize-1:0], FPCR());whenCmp_GT=> res =FPCompareGT{esize}(element,0[esize-1:0], FPCR());whenCmp_NE=> res =FPCompareNE{esize}(element,0[esize-1:0], FPCR());whenCmp_LT=> res =FPCompareGT{esize}(0[esize-1:0], element, FPCR());whenCmp_LE=> res =FPCompareGE{esize}(0[esize-1:0], element, FPCR());\n        end;letpbit : bit =ifres then'1'else'0';\n        result[e*:psize] = ZeroExtend{psize}(pbit);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;P{PL}(d) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the predicate register written by this instruction might be significantly delayed."
  },
  {
    "title": "FCMLA (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCMLA--indexed---Floating-point-complex-multiply-add-by-indexed-element-?lang=en",
    "description": "Floating-point complex multiply-add by indexed element This instruction multiplies the duplicated real components for rotations 0 and 180, or imaginary components for rotations 90 and 270, of the floating-point complex numbers in each 128-bit segment of the first source vector by the specified complex number in the corresponding the second source vector segment rotated by 0, 90, 180 or 270 degrees in the direction from the positive real axis towards the positive imaginary axis, when considered in polar representation. This instruction then destructively adds the products to the corresponding components of the complex numbers in the addend and destination vector, without intermediate rounding. These transformations permit the creation of a variety of multiply-add and multiply-subtract operations on complex numbers by combining two of these instructions with the same vector operands but with rotations that are 90 degrees apart. Each complex number is represented in a vector register as an even/odd pair of elements with the real part in the even-numbered element and the imaginary part in the odd-numbered element. The complex numbers within the second source vector are specified using an immediate index that selects the same complex number position within each 128-bit vector segment. The index range is from 0 to one less than the number of complex numbers per 128-bit segment. This instruction is unpredicated. It has encodings from 2 classes:Half-precisionandSingle-precision",
    "syntax": [
      "FCMLA<Zda>.S,<Zn>.S,<Zm>.S[<imm>],<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letpairs : integer = VL DIV (2* esize);letpairspersegment : integer =128DIV (2* esize);letoperand1 :bits(VL)= Z{}(n);letoperand2 :bits(VL)= Z{}(m);letoperand3 :bits(VL)= Z{}(da);varresult :bits(VL);forp =0to pairs-1do\n    let segmentbase : integer = p - (p MOD pairspersegment);let s : integer = segmentbase + index;varaddend_r:bits(esize)= operand3[(2* p +0)*:esize];varaddend_i:bits(esize)= operand3[(2* p +1)*:esize];let elt1_a :bits(esize)= operand1[(2* p +sel_a)*:esize];var elt2_a :bits(esize)= operand2[(2* s +sel_a)*:esize];var elt2_b :bits(esize)= operand2[(2* s +sel_b)*:esize];ifneg_r then elt2_a =FPNeg{esize}(elt2_a, FPCR()); end;ifneg_i then elt2_b =FPNeg{esize}(elt2_b, FPCR()); end;addend_r=FPMulAdd{esize}(addend_r,elt1_a, elt2_a, FPCR());addend_i=FPMulAdd{esize}(addend_i,elt1_a, elt2_b, FPCR());result[(2* p +0)*:esize] =addend_r;result[(2* p +1)*:esize] =addend_i;end;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FCMLA (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCMLA--vectors---Floating-point-complex-multiply-add--predicated--?lang=en",
    "description": "Floating-point complex multiply-add (predicated) This instruction multiplies the duplicated real components for rotations 0 and 180, or imaginary components for rotations 90 and 270, of the floating-point complex numbers in the first source vector by the corresponding complex number in the second source vector rotated by 0, 90, 180 or 270 degrees in the direction from the positive real axis towards the positive imaginary axis, when considered in polar representation. This instruction then destructively adds the products to the corresponding components of the complex numbers in the addend and destination vector, without intermediate rounding. These transformations permit the creation of a variety of multiply-add and multiply-subtract operations on complex numbers by combining two of these instructions with the same vector operands but with rotations that are 90 degrees apart. Each complex number is represented in a vector register as an even/odd pair of elements with the real part in the even-numbered element and the imaginary part in the odd-numbered element. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FCMLA<Zda>.<T>,<Pg>/M,<Zn>.<T>,<Zm>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letpairs : integer = VL DIV (2* esize);letmask :bits(PL)= P{}(g);letop1 :bits(VL)=ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letop2 :bits(VL)=ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letop3 :bits(VL)= Z{}(da);varresult :bits(VL);forp =0to pairs-1do\n    varaddend_r:bits(esize)= op3[(2* p +0)*:esize];varaddend_i:bits(esize)= op3[(2* p +1)*:esize];ifActivePredicateElement{PL}(mask,2* p +0, esize) then\n        let elt1_a :bits(esize)= op1[(2*p +sel_a)*:esize];let elt2_a :bits(esize)= (ifneg_r thenFPNeg{esize}(op2[(2* p +sel_a)*:esize],FPCR())elseop2[(2* p +sel_a)*:esize]);addend_r=FPMulAdd{esize}(addend_r,elt1_a, elt2_a, FPCR());end;ifActivePredicateElement{PL}(mask,2* p +1, esize) then\n        let elt1_a :bits(esize)= op1[(2* p +sel_a)*:esize];let elt2_b :bits(esize)= (ifneg_i thenFPNeg{esize}(op2[(2* p +sel_b)*:esize],FPCR())elseop2[(2* p +sel_b)*:esize]);addend_i=FPMulAdd{esize}(addend_i,elt1_a, elt2_b, FPCR());end;result[(2* p +0)*:esize] =addend_r;result[(2* p +1)*:esize] =addend_i;end;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FCMLE (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCMLE--vectors---Floating-point-compare-less-than-or-equal-to-vector--an-alias-of-FCM-cc---vectors--?lang=en",
    "description": "Floating-point compare less than or equal to vector This instruction compares active floating-point elements in the first source vector being less than or equal to the corresponding elements in the second source vector, and places the boolean results of the comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. This is a pseudo-instruction ofFCM<cc> (vectors). This means: The encodings in this description are named to match the encodings ofFCM<cc> (vectors).The assembler syntax is used only for assembly, and is not used on disassembly.The description ofFCM<cc> (vectors)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "FCMLE<Pd>.<T>,<Pg>/Z,<Zm>.<T>,<Zn>.<T>\nis equivalent to\nFCMGE<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofFCM<cc> (vectors)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "FCMLT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCMLT--vectors---Floating-point-compare-less-than-vector--an-alias-of-FCM-cc---vectors--?lang=en",
    "description": "Floating-point compare less than vector This instruction compares active floating-point elements in the first source vector being less than the corresponding elements in the second source vector, and places the boolean results of the comparison in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. This is a pseudo-instruction ofFCM<cc> (vectors). This means: The encodings in this description are named to match the encodings ofFCM<cc> (vectors).The assembler syntax is used only for assembly, and is not used on disassembly.The description ofFCM<cc> (vectors)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "FCMLT<Pd>.<T>,<Pg>/Z,<Zm>.<T>,<Zn>.<T>\nis equivalent to\nFCMGT<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofFCM<cc> (vectors)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "FCPY",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCPY--Copy-floating-point-immediate-to-vector-elements--predicated--?lang=en",
    "description": "Copy floating-point immediate to vector elements (predicated) This instruction copies a floating-point immediate into each Active element in the destination vector. Inactive elements in the destination vector register remain unmodified. This instruction is used by the aliasFMOV (immediate, predicated).",
    "syntax": [
      "FCPY<Zd>.<T>,<Pg>/M, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varresult : bits(VL) = Z{}(d);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = imm;\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FCVT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCVT--Floating-point-convert--predicated--?lang=en",
    "description": "Floating-point convert (predicated) This instruction converts the size and precision of each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. Since the input and result types have a different size the smaller type is held unpacked in the least significant bits of elements of the larger size. When the input is the smaller type the upper bits of each source element are ignored. When the result is the smaller type the results are zero-extended to fill each destination element. It has encodings from 12 classes:Half-precision to single-precision, merging,Half-precision to single-precision, zeroing,Half-precision to double-precision, merging,Half-precision to double-precision, zeroing,Single-precision to half-precision, merging,Single-precision to half-precision, zeroing,Single-precision to double-precision, merging,Single-precision to double-precision, zeroing,Double-precision to half-precision, merging,Double-precision to half-precision, zeroing,Double-precision to single-precision, mergingandDouble-precision to single-precision, zeroing",
    "syntax": [
      "FCVT<Zd>.S,<Pg>/Z,<Zn>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];letres : bits(d_esize) =FPConvertSVE{d_esize, s_esize}(element[s_esize-1:0], FPCR());\n        result[e*:esize] = ZeroExtend{esize}(res);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Half-precision to single-precision, merging\" , \"Half-precision to double-precision, merging\" , \"Single-precision to half-precision, merging\" , \"Single-precision to double-precision, merging\" , \"Double-precision to half-precision, merging\" , \"Double-precision to single-precision, merging\" variants:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FCVTLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCVTLT--Floating-point-widening-convert--top--predicated--?lang=en",
    "description": "Floating-point widening convert (top, predicated) This instruction converts odd-numbered floating-point elements from the source vector to the next higher precision, and places the results in the active overlapping double-width elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 4 classes:Half-precision to single-precision, merging,Half-precision to single-precision, zeroing,Single-precision to double-precision, mergingandSingle-precision to double-precision, zeroing",
    "syntax": [
      "FCVTLT<Zd>.D,<Pg>/Z,<Zn>.S"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;lethalfesize : integer{} = esize DIV2;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(halfesize) = operand[(2*e +1)*:halfesize];\n        result[e*:esize] =FPConvertSVE{esize, halfesize}(element, FPCR());\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FCVTN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCVTN--Half-precision-convert-to-interleaved-8-bit-floating-point-?lang=en",
    "description": "Half-precision convert to interleaved 8-bit floating-point This instruction converts each half-precision element of the pair of source vectors to 8-bit floating-point while scaling the value by 2SInt(FPMR.NSCALE[4:0]), and places the two-way interleaved results in the corresponding 8-bit elements of the destination vector. The 8-bit floating-point encoding format is selected by FPMR.F8D. This instruction is unpredicated.",
    "syntax": [
      "FCVTN<Zd>.B, {<Zn1>.H-<Zn2>.H }"
    ],
    "features": [
      "FEAT_FP8",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;varresult : bits(VL);letoperand1 : bits(VL) = Z{}(n+0);letoperand2 : bits(VL) = Z{}(n+1);fore =0to elements-1doletelement1 : bits(16) = operand1[e*:16];letelement2 : bits(16) = operand2[e*:16];\n    result[(2*e +0)*:8] =FPConvertFP8{8,16}(element1, FPCR(), FPMR());\n    result[(2*e +1)*:8] =FPConvertFP8{8,16}(element2, FPCR(), FPMR());\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FCVTNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCVTNB--Single-precision-convert-to-interleaved-8-bit-floating-point--bottom--?lang=en",
    "description": "Single-precision convert to interleaved 8-bit floating-point (bottom) This instruction converts each single-precision element of the pair of source vectors to 8-bit floating-point while scaling the value by 2SInt(FPMR.NSCALE), and places the two-way interleaved results in the corresponding even-numbered 8-bit elements of the destination vector, zeroing the odd-numbered elements. The 8-bit floating-point encoding format is selected by FPMR.F8D. This instruction is unpredicated.",
    "syntax": [
      "FCVTNB<Zd>.B, {<Zn1>.S-<Zn2>.S }"
    ],
    "features": [
      "FEAT_FP8",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;varresult : bits(VL);letoperand1 : bits(VL) = Z{}(n+0);letoperand2 : bits(VL) = Z{}(n+1);fore =0to elements-1doletelement1 : bits(32) = operand1[e*:32];letelement2 : bits(32) = operand2[e*:32];letres1 : bits(8) =FPConvertFP8{8,32}(element1, FPCR(), FPMR());letres2 : bits(8) =FPConvertFP8{8,32}(element2, FPCR(), FPMR());\n    result[(2*e +0)*:16] = ZeroExtend{16}(res1);\n    result[(2*e +1)*:16] = ZeroExtend{16}(res2);\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FCVTNT (predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCVTNT--predicated---Floating-point-narrowing-convert--top--predicated--?lang=en",
    "description": "Floating-point narrowing convert (top, predicated) This instruction converts active floating-point elements from the source vector to the next lower precision, and places the results in the odd-numbered half-width elements of the destination vector, leaving the even-numbered elements unchanged. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 4 classes:Single-precision to half-precision, merging,Single-precision to half-precision, zeroing,Double-precision to single-precision, mergingandDouble-precision to single-precision, zeroing",
    "syntax": [
      "FCVTNT<Zd>.S,<Pg>/Z,<Zn>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;lethalfesize : integer{} = esize DIV2;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) = Z{}(d);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[(2*e +1)*:halfesize] =FPConvertSVE{halfesize, esize}(element, FPCR());\n\n    elsif !merging then\n        result[(2*e +1)*:halfesize] = Zeros{halfesize};\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FCVTNT (unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCVTNT--unpredicated---Single-precision-convert-to-interleaved-8-bit-floating-point--top--?lang=en",
    "description": "Single-precision convert to interleaved 8-bit floating-point (top) This instruction converts each single-precision element of the pair of source vectors to 8-bit floating-point while scaling the value by 2SInt(FPMR.NSCALE), and places the two-way interleaved results in the corresponding odd-numbered 8-bit elements of the destination vector, leaving the even-numbered elements unchanged. The 8-bit floating-point encoding format is selected by FPMR.F8D. This instruction is unpredicated.",
    "syntax": [
      "FCVTNT<Zd>.B, {<Zn1>.S-<Zn2>.S }"
    ],
    "features": [
      "FEAT_FP8",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;varresult : bits(VL) = Z{}(d);letoperand1 : bits(VL) = Z{}(n+0);letoperand2 : bits(VL) = Z{}(n+1);fore =0to elements-1doletelement1 : bits(32) = operand1[e*:32];letelement2 : bits(32) = operand2[e*:32];\n    result[(4*e +1)*:8] =FPConvertFP8{8,32}(element1, FPCR(), FPMR());\n    result[(4*e +3)*:8] =FPConvertFP8{8,32}(element2, FPCR(), FPMR());\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FCVTX",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCVTX--Double-precision-convert-to-single-precision--rounding-to-odd--predicated--?lang=en",
    "description": "Double-precision convert to single-precision, rounding to odd (predicated) This instruction converts active double-precision elements from the source vector to single-precision, rounding to Odd, and places the results in the even-numbered 32-bit elements of the destination vector, while setting the odd-numbered elements to zero. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. Rounding to Odd (aka Von Neumann rounding) permits a two-step conversion from double-precision to half-precision without incurring intermediate rounding errors. It has encodings from 2 classes:Double-precision to single-precision, mergingandDouble-precision to single-precision, zeroing",
    "syntax": [
      "FCVTX<Zd>.S,<Pg>/Z,<Zn>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];letres : bits(d_esize) =FPConvertSVE{d_esize, s_esize}(element[s_esize-1:0], FPCR(),FPRounding_ODD);\n        result[e*:esize] = ZeroExtend{esize}(res);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Double-precision to single-precision, merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FCVTXNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCVTXNT--Double-precision-convert-to-single-precision--rounding-to-odd--top--predicated--?lang=en",
    "description": "Double-precision convert to single-precision, rounding to odd (top, predicated) This instruction converts active double-precision elements from the source vector to single-precision, rounding to Odd, and places the results in the odd-numbered 32-bit elements of the destination vector, leaving the even-numbered elements unchanged. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. Rounding to Odd (aka Von Neumann rounding) permits a two-step conversion from double-precision to half-precision without incurring intermediate rounding errors. It has encodings from 2 classes:Double-precision to single-precision, mergingandDouble-precision to single-precision, zeroing",
    "syntax": [
      "FCVTXNT<Zd>.S,<Pg>/Z,<Zn>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;lethalfesize : integer{} = esize DIV2;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) = Z{}(d);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[(2*e +1)*:halfesize] =FPConvertSVE{halfesize, esize}(element, FPCR(),FPRounding_ODD);\n\n    elsif !merging then\n        result[(2*e +1)*:halfesize] = Zeros{halfesize};\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FCVTZS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCVTZS--Floating-point-convert-to-signed-integer--rounding-toward-zero--predicated--?lang=en",
    "description": "Floating-point convert to signed integer, rounding toward zero (predicated) This instruction converts to the signed integer nearer to zero from each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. If the input and result types have a different size the smaller type is held unpacked in the least significant bits of elements of the larger size. When the input is the smaller type the upper bits of each source element are ignored. When the result is the smaller type the results are sign-extended to fill each destination element. It has encodings from 14 classes:Half-precision to 16-bit, merging,Half-precision to 16-bit, zeroing,Half-precision to 32-bit, merging,Half-precision to 32-bit, zeroing,Half-precision to 64-bit, merging,Half-precision to 64-bit, zeroing,Single-precision to 32-bit, merging,Single-precision to 32-bit, zeroing,Single-precision to 64-bit, merging,Single-precision to 64-bit, zeroing,Double-precision to 32-bit, merging,Double-precision to 32-bit, zeroing,Double-precision to 64-bit, mergingandDouble-precision to 64-bit, zeroing",
    "syntax": [
      "FCVTZS<Zd>.D,<Pg>/Z,<Zn>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];letres : bits(d_esize) =FPToFixed{d_esize, s_esize}(element[s_esize-1:0],0, unsigned,\n                                                              FPCR(), rounding);\n        result[e*:esize] = Extend{esize}(res, unsigned);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Half-precision to 16-bit, merging\" , \"Half-precision to 32-bit, merging\" , \"Half-precision to 64-bit, merging\" , \"Single-precision to 32-bit, merging\" , \"Single-precision to 64-bit, merging\" , \"Double-precision to 32-bit, merging\" , \"Double-precision to 64-bit, merging\" variants:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FCVTZSN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCVTZSN--Floating-point-narrowing-convert-to-interleaved-signed-integer--rounding-toward-zero-?lang=en",
    "description": "Floating-point narrowing convert to interleaved signed integer, rounding toward zero This instruction converts each floating-point element of the pair of source vectors to half-width signed integer nearer to zero, and places the two-way interleaved results in the corresponding half-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "FCVTZSN<Zd>.<T>, {<Zn1>.<Tb>-<Zn2>.<Tb>}"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n+0);letoperand2 : bits(VL) = Z{}(n+1);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[(2*e +0)*:(esize DIV2)] =FPToFixed{esize DIV2, esize}(element1,0, unsigned, FPCR(),\n                                                   rounding);\n    result[(2*e +1)*:(esize DIV2)] =FPToFixed{esize DIV2, esize}(element2,0, unsigned, FPCR(),\n                                                   rounding);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FCVTZU",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCVTZU--Floating-point-convert-to-unsigned-integer--rounding-toward-zero--predicated--?lang=en",
    "description": "Floating-point convert to unsigned integer, rounding toward zero (predicated) This instruction converts to the unsigned integer nearer to zero from each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. If the input and result types have a different size the smaller type is held unpacked in the least significant bits of elements of the larger size. When the input is the smaller type the upper bits of each source element are ignored. When the result is the smaller type the results are zero-extended to fill each destination element. It has encodings from 14 classes:Half-precision to 16-bit, merging,Half-precision to 16-bit, zeroing,Half-precision to 32-bit, merging,Half-precision to 32-bit, zeroing,Half-precision to 64-bit, merging,Half-precision to 64-bit, zeroing,Single-precision to 32-bit, merging,Single-precision to 32-bit, zeroing,Single-precision to 64-bit, merging,Single-precision to 64-bit, zeroing,Double-precision to 32-bit, merging,Double-precision to 32-bit, zeroing,Double-precision to 64-bit, mergingandDouble-precision to 64-bit, zeroing",
    "syntax": [
      "FCVTZU<Zd>.D,<Pg>/Z,<Zn>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];letres : bits(d_esize) =FPToFixed{d_esize, s_esize}(element[s_esize-1:0],0, unsigned,\n                                                              FPCR(), rounding);\n        result[e*:esize] = Extend{esize}(res, unsigned);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Half-precision to 16-bit, merging\" , \"Half-precision to 32-bit, merging\" , \"Half-precision to 64-bit, merging\" , \"Single-precision to 32-bit, merging\" , \"Single-precision to 64-bit, merging\" , \"Double-precision to 32-bit, merging\" , \"Double-precision to 64-bit, merging\" variants:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FCVTZUN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FCVTZUN--Floating-point-narrowing-convert-to-interleaved-unsigned-integer--rounding-toward-zero-?lang=en",
    "description": "Floating-point narrowing convert to interleaved unsigned integer, rounding toward zero This instruction converts each floating-point element of the pair of source vectors to half-width unsigned integer nearer to zero, and places the two-way interleaved results in the corresponding half-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "FCVTZUN<Zd>.<T>, {<Zn1>.<Tb>-<Zn2>.<Tb>}"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n+0);letoperand2 : bits(VL) = Z{}(n+1);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[(2*e +0)*:(esize DIV2)] =FPToFixed{esize DIV2, esize}(element1,0, unsigned, FPCR(),\n                                                   rounding);\n    result[(2*e +1)*:(esize DIV2)] =FPToFixed{esize DIV2, esize}(element2,0, unsigned, FPCR(),\n                                                   rounding);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FDIV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FDIV--Floating-point-divide--predicated--?lang=en",
    "description": "Floating-point divide (predicated) This instruction divides active floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector and destructively places the quotient in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FDIV<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPDiv{esize}(element1, element2, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FDIVR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FDIVR--Floating-point-reversed-divide--predicated--?lang=en",
    "description": "Floating-point reversed divide (predicated) This instruction performs a reversed divide on active floating-point elements of the second source vector by the corresponding floating-point elements of the first source vector, and destructively places the quotient in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FDIVR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPDiv{esize}(element2, element1, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FDOT (2-way, indexed, FP16 to FP32)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FDOT--2-way--indexed--FP16-to-FP32---Half-precision-dot-product-by-indexed-element-to-single-precision-?lang=en",
    "description": "Half-precision dot product by indexed element to single-precision This instruction calculates the fused sum-of-products of a pair of half-precision values held in each 32-bit element of the first source vector and a pair of half-precision values in an indexed 32-bit element of the second source vector, without intermediate rounding, and then destructively adds the single-precision sum-of-products to the corresponding single-precision element of the destination vector. The half-precision pairs within the second source vector are specified using an immediate index that selects the same pair position within each 128-bit vector segment. The index range is from 0 to 3. This instruction is unpredicated.",
    "syntax": [
      "FDOT<Zda>.S,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;leteltspersegment : integer =128DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelt1_a : bits(16) = operand1[(2* e +0)*:16];letelt1_b : bits(16) = operand1[(2* e +1)*:16];letelt2_a : bits(16) = operand2[(2* s +0)*:16];letelt2_b : bits(16) = operand2[(2* s +1)*:16];varsum : bits(32) = operand3[e*:32];\n\n    sum =FPDotAdd(sum, elt1_a, elt1_b, elt2_a, elt2_b, FPCR());\n    result[e*:32] = sum;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FDOT (2-way, indexed, FP8 to FP16)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FDOT--2-way--indexed--FP8-to-FP16---8-bit-floating-point-dot-product-by-indexed-element-to-half-precision-?lang=en",
    "description": "8-bit floating-point dot product by indexed element to half-precision This instruction calculates the fused sum-of-products of a pair of 8-bit floating-point values held in each 16-bit element of the first source vector and a pair of 8-bit floating-point values in an indexed 16-bit element of the second source vector. The half-precision sum-of-products are scaled by 2-UInt(FPMR.LSCALE[3:0]), before being destructively added without intermediate rounding to the corresponding half-precision elements of the destination vector. The 8-bit floating-point groups within the second source vector are specified using an immediate index that selects the same group position within each 128-bit vector segment. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FDOT<Zda>.H,<Zn>.B,<Zm>.B[<imm>]"
    ],
    "features": [
      "FEAT_FP8DOT2",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8DOT2) &&IsFeatureImplemented(FEAT_FP8DOT2) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8DOT2) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;leteltspersegment : integer =128DIV16;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letop1 : bits(16) = operand1[e*:(16)];letop2 : bits(16) = operand2[s*:(16)];varsum : bits(16)  = operand3[e*:(16)];\n\n    sum =FP8DotAddFP{16,16}(sum, op1, op2, FPCR(), FPMR());\n    result[e*:(16)] = sum;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FDOT (2-way, vectors, FP16 to FP32)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FDOT--2-way--vectors--FP16-to-FP32---Half-precision-dot-product-to-single-precision-?lang=en",
    "description": "Half-precision dot product to single-precision This instruction calculates the fused sum-of-products of a pair of half-precision values held in each 32-bit element of the first source and second source vectors, without intermediate rounding, and then destructively adds the single-precision sum-of-products to the corresponding single-precision element of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "FDOT<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelt1_a : bits(16) = operand1[(2* e +0)*:16];letelt1_b : bits(16) = operand1[(2* e +1)*:16];letelt2_a : bits(16) = operand2[(2* e +0)*:16];letelt2_b : bits(16) = operand2[(2* e +1)*:16];varsum : bits(32) = operand3[e*:32];\n\n    sum =FPDotAdd(sum, elt1_a, elt1_b, elt2_a, elt2_b, FPCR());\n    result[e*:32] = sum;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FDOT (2-way, vectors, FP8 to FP16)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FDOT--2-way--vectors--FP8-to-FP16---8-bit-floating-point-dot-product-to-half-precision-?lang=en",
    "description": "8-bit floating-point dot product to half-precision This instruction calculates the fused sum-of-products of a pair of 8-bit floating-point values held in each 16-bit element of the first source and second source vectors. The half-precision sum-of-products are scaled by 2-UInt(FPMR.LSCALE[3:0]), before being destructively added without intermediate rounding to the corresponding half-precision elements of the destination vector. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FDOT<Zda>.H,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_FP8DOT2",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8DOT2) &&IsFeatureImplemented(FEAT_FP8DOT2) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8DOT2) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletop1 : bits(16) = operand1[e*:(16)];letop2 : bits(16) = operand2[e*:(16)];varsum : bits(16)  = operand3[e*:(16)];\n\n    sum =FP8DotAddFP{16,16}(sum, op1, op2, FPCR(), FPMR());\n    result[e*:(16)] = sum;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FDOT (4-way, indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FDOT--4-way--indexed---8-bit-floating-point-dot-product-by-indexed-element-to-single-precision-?lang=en",
    "description": "8-bit floating-point dot product by indexed element to single-precision This instruction calculates the fused sum-of-products of a group of four 8-bit floating-point values held in each 32-bit element of the first source vector and a group of four 8-bit floating-point values in an indexed 32-bit element of the second source vector. The single-precision sum-of-products are scaled by 2-UInt(FPMR.LSCALE), before being destructively added without intermediate rounding to the corresponding single-precision elements of the destination vector. The 8-bit floating-point groups within the second source vector are specified using an immediate index that selects the same group position within each 128-bit vector segment. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FDOT<Zda>.S,<Zn>.B,<Zm>.B[<imm>]"
    ],
    "features": [
      "FEAT_FP8DOT4",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8DOT4) &&IsFeatureImplemented(FEAT_FP8DOT4) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8DOT4) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;leteltspersegment : integer =128DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letop1 : bits(32) = operand1[e*:(32)];letop2 : bits(32) = operand2[s*:(32)];varsum : bits(32)  = operand3[e*:(32)];\n\n    sum =FP8DotAddFP{32,32}(sum, op1, op2, FPCR(), FPMR());\n    result[e*:(32)] = sum;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FDOT (4-way, vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FDOT--4-way--vectors---8-bit-floating-point-dot-product-to-single-precision-?lang=en",
    "description": "8-bit floating-point dot product to single-precision This instruction calculates the fused sum-of-products of a group of four 8-bit floating-point values held in each 32-bit element of the first source and second source vectors. The single-precision sum-of-products are scaled by 2-UInt(FPMR.LSCALE), before being destructively added without intermediate rounding to the corresponding single-precision elements of the destination vector. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FDOT<Zda>.S,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_FP8DOT4",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8DOT4) &&IsFeatureImplemented(FEAT_FP8DOT4) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8DOT4) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletop1 : bits(32) = operand1[e*:(32)];letop2 : bits(32) = operand2[e*:(32)];varsum : bits(32)  = operand3[e*:(32)];\n\n    sum =FP8DotAddFP{32,32}(sum, op1, op2, FPCR(), FPMR());\n    result[e*:(32)] = sum;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FDUP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FDUP--Broadcast-floating-point-immediate-to-vector-elements--unpredicated--?lang=en",
    "description": "Broadcast floating-point immediate to vector elements (unpredicated) This instruction unconditionally broadcasts the floating-point immediate into each element of the destination vector. This instruction is unpredicated. This instruction is used by the aliasFMOV (immediate, unpredicated).",
    "syntax": [
      "FDUP<Zd>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;varresult : bits(VL);fore =0to elements-1doresult[e*:esize] = imm;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FEXPA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FEXPA--Floating-point-exponential-accelerator-?lang=en",
    "description": "Floating-point exponential accelerator This instruction calculates an exponentiation acceleration operation on each floating-point element in the source vector, where the result sign is zero, the result exponent field is copied from a set of significant bits of the input fraction, and the result fraction is inserted from a lookup table indexed by the least-significant input fraction bits, and returns each result in the corresponding element of the destination vector. This instruction is fully defined by its bit-manipulation semantics, does not generate floating-point exceptions, and does not guarantee NaN propagation. For double-precision variants, the result element exponent is copied from the source element bits<16:6>, and the result fraction is set based on the source element to the rounded value of 252× (2bits<5:0>/64- 1). For single-precision variants, the result element exponent is copied from the source element bits<13:6>, and the result fraction is set based on the source element to the rounded value of 223× (2bits<5:0>/64- 1). For half-precision variants, the result element exponent is copied from the source element bits<9:5>, and the result fraction is set based on the source element to the rounded value of 210× (2bits<4:0>/32- 1). This instruction is unpredicated. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled, or FEAT_SSVE_FEXPA is implemented. NoteFor a double-precision input value x in the range 70,368,744,177,655 <= x < 70,368,744,179,711, the operation performed by this instruction is equivalent to calculating 2x-70,368,744,178,687.For a single-precision input value x in the range 131,073 <= x < 131,327, the operation performed by this instruction is equivalent to calculating 2x-131,199.For a half-precision input value x in the range 33 <= x < 63, the operation performed by this instruction is equivalent to calculating 2x-47.",
    "syntax": [
      "FEXPA<Zd>.<T>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_FEXPA) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize) = operand[e*:esize];\n    result[e*:esize] =FPExpA{esize}(element);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FIRSTP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FIRSTP--Scalar-index-of-first-true-predicate-element--predicated--?lang=en",
    "description": "Scalar index of first true predicate element (predicated) This instruction determines the index of the first active and true element of the source predicate, and places the scalar result in the destination general-purpose register. If there are no active and true elements, the destination general-purpose register is updated to the value minus one.",
    "syntax": [
      "FIRSTP<Xd>,<Pg>,<Pn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(PL) = P{}(n);varindex : integer =-1;fore =0to elements-1doif(index ==-1&&ActivePredicateElement{PL}(mask, e, esize) &&ActivePredicateElement{PL}(operand, e, esize)) then\n        index = e;\n    end;\nend;X{64}(d) = index[63:0];",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "FLOGB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FLOGB--Floating-point-base-2-logarithm-as-integer-?lang=en",
    "description": "Floating-point base 2 logarithm as integer This instruction returns the signed integer base 2 logarithm of each floating-point input element |x| after normalization. This is the unbiased exponent of x used in the representation of the floating-point value, such that, for positive x, x = significand × 2exponent. The integer results are placed in elements of the destination vector that have the same width (esize) as the floating-point input elements: If x is normal, the result is the base 2 logarithm of x.If x is subnormal, the result corresponds to the normalized representation.If x is infinite, the result is 2(esize-1)-1.If x is ±0.0 or NaN, the result is -2(esize-1). Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "FLOGB<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =FPLogB{esize}(element, FPCR());\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FMAD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMAD--Floating-point-fused-multiply-add-to-multiplicand--predicated--?lang=en",
    "description": "Floating-point fused multiply-add to multiplicand (predicated) This instruction multiplies the active floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector, and adds the result to elements of the third (addend) vector without intermediate rounding. The results are destructively placed in the destination and first source (multiplicand) vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMAD<Zdn>.<T>,<Pg>/M,<Zm>.<T>,<Za>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letop1 : bits(VL) = Z{}(dn);letop2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letop3 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(a)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelem1 : bits(esize) = (ifop1_neg thenFPNeg{esize}(op1[e*:esize], FPCR())elseop1[e*:esize]);letelem2 : bits(esize) = op2[e*:esize];letelem3 : bits(esize) = (ifop3_neg thenFPNeg{esize}(op3[e*:esize], FPCR())elseop3[e*:esize]);\n\n        result[e*:esize] =FPMulAdd{esize}(elem3, elem1, elem2, FPCR());elseresult[e*:esize] = op1[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMAX (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMAX--immediate---Floating-point-maximum-with-immediate--predicated--?lang=en",
    "description": "Floating-point maximum with immediate (predicated) This instruction determines the maximum of an immediate and each active floating-point element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate may take the value +0.0 or +1.0 only. When FPCR.AH is 0, the behavior is as follows: Negative zero compares less than positive zero.When FPCR.DN is 0, if the element is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if the element is a NaN, the result is Default NaN. When FPCR.AH is 1, the behavior is as follows: If both the element and the immediate are zeros, regardless of the sign of either zero, the result is the immediate.If the element is a NaN, regardless of the value of FPCR.DN, the result is the immediate. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMAX<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =FPMax{esize}(element1, imm, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMAX (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMAX--vectors---Floating-point-maximum--predicated--?lang=en",
    "description": "Floating-point maximum (predicated) This instruction determines the maximum of active floating-point elements of the first source vector and the corresponding floating-point elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. When FPCR.AH is 0, the behavior is as follows: Negative zero compares less than positive zero.When FPCR.DN is 0, if either element is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if either element is a NaN, the result is Default NaN. When FPCR.AH is 1, the behavior is as follows: If both elements are zeros, regardless of the sign of either zero, the result is the second element.If either element is a NaN, regardless of the value of FPCR.DN, the result is the second element. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMAX<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPMax{esize}(element1, element2, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMAXNM (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMAXNM--immediate---Floating-point-maximum-number-with-immediate--predicated--?lang=en",
    "description": "Floating-point maximum number with immediate (predicated) This instruction determines the maximum number value of an immediate and each active floating-point element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate may take the value +0.0 or +1.0 only. Regardless of the value of FPCR.AH, the behavior is as follows: Negative zero compares less than positive zero.If the element is a quiet NaN, the result is the immediate value.When FPCR.DN is 0, if the element is a signaling NaN, the result is a quiet NaN.When FPCR.DN is 1, if the element is a signaling NaN, the result is Default NaN. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMAXNM<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =FPMaxNum{esize}(element1, imm, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMAXNM (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMAXNM--vectors---Floating-point-maximum-number--predicated--?lang=en",
    "description": "Floating-point maximum number (predicated) This instruction determines the maximum number value of active floating-point elements of the first source vector and the corresponding floating-point elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Regardless of the value of FPCR.AH, the behavior is as follows: Negative zero compares less than positive zero.If one element is numeric and the other is a quiet NaN, the result is the numeric value.When FPCR.DN is 0, if either element is a signaling NaN or if both elements are NaNs, the result is a quiet NaN.When FPCR.DN is 1, if either element is a signaling NaN or if both elements are NaNs, the result is Default NaN. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMAXNM<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPMaxNum{esize}(element1, element2, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMAXNMP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMAXNMP--Floating-point-maximum-number-pairwise-?lang=en",
    "description": "Floating-point maximum number pairwise This instruction calculates the maximum value of each pair of adjacent floating-point elements within each source vector, and interleaves the results from corresponding lanes. The interleaved result values are destructively placed in the first source vector. Regardless of the value of FPCR.AH, the behavior is as follows for each pairwise operation: Negative zero compares less than positive zero.If one element is numeric and the other is a quiet NaN, the result is the numeric value.When FPCR.DN is 0, if either element is a signaling NaN or if both elements are NaNs, the result is a quiet NaN.When FPCR.DN is 1, if either element is a signaling NaN or if both elements are NaNs, the result is Default NaN.",
    "syntax": [
      "FMAXNMP<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)=ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult :bits(VL)= Z{}(dn);varelement1 :bits(esize);varelement2 :bits(esize);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenifIsEven(e) then\n            element1 = operand1[(e +0)*:esize];element2 = operand1[(e +1)*:esize];elseelement1 = operand2[(e -1)*:esize];element2 = operand2[(e +0)*:esize];end;result[e*:esize] =FPMaxNum{esize}(element1, element2, FPCR());end;end;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMAXNMQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMAXNMQV--Floating-point-maximum-number-recursive-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Floating-point maximum number recursive reduction of quadword vector segments This instruction calculates a floating-point maximum number of the same element numbers from each 128-bit source vector segment using a recursive pairwise reduction, and places each result into the corresponding element number of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as the default NaN. Regardless of the value of FPCR.AH, the behavior is as follows: Negative zero compares less than positive zero.If one value is numeric and the other is a quiet NaN, the result is the numeric value.When FPCR.DN is 0, if either value is a signaling NaN or if both values are NaNs, the result is a quiet NaN.When FPCR.DN is 1, if either value is a signaling NaN or if both values are NaNs, the result is Default NaN.",
    "syntax": [
      "FMAXNMQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer{} = VL DIV128;letelempersegment : integer =128DIV esize;letsegbits : integer{} = segments*esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letidentity : bits(esize) =FPDefaultNaN{esize}(FPCR());varresult : bits(128) = Zeros{};fore =0to elempersegment-1dovarstmp : bits(segbits);fors =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) then\n            stmp[s*:esize] = operand[(s * elempersegment + e)*:esize];elsestmp[s*:esize] = identity;\n        end;\n    end;\n    result[e*:esize] =FPReduce{esize, segbits}(ReduceOp_FMAXNUM, stmp, FPCR());\nend;V{128}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FMAXNMV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMAXNMV--Floating-point-maximum-number-recursive-reduction-to-scalar-?lang=en",
    "description": "Floating-point maximum number recursive reduction to scalar This instruction calculates a floating-point maximum number horizontally over all lanes of a vector using a recursive pairwise reduction, and places the result in the SIMD&FP scalar destination register. Inactive elements in the source vector are treated as the default NaN. Regardless of the value of FPCR.AH, the behavior is as follows: Negative zero compares less than positive zero.If one value is numeric and the other is a quiet NaN, the result is the numeric value.When FPCR.DN is 0, if either value is a signaling NaN or if both values are NaNs, the result is a quiet NaN.When FPCR.DN is 1, if either value is a signaling NaN or if both values are NaNs, the result is Default NaN.",
    "syntax": [
      "FMAXNMV<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letidentity : bits(esize) =FPDefaultNaN{esize}(FPCR());V{esize}(d) =FPReducePredicated{esize, VL, PL}(ReduceOp_FMAXNUM, operand, mask,\n                                                                identity, FPCR());",
    "operational_info": ""
  },
  {
    "title": "FMAXP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMAXP--Floating-point-maximum-pairwise-?lang=en",
    "description": "Floating-point maximum pairwise This instruction calculates the maximum value of each pair of adjacent floating-point elements within each source vector, and interleaves the results from corresponding lanes. The interleaved result values are destructively placed in the first source vector. When FPCR.AH is 0, the behavior is as follows for each pairwise operation: Negative zero compares less than positive zero.When FPCR.DN is 0, if either element is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if either element is a NaN, the result is Default NaN. When FPCR.AH is 1, the behavior is as follows for each pairwise operation: If both elements are zeros, regardless of the sign of either zero, the result is the second element.If either element is a NaN, regardless of the value of FPCR.DN, the result is the second element.",
    "syntax": [
      "FMAXP<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)=ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult :bits(VL)= Z{}(dn);varelement1 :bits(esize);varelement2 :bits(esize);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenifIsEven(e) then\n            element1 = operand1[(e +0)*:esize];element2 = operand1[(e +1)*:esize];elseelement1 = operand2[(e -1)*:esize];element2 = operand2[(e +0)*:esize];end;result[e*:esize] =FPMax{esize}(element1, element2, FPCR());end;end;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMAXQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMAXQV--Floating-point-maximum-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Floating-point maximum reduction of quadword vector segments This instruction calculates a floating-point maximum of the same element numbers from each 128-bit source vector segment using a recursive pairwise reduction, and places each result into the corresponding element number of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as -Infinity. When FPCR.AH is 0, the behavior is as follows: Negative zero compares less than positive zero.When FPCR.DN is 0, if either value is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if either value is a NaN, the result is Default NaN. When FPCR.AH is 1, the behavior is as follows: If both values are zeros, regardless of the sign of either zero, the result is the second value.If either value is a NaN, regardless of the value of FPCR.DN, the result is the second value.",
    "syntax": [
      "FMAXQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer{} = VL DIV128;letelempersegment : integer =128DIV esize;letsegbits : integer{} = segments*esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letidentity : bits(esize) =FPInfinity{esize}('1');varresult : bits(128) = Zeros{};fore =0to elempersegment-1dovarstmp : bits(segbits);fors =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) then\n            stmp[s*:esize] = operand[(s * elempersegment + e)*:esize];elsestmp[s*:esize] = identity;\n        end;\n    end;\n    result[e*:esize] =FPReduce{esize, segbits}(ReduceOp_FMAX, stmp, FPCR());\nend;V{128}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FMAXV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMAXV--Floating-point-maximum-recursive-reduction-to-scalar-?lang=en",
    "description": "Floating-point maximum recursive reduction to scalar This instruction calculates a floating-point maximum horizontally over all lanes of a vector using a recursive pairwise reduction, and places the result in the SIMD&FP scalar destination register. Inactive elements in the source vector are treated as -Infinity. When FPCR.AH is 0, the behavior is as follows: Negative zero compares less than positive zero.When FPCR.DN is 0, if either value is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if either value is a NaN, the result is Default NaN. When FPCR.AH is 1, the behavior is as follows: If both values are zeros, regardless of the sign of either zero, the result is the second value.If either value is a NaN, regardless of the value of FPCR.DN, the result is the second value.",
    "syntax": [
      "FMAXV<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letidentity : bits(esize) =FPInfinity{esize}('1');V{esize}(d) =FPReducePredicated{esize, VL, PL}(ReduceOp_FMAX, operand, mask,\n                                                                identity, FPCR());",
    "operational_info": ""
  },
  {
    "title": "FMIN (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMIN--immediate---Floating-point-minimum-with-immediate--predicated--?lang=en",
    "description": "Floating-point minimum with immediate (predicated) This instruction determines the minimum of an immediate and each active floating-point element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate may take the value +0.0 or +1.0 only. When FPCR.AH is 0, the behavior is as follows: Negative zero compares less than positive zero.When FPCR.DN is 0, if the element is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if the element is a NaN, the result is Default NaN. When FPCR.AH is 1, the behavior is as follows: If both the element and the immediate are zeros, regardless of the sign of either zero, the result is the immediate.If the element is a NaN, regardless of the value of FPCR.DN, the result is the immediate. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMIN<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =FPMin{esize}(element1, imm, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMIN (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMIN--vectors---Floating-point-minimum--predicated--?lang=en",
    "description": "Floating-point minimum (predicated) This instruction determines the minimum of active floating-point elements of the first source vector and the corresponding floating-point elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. When FPCR.AH is 0, the behavior is as follows: Negative zero compares less than positive zero.When FPCR.DN is 0, if either element is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if either element is a NaN, the result is Default NaN. When FPCR.AH is 1, the behavior is as follows: If both elements are zeros, regardless of the sign of either zero, the result is the second element.If either element is a NaN, regardless of the value of FPCR.DN, the result is the second element. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMIN<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPMin{esize}(element1, element2, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMINNM (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMINNM--immediate---Floating-point-minimum-number-with-immediate--predicated--?lang=en",
    "description": "Floating-point minimum number with immediate (predicated) This instruction determines the minimum number value of an immediate and each active floating-point element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate may take the value +0.0 or +1.0 only. Regardless of the value of FPCR.AH, the behavior is as follows: Negative zero compares less than positive zero.If the element is a quiet NaN, the result is the immediate value.When FPCR.DN is 0, if the element is a signaling NaN, the result is a quiet NaN.When FPCR.DN is 1, if the element is a signaling NaN, the result is Default NaN. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMINNM<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =FPMinNum{esize}(element1, imm, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMINNM (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMINNM--vectors---Floating-point-minimum-number--predicated--?lang=en",
    "description": "Floating-point minimum number (predicated) This instruction determines the minimum number value of active floating-point elements of the first source vector and the corresponding floating-point elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Regardless of the value of FPCR.AH, the behavior is as follows: Negative zero compares less than positive zero.If one element is numeric and the other is a quiet NaN, the result is the numeric value.When FPCR.DN is 0, if either element is a signaling NaN or if both elements are NaNs, the result is a quiet NaN.When FPCR.DN is 1, if either element is a signaling NaN or if both elements are NaNs, the result is Default NaN. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMINNM<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPMinNum{esize}(element1, element2, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMINNMP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMINNMP--Floating-point-minimum-number-pairwise-?lang=en",
    "description": "Floating-point minimum number pairwise This instruction calculates the minimum value of each pair of adjacent floating-point elements within each source vector, and interleaves the results from corresponding lanes. The interleaved result values are destructively placed in the first source vector. Regardless of the value of FPCR.AH, the behavior is as follows for each pairwise operation: Negative zero compares less than positive zero.If one element is numeric and the other is a quiet NaN, the result is the numeric value.When FPCR.DN is 0, if either element is a signaling NaN or if both elements are NaNs, the result is a quiet NaN.When FPCR.DN is 1, if either element is a signaling NaN or if both elements are NaNs, the result is Default NaN.",
    "syntax": [
      "FMINNMP<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)=ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult :bits(VL)= Z{}(dn);varelement1 :bits(esize);varelement2 :bits(esize);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenifIsEven(e) then\n            element1 = operand1[(e +0)*:esize];element2 = operand1[(e +1)*:esize];elseelement1 = operand2[(e -1)*:esize];element2 = operand2[(e +0)*:esize];end;result[e*:esize] =FPMinNum{esize}(element1, element2, FPCR());end;end;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMINNMQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMINNMQV--Floating-point-minimum-number-recursive-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Floating-point minimum number recursive reduction of quadword vector segments This instruction calculates a floating-point minimum number of the same element numbers from each 128-bit source vector segment using a recursive pairwise reduction, and places each result into the corresponding element number of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as the default NaN. Regardless of the value of FPCR.AH, the behavior is as follows: Negative zero compares less than positive zero.If one value is numeric and the other is a quiet NaN, the result is the numeric value.When FPCR.DN is 0, if either value is a signaling NaN or if both values are NaNs, the result is a quiet NaN.When FPCR.DN is 1, if either value is a signaling NaN or if both values are NaNs, the result is Default NaN.",
    "syntax": [
      "FMINNMQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer{} = VL DIV128;letelempersegment : integer =128DIV esize;letsegbits : integer{} = segments*esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letidentity : bits(esize) =FPDefaultNaN{esize}(FPCR());varresult : bits(128) = Zeros{};fore =0to elempersegment-1dovarstmp : bits(segbits);fors =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) then\n            stmp[s*:esize] = operand[(s * elempersegment + e)*:esize];elsestmp[s*:esize] = identity;\n        end;\n    end;\n    result[e*:esize] =FPReduce{esize, segbits}(ReduceOp_FMINNUM, stmp, FPCR());\nend;V{128}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FMINNMV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMINNMV--Floating-point-minimum-number-recursive-reduction-to-scalar-?lang=en",
    "description": "Floating-point minimum number recursive reduction to scalar This instruction calculates a floating-point minimum number horizontally over all lanes of a vector using a recursive pairwise reduction, and places the result in the SIMD&FP scalar destination register. Inactive elements in the source vector are treated as the default NaN. Regardless of the value of FPCR.AH, the behavior is as follows: Negative zero compares less than positive zero.If one value is numeric and the other is a quiet NaN, the result is the numeric value.When FPCR.DN is 0, if either value is a signaling NaN or if both values are NaNs, the result is a quiet NaN.When FPCR.DN is 1, if either value is a signaling NaN or if both values are NaNs, the result is Default NaN.",
    "syntax": [
      "FMINNMV<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letidentity : bits(esize) =FPDefaultNaN{esize}(FPCR());V{esize}(d) =FPReducePredicated{esize, VL, PL}(ReduceOp_FMINNUM, operand, mask,\n                                                                identity, FPCR());",
    "operational_info": ""
  },
  {
    "title": "FMINP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMINP--Floating-point-minimum-pairwise-?lang=en",
    "description": "Floating-point minimum pairwise This instruction calculates the minimum value of each pair of adjacent floating-point elements within each source vector, and interleaves the results from corresponding lanes. The interleaved result values are destructively placed in the first source vector. When FPCR.AH is 0, the behavior is as follows for each pairwise operation: Negative zero compares less than positive zero.When FPCR.DN is 0, if either element is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if either element is a NaN, the result is Default NaN. When FPCR.AH is 1, the behavior is as follows for each pairwise operation: If both elements are zeros, regardless of the sign of either zero, the result is the second element.If either element is a NaN, regardless of the value of FPCR.DN, the result is the second element.",
    "syntax": [
      "FMINP<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)=ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult :bits(VL)= Z{}(dn);varelement1 :bits(esize);varelement2 :bits(esize);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenifIsEven(e) then\n            element1 = operand1[(e +0)*:esize];element2 = operand1[(e +1)*:esize];elseelement1 = operand2[(e -1)*:esize];element2 = operand2[(e +0)*:esize];end;result[e*:esize] =FPMin{esize}(element1, element2, FPCR());end;end;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMINQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMINQV--Floating-point-minimum-recursive-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Floating-point minimum recursive reduction of quadword vector segments This instruction calculates a floating-point minimum of the same element numbers from each 128-bit source vector segment using a recursive pairwise reduction, and places each result into the corresponding element number of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as +Infinity. When FPCR.AH is 0, the behavior is as follows: Negative zero compares less than positive zero.When FPCR.DN is 0, if either value is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if either value is a NaN, the result is Default NaN. When FPCR.AH is 1, the behavior is as follows: If both values are zeros, regardless of the sign of either zero, the result is the second value.If either value is a NaN, regardless of the value of FPCR.DN, the result is the second value.",
    "syntax": [
      "FMINQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer{} = VL DIV128;letelempersegment : integer =128DIV esize;letsegbits : integer{} = segments*esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letidentity : bits(esize) =FPInfinity{esize}('0');varresult : bits(128) = Zeros{};fore =0to elempersegment-1dovarstmp : bits(segbits);fors =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) then\n            stmp[s*:esize] = operand[(s * elempersegment + e)*:esize];elsestmp[s*:esize] = identity;\n        end;\n    end;\n    result[e*:esize] =FPReduce{esize, segbits}(ReduceOp_FMIN, stmp, FPCR());\nend;V{128}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FMINV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMINV--Floating-point-minimum-recursive-reduction-to-scalar-?lang=en",
    "description": "Floating-point minimum recursive reduction to scalar This instruction calculates a floating-point minimum horizontally over all lanes of a vector using a recursive pairwise reduction, and places the result in the SIMD&FP scalar destination register. Inactive elements in the source vector are treated as +Infinity. When FPCR.AH is 0, the behavior is as follows: Negative zero compares less than positive zero.When FPCR.DN is 0, if either value is a NaN, the result is a quiet NaN.When FPCR.DN is 1, if either value is a NaN, the result is Default NaN. When FPCR.AH is 1, the behavior is as follows: If both values are zeros, regardless of the sign of either zero, the result is the second value.If either value is a NaN, regardless of the value of FPCR.DN, the result is the second value.",
    "syntax": [
      "FMINV<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letidentity : bits(esize) =FPInfinity{esize}('0');V{esize}(d) =FPReducePredicated{esize, VL, PL}(ReduceOp_FMIN, operand, mask,\n                                                                identity, FPCR());",
    "operational_info": ""
  },
  {
    "title": "FMLA (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLA--indexed---Floating-point-fused-multiply-add-by-indexed-element-?lang=en",
    "description": "Floating-point fused multiply-add by indexed element This instruction multiplies all floating-point elements within each 128-bit segment of the first source vector by the specified element in the corresponding second source vector segment. The products are then destructively added without intermediate rounding to the corresponding elements of the addend and destination vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. This instruction is unpredicated. It has encodings from 3 classes:Half-precision,Single-precisionandDouble-precision",
    "syntax": [
      "FMLA<Zda>.D,<Zn>.D,<Zm>.D[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelem2 : bits(esize) = op2[s*:esize];letelem1 : bits(esize) = (ifop1_neg thenFPNeg{esize}(op1[e*:esize], FPCR())elseop1[e*:esize]);letelem3 : bits(esize) = (ifop3_neg thenFPNeg{esize}(result[e*:esize], FPCR())elseresult[e*:esize]);\n    result[e*:esize] =FPMulAdd{esize}(elem3, elem1, elem2, FPCR());\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLA (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLA--vectors---Floating-point-fused-multiply-add--predicated--?lang=en",
    "description": "Floating-point fused multiply-add (predicated) This instruction multiplies the active floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector, and adds the result to elements of the third source (addend) vector without intermediate rounding. The results are destructively placed in the destination and third source (addend) vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMLA<Zda>.<T>,<Pg>/M,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letop1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letop2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelem1 : bits(esize) = (ifop1_neg thenFPNeg{esize}(op1[e*:esize], FPCR())elseop1[e*:esize]);letelem2 : bits(esize) = op2[e*:esize];letelem3 : bits(esize) = (ifop3_neg thenFPNeg{esize}(op3[e*:esize], FPCR())elseop3[e*:esize]);\n\n        result[e*:esize] =FPMulAdd{esize}(elem3, elem1, elem2, FPCR());elseresult[e*:esize] = op3[e*:esize];\n\n    end;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALB (indexed, FP16 to FP32)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALB--indexed--FP16-to-FP32---Half-precision-multiply-add-by-indexed-element-to-single-precision--bottom--?lang=en",
    "description": "Half-precision multiply-add by indexed element to single-precision (bottom) This instruction widens the even-numbered half-precision elements in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to single-precision format and then destructively multiplies and adds these values without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding half-precision elements in the first source vector. This instruction is unpredicated.",
    "syntax": [
      "FMLALB<Zda>.S,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =2* segmentbase + index;letelem1 : bits(esize DIV2) = (ifop1_neg\n                                     thenFPNeg{esize DIV2}(op1[(2*e +0)*:(esize DIV2)], FPCR())elseop1[(2*e +0)*:(esize DIV2)]);letelem2 : bits(esize DIV2) = op2[s*:(esize DIV2)];letelem3 : bits(esize) = op3[e*:esize];\n    result[e*:esize] =FPMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALB (indexed, FP8 to FP16)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALB--indexed--FP8-to-FP16---8-bit-floating-point-multiply-add-by-indexed-element-to-half-precision--bottom--?lang=en",
    "description": "8-bit floating-point multiply-add by indexed element to half-precision (bottom) This instruction widens the even 8-bit elements in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to half-precision format and multiplies the corresponding elements. The intermediate products are scaled by 2-UInt(FPMR.LSCALE[3:0])before being destructively added without intermediate rounding to the half-precision elements of the destination vector that overlap with the corresponding 8-bit floating-point elements in the first source vector. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FMLALB<Zda>.H,<Zn>.B,<Zm>.B[<imm>]"
    ],
    "features": [
      "FEAT_FP8FMA",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8FMA) &&IsFeatureImplemented(FEAT_FP8FMA) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8FMA) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;leteltspersegment : integer =128DIV16;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =2* segmentbase + index;letelement1 : bits(8) = operand1[(2* e +0)*:8];letelement2 : bits(8) = operand2[s*:8];letelement3 : bits(16) = operand3[e*:16];\n    result[e*:16] =FP8MulAddFP{16}(element3, element1, element2, FPCR(), FPMR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALB (vectors, FP16 to FP32)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALB--vectors--FP16-to-FP32---Half-precision-multiply-add-to-single-precision--bottom--?lang=en",
    "description": "Half-precision multiply-add to single-precision (bottom) This instruction widens the even-numbered half-precision elements in the first source vector and the corresponding elements in the second source vector to single-precision format and then destructively multiplies and adds these values without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding half-precision elements in the source vectors. This instruction is unpredicated.",
    "syntax": [
      "FMLALB<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelem1 : bits(esize DIV2) = (ifop1_neg\n                                     thenFPNeg{esize DIV2}(op1[(2*e +0)*:(esize DIV2)], FPCR())elseop1[(2*e +0)*:(esize DIV2)]);letelem2 : bits(esize DIV2) = op2[(2*e +0)*:(esize DIV2)];letelem3 : bits(esize) = op3[e*:esize];\n    result[e*:esize] =FPMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALB (vectors, FP8 to FP16)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALB--vectors--FP8-to-FP16---8-bit-floating-point-multiply-add-to-half-precision--bottom--?lang=en",
    "description": "8-bit floating-point multiply-add to half-precision (bottom) This instruction widens the even 8-bit elements in the first and second source vectors to half-precision format and multiplies the corresponding elements. The intermediate products are scaled by 2-UInt(FPMR.LSCALE[3:0])before being destructively added without intermediate rounding to the half-precision elements of the destination vector that overlap with the corresponding 8-bit floating-point elements in the source vectors. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FMLALB<Zda>.H,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_FP8FMA",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8FMA) &&IsFeatureImplemented(FEAT_FP8FMA) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8FMA) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(8) = operand1[(2* e +0)*:8];letelement2 : bits(8) = operand2[(2* e +0)*:8];letelement3 : bits(16) = operand3[e*:16];\n    result[e*:16] =FP8MulAddFP{16}(element3, element1, element2, FPCR(), FPMR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALLBB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALLBB--indexed---8-bit-floating-point-multiply-add-by-indexed-element-to-single-precision--bottom-bottom--?lang=en",
    "description": "8-bit floating-point multiply-add by indexed element to single-precision (bottom bottom) This instruction widens the first 8-bit element of each 32-bit container in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to single-precision format and multiplies the corresponding elements. The intermediate products are scaled by 2-UInt(FPMR.LSCALE)before being destructively added without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding 8-bit floating-point elements in the first source vector. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FMLALLBB<Zda>.S,<Zn>.B,<Zm>.B[<imm>]"
    ],
    "features": [
      "FEAT_FP8FMA",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8FMA) &&IsFeatureImplemented(FEAT_FP8FMA) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8FMA) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;leteltspersegment : integer =128DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =4* segmentbase + index;letelement1 : bits(8) = operand1[(4* e +0)*:8];letelement2 : bits(8) = operand2[s*:8];letelement3 : bits(32) = operand3[e*:32];\n    result[e*:32] =FP8MulAddFP{32}(element3, element1, element2, FPCR(), FPMR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALLBB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALLBB--vectors---8-bit-floating-point-multiply-add-to-single-precision--bottom-bottom--?lang=en",
    "description": "8-bit floating-point multiply-add to single-precision (bottom bottom) This instruction widens the first 8-bit element of each 32-bit container in the first and second source vectors to single-precision format and multiplies the corresponding elements. The intermediate products are scaled by 2-UInt(FPMR.LSCALE)before being destructively added without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding 8-bit floating-point elements in the source vectors. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FMLALLBB<Zda>.S,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_FP8FMA",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8FMA) &&IsFeatureImplemented(FEAT_FP8FMA) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8FMA) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(8) = operand1[(4* e +0)*:8];letelement2 : bits(8) = operand2[(4* e +0)*:8];letelement3 : bits(32) = operand3[e*:32];\n    result[e*:32] =FP8MulAddFP{32}(element3, element1, element2, FPCR(), FPMR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALLBT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALLBT--indexed---8-bit-floating-point-multiply-add-by-indexed-element-to-single-precision--bottom-top--?lang=en",
    "description": "8-bit floating-point multiply-add by indexed element to single-precision (bottom top) This instruction widens the second 8-bit element of each 32-bit container in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to single-precision format and multiplies the corresponding elements. The intermediate products are scaled by 2-UInt(FPMR.LSCALE)before being destructively added without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding 8-bit floating-point elements in the first source vector. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FMLALLBT<Zda>.S,<Zn>.B,<Zm>.B[<imm>]"
    ],
    "features": [
      "FEAT_FP8FMA",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8FMA) &&IsFeatureImplemented(FEAT_FP8FMA) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8FMA) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;leteltspersegment : integer =128DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =4* segmentbase + index;letelement1 : bits(8) = operand1[(4* e +1)*:8];letelement2 : bits(8) = operand2[s*:8];letelement3 : bits(32) = operand3[e*:32];\n    result[e*:32] =FP8MulAddFP{32}(element3, element1, element2, FPCR(), FPMR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALLBT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALLBT--vectors---8-bit-floating-point-multiply-add-to-single-precision--bottom-top--?lang=en",
    "description": "8-bit floating-point multiply-add to single-precision (bottom top) This instruction widens the second 8-bit element of each 32-bit container in the first and second source vectors to single-precision format and multiplies the corresponding elements. The intermediate products are scaled by 2-UInt(FPMR.LSCALE)before being destructively added without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding 8-bit floating-point elements in the source vectors. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FMLALLBT<Zda>.S,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_FP8FMA",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8FMA) &&IsFeatureImplemented(FEAT_FP8FMA) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8FMA) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(8) = operand1[(4* e +1)*:8];letelement2 : bits(8) = operand2[(4* e +1)*:8];letelement3 : bits(32) = operand3[e*:32];\n    result[e*:32] =FP8MulAddFP{32}(element3, element1, element2, FPCR(), FPMR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALLTB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALLTB--indexed---8-bit-floating-point-multiply-add-by-indexed-element-to-single-precision--top-bottom--?lang=en",
    "description": "8-bit floating-point multiply-add by indexed element to single-precision (top bottom) This instruction widens the third 8-bit element of each 32-bit container in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to single-precision format and multiplies the corresponding elements. The intermediate products are scaled by 2-UInt(FPMR.LSCALE)before being destructively added without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding 8-bit floating-point elements in the first source vector. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FMLALLTB<Zda>.S,<Zn>.B,<Zm>.B[<imm>]"
    ],
    "features": [
      "FEAT_FP8FMA",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8FMA) &&IsFeatureImplemented(FEAT_FP8FMA) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8FMA) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;leteltspersegment : integer =128DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =4* segmentbase + index;letelement1 : bits(8) = operand1[(4* e +2)*:8];letelement2 : bits(8) = operand2[s*:8];letelement3 : bits(32) = operand3[e*:32];\n    result[e*:32] =FP8MulAddFP{32}(element3, element1, element2, FPCR(), FPMR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALLTB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALLTB--vectors---8-bit-floating-point-multiply-add-to-single-precision--top-bottom--?lang=en",
    "description": "8-bit floating-point multiply-add to single-precision (top bottom) This instruction widens the third 8-bit element of each 32-bit container in the first and second source vectors to single-precision format and multiplies the corresponding elements. The intermediate products are scaled by 2-UInt(FPMR.LSCALE)before being destructively added without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding 8-bit floating-point elements in the source vectors. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FMLALLTB<Zda>.S,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_FP8FMA",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8FMA) &&IsFeatureImplemented(FEAT_FP8FMA) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8FMA) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(8) = operand1[(4* e +2)*:8];letelement2 : bits(8) = operand2[(4* e +2)*:8];letelement3 : bits(32) = operand3[e*:32];\n    result[e*:32] =FP8MulAddFP{32}(element3, element1, element2, FPCR(), FPMR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALLTT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALLTT--indexed---8-bit-floating-point-multiply-add-by-indexed-element-to-single-precision--top-top--?lang=en",
    "description": "8-bit floating-point multiply-add by indexed element to single-precision (top top) This instruction widens the fourth 8-bit element of each 32-bit container in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to single-precision format and multiplies the corresponding elements. The intermediate products are scaled by 2-UInt(FPMR.LSCALE)before being destructively added without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding 8-bit floating-point elements in the first source vector. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FMLALLTT<Zda>.S,<Zn>.B,<Zm>.B[<imm>]"
    ],
    "features": [
      "FEAT_FP8FMA",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8FMA) &&IsFeatureImplemented(FEAT_FP8FMA) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8FMA) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;leteltspersegment : integer =128DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =4* segmentbase + index;letelement1 : bits(8) = operand1[(4* e +3)*:8];letelement2 : bits(8) = operand2[s*:8];letelement3 : bits(32) = operand3[e*:32];\n    result[e*:32] =FP8MulAddFP{32}(element3, element1, element2, FPCR(), FPMR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALLTT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALLTT--vectors---8-bit-floating-point-multiply-add-to-single-precision--top-top--?lang=en",
    "description": "8-bit floating-point multiply-add to single-precision (top top) This instruction widens the fourth 8-bit element of each 32-bit container in the first and second source vectors to single-precision format and multiplies the corresponding elements. The intermediate products are scaled by 2-UInt(FPMR.LSCALE)before being destructively added without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding 8-bit floating-point elements in the source vectors. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FMLALLTT<Zda>.S,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_FP8FMA",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8FMA) &&IsFeatureImplemented(FEAT_FP8FMA) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8FMA) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV32;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(8) = operand1[(4* e +3)*:8];letelement2 : bits(8) = operand2[(4* e +3)*:8];letelement3 : bits(32) = operand3[e*:32];\n    result[e*:32] =FP8MulAddFP{32}(element3, element1, element2, FPCR(), FPMR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALT (indexed, FP16 to FP32)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALT--indexed--FP16-to-FP32---Half-precision-multiply-add-by-indexed-element-to-single-precision--top--?lang=en",
    "description": "Half-precision multiply-add by indexed element to single-precision (top) This instruction widens the odd-numbered half-precision elements in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to single-precision format and then destructively multiplies and adds these values without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding half-precision elements in the first source vector. This instruction is unpredicated.",
    "syntax": [
      "FMLALT<Zda>.S,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =2* segmentbase + index;letelem1 : bits(esize DIV2) = (ifop1_neg\n                                     thenFPNeg{esize DIV2}(op1[(2*e +1)*:(esize DIV2)], FPCR())elseop1[(2*e +1)*:(esize DIV2)]);letelem2 : bits(esize DIV2) = op2[s*:(esize DIV2)];letelem3 : bits(esize) = op3[e*:esize];\n    result[e*:esize] =FPMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALT (indexed, FP8 to FP16)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALT--indexed--FP8-to-FP16---8-bit-floating-point-multiply-add-by-indexed-element-to-half-precision--top--?lang=en",
    "description": "8-bit floating-point multiply-add by indexed element to half-precision (top) This instruction widens the odd 8-bit elements in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to half-precision format and multiplies the corresponding elements. The intermediate products are scaled by 2-UInt(FPMR.LSCALE[3:0])before being destructively added without intermediate rounding to the half-precision elements of the destination vector that overlap with the corresponding 8-bit floating-point elements in the first source vector. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FMLALT<Zda>.H,<Zn>.B,<Zm>.B[<imm>]"
    ],
    "features": [
      "FEAT_FP8FMA",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8FMA) &&IsFeatureImplemented(FEAT_FP8FMA) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8FMA) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;leteltspersegment : integer =128DIV16;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =2* segmentbase + index;letelement1 : bits(8) = operand1[(2* e +1)*:8];letelement2 : bits(8) = operand2[s*:8];letelement3 : bits(16) = operand3[e*:16];\n    result[e*:16] =FP8MulAddFP{16}(element3, element1, element2, FPCR(), FPMR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALT (vectors, FP16 to FP32)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALT--vectors--FP16-to-FP32---Half-precision-multiply-add-to-single-precision--top--?lang=en",
    "description": "Half-precision multiply-add to single-precision (top) This instruction widens the odd-numbered half-precision elements in the first source vector and the corresponding elements in the second source vector to single-precision format and then destructively multiplies and adds these values without intermediate rounding to the single-precision elements of the destination vector that overlap with the corresponding half-precision elements in the source vectors. This instruction is unpredicated.",
    "syntax": [
      "FMLALT<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelem1 : bits(esize DIV2) = (ifop1_neg\n                                     thenFPNeg{esize DIV2}(op1[(2*e +1)*:(esize DIV2)], FPCR())elseop1[(2*e +1)*:(esize DIV2)]);letelem2 : bits(esize DIV2) = op2[(2*e +1)*:(esize DIV2)];letelem3 : bits(esize) = op3[e*:esize];\n    result[e*:esize] =FPMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLALT (vectors, FP8 to FP16)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLALT--vectors--FP8-to-FP16---8-bit-floating-point-multiply-add-to-half-precision--top--?lang=en",
    "description": "8-bit floating-point multiply-add to half-precision (top) This instruction widens the odd 8-bit elements in the first and second source vectors to half-precision format and multiplies the corresponding elements. The intermediate products are scaled by 2-UInt(FPMR.LSCALE[3:0])before being destructively added without intermediate rounding to the half-precision elements of the destination vector that overlap with the corresponding 8-bit floating-point elements in the source vectors. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated.",
    "syntax": [
      "FMLALT<Zda>.H,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_FP8FMA",
      "FEAT_SSVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SSVE_FP8FMA) &&IsFeatureImplemented(FEAT_FP8FMA) thenCheckSVEEnabled();elsifIsFeatureImplemented(FEAT_FP8FMA) thenCheckNonStreamingSVEEnabled();elseCheckStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV16;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(8) = operand1[(2* e +1)*:8];letelement2 : bits(8) = operand2[(2* e +1)*:8];letelement3 : bits(16) = operand3[e*:16];\n    result[e*:16] =FP8MulAddFP{16}(element3, element1, element2, FPCR(), FPMR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLS (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLS--indexed---Floating-point-fused-multiply-subtract-by-indexed-element-?lang=en",
    "description": "Floating-point fused multiply-subtract by indexed element This instruction multiplies all floating-point elements within each 128-bit segment of the first source vector by the specified element in the corresponding second source vector segment. The products are then destructively subtracted without intermediate rounding from the corresponding elements of the addend and destination vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. This instruction is unpredicated. It has encodings from 3 classes:Half-precision,Single-precisionandDouble-precision",
    "syntax": [
      "FMLS<Zda>.D,<Zn>.D,<Zm>.D[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelem2 : bits(esize) = op2[s*:esize];letelem1 : bits(esize) = (ifop1_neg thenFPNeg{esize}(op1[e*:esize], FPCR())elseop1[e*:esize]);letelem3 : bits(esize) = (ifop3_neg thenFPNeg{esize}(result[e*:esize], FPCR())elseresult[e*:esize]);\n    result[e*:esize] =FPMulAdd{esize}(elem3, elem1, elem2, FPCR());\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLS (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLS--vectors---Floating-point-fused-multiply-subtract--predicated--?lang=en",
    "description": "Floating-point fused multiply-subtract (predicated) This instruction multiplies the active floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector, and subtracts the result from elements of the third source (addend) vector without intermediate rounding. The results are destructively placed in the destination and third source (addend) vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMLS<Zda>.<T>,<Pg>/M,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letop1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letop2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelem1 : bits(esize) = (ifop1_neg thenFPNeg{esize}(op1[e*:esize], FPCR())elseop1[e*:esize]);letelem2 : bits(esize) = op2[e*:esize];letelem3 : bits(esize) = (ifop3_neg thenFPNeg{esize}(op3[e*:esize], FPCR())elseop3[e*:esize]);\n\n        result[e*:esize] =FPMulAdd{esize}(elem3, elem1, elem2, FPCR());elseresult[e*:esize] = op3[e*:esize];\n\n    end;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLSLB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLSLB--indexed---Half-precision-multiply-subtract-by-indexed-element-from-single-precision--bottom--?lang=en",
    "description": "Half-precision multiply-subtract by indexed element from single-precision (bottom) This instruction widens the even-numbered half-precision elements in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to single-precision format and then destructively multiplies and subtracts these values without intermediate rounding from the single-precision elements of the destination vector that overlap with the corresponding half-precision elements in the first source vector. This instruction is unpredicated.",
    "syntax": [
      "FMLSLB<Zda>.S,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =2* segmentbase + index;letelem1 : bits(esize DIV2) = (ifop1_neg\n                                     thenFPNeg{esize DIV2}(op1[(2*e +0)*:(esize DIV2)], FPCR())elseop1[(2*e +0)*:(esize DIV2)]);letelem2 : bits(esize DIV2) = op2[s*:(esize DIV2)];letelem3 : bits(esize) = op3[e*:esize];\n    result[e*:esize] =FPMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLSLB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLSLB--vectors---Half-precision-multiply-subtract-from-single-precision--bottom--?lang=en",
    "description": "Half-precision multiply-subtract from single-precision (bottom) This instruction widens the even-numbered half-precision elements in the first source vector and the corresponding elements in the second source vector to single-precision format and then destructively multiplies and subtracts these values without intermediate rounding from the single-precision elements of the destination vector that overlap with the corresponding half-precision elements in the source vectors. This instruction is unpredicated.",
    "syntax": [
      "FMLSLB<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelem1 : bits(esize DIV2) = (ifop1_neg\n                                     thenFPNeg{esize DIV2}(op1[(2*e +0)*:(esize DIV2)], FPCR())elseop1[(2*e +0)*:(esize DIV2)]);letelem2 : bits(esize DIV2) = op2[(2*e +0)*:(esize DIV2)];letelem3 : bits(esize) = op3[e*:esize];\n    result[e*:esize] =FPMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLSLT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLSLT--indexed---Half-precision-multiply-subtract-by-indexed-element-from-single-precision--top--?lang=en",
    "description": "Half-precision multiply-subtract by indexed element from single-precision (top) This instruction widens the odd-numbered half-precision elements in the first source vector and the indexed element from the corresponding 128-bit segment in the second source vector to single-precision format and then destructively multiplies and subtracts these values without intermediate rounding from the single-precision elements of the destination vector that overlap with the corresponding half-precision elements in the first source vector. This instruction is unpredicated.",
    "syntax": [
      "FMLSLT<Zda>.S,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer =2* segmentbase + index;letelem1 : bits(esize DIV2) = (ifop1_neg\n                                     thenFPNeg{esize DIV2}(op1[(2*e +1)*:(esize DIV2)], FPCR())elseop1[(2*e +1)*:(esize DIV2)]);letelem2 : bits(esize DIV2) = op2[s*:(esize DIV2)];letelem3 : bits(esize) = op3[e*:esize];\n    result[e*:esize] =FPMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMLSLT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMLSLT--vectors---Half-precision-multiply-subtract-from-single-precision--top--?lang=en",
    "description": "Half-precision multiply-subtract from single-precision (top) This instruction widens the odd-numbered half-precision elements in the first source vector and the corresponding elements in the second source vector to single-precision format and then destructively multiplies and subtracts these values without intermediate rounding from the single-precision elements of the destination vector that overlap with the corresponding half-precision elements in the source vectors. This instruction is unpredicated.",
    "syntax": [
      "FMLSLT<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelem1 : bits(esize DIV2) = (ifop1_neg\n                                     thenFPNeg{esize DIV2}(op1[(2*e +1)*:(esize DIV2)], FPCR())elseop1[(2*e +1)*:(esize DIV2)]);letelem2 : bits(esize DIV2) = op2[(2*e +1)*:(esize DIV2)];letelem3 : bits(esize) = op3[e*:esize];\n    result[e*:esize] =FPMulAddH(elem3, elem1, elem2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMMLA (non-widening)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMMLA--non-widening---Floating-point-matrix-multiply-accumulate-?lang=en",
    "description": "Floating-point matrix multiply-accumulate This instruction supports half-precision, single-precision, and double-precision data types in a 2x2 matrix contained in segments of 64, 128, or 256 bits, respectively. It multiplies the 2x2 matrix in each segment of the first source vector by the 2x2 matrix in the corresponding segment of the second source vector. The intermediate products are rounded before they are summed, and the intermediate sum is rounded before accumulation into the 2x2 matrix held in the corresponding segment of the addend and destination vector. This is equivalent to performing a 2-way dot product per destination element. This instruction is unpredicated. The half-precision and single-precision variants are vector length agnostic. The double-precision variant requires that the Effective SVE vector length is at least 256 bits. ID_AA64ZFR0_EL1.F32MM indicates whether the single-precision variant is implemented. ID_AA64ZFR0_EL1.F64MM indicates whether the double-precision variant is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "FMMLA<Zda>.D,<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_F16MM",
      "FEAT_F32MM",
      "FEAT_F64MM",
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();ifVL < esize *4thenEndOfDecode(Decode_UNDEF); end;letsegments : integer =VLDIV(4* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL) = Zeros{};varop1, op2 : bits(4*esize);varres, addend : bits(4*esize);fors =0to segments-1doop1    = operand1[s*:(4*esize)];\n    op2    = operand2[s*:(4*esize)];\n    addend = operand3[s*:(4*esize)];\n    res    =FPMatMulAdd{4*esize}(addend, op1, op2, esize, FPCR());\n    result[s*:(4*esize)] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMMLA (widening, FP16 to FP32)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMMLA--widening--FP16-to-FP32---Half-precision-matrix-multiply-accumulate-to-single-precision-?lang=en",
    "description": "Half-precision matrix multiply-accumulate to single-precision This instruction multiplies the 2×4 matrix of half-precision values held in each 128-bit segment of the first source vector by the 4×2 matrix of half-precision values in the corresponding segment of the second source vector. The intermediate single-precision results are calculated as a sum of two fused sum-of-products using two pairs of adjacent half-precision values from each of the source vectors, and rounded before accumulation into the 2×2 matrix of single-precision results in the corresponding segment of the destination vector. This is equivalent to performing a 4-way dot product per destination element. This instruction is unpredicated. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "FMMLA<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer =  VL DIV128;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fors =0to segments-1doletop1 : bits(128)    = operand1[s*:(128)];letop2 : bits(128)    = operand2[s*:(128)];letaddend : bits(128) = operand3[s*:(128)];\n    result[s*:(128)] =FPMatMulAddH{128}(addend, op1, op2, FPCR());\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMMLA (widening, FP8 to FP16)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMMLA--widening--FP8-to-FP16---8-bit-floating-point-matrix-multiply-accumulate-to-half-precision-?lang=en",
    "description": "8-bit floating-point matrix multiply-accumulate to half-precision This instruction multiplies the 2×4 matrix of 8-bit floating-point values held in each 64-bit segment of the first source vector by the 4×2 matrix of 8-bit floating-point values in the corresponding segment of the second source vector. The intermediate half-precision results calculated as a fused sum-of-products using four adjacent 8-bit floating-point values from each of the source vectors, are scaled by 2-UInt(FPMR.LSCALE[3:0]), before being destructively added without intermediate rounding to the 2×2 half-precision matrix in the corresponding segment of the destination vector. This is equivalent to accumulating 4-way dot product per destination element. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "FMMLA<Zda>.H,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_F8F16MM",
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer =  VL DIV64;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fors =0to segments-1doletop1 : bits(64)    = operand1[s*:(64)];letop2 : bits(64)    = operand2[s*:(64)];letaddend : bits(64) = operand3[s*:(64)];letway : integer{} =4;\n    result[s*:(64)] =FP8MatMulAddFP{64}(addend, op1, op2, way, FPCR(), FPMR());\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMMLA (widening, FP8 to FP32)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMMLA--widening--FP8-to-FP32---8-bit-floating-point-matrix-multiply-accumulate-to-single-precision-?lang=en",
    "description": "8-bit floating-point matrix multiply-accumulate to single-precision This instruction multiplies the 2×8 matrix of 8-bit floating-point values held in each 128-bit segment of the first source vector by the 8×2 matrix of 8-bit floating-point values in the corresponding segment of the second source vector. The intermediate single-precision results calculated as a fused sum-of-products using eight adjacent 8-bit floating-point values from each of the source vectors, are scaled by 2-UInt(FPMR.LSCALE), before being destructively added without intermediate rounding to the 2×2 single-precision matrix in the corresponding segment of the destination vector. This is equivalent to accumulating 8-way dot product per destination element. The 8-bit floating-point encoding format for the elements of the first source vector and the second source vector is selected by FPMR.F8S1 and FPMR.F8S2 respectively. This instruction is unpredicated. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "FMMLA<Zda>.S,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_F8F32MM",
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer =  VL DIV128;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fors =0to segments-1doletop1 : bits(128)    = operand1[s*:(128)];letop2 : bits(128)    = operand2[s*:(128)];letaddend : bits(128) = operand3[s*:(128)];letway : integer{} =8;\n    result[s*:(128)] =FP8MatMulAddFP{128}(addend, op1, op2, way, FPCR(), FPMR());\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMOV (immediate, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMOV--immediate--predicated---Move-floating-point-immediate-to-vector-elements--predicated---an-alias-of-FCPY-?lang=en",
    "description": "Move floating-point immediate to vector elements (predicated) This instruction moves a floating-point immediate into each Active element in the destination vector. Inactive elements in the destination vector register remain unmodified. This is an alias ofFCPY. This means: The encodings in this description are named to match the encodings ofFCPY.The description ofFCPYgives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "FMOV<Zd>.<T>,<Pg>/M, #<const>\nis equivalent to\nFCPY<Zd>.<T>,<Pg>/M, #<const>\nand is always the preferred disassembly."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofFCPYgives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "FMOV (immediate, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMOV--immediate--unpredicated---Move-floating-point-immediate-to-vector-elements--unpredicated---an-alias-of-FDUP-?lang=en",
    "description": "Move floating-point immediate to vector elements (unpredicated) This instruction unconditionally broadcasts the floating-point immediate into each element of the destination vector. This instruction is unpredicated. This is an alias ofFDUP. This means: The encodings in this description are named to match the encodings ofFDUP.The description ofFDUPgives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "FMOV<Zd>.<T>, #<const>\nis equivalent to\nFDUP<Zd>.<T>, #<const>\nand is always the preferred disassembly."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofFDUPgives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "FMOV (zero, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMOV--zero--predicated---Move-floating-point--0-0-to-vector-elements--predicated---an-alias-of-CPY--immediate--merging--?lang=en",
    "description": "Move floating-point +0.0 to vector elements (predicated) This instruction moves floating-point constant +0.0 to each Active element in the destination vector. Inactive elements in the destination vector register remain unmodified. This is a pseudo-instruction ofCPY (immediate, merging). This means: The encodings in this description are named to match the encodings ofCPY (immediate, merging).The assembler syntax is used only for assembly, and is not used on disassembly.The description ofCPY (immediate, merging)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "FMOV<Zd>.<T>,<Pg>/M, #0.0\nis equivalent to\nCPY<Zd>.<T>,<Pg>/M, #0"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofCPY (immediate, merging)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "FMOV (zero, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMOV--zero--unpredicated---Move-floating-point--0-0-to-vector-elements--unpredicated---an-alias-of-DUP--immediate--?lang=en",
    "description": "Move floating-point +0.0 to vector elements (unpredicated) This instruction unconditionally broadcasts the floating-point constant +0.0 into each element of the destination vector. This instruction is unpredicated. This is a pseudo-instruction ofDUP (immediate). This means: The encodings in this description are named to match the encodings ofDUP (immediate).The assembler syntax is used only for assembly, and is not used on disassembly.The description ofDUP (immediate)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "FMOV<Zd>.<T>, #0.0\nis equivalent to\nDUP<Zd>.<T>, #0"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofDUP (immediate)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "FMSB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMSB--Floating-point-fused-multiply-subtract-to-multiplicand--predicated--?lang=en",
    "description": "Floating-point fused multiply-subtract to multiplicand (predicated) This instruction multiplies the active floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector, and subtracts the result from elements of the third (addend) vector without intermediate rounding. The results are destructively placed in the destination and first source (multiplicand) vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMSB<Zdn>.<T>,<Pg>/M,<Zm>.<T>,<Za>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letop1 : bits(VL) = Z{}(dn);letop2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letop3 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(a)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelem1 : bits(esize) = (ifop1_neg thenFPNeg{esize}(op1[e*:esize], FPCR())elseop1[e*:esize]);letelem2 : bits(esize) = op2[e*:esize];letelem3 : bits(esize) = (ifop3_neg thenFPNeg{esize}(op3[e*:esize], FPCR())elseop3[e*:esize]);\n\n        result[e*:esize] =FPMulAdd{esize}(elem3, elem1, elem2, FPCR());elseresult[e*:esize] = op1[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMUL (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMUL--immediate---Floating-point-multiply-by-immediate--predicated--?lang=en",
    "description": "Floating-point multiply by immediate (predicated) This instruction multiplies each active floating-point element of the source vector by an immediate, and destructively places the results in the corresponding elements of the source vector. The immediate may take the value +0.5 or +2.0 only. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMUL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =FPMul{esize}(element1, imm, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMUL (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMUL--indexed---Floating-point-multiply-by-indexed-element-?lang=en",
    "description": "Floating-point multiply by indexed element This instruction multiplies all floating-point elements within each 128-bit segment of the first source vector by the specified element in the corresponding second source vector segment. The results are placed in the corresponding elements of the destination vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. This instruction is unpredicated. It has encodings from 3 classes:Half-precision,Single-precisionandDouble-precision",
    "syntax": [
      "FMUL<Zd>.D,<Zn>.D,<Zm>.D[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letop1 : bits(VL) = Z{}(n);letop2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelem2 : bits(esize) = op2[s*:esize];letelem1 : bits(esize) = op1[e*:esize];\n    result[e*:esize] =FPMul{esize}(elem1, elem2, FPCR());\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FMUL (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMUL--vectors--predicated---Floating-point-multiply--predicated--?lang=en",
    "description": "Floating-point multiply (predicated) This instruction multiplies active floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FMUL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPMul{esize}(element1, element2, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FMUL (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMUL--vectors--unpredicated---Floating-point-multiply--unpredicated--?lang=en",
    "description": "Floating-point multiply (unpredicated) This instruction multiplies all elements of the first source vector by the corresponding floating-point elements of the second source vector and places the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "FMUL<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[e*:esize] =FPMul{esize}(element1, element2, FPCR());\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FMULX",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FMULX--Floating-point-multiply-extended--predicated--?lang=en",
    "description": "Floating-point multiply extended (predicated) This instruction multiplies active floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector except that ∞×0.0 gives 2.0 instead of NaN, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified. The instruction can be used withFRECPXto safely convert arbitrary elements in mathematical vector space to unit vectors or direction vectors with length 1.",
    "syntax": [
      "FMULX<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPMulX{esize}(element1, element2, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FNEG",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FNEG--Floating-point-negate--predicated--?lang=en",
    "description": "Floating-point negate (predicated) This instruction negates each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. This inverts the sign bit and cannot signal a floating-point exception. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "FNEG<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =FPNeg{esize}(element, FPCR());\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FNMAD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FNMAD--Floating-point-negated-fused-multiply-add-to-multiplicand--predicated--?lang=en",
    "description": "Floating-point negated fused multiply-add to multiplicand (predicated) This instruction multiplies the active floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector, and adds the result to elements of the third (addend) vector without intermediate rounding. The negated results are destructively placed in the destination and first source (multiplicand) vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FNMAD<Zdn>.<T>,<Pg>/M,<Zm>.<T>,<Za>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letop1 : bits(VL) = Z{}(dn);letop2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letop3 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(a)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelem1 : bits(esize) = (ifop1_neg thenFPNeg{esize}(op1[e*:esize], FPCR())elseop1[e*:esize]);letelem2 : bits(esize) = op2[e*:esize];letelem3 : bits(esize) = (ifop3_neg thenFPNeg{esize}(op3[e*:esize], FPCR())elseop3[e*:esize]);\n\n        result[e*:esize] =FPMulAdd{esize}(elem3, elem1, elem2, FPCR());elseresult[e*:esize] = op1[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FNMLA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FNMLA--Floating-point-negated-fused-multiply-add--predicated--?lang=en",
    "description": "Floating-point negated fused multiply-add (predicated) This instruction multiplies the active floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector, and adds the result to elements of the third source (addend) vector without intermediate rounding. The negated results are destructively placed in the destination and third source (addend) vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FNMLA<Zda>.<T>,<Pg>/M,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letop1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letop2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelem1 : bits(esize) = (ifop1_neg thenFPNeg{esize}(op1[e*:esize], FPCR())elseop1[e*:esize]);letelem2 : bits(esize) = op2[e*:esize];letelem3 : bits(esize) = (ifop3_neg thenFPNeg{esize}(op3[e*:esize], FPCR())elseop3[e*:esize]);\n\n        result[e*:esize] =FPMulAdd{esize}(elem3, elem1, elem2, FPCR());elseresult[e*:esize] = op3[e*:esize];\n\n    end;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FNMLS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FNMLS--Floating-point-negated-fused-multiply-subtract--predicated--?lang=en",
    "description": "Floating-point negated fused multiply-subtract (predicated) This instruction multiplies the active floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector, and subtracts the result from elements of the third source (addend) vector without intermediate rounding. The negated results are destructively placed in the destination and third source (addend) vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FNMLS<Zda>.<T>,<Pg>/M,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letop1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letop2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letop3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelem1 : bits(esize) = (ifop1_neg thenFPNeg{esize}(op1[e*:esize], FPCR())elseop1[e*:esize]);letelem2 : bits(esize) = op2[e*:esize];letelem3 : bits(esize) = (ifop3_neg thenFPNeg{esize}(op3[e*:esize], FPCR())elseop3[e*:esize]);\n\n        result[e*:esize] =FPMulAdd{esize}(elem3, elem1, elem2, FPCR());elseresult[e*:esize] = op3[e*:esize];\n\n    end;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FNMSB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FNMSB--Floating-point-negated-fused-multiply-subtract-to-multiplicand--predicated--?lang=en",
    "description": "Floating-point negated fused multiply-subtract to multiplicand (predicated) This instruction multiplies the active floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector, and subtracts the result from elements of the third (addend) vector without intermediate rounding. The negated results are destructively placed in the destination and first source (multiplicand) vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FNMSB<Zdn>.<T>,<Pg>/M,<Zm>.<T>,<Za>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letop1 : bits(VL) = Z{}(dn);letop2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letop3 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(a)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelem1 : bits(esize) = (ifop1_neg thenFPNeg{esize}(op1[e*:esize], FPCR())elseop1[e*:esize]);letelem2 : bits(esize) = op2[e*:esize];letelem3 : bits(esize) = (ifop3_neg thenFPNeg{esize}(op3[e*:esize], FPCR())elseop3[e*:esize]);\n\n        result[e*:esize] =FPMulAdd{esize}(elem3, elem1, elem2, FPCR());elseresult[e*:esize] = op1[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FRECPE",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FRECPE--Floating-point-reciprocal-estimate--unpredicated--?lang=en",
    "description": "Floating-point reciprocal estimate (unpredicated) This instruction finds the approximate reciprocal of each floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "FRECPE<Zd>.<T>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize) = operand[e*:esize];\n    result[e*:esize] =FPRecipEstimate{esize}(element, FPCR());\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FRECPS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FRECPS--Floating-point-reciprocal-step--unpredicated--?lang=en",
    "description": "Floating-point reciprocal step (unpredicated) This instruction multiplies the floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector, subtracts the products from 2.0 without intermediate rounding, and places the results in the corresponding elements of the destination vector. This instruction is unpredicated. This instruction can be used to perform a single Newton-Raphson iteration for calculating the reciprocal of a vector of floating-point values.",
    "syntax": [
      "FRECPS<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[e*:esize] =FPRecipStepFused{esize}(element1, element2, FPCR());\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FRECPX",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FRECPX--Floating-point-reciprocal-exponent--predicated--?lang=en",
    "description": "Floating-point reciprocal exponent (predicated) This instruction inverts the exponent, zeroes the fractional part of each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. The result of this instruction can be used withFMULXto convert arbitrary elements in mathematical vector space to \"unit vectors\" or \"direction vectors\" of length 1. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "FRECPX<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =FPRecpX{esize}(element, FPCR());\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FRINT32X",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FRINT32X--Floating-point-round-to-32-bit-integer--predicated--?lang=en",
    "description": "Floating-point round to 32-bit integer (predicated) This instruction rounds to integral floating-point values that fit into a 32-bit integer size using the rounding mode that is determined by the FPCR from each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "FRINT32X<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =FPRoundIntN{esize}(element, FPCR(), rounding, intsize);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FRINT32Z",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FRINT32Z--Floating-point-round-to-32-bit-integer--rounding-toward-zero--predicated--?lang=en",
    "description": "Floating-point round to 32-bit integer, rounding toward zero (predicated) This instruction rounds to integral floating-point values that fit into a 32-bit integer size using the round towards zero rounding mode from each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "FRINT32Z<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =FPRoundIntN{esize}(element, FPCR(), rounding, intsize);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FRINT64X",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FRINT64X--Floating-point-round-to-64-bit-integer--predicated--?lang=en",
    "description": "Floating-point round to 64-bit integer (predicated) This instruction rounds to integral floating-point values that fit into a 64-bit integer size using the rounding mode that is determined by the FPCR from each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "FRINT64X<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =FPRoundIntN{esize}(element, FPCR(), rounding, intsize);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FRINT64Z",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FRINT64Z--Floating-point-round-to-64-bit-integer--rounding-toward-zero--predicated--?lang=en",
    "description": "Floating-point round to 64-bit integer, rounding toward zero (predicated) This instruction rounds to integral floating-point values that fit into a 64-bit integer size using the round towards zero rounding mode from each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "FRINT64Z<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =FPRoundIntN{esize}(element, FPCR(), rounding, intsize);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FRINT<r>",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FRINT-r---Floating-point-round-to-integral-value--predicated--?lang=en",
    "description": "Floating-point round to integral value (predicated) This instruction rounds to an integral floating-point value with the specified rounding option from each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. <r>Rounding OptionNto nearest, with ties to evenAto nearest, with ties away from zeroMtoward minus InfinityPtoward plus InfinityZtoward zeroIcurrent FPCR rounding modeXcurrent FPCR rounding mode, signalling inexact It has encodings from 14 classes:Current mode signalling inexact, merging,Current mode signalling inexact, zeroing,Current mode, merging,Current mode, zeroing,Nearest with ties to away, merging,Nearest with ties to away, zeroing,Nearest with ties to even, merging,Nearest with ties to even, zeroing,Toward zero, merging,Toward zero, zeroing,Toward minus infinity, merging,Toward minus infinity, zeroing,Toward plus infinity, mergingandToward plus infinity, zeroing",
    "syntax": [
      "FRINTP<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =FPRoundInt{esize}(element, FPCR(), rounding, exact);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Current mode signalling inexact, merging\" , \"Current mode, merging\" , \"Nearest with ties to away, merging\" , \"Nearest with ties to even, merging\" , \"Toward zero, merging\" , \"Toward minus infinity, merging\" , \"Toward plus infinity, merging\" variants:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FRSQRTE",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FRSQRTE--Floating-point-reciprocal-square-root-estimate--unpredicated--?lang=en",
    "description": "Floating-point reciprocal square root estimate (unpredicated) This instruction finds the approximate reciprocal square root of each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "FRSQRTE<Zd>.<T>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize) = operand[e*:esize];\n    result[e*:esize] =FPRSqrtEstimate{esize}(element, FPCR());\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FRSQRTS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FRSQRTS--Floating-point-reciprocal-square-root-step--unpredicated--?lang=en",
    "description": "Floating-point reciprocal square root step (unpredicated) This instruction multiplies the floating-point elements of the first source vector by the corresponding floating-point elements of the second source vector, subtracts the products from 3.0 and divides the results by 2.0 without any intermediate rounding, and places the results in the corresponding elements of the destination vector. This instruction is unpredicated. This instruction can be used to perform a single Newton-Raphson iteration for calculating the reciprocal square root of a vector of floating-point values.",
    "syntax": [
      "FRSQRTS<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[e*:esize] =FPRSqrtStepFused{esize}(element1, element2, FPCR());\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FSCALE",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FSCALE--Floating-point-adjust-exponent--predicated--?lang=en",
    "description": "Floating-point adjust exponent (predicated) This instruction multiplies the active floating-point elements of the first source vector by 2.0 to the power of the signed integer values in the corresponding elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FSCALE<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:(esize)];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : integer = SInt(operand2[e*:(esize)]);\n        result[e*:(esize)] =FPScale{esize}(element1, element2, FPCR());elseresult[e*:(esize)] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FSQRT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FSQRT--Floating-point-square-root--predicated--?lang=en",
    "description": "Floating-point square root (predicated) This instruction calculates the square root of each active floating-point element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "FSQRT<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =FPSqrt{esize}(element, FPCR());\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "FSUB (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FSUB--immediate---Floating-point-subtract-immediate--predicated--?lang=en",
    "description": "Floating-point subtract immediate (predicated) This instruction subtracts an immediate from each active floating-point element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate may take the value +0.5 or +1.0 only. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FSUB<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =FPSub{esize}(element1, imm, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FSUB (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FSUB--vectors--predicated---Floating-point-subtract--predicated--?lang=en",
    "description": "Floating-point subtract (predicated) This instruction subtracts active floating-point elements of the second source vector from the corresponding floating-point elements of the first source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FSUB<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPSub{esize}(element1, element2, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FSUB (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FSUB--vectors--unpredicated---Floating-point-subtract--unpredicated--?lang=en",
    "description": "Floating-point subtract (unpredicated) This instruction subtracts all floating-point elements of the second source vector from the corresponding elements of the first source vector, and places the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "FSUB<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[e*:esize] =FPSub{esize}(element1, element2, FPCR());\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FSUBR (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FSUBR--immediate---Floating-point-reversed-subtract-from-immediate--predicated--?lang=en",
    "description": "Floating-point reversed subtract from immediate (predicated) This instruction reverse subtracts from an immediate each active floating-point element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate may take the value +0.5 or +1.0 only. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FSUBR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =FPSub{esize}(imm, element1, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FSUBR (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FSUBR--vectors---Floating-point-reversed-subtract--predicated--?lang=en",
    "description": "Floating-point reversed subtract (predicated) This instruction reverse subtracts active floating-point elements of the first source vector from the corresponding floating-point elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "FSUBR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) thenletelement2 : bits(esize) = operand2[e*:esize];\n        result[e*:esize] =FPSub{esize}(element2, element1, FPCR());elseresult[e*:esize] = element1;\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FTMAD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FTMAD--Floating-point-trigonometric-multiply-add-coefficient-?lang=en",
    "description": "Floating-point trigonometric multiply-add coefficient This instruction multiplies each element of the first source vector by the absolute value of the corresponding element of the second source vector and performs a fused addition of each product with a value obtained from a table of hard-wired coefficients, and destructively places the results in the first source vector. This instruction is unpredicated. The coefficients are selected by a combination of the sign bit in the second source element and an immediate index in the range 0 to 7. FTMADcan be combined withFTSMULandFTSSELto calculate values for sin(x) and cos(x). For more information, seeFTSMUL. The coefficients are intended to provide accurate results forFTSMULinputs in the range -π/4 < x ≤ π/4. For double-precision operations, the coefficients are: element2<63>IndexHexadecimalApproximate value003ff0 0000 0000 0000101bfc5 5555 5555 5543-1/3!023f81 1111 1110 f30c1/5!03bf2a 01a0 19b9 2fc6-1/7!043ec7 1de3 51f3 d22b1/9!05be5a e5e2 b60f 7b91-1/11!063de5 d840 8868 552f1/13!070000 0000 0000 00000103ff0 0000 0000 0000111bfe0 0000 0000 0000-1/2!123fa5 5555 5555 55361/4!13bf56 c16c 16c1 3a0b-1/6!143efa 01a0 19b1 e8d81/8!15be92 7e4f 7282 f468-1/10!163e21 ee96 d264 1b131/12!17bda8 f763 80fb b401-1/14! For single-precision operations, the coefficients are: element2<63>IndexHexadecimalApproximate value003f80 0000101be2a aaab-1/3!023c08 88861/5!03b950 08b9-1/7!043636 9d6d1/9!050000 00000060000 00000070000 00000103f80 0000111bf00 0000-1/2!123d2a aaa61/4!13bab6 0705-1/6!1437cd 37cc1/8!150000 00000160000 00000170000 00000 For half-precision operations, the coefficients are: element2<63>IndexHexadecimalApproximate value003c00101b155-1/3!0220301/5!03000000400000050000006000000700000103c00111b800-1/2!12293a1/4!13000001400000150000016000001700000 This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "FTMAD<Zdn>.<T>,<Zdn>.<T>,<Zm>.<T>, #<imm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[e*:esize] =FPTrigMAdd{esize}(imm, element1, element2, FPCR());\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "FTSMUL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FTSMUL--Floating-point-trigonometric-starting-value-?lang=en",
    "description": "Floating-point trigonometric starting value This instruction multiplies each element of the first source vector by itself, replaces the sign-bit of each product with the least-significant bit of the corresponding element of the second source vector, and places the results in the destination vector. This instruction is unpredicated. FTSMULcan be combined withFTMADandFTSSELto calculate values for sin(x) and cos(x). The use of the second operand is consistent with it holding an integer corresponding to the desired sine-wave quadrant when used in conjunction withFTMAD. Note:FTSMUL,FTMAD, andFTSSELcan be used to calculate values for sin(k) and correspondingly cos(k-π/2) via an intermediate value x, in the range -π/4 < x ≤ π/4, and a quadrant q where k = qπ/2 + x, using a Taylor Series approximation.FTSMULcan be used to calculate x2, and to insert q<0> into the most-significant bit, indicating the desired odd versus even Taylor Series to be used inFTMAD. Repeated uses ofFTMADcan be performed on this value with decreasing immediate index operands, to produce a single accumulated value approximating the Taylor Series result with a single outstanding factor.FTSSELcan be used to apply the final factor of x, 1.0, -x, or -1.0, dependent on the corresponding sine-wave quadrant q<1:0>, to produce a final sin() or cos() value. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "FTSMUL<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[e*:esize] =FPTrigSMul{esize}(element1, element2, FPCR());\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "FTSSEL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/FTSSEL--Floating-point-trigonometric-select-coefficient-?lang=en",
    "description": "Floating-point trigonometric select coefficient This instruction selects either the element of the first source vector or the value 1.0, based on bit<0> of the corresponding element of the second source element, negates the result if bit<1> of the corresponding element of the second source element is set, and places the results in the destination vector. This instruction is unpredicated. FTSSELcan be combined withFTSMULandFTMADto calculate values for sin(k) and cos(k). For more information, seeFTSMUL. The use of the second operand is consistent with it holding an integer corresponding to the desired sine-wave quadrant. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "FTSSEL<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[e*:esize] =FPTrigSSel{esize}(element1, element2);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "HISTCNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/HISTCNT--Count-matching-elements-in-vector-?lang=en",
    "description": "Count matching elements in vector This instruction compares each active 32 or 64-bit element of the first source vector with all Active elements with an element number less than or equal to its own in the second source vector, and places the count of matching elements in the corresponding element of the destination vector. Inactive elements in the destination vector are set to zero. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "HISTCNT<Zd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1dovarcount : integer =0;ifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : bits(esize) = operand1[e*:esize];fori =0to edoifActivePredicateElement{PL}(mask, i, esize) thenletelement2 : bits(esize) = operand2[i*:esize];ifelement1 == element2 then\n                    count = count +1;\n                end;\n            end;\n        end;\n    end;\n    result[e*:esize] = count[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "HISTSEG",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/HISTSEG--Count-matching-elements-in-vector-segments-?lang=en",
    "description": "Count matching elements in vector segments This instruction compares each 8-bit byte element of the first source vector with all of the elements in the corresponding 128-bit segment of the second source vector and places the count of matching elements in the corresponding element of the destination vector. This instruction is unpredicated. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "HISTSEG<Zd>.B,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);forb =0to segments-1dofors =0to eltspersegment-1dovarcount : integer =0;lete : integer = eltspersegment * b + s;letelement1 : bits(esize) = operand1[e*:esize];fori =0to eltspersegment-1dolete2 : integer = eltspersegment * b + i;letelement2 : bits(esize) = operand2[e2*:esize];ifelement1 == element2 then\n                count = count +1;\n            end;\n        end;\n        result[e*:esize] = count[esize-1:0];\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "INCB, INCD, INCH, INCW (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/INCB--INCD--INCH--INCW--scalar---Increment-scalar-by-multiple-of-predicate-constraint-element-count-?lang=en",
    "description": "Increment scalar by multiple of predicate constraint element count This instruction determines the number of active elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment the scalar destination. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 4 classes:Byte,Doubleword,HalfwordandWord",
    "syntax": [
      "INCW<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letVL : integer{} =CurrentVL();letoperand1 :bits(64)= X{}(dn);X{64}(dn) = operand1 + (count * imm);",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "INCD, INCH, INCW (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/INCD--INCH--INCW--vector---Increment-vector-by-multiple-of-predicate-constraint-element-count-?lang=en",
    "description": "Increment vector by multiple of predicate constraint element count This instruction determines the number of active elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment all destination vector elements. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 3 classes:Doubleword,HalfwordandWord",
    "syntax": [
      "INCW<Zdn>.S{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doresult[e*:esize] = operand1[e*:esize] + (count * imm);\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "INCP (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/INCP--scalar---Increment-scalar-by-count-of-true-predicate-elements-?lang=en",
    "description": "Increment scalar by count of true predicate elements This instruction determines the number of true elements in the source predicate and then uses the result to increment the scalar destination.",
    "syntax": [
      "INCP<Xdn>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(64) = X{}(dn);letoperand2 : bits(PL) = P{}(m);varcount : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(operand2, e, esize) then\n        count = count +1;\n    end;\nend;X{64}(dn) = operand1 + count;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "INCP (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/INCP--vector---Increment-vector-by-count-of-true-predicate-elements-?lang=en",
    "description": "Increment vector by count of true predicate elements This instruction determines the number of true elements in the source predicate and then uses the result to increment all destination vector elements. The predicate size specifier may be omitted in assembler source code, but this is deprecated and will be prohibited in a future release of the architecture.",
    "syntax": [
      "INCP<Zdn>.<T>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(PL) = P{}(m);varresult : bits(VL);varcount : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(operand2, e, esize) then\n        count = count +1;\n    end;\nend;fore =0to elements-1doresult[e*:esize] = operand1[e*:esize] + count;\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "INDEX (immediate, scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/INDEX--immediate--scalar---Create-index-starting-from-immediate-and-incremented-by-general-purpose-register-?lang=en",
    "description": "Create index starting from immediate and incremented by general-purpose register This instruction populates the destination vector by setting the first element to the first signed immediate integer operand and monotonically incrementing the value by the second signed scalar integer operand for each subsequent element. The scalar source operand is a general-purpose register in which only the least significant bits corresponding to the vector element size are used and any remaining bits are ignored. This instruction is unpredicated.",
    "syntax": [
      "INDEX<Zd>.<T>, #<imm>,<R><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand2 : bits(esize) = X{}(m);letelement2 : integer = SInt(operand2);varresult : bits(VL);fore =0to elements-1doletindex : integer = imm + e * element2;\n    result[e*:esize] = index[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "INDEX (immediates)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/INDEX--immediates---Create-index-starting-from-and-incremented-by-immediate-?lang=en",
    "description": "Create index starting from and incremented by immediate This instruction populates the destination vector by setting the first element to the first signed immediate integer operand and monotonically incrementing the value by the second signed immediate integer operand for each subsequent element. This instruction is unpredicated.",
    "syntax": [
      "INDEX<Zd>.<T>, #<imm1>, #<imm2>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;varresult : bits(VL);fore =0to elements-1doletindex : integer = imm1 + e * imm2;\n    result[e*:esize] = index[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "INDEX (scalar, immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/INDEX--scalar--immediate---Create-index-starting-from-general-purpose-register-and-incremented-by-immediate-?lang=en",
    "description": "Create index starting from general-purpose register and incremented by immediate This instruction populates the destination vector by setting the first element to the first signed scalar integer operand and monotonically incrementing the value by the second signed immediate integer operand for each subsequent element. The scalar source operand is a general-purpose register in which only the least significant bits corresponding to the vector element size are used and any remaining bits are ignored. This instruction is unpredicated.",
    "syntax": [
      "INDEX<Zd>.<T>,<R><n>, #<imm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(esize) = X{}(n);letelement1 : integer = SInt(operand1);varresult : bits(VL);fore =0to elements-1doletindex : integer = element1 + e * imm;\n    result[e*:esize] = index[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "INDEX (scalars)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/INDEX--scalars---Create-index-starting-from-and-incremented-by-general-purpose-register-?lang=en",
    "description": "Create index starting from and incremented by general-purpose register This instruction populates the destination vector by setting the first element to the first signed scalar integer operand and monotonically incrementing the value by the second signed scalar integer operand for each subsequent element. The scalar source operands are general-purpose registers in which only the least significant bits corresponding to the vector element size are used and any remaining bits are ignored. This instruction is unpredicated.",
    "syntax": [
      "INDEX<Zd>.<T>,<R><n>,<R><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(esize) = X{}(n);letelement1 : integer = SInt(operand1);letoperand2 : bits(esize) = X{}(m);letelement2 : integer = SInt(operand2);varresult : bits(VL);fore =0to elements-1doletindex : integer = element1 + e * element2;\n    result[e*:esize] = index[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "INSR (SIMD&FP scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/INSR--SIMD-FP-scalar---Insert-SIMD-FP-scalar-register-in-shifted-vector-?lang=en",
    "description": "Insert SIMD&FP scalar register in shifted vector This instruction shifts the destination vector left by one element, and then places a copy of the SIMD&FP scalar register in element 0 of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "INSR<Zdn>.<T>,<V><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letdest :bits(VL)= Z{}(dn);letsrc :bits(esize)= V{}(m);Z{VL}(dn) = dest[(VL-esize)-1:0] :: src;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "INSR (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/INSR--scalar---Insert-general-purpose-register-in-shifted-vector-?lang=en",
    "description": "Insert general-purpose register in shifted vector This instruction shifts the destination vector left by one element, and then places a copy of the least-significant bits of the general-purpose register in element 0 of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "INSR<Zdn>.<T>,<R><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letdest :bits(VL)= Z{}(dn);letsrc :bits(esize)= X{}(m);Z{VL}(dn) = dest[(VL-esize)-1:0] :: src;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "LASTA (SIMD&FP scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LASTA--SIMD-FP-scalar---Extract-element-after-last-to-SIMD-FP-scalar-register-?lang=en",
    "description": "Extract element after last to SIMD&FP scalar register This instruction extracts the element after the Last active element to a SIMD&FP scalar register. If there is an Active element, the element after the Last active element modulo the number of elements from the final source vector register is extracted. If there are no Active elements, element zero is extracted. Then the extracted element is placed in the destination SIMD&FP scalar register.",
    "syntax": [
      "LASTA<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) = Z{}(n);varlast : integer =LastActiveElement{PL}(mask, esize);ifisBefore theniflast <0then last = elements -1; end;elselast = last +1;iflast >= elements then last =0; end;\nend;V{esize}(d) = operand[last*:esize];",
    "operational_info": ""
  },
  {
    "title": "LASTA (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LASTA--scalar---Extract-element-after-last-to-general-purpose-register-?lang=en",
    "description": "Extract element after last to general-purpose register This instruction extracts the element after the Last active element to a general-purpose register. If there is an Active element, the element after the Last active element modulo the number of elements from the final source vector register is extracted. If there are no Active elements, element zero is extracted. Then the extracted element is zero-extended and placed in the destination general-purpose register.",
    "syntax": [
      "LASTA<R><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) = Z{}(n);varresult : bits(rsize);varlast : integer =LastActiveElement{PL}(mask, esize);ifisBefore theniflast <0then last = elements -1; end;elselast = last +1;iflast >= elements then last =0; end;\nend;\nresult = ZeroExtend{rsize}(operand[last*:esize]);X{rsize}(d) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "LASTB (SIMD&FP scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LASTB--SIMD-FP-scalar---Extract-last-element-to-SIMD-FP-scalar-register-?lang=en",
    "description": "Extract last element to SIMD&FP scalar register This instruction extracts the Last active element to a SIMD&FP scalar register. If there is an Active element, the Last active element from the final source vector register is extracted. If there are no Active elements, the highest-numbered element is extracted. Then the extracted element is placed in the destination SIMD&FP register.",
    "syntax": [
      "LASTB<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) = Z{}(n);varlast : integer =LastActiveElement{PL}(mask, esize);ifisBefore theniflast <0then last = elements -1; end;elselast = last +1;iflast >= elements then last =0; end;\nend;V{esize}(d) = operand[last*:esize];",
    "operational_info": ""
  },
  {
    "title": "LASTB (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LASTB--scalar---Extract-last-element-to-general-purpose-register-?lang=en",
    "description": "Extract last element to general-purpose register This instruction extracts the Last active element to a general-purpose register. If there is an Active element, the Last active element from the final source vector register is extracted. If there are no Active elements, the highest-numbered element is extracted. Then the extracted element is zero-extended and placed in the destination general-purpose register.",
    "syntax": [
      "LASTB<R><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) = Z{}(n);varresult : bits(rsize);varlast : integer =LastActiveElement{PL}(mask, esize);ifisBefore theniflast <0then last = elements -1; end;elselast = last +1;iflast >= elements then last =0; end;\nend;\nresult = ZeroExtend{rsize}(operand[last*:esize]);X{rsize}(d) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "LASTP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LASTP--Scalar-index-of-last-true-predicate-element--predicated--?lang=en",
    "description": "Scalar index of last true predicate element (predicated) This instruction determines the index of the last active and true element of the source predicate, and places the scalar result in the destination general-purpose register. If there are no active and true elements, the destination general-purpose register is updated to the value minus one.",
    "syntax": [
      "LASTP<Xd>,<Pg>,<Pn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(PL) = P{}(n);varindex : integer =-1;fore = elements-1downto0doif(index ==-1&&ActivePredicateElement{PL}(mask, e, esize) &&ActivePredicateElement{PL}(operand, e, esize)) then\n        index = e;\n    end;\nend;X{64}(d) = index[63:0];",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "LD1B (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1B--scalar-plus-immediate--consecutive-registers---Contiguous-load-of-bytes-to-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous load of bytes to multiple consecutive vectors (immediate index) This instruction performs a contiguous load of unsigned bytes to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LD1B {<Zt1>.B-<Zt4>.B },<PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1B (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1B--scalar-plus-immediate--single-register---Contiguous-load-unsigned-bytes-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load unsigned bytes to vector (immediate index) This instruction performs a contiguous load of unsigned bytes to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 4 classes:8-bit element,16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LD1B {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varaddr:bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);addr=AddressAdd(base,offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1B (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1B--scalar-plus-scalar--consecutive-registers---Contiguous-load-of-bytes-to-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous load of bytes to multiple consecutive vectors (scalar index) This instruction performs a contiguous load of unsigned bytes to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LD1B {<Zt1>.B-<Zt4>.B },<PNg>/Z, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1B (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1B--scalar-plus-scalar--single-register---Contiguous-load-unsigned-bytes-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load unsigned bytes to vector (scalar index) This instruction performs a contiguous load of unsigned bytes to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 4 classes:8-bit element,16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LD1B {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varresult :bits(VL);vardata:bits(msize);varoffset :bits(64);varaddr:bits(64);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);offset=X{64}(m);addr=AddressAdd(base,UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1B (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1B--scalar-plus-vector---Gather-load-unsigned-bytes-to-vector--vector-index--?lang=en",
    "description": "Gather load unsigned bytes to vector (vector index) This instruction performs a gather load of unsigned bytes to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally sign-extended or zero-extended from 32 to 64 bits. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:32-bit unpacked unscaled offset,32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "LD1B {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(VL);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);\n        data =Mem{msize}(addr, accdesc);\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1B (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1B--vector-plus-immediate---Gather-load-unsigned-bytes-to-vector--immediate-index--?lang=en",
    "description": "Gather load unsigned bytes to vector (immediate index) This instruction performs a gather load of unsigned bytes to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is in the range 0 to 31. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LD1B {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset * mbytes, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1D (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1D--scalar-plus-immediate--consecutive-registers---Contiguous-load-of-doublewords-to-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous load of doublewords to multiple consecutive vectors (immediate index) This instruction performs a contiguous load of unsigned doublewords to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LD1D {<Zt1>.D-<Zt4>.D },<PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1D (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1D--scalar-plus-immediate--single-register---Contiguous-load-unsigned-doublewords-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load unsigned doublewords to vector (immediate index) This instruction performs a contiguous load of unsigned doublewords to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. The 128-bit element variant is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:64-bit elementand128-bit element",
    "syntax": [
      "LD1D {<Zt>.Q },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifesize <128thenCheckSVEEnabled(); elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varaddr:bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);addr=AddressAdd(base,offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1D (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1D--scalar-plus-scalar--consecutive-registers---Contiguous-load-of-doublewords-to-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous load of doublewords to multiple consecutive vectors (scalar index) This instruction performs a contiguous load of unsigned doublewords to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LD1D {<Zt1>.D-<Zt4>.D },<PNg>/Z, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1D (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1D--scalar-plus-scalar--single-register---Contiguous-load-unsigned-doublewords-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load unsigned doublewords to vector (scalar index) This instruction performs a contiguous load of unsigned doublewords to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 8 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. The 128-bit element variant is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:64-bit elementand128-bit element",
    "syntax": [
      "LD1D {<Zt>.Q },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifesize <128thenCheckSVEEnabled(); elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varresult :bits(VL);vardata:bits(msize);varoffset :bits(64);varaddr:bits(64);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);offset=X{64}(m);addr=AddressAdd(base,UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1D (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1D--scalar-plus-vector---Gather-load-doublewords-to-vector--vector-index--?lang=en",
    "description": "Gather load doublewords to vector (vector index) This instruction performs a gather load of doublewords to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 8. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 4 classes:32-bit unpacked scaled offset,32-bit unpacked unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "LD1D {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(VL);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);\n        data =Mem{msize}(addr, accdesc);\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1D (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1D--vector-plus-immediate---Gather-load-doublewords-to-vector--immediate-index--?lang=en",
    "description": "Gather load doublewords to vector (immediate index) This instruction performs a gather load of doublewords to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is a multiple of 8 in the range 0 to 248. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LD1D {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset * mbytes, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1H (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1H--scalar-plus-immediate--consecutive-registers---Contiguous-load-of-halfwords-to-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous load of halfwords to multiple consecutive vectors (immediate index) This instruction performs a contiguous load of unsigned halfwords to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LD1H {<Zt1>.H-<Zt4>.H },<PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1H (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1H--scalar-plus-immediate--single-register---Contiguous-load-unsigned-halfwords-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load unsigned halfwords to vector (immediate index) This instruction performs a contiguous load of unsigned halfwords to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 3 classes:16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LD1H {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varaddr:bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);addr=AddressAdd(base,offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1H (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1H--scalar-plus-scalar--consecutive-registers---Contiguous-load-of-halfwords-to-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous load of halfwords to multiple consecutive vectors (scalar index) This instruction performs a contiguous load of unsigned halfwords to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LD1H {<Zt1>.H-<Zt4>.H },<PNg>/Z, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1H (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1H--scalar-plus-scalar--single-register---Contiguous-load-unsigned-halfwords-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load unsigned halfwords to vector (scalar index) This instruction performs a contiguous load of unsigned halfwords to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 2 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 3 classes:16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LD1H {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varresult :bits(VL);vardata:bits(msize);varoffset :bits(64);varaddr:bits(64);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);offset=X{64}(m);addr=AddressAdd(base,UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1H (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1H--scalar-plus-vector---Gather-load-unsigned-halfwords-to-vector--vector-index--?lang=en",
    "description": "Gather load unsigned halfwords to vector (vector index) This instruction performs a gather load of unsigned halfwords to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 2. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 6 classes:32-bit scaled offset,32-bit unpacked scaled offset,32-bit unpacked unscaled offset,32-bit unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "LD1H {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(VL);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);\n        data =Mem{msize}(addr, accdesc);\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1H (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1H--vector-plus-immediate---Gather-load-unsigned-halfwords-to-vector--immediate-index--?lang=en",
    "description": "Gather load unsigned halfwords to vector (immediate index) This instruction performs a gather load of unsigned halfwords to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is a multiple of 2 in the range 0 to 62. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LD1H {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset * mbytes, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1Q",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1Q--Gather-load-quadwords-?lang=en",
    "description": "Gather load quadwords This instruction performs a gather load of quadwords to Active elements of a vector register from memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LD1Q {<Zt>.Q },<Pg>/Z, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;CheckNonStreamingSVEEnabled();letelements : integer = VL DIV128;letmask :bits(PL)= P{}(g);varbase:bits(VL);varoffset :bits(64);varresult :bits(VL);letcontiguous :boolean= FALSE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask,128) thenbase=Z{VL}(n);offset =X{64}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e,128) then\n        letbaddr:bits(64)=base[(2*e)*:64];letaddr:bits(64)=AddressAdd(baddr,offset, accdesc);result[e*:128] =Mem{128}(addr,accdesc);elseresult[e*:128] = Zeros{128};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RB--Load-and-broadcast-unsigned-byte-to-vector-?lang=en",
    "description": "Load and broadcast unsigned byte to vector This instruction loads a single unsigned byte from a memory address generated by a 64-bit scalar base address plus an immediate offset that is in the range 0 to 63. The loaded data is broadcast into all Active elements of the destination vector, setting the Inactive elements to zero. If all elements are inactive, then the instruction will not perform a read from Device memory or cause a data abort. It has encodings from 4 classes:8-bit element,16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LD1RB {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);letaddr : bits(64) =AddressAdd(base, offset * mbytes, accdesc);\n    data =Mem{msize}(addr, accdesc);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RD--Load-and-broadcast-doubleword-to-vector-?lang=en",
    "description": "Load and broadcast doubleword to vector This instruction loads a single doubleword from a memory address generated by a 64-bit scalar base address plus an immediate offset that is a multiple of 8 in the range 0 to 504. The loaded data is broadcast into all Active elements of the destination vector, setting the Inactive elements to zero. If all elements are inactive, then the instruction will not perform a read from Device memory or cause a data abort.",
    "syntax": [
      "LD1RD {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);letaddr : bits(64) =AddressAdd(base, offset * mbytes, accdesc);\n    data =Mem{msize}(addr, accdesc);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RH",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RH--Load-and-broadcast-unsigned-halfword-to-vector-?lang=en",
    "description": "Load and broadcast unsigned halfword to vector This instruction loads a single unsigned halfword from a memory address generated by a 64-bit scalar base address plus an immediate offset that is a multiple of 2 in the range 0 to 126. The loaded data is broadcast into all Active elements of the destination vector, setting the Inactive elements to zero. If all elements are inactive, then the instruction will not perform a read from Device memory or cause a data abort. It has encodings from 3 classes:16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LD1RH {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);letaddr : bits(64) =AddressAdd(base, offset * mbytes, accdesc);\n    data =Mem{msize}(addr, accdesc);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1ROB (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1ROB--scalar-plus-immediate---Contiguous-load-and-replicate-thirty-two-bytes--immediate-index--?lang=en",
    "description": "Contiguous load and replicate thirty-two bytes (immediate index) This instruction loads thirty-two contiguous bytes to elements of a 256-bit (octaword) vector from the memory address generated by a 64-bit scalar base address and immediate index that is a multiple of 32 in the range -256 to +224 added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting 256-bit vector is then replicated to fill the destination vector. The instruction requires that the Effective SVE vector length is at least 256 bits. Only the first thirty-two predicate elements are used and higher numbered predicate elements are ignored. ID_AA64ZFR0_EL1.F64MM indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LD1ROB {<Zt>.B },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_F64MM",
      "FEAT_SME"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;ifVL <256thenEndOfDecode(Decode_UNDEF); end;letelements : integer =256DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low bits onlyvaraddr : bits(64);varresult : bits(256);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = ZeroExtend{VL}(Replicate{VL}(result));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1ROB (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1ROB--scalar-plus-scalar---Contiguous-load-and-replicate-thirty-two-bytes--scalar-index--?lang=en",
    "description": "Contiguous load and replicate thirty-two bytes (scalar index) This instruction loads thirty-two contiguous bytes to elements of a 256-bit (octaword) vector from the memory address generated by a 64-bit scalar base address and scalar index that is added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting 256-bit vector is then replicated to fill the destination vector. The instruction requires that the Effective SVE vector length is at least 256 bits. Only the first thirty-two predicate elements are used and higher numbered predicate elements are ignored. ID_AA64ZFR0_EL1.F64MM indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LD1ROB {<Zt>.B },<Pg>/Z, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_F64MM",
      "FEAT_SME"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;ifVL <256thenEndOfDecode(Decode_UNDEF); end;letelements : integer =256DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low bits onlyvaroffset : bits(64);varaddr : bits(64);varresult : bits(256);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = ZeroExtend{VL}(Replicate{VL}(result));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1ROD (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1ROD--scalar-plus-immediate---Contiguous-load-and-replicate-four-doublewords--immediate-index--?lang=en",
    "description": "Contiguous load and replicate four doublewords (immediate index) This instruction loads four contiguous doublewords to elements of a 256-bit (octaword) vector from the memory address generated by a 64-bit scalar base address and immediate index that is a multiple of 32 in the range -256 to +224 added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting 256-bit vector is then replicated to fill the destination vector. The instruction requires that the Effective SVE vector length is at least 256 bits. Only the first four predicate elements are used and higher numbered predicate elements are ignored. ID_AA64ZFR0_EL1.F64MM indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LD1ROD {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_F64MM",
      "FEAT_SME"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;ifVL <256thenEndOfDecode(Decode_UNDEF); end;letelements : integer =256DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low bits onlyvaraddr : bits(64);varresult : bits(256);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = ZeroExtend{VL}(Replicate{VL}(result));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1ROD (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1ROD--scalar-plus-scalar---Contiguous-load-and-replicate-four-doublewords--scalar-index--?lang=en",
    "description": "Contiguous load and replicate four doublewords (scalar index) This instruction loads four contiguous doublewords to elements of a 256-bit (octaword) vector from the memory address generated by a 64-bit scalar base address and scalar index that is multiplied by 8 and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting 256-bit vector is then replicated to fill the destination vector. The instruction requires that the Effective SVE vector length is at least 256 bits. Only the first four predicate elements are used and higher numbered predicate elements are ignored. ID_AA64ZFR0_EL1.F64MM indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LD1ROD {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_F64MM",
      "FEAT_SME"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;ifVL <256thenEndOfDecode(Decode_UNDEF); end;letelements : integer =256DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low bits onlyvaroffset : bits(64);varaddr : bits(64);varresult : bits(256);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = ZeroExtend{VL}(Replicate{VL}(result));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1ROH (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1ROH--scalar-plus-immediate---Contiguous-load-and-replicate-sixteen-halfwords--immediate-index--?lang=en",
    "description": "Contiguous load and replicate sixteen halfwords (immediate index) This instruction loads sixteen contiguous halfwords to elements of a 256-bit (octaword) vector from the memory address generated by a 64-bit scalar base address and immediate index that is a multiple of 32 in the range -256 to +224 added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting 256-bit vector is then replicated to fill the destination vector. The instruction requires that the Effective SVE vector length is at least 256 bits. Only the first sixteen predicate elements are used and higher numbered predicate elements are ignored. ID_AA64ZFR0_EL1.F64MM indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LD1ROH {<Zt>.H },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_F64MM",
      "FEAT_SME"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;ifVL <256thenEndOfDecode(Decode_UNDEF); end;letelements : integer =256DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low bits onlyvaraddr : bits(64);varresult : bits(256);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = ZeroExtend{VL}(Replicate{VL}(result));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1ROH (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1ROH--scalar-plus-scalar---Contiguous-load-and-replicate-sixteen-halfwords--scalar-index--?lang=en",
    "description": "Contiguous load and replicate sixteen halfwords (scalar index) This instruction loads sixteen contiguous halfwords to elements of a 256-bit (octaword) vector from the memory address generated by a 64-bit scalar base address and scalar index that is multiplied by 2 and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting 256-bit vector is then replicated to fill the destination vector. The instruction requires that the Effective SVE vector length is at least 256 bits. Only the first sixteen predicate elements are used and higher numbered predicate elements are ignored. ID_AA64ZFR0_EL1.F64MM indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LD1ROH {<Zt>.H },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_F64MM",
      "FEAT_SME"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;ifVL <256thenEndOfDecode(Decode_UNDEF); end;letelements : integer =256DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low bits onlyvaroffset : bits(64);varaddr : bits(64);varresult : bits(256);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = ZeroExtend{VL}(Replicate{VL}(result));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1ROW (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1ROW--scalar-plus-immediate---Contiguous-load-and-replicate-eight-words--immediate-index--?lang=en",
    "description": "Contiguous load and replicate eight words (immediate index) This instruction loads eight contiguous words to elements of a 256-bit (octaword) vector from the memory address generated by a 64-bit scalar base address and immediate index that is a multiple of 32 in the range -256 to +224 added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting 256-bit vector is then replicated to fill the destination vector. The instruction requires that the Effective SVE vector length is at least 256 bits. Only the first eight predicate elements are used and higher numbered predicate elements are ignored. ID_AA64ZFR0_EL1.F64MM indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LD1ROW {<Zt>.S },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_F64MM",
      "FEAT_SME"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;ifVL <256thenEndOfDecode(Decode_UNDEF); end;letelements : integer =256DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low bits onlyvaraddr : bits(64);varresult : bits(256);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = ZeroExtend{VL}(Replicate{VL}(result));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1ROW (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1ROW--scalar-plus-scalar---Contiguous-load-and-replicate-eight-words--scalar-index--?lang=en",
    "description": "Contiguous load and replicate eight words (scalar index) This instruction loads eight contiguous words to elements of a 256-bit (octaword) vector from the memory address generated by a 64-bit scalar base address and scalar index that is multiplied by 4 and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting 256-bit vector is then replicated to fill the destination vector. The instruction requires that the Effective SVE vector length is at least 256 bits. Only the first eight predicate elements are used and higher numbered predicate elements are ignored. ID_AA64ZFR0_EL1.F64MM indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LD1ROW {<Zt>.S },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_F64MM",
      "FEAT_SME"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;ifVL <256thenEndOfDecode(Decode_UNDEF); end;letelements : integer =256DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low bits onlyvaroffset : bits(64);varaddr : bits(64);varresult : bits(256);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = ZeroExtend{VL}(Replicate{VL}(result));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RQB (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RQB--scalar-plus-immediate---Contiguous-load-and-replicate-sixteen-bytes--immediate-index--?lang=en",
    "description": "Contiguous load and replicate sixteen bytes (immediate index) This instruction loads sixteen contiguous bytes to elements of a short, 128-bit (quadword) vector from the memory address generated by a 64-bit scalar base address and immediate index that is a multiple of 16 in the range -128 to +112 added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting short vector is then replicated to fill the long destination vector. Only the first sixteen predicate elements are used and higher numbered predicate elements are ignored.",
    "syntax": [
      "LD1RQB {<Zt>.B },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer =128DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low 16 bits onlyvaraddr : bits(64);varresult : bits(128);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset *16, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = Replicate{VL}(result);",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RQB (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RQB--scalar-plus-scalar---Contiguous-load-and-replicate-sixteen-bytes--scalar-index--?lang=en",
    "description": "Contiguous load and replicate sixteen bytes (scalar index) This instruction loads sixteen contiguous bytes to elements of a short, 128-bit (quadword) vector from the memory address generated by a 64-bit scalar base address and scalar index that is added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting short vector is then replicated to fill the long destination vector. Only the first sixteen predicate elements are used and higher numbered predicate elements are ignored.",
    "syntax": [
      "LD1RQB {<Zt>.B },<Pg>/Z, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer =128DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low 16 bits onlyvaroffset : bits(64);varaddr : bits(64);varresult : bits(128);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = Replicate{VL}(result);",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RQD (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RQD--scalar-plus-immediate---Contiguous-load-and-replicate-two-doublewords--immediate-index--?lang=en",
    "description": "Contiguous load and replicate two doublewords (immediate index) This instruction loads two contiguous doublewords to elements of a short, 128-bit (quadword) vector from the memory address generated by a 64-bit scalar base address and immediate index that is a multiple of 16 in the range -128 to +112 added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting short vector is then replicated to fill the long destination vector. Only the first two predicate elements are used and higher numbered predicate elements are ignored.",
    "syntax": [
      "LD1RQD {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer =128DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low 16 bits onlyvaraddr : bits(64);varresult : bits(128);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset *16, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = Replicate{VL}(result);",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RQD (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RQD--scalar-plus-scalar---Contiguous-load-and-replicate-two-doublewords--scalar-index--?lang=en",
    "description": "Contiguous load and replicate two doublewords (scalar index) This instruction loads two contiguous doublewords to elements of a short, 128-bit (quadword) vector from the memory address generated by a 64-bit scalar base address and scalar index that is multiplied by 8 and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting short vector is then replicated to fill the long destination vector. Only the first two predicate elements are used and higher numbered predicate elements are ignored.",
    "syntax": [
      "LD1RQD {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer =128DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low 16 bits onlyvaroffset : bits(64);varaddr : bits(64);varresult : bits(128);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = Replicate{VL}(result);",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RQH (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RQH--scalar-plus-immediate---Contiguous-load-and-replicate-eight-halfwords--immediate-index--?lang=en",
    "description": "Contiguous load and replicate eight halfwords (immediate index) This instruction loads eight contiguous halfwords to elements of a short, 128-bit (quadword) vector from the memory address generated by a 64-bit scalar base address and immediate index that is a multiple of 16 in the range -128 to +112 added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting short vector is then replicated to fill the long destination vector. Only the first eight predicate elements are used and higher numbered predicate elements are ignored.",
    "syntax": [
      "LD1RQH {<Zt>.H },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer =128DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low 16 bits onlyvaraddr : bits(64);varresult : bits(128);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset *16, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = Replicate{VL}(result);",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RQH (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RQH--scalar-plus-scalar---Contiguous-load-and-replicate-eight-halfwords--scalar-index--?lang=en",
    "description": "Contiguous load and replicate eight halfwords (scalar index) This instruction loads eight contiguous halfwords to elements of a short, 128-bit (quadword) vector from the memory address generated by a 64-bit scalar base address and scalar index that is multiplied by 2 and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting short vector is then replicated to fill the long destination vector. Only the first eight predicate elements are used and higher numbered predicate elements are ignored.",
    "syntax": [
      "LD1RQH {<Zt>.H },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer =128DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low 16 bits onlyvaroffset : bits(64);varaddr : bits(64);varresult : bits(128);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = Replicate{VL}(result);",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RQW (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RQW--scalar-plus-immediate---Contiguous-load-and-replicate-four-words--immediate-index--?lang=en",
    "description": "Contiguous load and replicate four words (immediate index) This instruction loads four contiguous words to elements of a short, 128-bit (quadword) vector from the memory address generated by a 64-bit scalar base address and immediate index that is a multiple of 16 in the range -128 to +112 added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting short vector is then replicated to fill the long destination vector. Only the first four predicate elements are used and higher numbered predicate elements are ignored.",
    "syntax": [
      "LD1RQW {<Zt>.S },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer =128DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low 16 bits onlyvaraddr : bits(64);varresult : bits(128);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset *16, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = Replicate{VL}(result);",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RQW (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RQW--scalar-plus-scalar---Contiguous-load-and-replicate-four-words--scalar-index--?lang=en",
    "description": "Contiguous load and replicate four words (scalar index) This instruction loads four contiguous words to elements of a short, 128-bit (quadword) vector from the memory address generated by a 64-bit scalar base address and scalar index that is multiplied by 4 and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero. The resulting short vector is then replicated to fill the long destination vector. Only the first four predicate elements are used and higher numbered predicate elements are ignored.",
    "syntax": [
      "LD1RQW {<Zt>.S },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer =128DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);// low 16 bits onlyvaroffset : bits(64);varaddr : bits(64);varresult : bits(128);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = Replicate{VL}(result);",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RSB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RSB--Load-and-broadcast-signed-byte-to-vector-?lang=en",
    "description": "Load and broadcast signed byte to vector This instruction loads a single signed byte from a memory address generated by a 64-bit scalar base address plus an immediate offset that is in the range 0 to 63. The loaded data is broadcast into all Active elements of the destination vector, setting the Inactive elements to zero. If all elements are inactive, then the instruction will not perform a read from Device memory or cause a data abort. It has encodings from 3 classes:16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LD1RSB {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);letaddr : bits(64) =AddressAdd(base, offset * mbytes, accdesc);\n    data =Mem{msize}(addr, accdesc);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RSH",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RSH--Load-and-broadcast-signed-halfword-to-vector-?lang=en",
    "description": "Load and broadcast signed halfword to vector This instruction loads a single signed halfword from a memory address generated by a 64-bit scalar base address plus an immediate offset that is a multiple of 2 in the range 0 to 126. The loaded data is broadcast into all Active elements of the destination vector, setting the Inactive elements to zero. If all elements are inactive, then the instruction will not perform a read from Device memory or cause a data abort. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LD1RSH {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);letaddr : bits(64) =AddressAdd(base, offset * mbytes, accdesc);\n    data =Mem{msize}(addr, accdesc);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RSW",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RSW--Load-and-broadcast-signed-word-to-vector-?lang=en",
    "description": "Load and broadcast signed word to vector This instruction loads a single signed word from a memory address generated by a 64-bit scalar base address plus an immediate offset that is a multiple of 4 in the range 0 to 252. The loaded data is broadcast into all Active elements of the destination vector, setting the Inactive elements to zero. If all elements are inactive, then the instruction will not perform a read from Device memory or cause a data abort.",
    "syntax": [
      "LD1RSW {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);letaddr : bits(64) =AddressAdd(base, offset * mbytes, accdesc);\n    data =Mem{msize}(addr, accdesc);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1RW",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1RW--Load-and-broadcast-unsigned-word-to-vector-?lang=en",
    "description": "Load and broadcast unsigned word to vector This instruction loads a single unsigned word from a memory address generated by a 64-bit scalar base address plus an immediate offset that is a multiple of 4 in the range 0 to 252. The loaded data is broadcast into all Active elements of the destination vector, setting the Inactive elements to zero. If all elements are inactive, then the instruction will not perform a read from Device memory or cause a data abort. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LD1RW {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);letaddr : bits(64) =AddressAdd(base, offset * mbytes, accdesc);\n    data =Mem{msize}(addr, accdesc);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1SB (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1SB--scalar-plus-immediate---Contiguous-load-signed-bytes-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load signed bytes to vector (immediate index) This instruction performs a contiguous load of signed bytes to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 3 classes:16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LD1SB {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varaddr:bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);addr=AddressAdd(base,offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1SB (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1SB--scalar-plus-scalar---Contiguous-load-signed-bytes-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load signed bytes to vector (scalar index) This instruction performs a contiguous load of signed bytes to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 3 classes:16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LD1SB {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varresult :bits(VL);vardata:bits(msize);varoffset :bits(64);varaddr:bits(64);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);offset=X{64}(m);addr=AddressAdd(base,UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1SB (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1SB--scalar-plus-vector---Gather-load-signed-bytes-to-vector--vector-index--?lang=en",
    "description": "Gather load signed bytes to vector (vector index) This instruction performs a gather load of signed bytes to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally sign-extended or zero-extended from 32 to 64 bits. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:32-bit unpacked unscaled offset,32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "LD1SB {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(VL);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);\n        data =Mem{msize}(addr, accdesc);\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1SB (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1SB--vector-plus-immediate---Gather-load-signed-bytes-to-vector--immediate-index--?lang=en",
    "description": "Gather load signed bytes to vector (immediate index) This instruction performs a gather load of signed bytes to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is in the range 0 to 31. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LD1SB {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset * mbytes, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1SH (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1SH--scalar-plus-immediate---Contiguous-load-signed-halfwords-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load signed halfwords to vector (immediate index) This instruction performs a contiguous load of signed halfwords to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LD1SH {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varaddr:bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);addr=AddressAdd(base,offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1SH (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1SH--scalar-plus-scalar---Contiguous-load-signed-halfwords-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load signed halfwords to vector (scalar index) This instruction performs a contiguous load of signed halfwords to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 2 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LD1SH {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varresult :bits(VL);vardata:bits(msize);varoffset :bits(64);varaddr:bits(64);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);offset=X{64}(m);addr=AddressAdd(base,UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1SH (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1SH--scalar-plus-vector---Gather-load-signed-halfwords-to-vector--vector-index--?lang=en",
    "description": "Gather load signed halfwords to vector (vector index) This instruction performs a gather load of signed halfwords to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 2. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 6 classes:32-bit scaled offset,32-bit unpacked scaled offset,32-bit unpacked unscaled offset,32-bit unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "LD1SH {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(VL);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);\n        data =Mem{msize}(addr, accdesc);\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1SH (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1SH--vector-plus-immediate---Gather-load-signed-halfwords-to-vector--immediate-index--?lang=en",
    "description": "Gather load signed halfwords to vector (immediate index) This instruction performs a gather load of signed halfwords to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is a multiple of 2 in the range 0 to 62. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LD1SH {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset * mbytes, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1SW (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1SW--scalar-plus-immediate---Contiguous-load-signed-words-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load signed words to vector (immediate index) This instruction performs a contiguous load of signed words to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector.",
    "syntax": [
      "LD1SW {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varaddr:bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);addr=AddressAdd(base,offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1SW (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1SW--scalar-plus-scalar---Contiguous-load-signed-words-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load signed words to vector (scalar index) This instruction performs a contiguous load of signed words to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 4 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector.",
    "syntax": [
      "LD1SW {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varresult :bits(VL);vardata:bits(msize);varoffset :bits(64);varaddr:bits(64);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);offset=X{64}(m);addr=AddressAdd(base,UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1SW (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1SW--scalar-plus-vector---Gather-load-signed-words-to-vector--vector-index--?lang=en",
    "description": "Gather load signed words to vector (vector index) This instruction performs a gather load of signed words to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 4. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 4 classes:32-bit unpacked scaled offset,32-bit unpacked unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "LD1SW {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(VL);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);\n        data =Mem{msize}(addr, accdesc);\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1SW (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1SW--vector-plus-immediate---Gather-load-signed-words-to-vector--immediate-index--?lang=en",
    "description": "Gather load signed words to vector (immediate index) This instruction performs a gather load of signed words to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is a multiple of 4 in the range 0 to 124. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LD1SW {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset * mbytes, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1W (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1W--scalar-plus-immediate--consecutive-registers---Contiguous-load-of-words-to-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous load of words to multiple consecutive vectors (immediate index) This instruction performs a contiguous load of unsigned words to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LD1W {<Zt1>.S-<Zt4>.S },<PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1W (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1W--scalar-plus-immediate--single-register---Contiguous-load-unsigned-words-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load unsigned words to vector (immediate index) This instruction performs a contiguous load of unsigned words to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. The 128-bit element variant is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:32-bit element,64-bit elementand128-bit element",
    "syntax": [
      "LD1W {<Zt>.Q },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifesize <128thenCheckSVEEnabled(); elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varaddr:bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);addr=AddressAdd(base,offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1W (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1W--scalar-plus-scalar--consecutive-registers---Contiguous-load-of-words-to-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous load of words to multiple consecutive vectors (scalar index) This instruction performs a contiguous load of unsigned words to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LD1W {<Zt1>.S-<Zt4>.S },<PNg>/Z, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1W (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1W--scalar-plus-scalar--single-register---Contiguous-load-unsigned-words-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load unsigned words to vector (scalar index) This instruction performs a contiguous load of unsigned words to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 4 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. The 128-bit element variant is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:32-bit element,64-bit elementand128-bit element",
    "syntax": [
      "LD1W {<Zt>.Q },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifesize <128thenCheckSVEEnabled(); elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase:bits(64);letmask :bits(PL)= P{}(g);varresult :bits(VL);vardata:bits(msize);varoffset :bits(64);varaddr:bits(64);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= TRUE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE) thenCheckSPAlignment();end;elseifn ==31thenCheckSPAlignment(); end;end;base=ifn ==31thenSP{64}()elseX{64}(n);offset=X{64}(m);addr=AddressAdd(base,UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thendata=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;addr=AddressIncrement(addr,mbytes, accdesc);end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1W (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1W--scalar-plus-vector---Gather-load-unsigned-words-to-vector--vector-index--?lang=en",
    "description": "Gather load unsigned words to vector (vector index) This instruction performs a gather load of unsigned words to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 4. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 6 classes:32-bit scaled offset,32-bit unpacked scaled offset,32-bit unpacked unscaled offset,32-bit unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "LD1W {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(VL);varresult : bits(VL);vardata : bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);\n        data =Mem{msize}(addr, accdesc);\n        result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD1W (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD1W--vector-plus-immediate---Gather-load-unsigned-words-to-vector--immediate-index--?lang=en",
    "description": "Gather load unsigned words to vector (immediate index) This instruction performs a gather load of unsigned words to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is a multiple of 4 in the range 0 to 124. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LD1W {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset * mbytes, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD2B (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD2B--scalar-plus-immediate---Contiguous-load-two-byte-structures-to-two-vectors--immediate-index--?lang=en",
    "description": "Contiguous load two-byte structures to two vectors (immediate index) This instruction performs a contiguous load of two-byte structures, each to the same element number in two vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 2 in the range -16 to 14 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive bytes in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the two destination vector registers.",
    "syntax": [
      "LD2B {<Zt1>.B,<Zt2>.B },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD2B (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD2B--scalar-plus-scalar---Contiguous-load-two-byte-structures-to-two-vectors--scalar-index--?lang=en",
    "description": "Contiguous load two-byte structures to two vectors (scalar index) This instruction performs a contiguous load of two-byte structures, each to the same element number in two vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register and added to the base address. After each structure access the index value is incremented by two. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive bytes in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the two destination vector registers.",
    "syntax": [
      "LD2B {<Zt1>.B,<Zt2>.B },<Pg>/Z, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD2D (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD2D--scalar-plus-immediate---Contiguous-load-two-doubleword-structures-to-two-vectors--immediate-index--?lang=en",
    "description": "Contiguous load two-doubleword structures to two vectors (immediate index) This instruction performs a contiguous load of two-doubleword structures, each to the same element number in two vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 2 in the range -16 to 14 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive doublewords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the two destination vector registers.",
    "syntax": [
      "LD2D {<Zt1>.D,<Zt2>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD2D (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD2D--scalar-plus-scalar---Contiguous-load-two-doubleword-structures-to-two-vectors--scalar-index--?lang=en",
    "description": "Contiguous load two-doubleword structures to two vectors (scalar index) This instruction performs a contiguous load of two-doubleword structures, each to the same element number in two vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by two. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive doublewords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the two destination vector registers.",
    "syntax": [
      "LD2D {<Zt1>.D,<Zt2>.D },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD2H (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD2H--scalar-plus-immediate---Contiguous-load-two-halfword-structures-to-two-vectors--immediate-index--?lang=en",
    "description": "Contiguous load two-halfword structures to two vectors (immediate index) This instruction performs a contiguous load of two-halfword structures, each to the same element number in two vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 2 in the range -16 to 14 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive halfwords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the two destination vector registers.",
    "syntax": [
      "LD2H {<Zt1>.H,<Zt2>.H },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD2H (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD2H--scalar-plus-scalar---Contiguous-load-two-halfword-structures-to-two-vectors--scalar-index--?lang=en",
    "description": "Contiguous load two-halfword structures to two vectors (scalar index) This instruction performs a contiguous load of two-halfword structures, each to the same element number in two vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by two. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive halfwords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the two destination vector registers.",
    "syntax": [
      "LD2H {<Zt1>.H,<Zt2>.H },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD2Q (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD2Q--scalar-plus-immediate---Contiguous-load-two-quadword-structures-to-two-vectors--immediate-index--?lang=en",
    "description": "Contiguous load two-quadword structures to two vectors (immediate index) This instruction performs a contiguous load of two-quadword structures, each to the same element number in two vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 2 in the range -16 to 14 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive quadwords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the two destination vector registers.",
    "syntax": [
      "LD2Q {<Zt1>.Q,<Zt2>.Q },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD2Q (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD2Q--scalar-plus-scalar---Contiguous-load-two-quadword-structures-to-two-vectors--scalar-index--?lang=en",
    "description": "Contiguous load two-quadword structures to two vectors (scalar index) This instruction performs a contiguous load of two-quadword structures, each to the same element number in two vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by two. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive quadwords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the two destination vector registers.",
    "syntax": [
      "LD2Q {<Zt1>.Q,<Zt2>.Q },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #4]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD2W (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD2W--scalar-plus-immediate---Contiguous-load-two-word-structures-to-two-vectors--immediate-index--?lang=en",
    "description": "Contiguous load two-word structures to two vectors (immediate index) This instruction performs a contiguous load of two-word structures, each to the same element number in two vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 2 in the range -16 to 14 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive words in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the two destination vector registers.",
    "syntax": [
      "LD2W {<Zt1>.S,<Zt2>.S },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD2W (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD2W--scalar-plus-scalar---Contiguous-load-two-word-structures-to-two-vectors--scalar-index--?lang=en",
    "description": "Contiguous load two-word structures to two vectors (scalar index) This instruction performs a contiguous load of two-word structures, each to the same element number in two vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by two. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive words in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the two destination vector registers.",
    "syntax": [
      "LD2W {<Zt1>.S,<Zt2>.S },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD3B (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD3B--scalar-plus-immediate---Contiguous-load-three-byte-structures-to-three-vectors--immediate-index--?lang=en",
    "description": "Contiguous load three-byte structures to three vectors (immediate index) This instruction performs a contiguous load of three-byte structures, each to the same element number in three vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 3 in the range -24 to 21 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive bytes in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the three destination vector registers.",
    "syntax": [
      "LD3B {<Zt1>.B,<Zt2>.B,<Zt3>.B },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD3B (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD3B--scalar-plus-scalar---Contiguous-load-three-byte-structures-to-three-vectors--scalar-index--?lang=en",
    "description": "Contiguous load three-byte structures to three vectors (scalar index) This instruction performs a contiguous load of three-byte structures, each to the same element number in three vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register and added to the base address. After each structure access the index value is incremented by three. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive bytes in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the three destination vector registers.",
    "syntax": [
      "LD3B {<Zt1>.B,<Zt2>.B,<Zt3>.B },<Pg>/Z, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD3D (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD3D--scalar-plus-immediate---Contiguous-load-three-doubleword-structures-to-three-vectors--immediate-index--?lang=en",
    "description": "Contiguous load three-doubleword structures to three vectors (immediate index) This instruction performs a contiguous load of three-doubleword structures, each to the same element number in three vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 3 in the range -24 to 21 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive doublewords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the three destination vector registers.",
    "syntax": [
      "LD3D {<Zt1>.D,<Zt2>.D,<Zt3>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD3D (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD3D--scalar-plus-scalar---Contiguous-load-three-doubleword-structures-to-three-vectors--scalar-index--?lang=en",
    "description": "Contiguous load three-doubleword structures to three vectors (scalar index) This instruction performs a contiguous load of three-doubleword structures, each to the same element number in three vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by three. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive doublewords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the three destination vector registers.",
    "syntax": [
      "LD3D {<Zt1>.D,<Zt2>.D,<Zt3>.D },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD3H (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD3H--scalar-plus-immediate---Contiguous-load-three-halfword-structures-to-three-vectors--immediate-index--?lang=en",
    "description": "Contiguous load three-halfword structures to three vectors (immediate index) This instruction performs a contiguous load of three-halfword structures, each to the same element number in three vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 3 in the range -24 to 21 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive halfwords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the three destination vector registers.",
    "syntax": [
      "LD3H {<Zt1>.H,<Zt2>.H,<Zt3>.H },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD3H (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD3H--scalar-plus-scalar---Contiguous-load-three-halfword-structures-to-three-vectors--scalar-index--?lang=en",
    "description": "Contiguous load three-halfword structures to three vectors (scalar index) This instruction performs a contiguous load of three-halfword structures, each to the same element number in three vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by three. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive halfwords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the three destination vector registers.",
    "syntax": [
      "LD3H {<Zt1>.H,<Zt2>.H,<Zt3>.H },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD3Q (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD3Q--scalar-plus-immediate---Contiguous-load-three-quadword-structures-to-three-vectors--immediate-index--?lang=en",
    "description": "Contiguous load three-quadword structures to three vectors (immediate index) This instruction performs a contiguous load of three-quadword structures, each to the same element number in three vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 3 in the range -24 to 21 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive quadwords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the three destination vector registers.",
    "syntax": [
      "LD3Q {<Zt1>.Q,<Zt2>.Q,<Zt3>.Q },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD3Q (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD3Q--scalar-plus-scalar---Contiguous-load-three-quadword-structures-to-three-vectors--scalar-index--?lang=en",
    "description": "Contiguous load three-quadword structures to three vectors (scalar index) This instruction performs a contiguous load of three-quadword structures, each to the same element number in three vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by three. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive quadwords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the three destination vector registers.",
    "syntax": [
      "LD3Q {<Zt1>.Q,<Zt2>.Q,<Zt3>.Q },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #4]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD3W (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD3W--scalar-plus-immediate---Contiguous-load-three-word-structures-to-three-vectors--immediate-index--?lang=en",
    "description": "Contiguous load three-word structures to three vectors (immediate index) This instruction performs a contiguous load of three-word structures, each to the same element number in three vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 3 in the range -24 to 21 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive words in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the three destination vector registers.",
    "syntax": [
      "LD3W {<Zt1>.S,<Zt2>.S,<Zt3>.S },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD3W (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD3W--scalar-plus-scalar---Contiguous-load-three-word-structures-to-three-vectors--scalar-index--?lang=en",
    "description": "Contiguous load three-word structures to three vectors (scalar index) This instruction performs a contiguous load of three-word structures, each to the same element number in three vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by three. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive words in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the three destination vector registers.",
    "syntax": [
      "LD3W {<Zt1>.S,<Zt2>.S,<Zt3>.S },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD4B (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD4B--scalar-plus-immediate---Contiguous-load-four-byte-structures-to-four-vectors--immediate-index--?lang=en",
    "description": "Contiguous load four-byte structures to four vectors (immediate index) This instruction performs a contiguous load of four-byte structures, each to the same element number in four vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 4 in the range -32 to 28 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive bytes in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the four destination vector registers.",
    "syntax": [
      "LD4B {<Zt1>.B,<Zt2>.B,<Zt3>.B,<Zt4>.B },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD4B (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD4B--scalar-plus-scalar---Contiguous-load-four-byte-structures-to-four-vectors--scalar-index--?lang=en",
    "description": "Contiguous load four-byte structures to four vectors (scalar index) This instruction performs a contiguous load of four-byte structures, each to the same element number in four vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register and added to the base address. After each structure access the index value is incremented by four. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive bytes in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the four destination vector registers.",
    "syntax": [
      "LD4B {<Zt1>.B,<Zt2>.B,<Zt3>.B,<Zt4>.B },<Pg>/Z, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD4D (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD4D--scalar-plus-immediate---Contiguous-load-four-doubleword-structures-to-four-vectors--immediate-index--?lang=en",
    "description": "Contiguous load four-doubleword structures to four vectors (immediate index) This instruction performs a contiguous load of four-doubleword structures, each to the same element number in four vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 4 in the range -32 to 28 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive doublewords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the four destination vector registers.",
    "syntax": [
      "LD4D {<Zt1>.D,<Zt2>.D,<Zt3>.D,<Zt4>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD4D (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD4D--scalar-plus-scalar---Contiguous-load-four-doubleword-structures-to-four-vectors--scalar-index--?lang=en",
    "description": "Contiguous load four-doubleword structures to four vectors (scalar index) This instruction performs a contiguous load of four-doubleword structures, each to the same element number in four vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by four. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive doublewords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the four destination vector registers.",
    "syntax": [
      "LD4D {<Zt1>.D,<Zt2>.D,<Zt3>.D,<Zt4>.D },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD4H (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD4H--scalar-plus-immediate---Contiguous-load-four-halfword-structures-to-four-vectors--immediate-index--?lang=en",
    "description": "Contiguous load four-halfword structures to four vectors (immediate index) This instruction performs a contiguous load of four-halfword structures, each to the same element number in four vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 4 in the range -32 to 28 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive halfwords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the four destination vector registers.",
    "syntax": [
      "LD4H {<Zt1>.H,<Zt2>.H,<Zt3>.H,<Zt4>.H },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD4H (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD4H--scalar-plus-scalar---Contiguous-load-four-halfword-structures-to-four-vectors--scalar-index--?lang=en",
    "description": "Contiguous load four-halfword structures to four vectors (scalar index) This instruction performs a contiguous load of four-halfword structures, each to the same element number in four vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by four. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive halfwords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the four destination vector registers.",
    "syntax": [
      "LD4H {<Zt1>.H,<Zt2>.H,<Zt3>.H,<Zt4>.H },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD4Q (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD4Q--scalar-plus-immediate---Contiguous-load-four-quadword-structures-to-four-vectors--immediate-index--?lang=en",
    "description": "Contiguous load four-quadword structures to four vectors (immediate index) This instruction performs a contiguous load of four-quadword structures, each to the same element number in four vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 4 in the range -32 to 28 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive quadwords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the four destination vector registers.",
    "syntax": [
      "LD4Q {<Zt1>.Q,<Zt2>.Q,<Zt3>.Q,<Zt4>.Q },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD4Q (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD4Q--scalar-plus-scalar---Contiguous-load-four-quadword-structures-to-four-vectors--scalar-index--?lang=en",
    "description": "Contiguous load four-quadword structures to four vectors (scalar index) This instruction performs a contiguous load of four-quadword structures, each to the same element number in four vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by four. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive quadwords in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the four destination vector registers.",
    "syntax": [
      "LD4Q {<Zt1>.Q,<Zt2>.Q,<Zt3>.Q,<Zt4>.Q },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #4]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD4W (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD4W--scalar-plus-immediate---Contiguous-load-four-word-structures-to-four-vectors--immediate-index--?lang=en",
    "description": "Contiguous load four-word structures to four vectors (immediate index) This instruction performs a contiguous load of four-word structures, each to the same element number in four vector registers from the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 4 in the range -32 to 28 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive words in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the four destination vector registers.",
    "syntax": [
      "LD4W {<Zt1>.S,<Zt2>.S,<Zt3>.S,<Zt4>.S },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LD4W (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LD4W--scalar-plus-scalar---Contiguous-load-four-word-structures-to-four-vectors--scalar-index--?lang=en",
    "description": "Contiguous load four-word structures to four vectors (scalar index) This instruction performs a contiguous load of four-word structures, each to the same element number in four vector registers from the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by four. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive words in memory that make up each structure. Inactive elements will not cause a read from Device memory or signal a fault, and the corresponding element is set to zero in each of the four destination vector registers.",
    "syntax": [
      "LD4W {<Zt1>.S,<Zt2>.S,<Zt3>.S,<Zt4>.S },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}((t+r) MOD32) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDFF1B (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1B--scalar-plus-scalar---Contiguous-load-first-fault-unsigned-bytes-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load first-fault unsigned bytes to vector (scalar index) This instruction performs a contiguous load with first-faulting behavior of unsigned bytes to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 4 classes:8-bit element,16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LDFF1B {<Zt>.D },<Pg>/Z, [<Xn|SP>{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1B (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1B--scalar-plus-vector---Gather-load-first-fault-unsigned-bytes-to-vector--vector-index--?lang=en",
    "description": "Gather load first-fault unsigned bytes to vector (vector index) This instruction performs a gather load with first-faulting behavior of unsigned bytes to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally sign-extended or zero-extended from 32 to 64 bits. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:32-bit unpacked unscaled offset,32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "LDFF1B {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(64);varoffset : bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1B (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1B--vector-plus-immediate---Gather-load-first-fault-unsigned-bytes-to-vector--immediate-index--?lang=en",
    "description": "Gather load first-fault unsigned bytes to vector (immediate index) This instruction performs a gather load with first-faulting behavior of unsigned bytes to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is in the range 0 to 31. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LDFF1B {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset * mbytes, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1D (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1D--scalar-plus-scalar---Contiguous-load-first-fault-doublewords-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load first-fault doublewords to vector (scalar index) This instruction performs a contiguous load with first-faulting behavior of doublewords to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 8 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LDFF1D {<Zt>.D },<Pg>/Z, [<Xn|SP>{,<Xm>, LSL #3}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1D (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1D--scalar-plus-vector---Gather-load-first-fault-doublewords-to-vector--vector-index--?lang=en",
    "description": "Gather load first-fault doublewords to vector (vector index) This instruction performs a gather load with first-faulting behavior of doublewords to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 8. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 4 classes:32-bit unpacked scaled offset,32-bit unpacked unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "LDFF1D {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(64);varoffset : bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1D (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1D--vector-plus-immediate---Gather-load-first-fault-doublewords-to-vector--immediate-index--?lang=en",
    "description": "Gather load first-fault doublewords to vector (immediate index) This instruction performs a gather load with first-faulting behavior of doublewords to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is a multiple of 8 in the range 0 to 248. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LDFF1D {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset * mbytes, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1H (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1H--scalar-plus-scalar---Contiguous-load-first-fault-unsigned-halfwords-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load first-fault unsigned halfwords to vector (scalar index) This instruction performs a contiguous load with first-faulting behavior of unsigned halfwords to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 2 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LDFF1H {<Zt>.D },<Pg>/Z, [<Xn|SP>{,<Xm>, LSL #1}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1H (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1H--scalar-plus-vector---Gather-load-first-fault-unsigned-halfwords-to-vector--vector-index--?lang=en",
    "description": "Gather load first-fault unsigned halfwords to vector (vector index) This instruction performs a gather load with first-faulting behavior of unsigned halfwords to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 2. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 6 classes:32-bit scaled offset,32-bit unpacked scaled offset,32-bit unpacked unscaled offset,32-bit unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "LDFF1H {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(64);varoffset : bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1H (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1H--vector-plus-immediate---Gather-load-first-fault-unsigned-halfwords-to-vector--immediate-index--?lang=en",
    "description": "Gather load first-fault unsigned halfwords to vector (immediate index) This instruction performs a gather load with first-faulting behavior of unsigned halfwords to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is a multiple of 2 in the range 0 to 62. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LDFF1H {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset * mbytes, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1SB (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1SB--scalar-plus-scalar---Contiguous-load-first-fault-signed-bytes-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load first-fault signed bytes to vector (scalar index) This instruction performs a contiguous load with first-faulting behavior of signed bytes to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LDFF1SB {<Zt>.D },<Pg>/Z, [<Xn|SP>{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1SB (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1SB--scalar-plus-vector---Gather-load-first-fault-signed-bytes-to-vector--vector-index--?lang=en",
    "description": "Gather load first-fault signed bytes to vector (vector index) This instruction performs a gather load with first-faulting behavior of signed bytes to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally sign-extended or zero-extended from 32 to 64 bits. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:32-bit unpacked unscaled offset,32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "LDFF1SB {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(64);varoffset : bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1SB (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1SB--vector-plus-immediate---Gather-load-first-fault-signed-bytes-to-vector--immediate-index--?lang=en",
    "description": "Gather load first-fault signed bytes to vector (immediate index) This instruction performs a gather load with first-faulting behavior of signed bytes to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is in the range 0 to 31. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LDFF1SB {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset * mbytes, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1SH (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1SH--scalar-plus-scalar---Contiguous-load-first-fault-signed-halfwords-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load first-fault signed halfwords to vector (scalar index) This instruction performs a contiguous load with first-faulting behavior of signed halfwords to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 2 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LDFF1SH {<Zt>.D },<Pg>/Z, [<Xn|SP>{,<Xm>, LSL #1}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1SH (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1SH--scalar-plus-vector---Gather-load-first-fault-signed-halfwords-to-vector--vector-index--?lang=en",
    "description": "Gather load first-fault signed halfwords to vector (vector index) This instruction performs a gather load with first-faulting behavior of signed halfwords to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 2. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 6 classes:32-bit scaled offset,32-bit unpacked scaled offset,32-bit unpacked unscaled offset,32-bit unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "LDFF1SH {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(64);varoffset : bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1SH (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1SH--vector-plus-immediate---Gather-load-first-fault-signed-halfwords-to-vector--immediate-index--?lang=en",
    "description": "Gather load first-fault signed halfwords to vector (immediate index) This instruction performs a gather load with first-faulting behavior of signed halfwords to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is a multiple of 2 in the range 0 to 62. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LDFF1SH {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset * mbytes, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1SW (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1SW--scalar-plus-scalar---Contiguous-load-first-fault-signed-words-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load first-fault signed words to vector (scalar index) This instruction performs a contiguous load with first-faulting behavior of signed words to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 4 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LDFF1SW {<Zt>.D },<Pg>/Z, [<Xn|SP>{,<Xm>, LSL #2}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1SW (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1SW--scalar-plus-vector---Gather-load-first-fault-signed-words-to-vector--vector-index--?lang=en",
    "description": "Gather load first-fault signed words to vector (vector index) This instruction performs a gather load with first-faulting behavior of signed words to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 4. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 4 classes:32-bit unpacked scaled offset,32-bit unpacked unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "LDFF1SW {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(64);varoffset : bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1SW (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1SW--vector-plus-immediate---Gather-load-first-fault-signed-words-to-vector--immediate-index--?lang=en",
    "description": "Gather load first-fault signed words to vector (immediate index) This instruction performs a gather load with first-faulting behavior of signed words to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is a multiple of 4 in the range 0 to 124. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LDFF1SW {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset * mbytes, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1W (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1W--scalar-plus-scalar---Contiguous-load-first-fault-unsigned-words-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load first-fault unsigned words to vector (scalar index) This instruction performs a contiguous load with first-faulting behavior of unsigned words to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 4 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LDFF1W {<Zt>.D },<Pg>/Z, [<Xn|SP>{,<Xm>, LSL #2}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1W (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1W--scalar-plus-vector---Gather-load-first-fault-unsigned-words-to-vector--vector-index--?lang=en",
    "description": "Gather load first-fault unsigned words to vector (vector index) This instruction performs a gather load with first-faulting behavior of unsigned words to Active elements of a vector register from memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 4. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 6 classes:32-bit scaled offset,32-bit unpacked scaled offset,32-bit unpacked unscaled offset,32-bit unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "LDFF1W {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(64);varoffset : bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDFF1W (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDFF1W--vector-plus-immediate---Gather-load-first-fault-unsigned-words-to-vector--immediate-index--?lang=en",
    "description": "Gather load first-fault unsigned words to vector (immediate index) This instruction performs a gather load with first-faulting behavior of unsigned words to Active elements of a vector register from memory addresses generated by a vector base plus immediate index. The index is a multiple of 4 in the range 0 to 124. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LDFF1W {<Zt>.D },<Pg>/Z, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = FALSE;lettagchecked : boolean = TRUE;varaccdesc :AccessDescriptor=CreateAccDescSVEFF(contiguous, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\nend;\n\nassert accdesc.first;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset * mbytes, accdesc);ifaccdesc.first then// Mem[] will not return if a fault is detected for the First active elementdata =Mem{msize}(addr, accdesc);\n            accdesc.first = FALSE;else// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n            faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);\n        end;else(data, fault) = (Zeros{msize}, FALSE);\n    end;// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDNF1B",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNF1B--Contiguous-load-non-fault-unsigned-bytes-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load non-fault unsigned bytes to vector (immediate index) This instruction performs a contiguous load with non-faulting behavior of unsigned bytes to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 4 classes:8-bit element,16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LDNF1B {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVENF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n        faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDNF1D",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNF1D--Contiguous-load-non-fault-doublewords-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load non-fault doublewords to vector (immediate index) This instruction performs a contiguous load with non-faulting behavior of doublewords to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LDNF1D {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVENF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n        faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDNF1H",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNF1H--Contiguous-load-non-fault-unsigned-halfwords-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load non-fault unsigned halfwords to vector (immediate index) This instruction performs a contiguous load with non-faulting behavior of unsigned halfwords to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LDNF1H {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVENF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n        faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDNF1SB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNF1SB--Contiguous-load-non-fault-signed-bytes-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load non-fault signed bytes to vector (immediate index) This instruction performs a contiguous load with non-faulting behavior of signed bytes to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:16-bit element,32-bit elementand64-bit element",
    "syntax": [
      "LDNF1SB {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVENF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n        faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDNF1SH",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNF1SH--Contiguous-load-non-fault-signed-halfwords-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load non-fault signed halfwords to vector (immediate index) This instruction performs a contiguous load with non-faulting behavior of signed halfwords to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LDNF1SH {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVENF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n        faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDNF1SW",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNF1SW--Contiguous-load-non-fault-signed-words-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load non-fault signed words to vector (immediate index) This instruction performs a contiguous load with non-faulting behavior of signed words to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LDNF1SW {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVENF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n        faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDNF1W",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNF1W--Contiguous-load-non-fault-unsigned-words-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load non-fault unsigned words to vector (immediate index) This instruction performs a contiguous load with non-faulting behavior of unsigned words to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "LDNF1W {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letorig : bits(VL) = Z{}(t);vardata : bits(msize);letmbytes : integer{} = msize DIV8;varfault : boolean = FALSE;varfaulted : boolean = FALSE;varunknown : boolean = FALSE;letcontiguous : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVENF(contiguous, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then// MemNF[] will return fault=TRUE if access is not performed for any reason(data, fault) =MemNF{msize}(addr, accdesc);\n        faulted = faulted ||ConstrainUnpredictableBool(Unpredictable_NONFAULT);else(data, fault) = (Zeros{msize}, FALSE);\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);// FFR elements set to FALSE following a suppressed access/faultfaulted = faulted || fault;iffaulted thenElemFFR(e, esize)='0';\n    end;// Value becomes CONSTRAINED UNPREDICTABLE after an FFR element is FALSEunknown = unknown ||ElemFFR(e, esize) =='0';ifunknown thenif!fault &&ConstrainUnpredictableBool(Unpredictable_SVELDNFDATA) then\n            result[e*:esize] = Extend{esize}(data, unsigned);elsifConstrainUnpredictableBool(Unpredictable_SVELDNFZERO) then\n            result[e*:esize]= Zeros{esize};else// mergeresult[e*:esize] = orig[e*:esize];\n        end;elseresult[e*:esize] = Extend{esize}(data, unsigned);\n    end;\nend;Z{VL}(t) = result;",
    "operational_info": ""
  },
  {
    "title": "LDNT1B (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1B--scalar-plus-immediate--consecutive-registers---Contiguous-load-non-temporal-of-bytes-to-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous load non-temporal of bytes to multiple consecutive vectors (immediate index) This instruction performs a contiguous non-temporal load of bytes to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LDNT1B {<Zt1>.B-<Zt4>.B },<PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1B (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1B--scalar-plus-immediate--single-register---Contiguous-load-non-temporal-bytes-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load non-temporal bytes to vector (immediate index) This instruction performs a contiguous non-temporal load of bytes to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "LDNT1B {<Zt>.B },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1B (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1B--scalar-plus-scalar--consecutive-registers---Contiguous-load-non-temporal-of-bytes-to-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous load non-temporal of bytes to multiple consecutive vectors (scalar index) This instruction performs a contiguous non-temporal load of bytes to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LDNT1B {<Zt1>.B-<Zt4>.B },<PNg>/Z, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1B (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1B--scalar-plus-scalar--single-register---Contiguous-load-non-temporal-bytes-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load non-temporal bytes to vector (scalar index) This instruction performs a contiguous non-temporal load of bytes to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "LDNT1B {<Zt>.B },<Pg>/Z, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);varoffset : bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1B (vector plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1B--vector-plus-scalar---Gather-load-non-temporal-unsigned-bytes-?lang=en",
    "description": "Gather load non-temporal unsigned bytes This instruction performs a non-temporal gather load of unsigned bytes to Active elements of a vector register from memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "LDNT1B {<Zt>.D },<Pg>/Z, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varoffset :bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= TRUE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);offset =X{64}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1D (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1D--scalar-plus-immediate--consecutive-registers---Contiguous-load-non-temporal-of-doublewords-to-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous load non-temporal of doublewords to multiple consecutive vectors (immediate index) This instruction performs a contiguous non-temporal load of doublewords to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LDNT1D {<Zt1>.D-<Zt4>.D },<PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1D (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1D--scalar-plus-immediate--single-register---Contiguous-load-non-temporal-doublewords-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load non-temporal doublewords to vector (immediate index) This instruction performs a contiguous non-temporal load of doublewords to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "LDNT1D {<Zt>.D },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1D (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1D--scalar-plus-scalar--consecutive-registers---Contiguous-load-non-temporal-of-doublewords-to-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous load non-temporal of doublewords to multiple consecutive vectors (scalar index) This instruction performs a contiguous non-temporal load of doublewords to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LDNT1D {<Zt1>.D-<Zt4>.D },<PNg>/Z, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1D (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1D--scalar-plus-scalar--single-register---Contiguous-load-non-temporal-doublewords-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load non-temporal doublewords to vector (scalar index) This instruction performs a contiguous non-temporal load of doublewords to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 8 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "LDNT1D {<Zt>.D },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);varoffset : bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1D (vector plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1D--vector-plus-scalar---Gather-load-non-temporal-unsigned-doublewords-?lang=en",
    "description": "Gather load non-temporal unsigned doublewords This instruction performs a non-temporal gather load of doublewords to Active elements of a vector register from memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LDNT1D {<Zt>.D },<Pg>/Z, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varoffset :bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= TRUE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);offset =X{64}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1H (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1H--scalar-plus-immediate--consecutive-registers---Contiguous-load-non-temporal-of-halfwords-to-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous load non-temporal of halfwords to multiple consecutive vectors (immediate index) This instruction performs a contiguous non-temporal load of halfwords to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LDNT1H {<Zt1>.H-<Zt4>.H },<PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1H (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1H--scalar-plus-immediate--single-register---Contiguous-load-non-temporal-halfwords-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load non-temporal halfwords to vector (immediate index) This instruction performs a contiguous non-temporal load of halfwords to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "LDNT1H {<Zt>.H },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1H (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1H--scalar-plus-scalar--consecutive-registers---Contiguous-load-non-temporal-of-halfwords-to-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous load non-temporal of halfwords to multiple consecutive vectors (scalar index) This instruction performs a contiguous non-temporal load of halfwords to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LDNT1H {<Zt1>.H-<Zt4>.H },<PNg>/Z, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1H (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1H--scalar-plus-scalar--single-register---Contiguous-load-non-temporal-halfwords-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load non-temporal halfwords to vector (scalar index) This instruction performs a contiguous non-temporal load of halfwords to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 2 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "LDNT1H {<Zt>.H },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);varoffset : bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1H (vector plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1H--vector-plus-scalar---Gather-load-non-temporal-unsigned-halfwords-?lang=en",
    "description": "Gather load non-temporal unsigned halfwords This instruction performs a non-temporal gather load of unsigned halfwords to Active elements of a vector register from memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "LDNT1H {<Zt>.D },<Pg>/Z, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varoffset :bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= TRUE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);offset =X{64}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1SB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1SB--Gather-load-non-temporal-signed-bytes-?lang=en",
    "description": "Gather load non-temporal signed bytes This instruction performs a non-temporal gather load of signed bytes to Active elements of a vector register from memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "LDNT1SB {<Zt>.D },<Pg>/Z, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varoffset :bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= TRUE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);offset =X{64}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1SH",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1SH--Gather-load-non-temporal-signed-halfwords-?lang=en",
    "description": "Gather load non-temporal signed halfwords This instruction performs a non-temporal gather load of signed halfwords to Active elements of a vector register from memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "LDNT1SH {<Zt>.D },<Pg>/Z, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varoffset :bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= TRUE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);offset =X{64}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1SW",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1SW--Gather-load-non-temporal-signed-words-?lang=en",
    "description": "Gather load non-temporal signed words This instruction performs a non-temporal gather load of signed words to Active elements of a vector register from memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LDNT1SW {<Zt>.D },<Pg>/Z, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varoffset :bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= TRUE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);offset =X{64}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1W (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1W--scalar-plus-immediate--consecutive-registers---Contiguous-load-non-temporal-of-words-to-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous load non-temporal of words to multiple consecutive vectors (immediate index) This instruction performs a contiguous non-temporal load of words to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LDNT1W {<Zt1>.S-<Zt4>.S },<PNg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1W (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1W--scalar-plus-immediate--single-register---Contiguous-load-non-temporal-words-to-vector--immediate-index--?lang=en",
    "description": "Contiguous load non-temporal words to vector (immediate index) This instruction performs a contiguous non-temporal load of words to elements of a vector register from the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "LDNT1W {<Zt>.S },<Pg>/Z, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1W (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1W--scalar-plus-scalar--consecutive-registers---Contiguous-load-non-temporal-of-words-to-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous load non-temporal of words to multiple consecutive vectors (scalar index) This instruction performs a contiguous non-temporal load of words to elements of two or four consecutive vector registers from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "LDNT1W {<Zt1>.S-<Zt4>.S },<PNg>/Z, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dofore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) then\n            values[[r]][e*:esize] =Mem{esize}(addr, accdesc);elsevalues[[r]][e*:esize] = Zeros{esize};\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;forr =0to nreg-1doZ{VL}(transfer+r) = values[[r]];\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1W (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1W--scalar-plus-scalar--single-register---Contiguous-load-non-temporal-words-to-vector--scalar-index--?lang=en",
    "description": "Contiguous load non-temporal words to vector (scalar index) This instruction performs a contiguous non-temporal load of words to elements of a vector register from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 4 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements will not cause a read from Device memory or signal a fault, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "LDNT1W {<Zt>.S },<Pg>/Z, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);varoffset : bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varresult : bits(VL);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =Mem{esize}(addr, accdesc);elseresult[e*:esize] = Zeros{esize};\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDNT1W (vector plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDNT1W--vector-plus-scalar---Gather-load-non-temporal-unsigned-words-?lang=en",
    "description": "Gather load non-temporal unsigned words This instruction performs a non-temporal gather load of unsigned words to Active elements of a vector register from memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements will not cause a read from Device memory or signal faults, and are set to zero in the destination vector. A non-temporal load is a hint to the system that this data is unlikely to be referenced again soon. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "LDNT1W {<Zt>.D },<Pg>/Z, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);varoffset :bits(64);varresult :bits(VL);vardata:bits(msize);letmbytes : integer{} = msize DIV8;letcontiguous :boolean= FALSE;letnontemporal :boolean= TRUE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);offset =X{64}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letbaddr:bits(64)= ZeroExtend{}(base[e*:esize]);letaddr:bits(64)=AddressAdd(baddr,offset, accdesc);data=Mem{msize}(addr,accdesc);result[e*:esize] = Extend{esize}(data, unsigned);elseresult[e*:esize] = Zeros{esize};end;end;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDR (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDR--predicate---Load-predicate-register-?lang=en",
    "description": "Load predicate register This instruction loads a predicate register from a memory address generated by a 64-bit scalar base, plus an immediate offset in the range -256 to 255 that is multiplied by the current predicate register size in bytes. This instruction is unpredicated. The load is performed as contiguous byte accesses, each containing 8 consecutive predicate bits in ascending element order, with no endian conversion and no guarantee of single-copy atomicity larger than a byte. However, if alignment is checked, then a general-purpose base register must be aligned to 2 bytes. For programmer convenience, an assembler must also accept a predicate-as-counter register name for the destination predicate register.",
    "syntax": [
      "LDR<Pt>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = PL DIV8;varbase: bits(64);letoffset : integer = imm * elements;varresult : bits(PL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = FALSE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifn ==31thenCheckSPAlignment();base=SP{64}();elsebase=X{64}(n);\nend;varaddr : bits(64) =AddressAdd(base, offset, accdesc);letaligned : boolean = IsAlignedSize(base,2);if!aligned &&AlignmentEnforced() thenletfault :FaultRecord=AlignmentFault(accdesc, addr);AArch64_Abort(fault);\nend;fore =0to elements-1doresult[e*:8] =AArch64_MemSingle{8}(addr, accdesc, aligned);\n    addr =AddressIncrement(addr,1, accdesc);\nend;P{PL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LDR (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LDR--vector---Load-vector-register-?lang=en",
    "description": "Load vector register This instruction loads a vector register from a memory address generated by a 64-bit scalar base, plus an immediate offset in the range -256 to 255 that is multiplied by the current vector register size in bytes. This instruction is unpredicated. The load is performed as contiguous byte accesses, with no endian conversion and no guarantee of single-copy atomicity larger than a byte. However, if alignment is checked, then the base register must be aligned to 16 bytes.",
    "syntax": [
      "LDR<Zt>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV8;varbase: bits(64);letoffset : integer = imm * elements;varresult : bits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = FALSE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_LOAD, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifn ==31thenCheckSPAlignment();base=SP{64}();elsebase=X{64}(n);\nend;varaddr : bits(64) =AddressAdd(base, offset, accdesc);letaligned : boolean = IsAlignedSize(base,16);if!aligned &&AlignmentEnforced() thenletfault :FaultRecord=AlignmentFault(accdesc, addr);AArch64_Abort(fault);\nend;fore =0to elements-1doresult[e*:8] =AArch64_MemSingle{8}(addr, accdesc, aligned);\n    addr =AddressIncrement(addr,1, accdesc);\nend;Z{VL}(t) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LSL (immediate, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LSL--immediate--predicated---Logical-shift-left-by-immediate--predicated--?lang=en",
    "description": "Logical shift left by immediate (predicated) This instruction shifts left by immediate each active element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate shift amount is an unsigned value in the range 0 to number of bits per element minus 1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "LSL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letPL : integer{} = VL DIV8;letoperand1 :bits(VL)= Z{}(dn);letmask :bits(PL)= P{}(g);varresult :bits(VL);fore =0to elements-1do\n    let element1 :bits(esize)= operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =LSL(element1,shift);elseresult[e*:esize] = operand1[e*:esize];end;end;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "LSL (immediate, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LSL--immediate--unpredicated---Logical-shift-left-by-immediate--unpredicated--?lang=en",
    "description": "Logical shift left by immediate (unpredicated) This instruction shifts left by immediate each element of the source vector, and places the results in the corresponding elements of the destination vector. The immediate shift amount is an unsigned value in the range 0 to number of bits per element minus 1. This instruction is unpredicated.",
    "syntax": [
      "LSL<Zd>.<T>,<Zn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];\n    result[e*:esize] = LSL(element1, shift);\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LSL (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LSL--vectors---Logical-shift-left-by-vector--predicated--?lang=en",
    "description": "Logical shift left by vector (predicated) This instruction shifts left active elements of the first source vector by the corresponding elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. The shift amount operand is a vector of unsigned elements in which all bits are significant, and not used modulo the element size. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "LSL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];letshift : integer = Min(UInt(element2), esize);\n        result[e*:esize] = LSL(element1, shift);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "LSL (wide elements, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LSL--wide-elements--predicated---Logical-shift-left-by-64-bit-wide-elements--predicated--?lang=en",
    "description": "Logical shift left by 64-bit wide elements (predicated) This instruction shifts left active elements of the first source vector by the corresponding overlapping 64-bit elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. The shift amount is a vector of unsigned 64-bit doubleword elements in which all bits are significant, and not used modulo the destination element size. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "LSL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(64) = operand2[((e * esize) DIVRM64)*:64];letshift : integer = Min(UInt(element2), esize);\n        result[e*:esize] = LSL(element1, shift);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "LSL (wide elements, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LSL--wide-elements--unpredicated---Logical-shift-left-by-64-bit-wide-elements--unpredicated--?lang=en",
    "description": "Logical shift left by 64-bit wide elements (unpredicated) This instruction shifts left all elements of the first source vector by the corresponding overlapping 64-bit elements of the second source vector and places the first in the corresponding elements of the destination vector. The shift amount is a vector of unsigned 64-bit doubleword elements in which all bits are significant, and not used modulo the destination element size. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "LSL<Zd>.<T>,<Zn>.<T>,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(64) = operand2[((e * esize) DIVRM64)*:64];letshift : integer = Min(UInt(element2), esize);\n    result[e*:esize] = LSL(element1, shift);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LSLR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LSLR--Reversed-logical-shift-left-by-vector--predicated--?lang=en",
    "description": "Reversed logical shift left by vector (predicated) This instruction reverse shifts left active elements of the second source vector by the corresponding elements of the first source vector, and destructively places the results in the corresponding elements of the first source vector. The shift amount operand is a vector of unsigned elements in which all bits are significant, and not used modulo the element size. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "LSLR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];letshift : integer = Min(UInt(element1), esize);\n        result[e*:esize] = LSL(element2, shift);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "LSR (immediate, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LSR--immediate--predicated---Logical-shift-right-by-immediate--predicated--?lang=en",
    "description": "Logical shift right by immediate (predicated) This instruction shifts right by immediate, inserting zeroes, each active element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "LSR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letPL : integer{} = VL DIV8;letoperand1 :bits(VL)= Z{}(dn);letmask :bits(PL)= P{}(g);varresult :bits(VL);fore =0to elements-1do\n    let element1 :bits(esize)= operand1[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =LSR(element1,shift);elseresult[e*:esize] = operand1[e*:esize];end;end;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "LSR (immediate, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LSR--immediate--unpredicated---Logical-shift-right-by-immediate--unpredicated--?lang=en",
    "description": "Logical shift right by immediate (unpredicated) This instruction shifts right by immediate, inserting zeroes, each element of the source vector, and places the results in the corresponding elements of the destination vector. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "LSR<Zd>.<T>,<Zn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];\n    result[e*:esize] = LSR(element1, shift);\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LSR (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LSR--vectors---Logical-shift-right-by-vector--predicated--?lang=en",
    "description": "Logical shift right by vector (predicated) This instruction shifts right, inserting zeroes, active elements of the first source vector by the corresponding elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. The shift amount operand is a vector of unsigned elements in which all bits are significant, and not used modulo the element size. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "LSR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];letshift : integer = Min(UInt(element2), esize);\n        result[e*:esize] = LSR(element1, shift);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "LSR (wide elements, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LSR--wide-elements--predicated---Logical-shift-right-by-64-bit-wide-elements--predicated--?lang=en",
    "description": "Logical shift right by 64-bit wide elements (predicated) This instruction shifts right, inserting zeroes, active elements of the first source vector by the corresponding overlapping 64-bit elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. The shift amount is a vector of unsigned 64-bit doubleword elements in which all bits are significant, and not used modulo the destination element size. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "LSR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(64) = operand2[((e * esize) DIVRM64)*:64];letshift : integer = Min(UInt(element2), esize);\n        result[e*:esize] = LSR(element1, shift);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "LSR (wide elements, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LSR--wide-elements--unpredicated---Logical-shift-right-by-64-bit-wide-elements--unpredicated--?lang=en",
    "description": "Logical shift right by 64-bit wide elements (unpredicated) This instruction shifts right, inserting zeroes, all elements of the first source vector by the corresponding overlapping 64-bit elements of the second source vector and places the first in the corresponding elements of the destination vector. The shift amount is a vector of unsigned 64-bit doubleword elements in which all bits are significant, and not used modulo the destination element size. This instruction is unpredicated.",
    "syntax": [
      "LSR<Zd>.<T>,<Zn>.<T>,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(64) = operand2[((e * esize) DIVRM64)*:64];letshift : integer = Min(UInt(element2), esize);\n    result[e*:esize] = LSR(element1, shift);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LSRR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LSRR--Reversed-logical-shift-right-by-vector--predicated--?lang=en",
    "description": "Reversed logical shift right by vector (predicated) This instruction reverse shifts right, inserting zeroes, active elements of the second source vector by the corresponding elements of the first source vector, and destructively places the results in the corresponding elements of the first source vector. The shift amount operand is a vector of unsigned elements in which all bits are significant, and not used modulo the element size. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "LSRR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];letshift : integer = Min(UInt(element1), esize);\n        result[e*:esize] = LSR(element2, shift);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "LUTI2 (8-bit and 16-bit)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LUTI2--8-bit-and-16-bit---Lookup-table-read-with-2-bit-indices--8-bit-and-16-bit--?lang=en",
    "description": "Lookup table read with 2-bit indices (8-bit and 16-bit) This instruction copies indexed 8-bit or 16-bit elements from the low 128 bits of the table vector to the destination vector using packed 2-bit indices from a segment of the source vector. A segment corresponds to a portion of the source vector that is consumed in order to fill the destination vector. The segment is selected by the vector segment index. This instruction is unpredicated. It has encodings from 2 classes:ByteandHalfword",
    "syntax": [
      "LUTI2<Zd>.H, {<Zn>.H },<Zm>[<index>]"
    ],
    "features": [
      "FEAT_LUT",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letibase : integer = elements * part;letindexes : bits(VL) = Z{}(m);lettable : bits(VL)   = Z{}(n);varresult : bits(VL);fore =0to elements-1doletindex : integer = UInt(indexes[(ibase + e)*:isize]);\n    result[e*:esize] = table[index*:esize];\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LUTI4 (8-bit and 16-bit)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LUTI4--8-bit-and-16-bit---Lookup-table-read-with-4-bit-indices--8-bit-and-16-bit--?lang=en",
    "description": "Lookup table read with 4-bit indices (8-bit and 16-bit) This instruction copies indexed 8-bit or 16-bit elements from the low 128 or 256 bits of the table vector, or from the low 128 bits of the two table vectors to the destination vector using packed 4-bit indices from a segment of the source vector. A segment corresponds to a portion of the source vector that is consumed in order to fill the destination vector. The segment is selected by the vector segment index. This instruction is unpredicated. It has encodings from 3 classes:Byte, single register table,Halfword, two register tableandHalfword, single register table",
    "syntax": [
      "LUTI4<Zd>.H, {<Zn>.H },<Zm>[<index>]"
    ],
    "features": [
      "FEAT_LUT",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();ifntblr ==1&& esize ==16&& VL <256thenEndOfDecode(Decode_UNDEF); end;letelements : integer = VL DIV esize;lettablesize : integer =ifntblr ==1&& esize ==16then256else128;leteltspertable : integer = tablesize DIV esize;letibase : integer = elements * part;letindexes : bits(VL) = Z{}(m);lettable1 : bits(VL)  = Z{}(n+0);lettable2 : bits(VL)  =ifntblr ==2thenZ{VL}((n+1) MOD32)elseZeros{VL};varresult : bits(VL);varres : bits(esize);fore =0to elements-1doletindex : integer = UInt(indexes[(ibase + e)*:isize]);ifindex < eltspertable then\n        res = table1[index*:esize];elseassert ntblr ==2;\n        res = table2[(index - eltspertable)*:esize];\n    end;\n    result[e*:esize] = res;\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LUTI6 (16-bit)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LUTI6--16-bit---Lookup-table-read-with-6-bit-indices--16-bit--?lang=en",
    "description": "Lookup table read with 6-bit indices (16-bit) This instruction copies indexed 16-bit elements from the low 512 bits of the two table vectors to the destination vector using packed 6-bit indices from a segment in the bottom three quarters of the source vector register. This instruction is unpredicated. This instruction requires that the Effective SVE vector length is at least 512 bits.",
    "syntax": [
      "LUTI6<Zd>.H, {<Zn1>.H,<Zn2>.H },<Zm>[<index>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();ifVL <512thenEndOfDecode(Decode_UNDEF); end;letelements : integer = VL DIV esize;letibase : integer = elements * part;letindexes :bits(VL)= Z{}(m); // Only bottom 3/4 usedlettable :bits(1024)=Z{VL}((n+1) MOD32)[511:0] ::Z{VL}(n+0)[511:0];varresult :bits(VL);fore =0to elements-1do\n    let index : integer = UInt(indexes[(ibase + e)*:isize]);let res :bits(esize)= table[index*:esize];result[e*:esize] = res;end;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "LUTI6 (8-bit)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/LUTI6--8-bit---Lookup-table-read-with-6-bit-indices--8-bit--?lang=en",
    "description": "Lookup table read with 6-bit indices (8-bit) This instruction copies indexed 8-bit elements from the low 256 bits of the two table vectors to the destination vector using packed 6-bit indices from the bottom three quarters of the source vector. This instruction is unpredicated. This instruction requires that the Effective SVE vector length is at least 256 bits. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "LUTI6<Zd>.B, {<Zn1>.B,<Zn2>.B },<Zm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();ifVL <256thenEndOfDecode(Decode_UNDEF); end;letelements : integer = VL DIV esize;letindexes :bits(VL)= Z{}(m); // Only bottom 3/4 usedlettable :bits(512)=Z{VL}((n+1) MOD32)[255:0] ::Z{VL}(n+0)[255:0];varresult :bits(VL);fore =0to elements-1do\n    let index : integer = UInt(indexes[e*:isize]);let res :bits(esize)= table[index*:esize];result[e*:esize] = res;end;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "MAD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MAD--Multiply-add-to-multiplicand--predicated--?lang=en",
    "description": "Multiply-add to multiplicand (predicated) This instruction multiplies the active elements of the first source vector by the corresponding elements of the second source vector, and adds the result to elements of the third (addend) vector. The results are destructively placed in the destination and first source (multiplicand) vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "MAD<Zdn>.<T>,<Pg>/M,<Zm>.<T>,<Za>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letoperand3 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(a)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letproduct : integer = element1 * element2;ifsub_op then\n            result[e*:esize] = operand3[e*:esize] - product;elseresult[e*:esize] = operand3[e*:esize] + product;\n        end;elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "MADPT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MADPT--Multiply-add-checked-pointer-vectors-to-multiplicand-?lang=en",
    "description": "Multiply-add checked pointer vectors to multiplicand This instruction multiplies, with overflow check, the elements of the first and second source vectors and adds the results of multiplication, with pointer check, to elements of the third (addend) vector. The final results are destructively placed in the destination and first source (multiplicand) vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "MADPT<Zdn>.D,<Zm>.D,<Za>.D"
    ],
    "features": [
      "FEAT_CPA",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV64;letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(a);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:64]);letelement2 : integer = SInt(operand2[e*:64]);letproduct : integer = element1 * element2;letoverflow : boolean = (product != SInt(product[63:0]));letaddend : bits(64) = operand3[e*:64];\n    result[e*:64] =PointerMultiplyAddCheck(addend + product, addend, overflow);\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "MATCH",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MATCH--Detect-any-matching-elements--setting-the-condition-flags-?lang=en",
    "description": "Detect any matching elements, setting the condition flags This instruction compares each active 8-bit or 16-bit element in the first source vector with all of the elements in the corresponding 128-bit segment of the second source vector. For each element in the first source vector, if any matching element is found in the corresponding segment of the second source vector, the corresponding element of the destination predicate is set to true, otherwise it is set to false. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "MATCH<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletsegmentbase : integer = e - (e MOD eltspersegment);\n        result[e*:psize] = ZeroExtend{psize}('0');letelement1 : bits(esize) = operand1[e*:esize];fori =segmentbaseto(segmentbase + eltspersegment) - 1doletelement2 :bits(esize)= operand2[i*:esize];ifelement1 == element2 then\n                result[e*:psize] = ZeroExtend{psize}('1');\n            end;\n        end;elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "MLA (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MLA--indexed---Multiply-add-by-indexed-element-?lang=en",
    "description": "Multiply-add by indexed element This instruction multiplies all integer elements within each 128-bit segment of the first source vector by the specified element in the corresponding second source vector segment. The products are then destructively added to the corresponding elements of the addend and destination vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. This instruction is unpredicated. It has encodings from 3 classes:16-bit,32-bitand64-bit",
    "syntax": [
      "MLA<Zda>.D,<Zn>.D,<Zm>.D[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[s*:esize]);letproduct : bits(esize) = (element1 * element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] + product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "MLA (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MLA--vectors---Multiply-add--predicated--?lang=en",
    "description": "Multiply-add (predicated) This instruction multiplies the active elements of the first source vector by the corresponding elements of the second source vector, and adds the result to elements of the third source (addend) vector. The results are destructively placed in the destination and third source (addend) vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "MLA<Zda>.<T>,<Pg>/M,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letproduct : integer = element1 * element2;ifsub_op then\n            result[e*:esize] = operand3[e*:esize] - product;elseresult[e*:esize] = operand3[e*:esize] + product;\n        end;elseresult[e*:esize] = operand3[e*:esize];\n    end;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "MLAPT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MLAPT--Multiply-add-checked-pointer-vectors-?lang=en",
    "description": "Multiply-add checked pointer vectors This instruction multiplies, with overflow check, the elements of the first and second source vectors and adds the results of multiplication, with pointer check, to elements of the third source (addend) vector. The final results are destructively placed in the destination and third source (addend) vector. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "MLAPT<Zda>.D,<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_CPA",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV64;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:64]);letelement2 : integer = SInt(operand2[e*:64]);letproduct : integer = element1 * element2;letoverflow : boolean = (product != SInt(product[63:0]));letaddend : bits(64) = operand3[e*:64];\n    result[e*:64] =PointerMultiplyAddCheck(addend + product, addend, overflow);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "MLS (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MLS--indexed---Multiply-subtract-by-indexed-element-?lang=en",
    "description": "Multiply-subtract by indexed element This instruction multiplies all integer elements within each 128-bit segment of the first source vector by the specified element in the corresponding second source vector segment. The products are then destructively subtracted from the corresponding elements of the addend and destination vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. This instruction is unpredicated. It has encodings from 3 classes:16-bit,32-bitand64-bit",
    "syntax": [
      "MLS<Zda>.D,<Zn>.D,<Zm>.D[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[s*:esize]);letproduct : bits(esize) = (element1 * element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] - product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "MLS (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MLS--vectors---Multiply-subtract--predicated--?lang=en",
    "description": "Multiply-subtract (predicated) This instruction multiplies the active elements of the first source vector by the corresponding elements of the second source vector, and subtracts the result from elements of the third source (addend) vector. The results are destructively placed in the destination and third source (addend) vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "MLS<Zda>.<T>,<Pg>/M,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letproduct : integer = element1 * element2;ifsub_op then\n            result[e*:esize] = operand3[e*:esize] - product;elseresult[e*:esize] = operand3[e*:esize] + product;\n        end;elseresult[e*:esize] = operand3[e*:esize];\n    end;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "MOV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--Move-logical-bitmask-immediate-to-vector--unpredicated---an-alias-of-DUPM-?lang=en",
    "description": "Move logical bitmask immediate to vector (unpredicated) This instruction unconditionally broadcasts the logical bitmask immediate into each element of the destination vector. This instruction is unpredicated. The immediate is a 64-bit value consisting of a single run of ones or zeros repeating every 2, 4, 8, 16, 32 or 64 bits. This is an alias ofDUPM. This means: The encodings in this description are named to match the encodings ofDUPM.The description ofDUPMgives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Zd>.<T>, #<const>\nis equivalent to\nDUPM<Zd>.<T>, #<const>\nand is the preferred disassembly whenSVEMoveMaskPreferred(imm13)."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofDUPMgives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--Move-predicate--unpredicated---an-alias-of-ORR--predicates--?lang=en",
    "description": "Move predicate (unpredicated) This instruction reads all elements from the source predicate and places those elements in the destination predicate. This instruction is unpredicated. This instruction does not set the condition flags. For programmer convenience, an assembler must also accept predicate-as-counter register names for the source and destination predicate registers. This is an alias ofORR (predicates). This means: The encodings in this description are named to match the encodings ofORR (predicates).The description ofORR (predicates)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Pd>.B,<Pn>.B\nis equivalent to\nORR<Pd>.B,<Pn>/Z,<Pn>.B,<Pn>.B\nand is the preferred disassembly whenS == '0' && Pn == Pm && Pm == Pg."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofORR (predicates)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOV (SIMD&FP scalar, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--SIMD-FP-scalar--predicated---Move-SIMD-FP-scalar-register-to-vector-elements--predicated---an-alias-of-CPY--SIMD-FP-scalar--?lang=en",
    "description": "Move SIMD&FP scalar register to vector elements (predicated) This instruction moves the SIMD & floating-point scalar source register to each Active element in the destination vector. Inactive elements in the destination vector register remain unmodified. This is an alias ofCPY (SIMD&FP scalar). This means: The encodings in this description are named to match the encodings ofCPY (SIMD&FP scalar).The description ofCPY (SIMD&FP scalar)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Zd>.<T>,<Pg>/M,<V><n>\nis equivalent to\nCPY<Zd>.<T>,<Pg>/M,<V><n>\nand is always the preferred disassembly."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofCPY (SIMD&FP scalar)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOV (SIMD&FP scalar, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--SIMD-FP-scalar--unpredicated---Move-indexed-element-or-SIMD-FP-scalar-to-vector--unpredicated---an-alias-of-DUP--indexed--?lang=en",
    "description": "Move indexed element or SIMD&FP scalar to vector (unpredicated) This instruction unconditionally broadcasts the SIMD&FP scalar into each element of the destination vector. This instruction is unpredicated. This is an alias ofDUP (indexed). This means: The encodings in this description are named to match the encodings ofDUP (indexed).The description ofDUP (indexed)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Zd>.<T>,<Zn>.<T>[<imm>]\nis equivalent to\nDUP<Zd>.<T>,<Zn>.<T>[<imm>]\nand is the preferred disassembly whenBitCount(imm2 :: tsz) > 1."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofDUP (indexed)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOV (immediate, merging)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--immediate--merging---Move-signed-integer-immediate-to-vector-elements--merging---an-alias-of-CPY--immediate--merging--?lang=en",
    "description": "Move signed integer immediate to vector elements (merging) This instruction moves a signed integer immediate to each Active element in the destination vector. Inactive elements in the destination vector register remain unmodified. The immediate operand is a signed value in the range -128 to +127, and for element widths of 16 bits or higher it may also be a signed multiple of 256 in the range -32768 to +32512 (excluding 0). The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\". This is an alias ofCPY (immediate, merging). This means: The encodings in this description are named to match the encodings ofCPY (immediate, merging).The description ofCPY (immediate, merging)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Zd>.<T>,<Pg>/M, #<imm>{,<shift>}\nis equivalent to\nCPY<Zd>.<T>,<Pg>/M, #<imm>{,<shift>}\nand is always the preferred disassembly."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofCPY (immediate, merging)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOV (immediate, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--immediate--unpredicated---Move-signed-immediate-to-vector-elements--unpredicated---an-alias-of-DUP--immediate--?lang=en",
    "description": "Move signed immediate to vector elements (unpredicated) This instruction unconditionally broadcasts the signed integer immediate into each element of the destination vector. This instruction is unpredicated. The immediate operand is a signed value in the range -128 to +127, and for element widths of 16 bits or higher it may also be a signed multiple of 256 in the range -32768 to +32512 (excluding 0). The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\". This is an alias ofDUP (immediate). This means: The encodings in this description are named to match the encodings ofDUP (immediate).The description ofDUP (immediate)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Zd>.<T>, #<imm>{,<shift>}\nis equivalent to\nDUP<Zd>.<T>, #<imm>{,<shift>}\nand is always the preferred disassembly."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofDUP (immediate)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOV (immediate, zeroing)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--immediate--zeroing---Move-signed-integer-immediate-to-vector-elements--zeroing---an-alias-of-CPY--immediate--zeroing--?lang=en",
    "description": "Move signed integer immediate to vector elements (zeroing) This instruction moves a signed integer immediate to each Active element in the destination vector. Inactive elements in the destination vector register are set to zero. The immediate operand is a signed value in the range -128 to +127, and for element widths of 16 bits or higher it may also be a signed multiple of 256 in the range -32768 to +32512 (excluding 0). The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\". This is an alias ofCPY (immediate, zeroing). This means: The encodings in this description are named to match the encodings ofCPY (immediate, zeroing).The description ofCPY (immediate, zeroing)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Zd>.<T>,<Pg>/Z, #<imm>{,<shift>}\nis equivalent to\nCPY<Zd>.<T>,<Pg>/Z, #<imm>{,<shift>}\nand is always the preferred disassembly."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofCPY (immediate, zeroing)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOV (predicate, merging)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--predicate--merging---Move-predicates--merging---an-alias-of-SEL--predicates--?lang=en",
    "description": "Move predicates (merging) This instruction reads Active elements from the source predicate and places those elements in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register remain unmodified. This instruction does not set the condition flags. This is an alias ofSEL (predicates). This means: The encodings in this description are named to match the encodings ofSEL (predicates).The description ofSEL (predicates)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Pd>.B,<Pg>/M,<Pn>.B\nis equivalent to\nSEL<Pd>.B,<Pg>,<Pn>.B,<Pd>.B\nand is the preferred disassembly whenPd == Pm."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofSEL (predicates)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOV (predicate, zeroing)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--predicate--zeroing---Move-predicates--zeroing---an-alias-of-AND--predicates--?lang=en",
    "description": "Move predicates (zeroing) This instruction reads Active elements from the source predicate and places those elements in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. This is an alias ofAND (predicates). This means: The encodings in this description are named to match the encodings ofAND (predicates).The description ofAND (predicates)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Pd>.B,<Pg>/Z,<Pn>.B\nis equivalent to\nAND<Pd>.B,<Pg>/Z,<Pn>.B,<Pn>.B\nand is the preferred disassembly whenS == '0' && Pn == Pm."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofAND (predicates)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOV (scalar, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--scalar--predicated---Move-general-purpose-register-to-vector-elements--predicated---an-alias-of-CPY--scalar--?lang=en",
    "description": "Move general-purpose register to vector elements (predicated) This instruction moves the general-purpose scalar source register to each Active element in the destination vector. Inactive elements in the destination vector register remain unmodified. This is an alias ofCPY (scalar). This means: The encodings in this description are named to match the encodings ofCPY (scalar).The description ofCPY (scalar)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Zd>.<T>,<Pg>/M,<R><n|SP>\nis equivalent to\nCPY<Zd>.<T>,<Pg>/M,<R><n|SP>\nand is always the preferred disassembly."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofCPY (scalar)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOV (scalar, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--scalar--unpredicated---Move-general-purpose-register-to-vector-elements--unpredicated---an-alias-of-DUP--scalar--?lang=en",
    "description": "Move general-purpose register to vector elements (unpredicated) This instruction unconditionally broadcasts the general-purpose scalar source register into each element of the destination vector. This instruction is unpredicated. This is an alias ofDUP (scalar). This means: The encodings in this description are named to match the encodings ofDUP (scalar).The description ofDUP (scalar)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Zd>.<T>,<R><n|SP>\nis equivalent to\nDUP<Zd>.<T>,<R><n|SP>\nand is always the preferred disassembly."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofDUP (scalar)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOV (vector, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--vector--predicated---Move-vector-elements--predicated---an-alias-of-SEL--vectors--?lang=en",
    "description": "Move vector elements (predicated) This instruction moves elements from the source vector to the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified. This is an alias ofSEL (vectors). This means: The encodings in this description are named to match the encodings ofSEL (vectors).The description ofSEL (vectors)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Zd>.<T>,<Pv>/M,<Zn>.<T>\nis equivalent to\nSEL<Zd>.<T>,<Pv>,<Zn>.<T>,<Zd>.<T>\nand is the preferred disassembly whenZd == Zm."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofSEL (vectors)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOV (vector, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOV--vector--unpredicated---Move-vector-register--unpredicated---an-alias-of-ORR--vectors--unpredicated--?lang=en",
    "description": "Move vector register (unpredicated) This instruction copies the contents of the source vector register to the destination vector register. This instruction is unpredicated. This is an alias ofORR (vectors, unpredicated). This means: The encodings in this description are named to match the encodings ofORR (vectors, unpredicated).The description ofORR (vectors, unpredicated)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOV<Zd>.D,<Zn>.D\nis equivalent to\nORR<Zd>.D,<Zn>.D,<Zn>.D\nand is the preferred disassembly whenZn == Zm."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofORR (vectors, unpredicated)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOVPRFX (predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOVPRFX--predicated---Move-prefix--predicated--?lang=en",
    "description": "Move prefix (predicated) This instruction is a hint to hardware that the instruction may be combined with the destructive instruction that follows it in program order to create a single constructive operation. This instruction is also permitted to be implemented as a discrete vector copy, and the result of executing the pair of instructions with or without combining is identical. The choice of combined versus discrete operation may vary dynamically. Unless the combination of a constructive operation with merging predication is specifically required, it is strongly recommended that for performance reasons software should prefer to use the zeroing form of predicatedMOVPRFXor the unpredicatedMOVPRFXinstruction. Although the operation of the instruction is defined as a simple predicated vector copy, it is required that the prefixed instruction at PC+4 must be an SVE destructive binary or ternary instruction encoding, or a unary operation with merging predication, but excluding otherMOVPRFXinstructions. The prefixed instruction must specify the same predicate register, and have the same maximum element size (ignoring a fixed 64-bit \"wide vector\" operand), and the same destination vector as theMOVPRFXinstruction. The prefixed instruction must not use the destination register in any other operand position, even if they have different names but refer to the same architectural register state. Any other use isUNPREDICTABLE.",
    "syntax": [
      "MOVPRFX<Zd>.<T>,<Pg>/<ZM>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letdest : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand1[e*:esize];\n        result[e*:esize] = element;elseresult[e*:esize] = dest[e*:esize];\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "MOVPRFX (unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOVPRFX--unpredicated---Move-prefix--unpredicated--?lang=en",
    "description": "Move prefix (unpredicated) This instruction is a hint to hardware that the instruction may be combined with the destructive instruction that follows it in program order to create a single constructive operation. This instruction is also permitted to be implemented as a discrete vector copy, and the result of executing the pair of instructions with or without combining is identical. The choice of combined versus discrete operation may vary dynamically. Although the operation of the instruction is defined as a simple unpredicated vector copy, it is required that the prefixed instruction at PC+4 must be an SVE destructive binary or ternary instruction encoding, or a unary operation with merging predication, but excluding otherMOVPRFXinstructions. The prefixed instruction must specify the same destination vector as theMOVPRFXinstruction. The prefixed instruction must not use the destination register in any other operand position, even if they have different names but refer to the same architectural register state. Any other use isUNPREDICTABLE.",
    "syntax": [
      "MOVPRFX<Zd>,<Zn>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letresult : bits(VL) = Z{}(n);Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "MOVS (predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOVS--predicated---Move-predicates--zeroing---setting-the-condition-flags--an-alias-of-ANDS-?lang=en",
    "description": "Move predicates (zeroing), setting the condition flags This instruction reads Active elements from the source predicate and places those elements in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This is an alias ofANDS. This means: The encodings in this description are named to match the encodings ofANDS.The description ofANDSgives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOVS<Pd>.B,<Pg>/Z,<Pn>.B\nis equivalent to\nANDS<Pd>.B,<Pg>/Z,<Pn>.B,<Pn>.B\nand is the preferred disassembly whenS == '1' && Pn == Pm."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofANDSgives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MOVS (unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MOVS--unpredicated---Move-predicate--unpredicated---setting-the-condition-flags--an-alias-of-ORRS-?lang=en",
    "description": "Move predicate (unpredicated), setting the condition flags This instruction reads all elements from the source predicate and places those elements in the destination predicate. This instruction is unpredicated. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This is an alias ofORRS. This means: The encodings in this description are named to match the encodings ofORRS.The description ofORRSgives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "MOVS<Pd>.B,<Pn>.B\nis equivalent to\nORRS<Pd>.B,<Pn>/Z,<Pn>.B,<Pn>.B\nand is the preferred disassembly whenS == '1' && Pn == Pm && Pm == Pg."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofORRSgives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "MSB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MSB--Multiply-subtract-to-multiplicand-?lang=en",
    "description": "Multiply-subtract to multiplicand This instruction multiplies the active elements of the first source vector by the corresponding elements of the second source vector, and subtracts the result from elements of the third (addend) vector. The results are destructively placed in the destination and first source (multiplicand) vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "MSB<Zdn>.<T>,<Pg>/M,<Zm>.<T>,<Za>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letoperand3 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(a)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letproduct : integer = element1 * element2;ifsub_op then\n            result[e*:esize] = operand3[e*:esize] - product;elseresult[e*:esize] = operand3[e*:esize] + product;\n        end;elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "MUL (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MUL--immediate---Multiply-by-immediate--unpredicated--?lang=en",
    "description": "Multiply by immediate (unpredicated) This instruction multiplies each element of the source vector by an immediate, and destructively places the results in the corresponding elements of the source vector. The immediate is a signed 8-bit value in the range -128 to +127, inclusive. This instruction is unpredicated.",
    "syntax": [
      "MUL<Zdn>.<T>,<Zdn>.<T>, #<imm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);\n    result[e*:esize] = (element1 * imm)[esize-1:0];\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "MUL (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MUL--indexed---Multiply-by-indexed-element-?lang=en",
    "description": "Multiply by indexed element This instruction multiplies all integer elements within each 128-bit segment of the first source vector by the specified element in the corresponding second source vector segment. The results are placed in the corresponding elements of the destination vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. This instruction is unpredicated. It has encodings from 3 classes:16-bit,32-bitand64-bit",
    "syntax": [
      "MUL<Zd>.D,<Zn>.D,<Zm>.D[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[s*:esize]);letres : integer = element1 * element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "MUL (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MUL--vectors--predicated---Multiply--predicated--?lang=en",
    "description": "Multiply (predicated) This instruction multiplies active elements of the first source vector by the corresponding elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "MUL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletproduct : integer = element1 * element2;\n        result[e*:esize] = product[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "MUL (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/MUL--vectors--unpredicated---Multiply--unpredicated--?lang=en",
    "description": "Multiply (unpredicated) This instruction multiplies elements of the first source vector by the corresponding elements of the second source vector and places the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "MUL<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letproduct : integer = element1 * element2;\n    result[e*:esize] = product[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "NAND",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/NAND--Bitwise-NAND-predicates-?lang=en",
    "description": "Bitwise NAND predicates This instruction performs a bitwise NAND between the active elements of the first source predicate and the corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags.",
    "syntax": [
      "NAND<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(NOT(element1 AND element2));elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "NANDS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/NANDS--Bitwise-NAND-predicates--setting-the-condition-flags-?lang=en",
    "description": "Bitwise NAND predicates, setting the condition flags This instruction performs a bitwise NAND between the active elements of the first source predicate and the corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "NANDS<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(NOT(element1 AND element2));elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nIf FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "NBSL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/NBSL--Bitwise-inverted-select-?lang=en",
    "description": "Bitwise inverted select This instruction selects bits from the first source vector where the corresponding bit in the third source vector is '1', and from the second source vector where the corresponding bit in the third source vector is '0'. The inverted result is destructively placed in the destination and first source vector. This instruction is unpredicated.",
    "syntax": [
      "NBSL<Zdn>.D,<Zdn>.D,<Zm>.D,<Zk>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)= Z{}(m);letoperand3 :bits(VL)= Z{}(k);Z{VL}(dn) = NOT((operand1ANDoperand3) OR (operand2ANDNOT(operand3)));",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "NEG",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/NEG--Negate--predicated--?lang=en",
    "description": "Negate (predicated) This instruction negates the signed integer value in each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "NEG<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenvarelement : integer = SInt(operand[e*:esize]);\n        element = -element;\n        result[e*:esize] = element[esize-1:0];\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "NMATCH",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/NMATCH--Detect-no-matching-elements--setting-the-condition-flags-?lang=en",
    "description": "Detect no matching elements, setting the condition flags This instruction compares each active 8-bit or 16-bit element in the first source vector with all of the elements in the corresponding 128-bit segment of the second source vector. For each element in the first source vector, if no matching element is found in the corresponding segment of the second source vector, the corresponding element of the destination predicate is set to true, otherwise it is set to false. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "NMATCH<Pd>.<T>,<Pg>/Z,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletsegmentbase : integer = e - (e MOD eltspersegment);\n        result[e*:psize] = ZeroExtend{psize}('1');letelement1 : bits(esize) = operand1[e*:esize];fori =segmentbaseto(segmentbase + eltspersegment) - 1doletelement2 :bits(esize)= operand2[i*:esize];ifelement1 == element2 then\n                result[e*:psize] = ZeroExtend{psize}('0');\n            end;\n        end;elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "NOR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/NOR--Bitwise-NOR-predicates-?lang=en",
    "description": "Bitwise NOR predicates This instruction performs a bitwise NOR between the active elements of the first source predicate and the corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags.",
    "syntax": [
      "NOR<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(NOT(element1 OR element2));elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "NORS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/NORS--Bitwise-NOR-predicates--setting-the-condition-flags-?lang=en",
    "description": "Bitwise NOR predicates, setting the condition flags This instruction performs a bitwise NOR between the active elements of the first source predicate and the corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "NORS<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(NOT(element1 OR element2));elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nIf FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "NOT (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/NOT--predicate---Bitwise-invert-predicate--an-alias-of-EOR--predicates--?lang=en",
    "description": "Bitwise invert predicate This instruction performs a bitwise invert of each active element of the source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. This is an alias ofEOR (predicates). This means: The encodings in this description are named to match the encodings ofEOR (predicates).The description ofEOR (predicates)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "NOT<Pd>.B,<Pg>/Z,<Pn>.B\nis equivalent to\nEOR<Pd>.B,<Pg>/Z,<Pn>.B,<Pg>.B\nand is the preferred disassembly whenPm == Pg."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofEOR (predicates)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "NOT (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/NOT--vector---Bitwise-invert--predicated--?lang=en",
    "description": "Bitwise invert (predicated) This instruction performs a bitwise invert of each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "NOT<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] = NOT element;\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "NOTS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/NOTS--Bitwise-invert-predicate--setting-the-condition-flags--an-alias-of-EORS-?lang=en",
    "description": "Bitwise invert predicate, setting the condition flags This instruction performs a bitwise invert of each active element of the source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This is an alias ofEORS. This means: The encodings in this description are named to match the encodings ofEORS.The description ofEORSgives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "NOTS<Pd>.B,<Pg>/Z,<Pn>.B\nis equivalent to\nEORS<Pd>.B,<Pg>/Z,<Pn>.B,<Pg>.B\nand is the preferred disassembly whenPm == Pg."
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofEORSgives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "ORN (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ORN--immediate---Bitwise-inclusive-OR-with-inverted-immediate--unpredicated---an-alias-of-ORR--immediate--?lang=en",
    "description": "Bitwise inclusive OR with inverted immediate (unpredicated) This instruction performs a bitwise inclusive OR between an inverted immediate and each 64-bit element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate is a 64-bit value consisting of a single run of ones or zeros repeating every 2, 4, 8, 16, 32 or 64 bits. This instruction is unpredicated. This is a pseudo-instruction ofORR (immediate). This means: The encodings in this description are named to match the encodings ofORR (immediate).The assembler syntax is used only for assembly, and is not used on disassembly.The description ofORR (immediate)gives the operational pseudocode, anyCONSTRAINED UNPREDICTABLEbehavior, and any operational information for this instruction.",
    "syntax": [
      "ORN<Zdn>.<T>,<Zdn>.<T>, #<const>\nis equivalent to\nORR<Zdn>.<T>,<Zdn>.<T>, #(-<const>- 1)"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "The description ofORR (immediate)gives the operational pseudocode for this instruction.",
    "operational_info": ""
  },
  {
    "title": "ORN (predicates)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ORN--predicates---Bitwise-inclusive-OR-inverted-predicate-?lang=en",
    "description": "Bitwise inclusive OR inverted predicate This instruction performs a bitwise inclusive OR between the active elements of the first source predicate and the inverted corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags.",
    "syntax": [
      "ORN<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(element1OR(NOT element2));elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ORNS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ORNS--Bitwise-inclusive-OR-inverted-predicate--setting-the-condition-flags-?lang=en",
    "description": "Bitwise inclusive OR inverted predicate, setting the condition flags This instruction performs a bitwise inclusive OR between the active elements of the first source predicate and the inverted corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "ORNS<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(element1OR(NOT element2));elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nIf FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "ORQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ORQV--Bitwise-inclusive-OR-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Bitwise inclusive OR reduction of quadword vector segments This instruction performs a bitwise inclusive OR between elements in the same position from each 128-bit source vector segment, and places each result into the corresponding element number of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as all zeros.",
    "syntax": [
      "ORQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer = VL DIV128;letelempersegment : integer =128DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(128) = Zeros{};varstmp : bits(128) = Zeros{};vardtmp : bits(esize);fore =0to elempersegment-1dodtmp = Zeros{esize};fors =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) then\n            stmp = operand[s*:128];\n            dtmp = dtmp OR stmp[e*:esize];\n        end;\n    end;\n    result[e*:esize] = dtmp[esize-1:0];\nend;V{128}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ORR (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ORR--immediate---Bitwise-inclusive-OR-with-immediate--unpredicated--?lang=en",
    "description": "Bitwise inclusive OR with immediate (unpredicated) This instruction performs a bitwise inclusive OR between an immediate and each 64-bit element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate is a 64-bit value consisting of a single run of ones or zeros repeating every 2, 4, 8, 16, 32 or 64 bits. This instruction is unpredicated. This instruction is used by the pseudo-instructionORN (immediate).",
    "syntax": [
      "ORR<Zdn>.<T>,<Zdn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV64;letoperand : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(64) = operand[e*:64];\n    result[e*:64] = element1 OR imm;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ORR (predicates)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ORR--predicates---Bitwise-inclusive-OR-predicates-?lang=en",
    "description": "Bitwise inclusive OR predicates This instruction performs a bitwise inclusive OR between the active elements of the first source predicate and the corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. This instruction is used by the aliasMOV.",
    "syntax": [
      "ORR<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(element1 OR element2);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ORR (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ORR--vectors--predicated---Bitwise-inclusive-OR--predicated--?lang=en",
    "description": "Bitwise inclusive OR (predicated) This instruction performs a bitwise inclusive OR between the active elements of the first source vector and the corresponding elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "ORR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = element1 OR element2;elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ORR (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ORR--vectors--unpredicated---Bitwise-inclusive-OR--unpredicated--?lang=en",
    "description": "Bitwise inclusive OR (unpredicated) This instruction performs a bitwise inclusive OR between the elements of the first source vector and the corresponding elements of the second source vector, and places the first in the corresponding elements of the destination vector. This instruction is unpredicated. This instruction is used by the aliasMOV (vector, unpredicated).",
    "syntax": [
      "ORR<Zd>.D,<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);Z{VL}(d) = operand1 OR operand2;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ORRS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ORRS--Bitwise-inclusive-OR-predicates--setting-the-condition-flags-?lang=en",
    "description": "Bitwise inclusive OR predicates, setting the condition flags This instruction performs a bitwise inclusive OR between the active elements of the first source predicate and the corresponding elements of the second source predicate, and places the results in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This instruction is used by the aliasMOVS (unpredicated).",
    "syntax": [
      "ORRS<Pd>.B,<Pg>/Z,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(element1 OR element2);elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nIf FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "ORV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ORV--Bitwise-inclusive-OR-reduction-to-scalar-?lang=en",
    "description": "Bitwise inclusive OR reduction to scalar This instruction performs a bitwise inclusive OR between the elements of the source vector, and places the result in the SIMD&FP scalar destination register. Inactive elements in the source vector are treated as zero.",
    "syntax": [
      "ORV<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(esize) = Zeros{esize};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result = result OR operand[e*:esize];\n\n    end;\nend;V{esize}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "PEXT (predicate pair)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PEXT--predicate-pair---Predicate-extract-pair-from-predicate-as-counter-?lang=en",
    "description": "Predicate extract pair from predicate-as-counter This instruction converts the source predicate-as-counter into a four register wide predicate-as-mask, and copies the two portions of the mask value selected by the portion index to the two destination predicate registers. A portion corresponds to a one predicate register fraction of the wider predicate-as-mask value.",
    "syntax": [
      "PEXT {<Pd1>.<T>,<Pd2>.<T>},<PNn>[<imm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letpred : bits(PL) = P{}(n);letmask : bits(PL*4) = CounterToPredicate{}(pred[15:0]);varresult0 : bits(PL);varresult1 : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletpbit : bit =PredicateElement{PL*4}(mask, part *2* elements + e, esize);\n    result0[e*:psize] = ZeroExtend{psize}(pbit);\nend;fore =0to elements-1doletpbit : bit =PredicateElement{PL*4}(mask, part *2* elements + elements + e, esize);\n    result1[e*:psize] = ZeroExtend{psize}(pbit);\nend;P{PL}(d0) = result0;P{PL}(d1) = result1;",
    "operational_info": ""
  },
  {
    "title": "PEXT (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PEXT--predicate---Predicate-extract-from-predicate-as-counter-?lang=en",
    "description": "Predicate extract from predicate-as-counter This instruction converts the source predicate-as-counter into a four register wide predicate-as-mask, and copies the portion of the mask value selected by the portion index to the destination predicate register. A portion corresponds to a one predicate register fraction of the wider predicate-as-mask value.",
    "syntax": [
      "PEXT<Pd>.<T>,<PNn>[<imm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letpred : bits(PL) = P{}(n);letmask : bits(PL*4) = CounterToPredicate{}(pred[15:0]);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletpbit : bit =PredicateElement{PL*4}(mask, part * elements + e, esize);\n    result[e*:psize] = ZeroExtend{psize}(pbit);\nend;P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "PFALSE",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PFALSE--Set-all-predicate-elements-to-false-?lang=en",
    "description": "Set all predicate elements to false This instruction sets all elements in the destination predicate to false. For programmer convenience, an assembler must also accept predicate-as-counter register name for the destination predicate register.",
    "syntax": [
      "PFALSE<Pd>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;P{PL}(d) = Zeros{PL};",
    "operational_info": ""
  },
  {
    "title": "PFIRST",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PFIRST--Set-the-First-active-predicate-element-to-true-?lang=en",
    "description": "Set the First active predicate element to true This instruction sets the First active element in the destination predicate to true, otherwise elements from the source predicate are passed through unchanged. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "PFIRST<Pdn>.B,<Pg>,<Pdn>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varresult : bits(PL) = P{}(dn);varfirst : integer =-1;letpsize : integer{} = esize DIV8;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) && first ==-1then\n        first = e;\n    end;\nend;iffirst >=0then\n    result[first*:psize] = ZeroExtend{psize}('1');\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(dn) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "PMLAL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PMLAL--Multi-vector-polynomial-multiply-long-and-accumulate-?lang=en",
    "description": "Multi-vector polynomial multiply long and accumulate This instruction performs a polynomial multiplication over [0, 1] of the even-numbered elements of the first source vector by the corresponding elements of the second source vector, and then performs a bitwise exclusive-OR between the result and the overlapping double-width elements of the first destination vector. The same operation is performed with odd-numbered elements of the source vectors, writing to the second destination vector. This instruction is unpredicated. This instruction is legal when executed in Streaming SVE mode if both FEAT_SSVE_AES and FEAT_SVE_AES2 are implemented.",
    "syntax": [
      "PMLAL {<Zda1>.Q-<Zda2>.Q },<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_AES) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult_lo : bits(VL) = Z{}(da +0);varresult_hi : bits(VL) = Z{}(da +1);fore =0to elements-1doletelement1_lo : bits(esize DIV2) = operand1[(2*e +0)*:(esize DIV2)];letelement2_lo : bits(esize DIV2) = operand2[(2*e +0)*:(esize DIV2)];letelement1_hi : bits(esize DIV2) = operand1[(2*e +1)*:(esize DIV2)];letelement2_hi : bits(esize DIV2) = operand2[(2*e +1)*:(esize DIV2)];letproduct_lo : bits(esize) =PolynomialMult{esize DIV2, esize DIV2}(element1_lo,\n                                                                            element2_lo);letproduct_hi : bits(esize) =PolynomialMult{esize DIV2, esize DIV2}(element1_hi,\n                                                                            element2_hi);\n    result_lo[e*:esize] = result_lo[e*:esize] XOR product_lo;\n    result_hi[e*:esize] = result_hi[e*:esize] XOR product_hi;\nend;Z{VL}(da +0) = result_lo;Z{VL}(da +1) = result_hi;",
    "operational_info": ""
  },
  {
    "title": "PMOV (to predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PMOV--to-predicate---Move-predicate-from-vector-?lang=en",
    "description": "Move predicate from vector This instruction copies a packed bitmap, where bit value 0b1 represents TRUE and bit value 0b0 represents FALSE, from a portion of the source vector register to elements of the destination SVE predicate register. Because the number of bits in an SVE predicate element scales with the vector element size, the behavior varies according to the specified element size. When the predicate element specifier is B, each bit [N] from the least-significant VL/8 bits in the source vector register is copied to bit [N] of the destination predicate register. The portion index, if specified, must be 0.When the predicate element specifier is H, each bit [N] within the indexed block of VL/16 bits in the source vector register is copied to bit [N*2] of the destination predicate register, and the other bits in the predicate are set to zero. The portion index is in the range 0 to 1, inclusive.When the predicate element specifier is S, each bit [N] within the indexed block of VL/32 bits in the source vector register is copied to bit [N*4] of the destination predicate register, and the other bits in the predicate are set to zero. The portion index is in the range 0 to 3, inclusive.When the predicate element specifier is D, each bit [N] within the indexed block of VL/64 bits in the source vector register is copied to bit [N*8] of the destination predicate register, and the other bits in the predicate are set to zero. The portion index is in the range 0 to 7, inclusive. The portion index is optional, defaulting to 0 if omitted. It has encodings from 4 classes:Byte,Doubleword,HalfwordandWord",
    "syntax": [
      "PMOV<Pd>.S,<Zn>{[<imm>]}"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doresult[e*:psize] = ZeroExtend{psize}(operand[(elements * imm) + e]);\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "PMOV (to vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PMOV--to-vector---Move-predicate-to-vector-?lang=en",
    "description": "Move predicate to vector This instruction copies the source SVE predicate register elements into the destination vector register as a packed bitmap with one bit per predicate element, where bit value 0b1 represents a TRUE predicate element, and bit value 0b0 represents a FALSE predicate element. Because the number of bits in an SVE predicate element scales with the vector element size, the behavior varies according to the specified element size. When the predicate element specifier is B, every bit in the predicate register is copied to the least-significant VL/8 bits of the destination vector register. The portion index, if specified, must be 0.When the predicate element specifier is H, every second bit in the predicate register is copied to the indexed block of VL/16 bits in the destination vector register, where the portion index is in the range 0 to 1, inclusive.When the predicate element specifier is S, every fourth bit in the predicate register is copied to the indexed block of VL/32 bits in the destination vector register, where the portion index is in the range 0 to 3, inclusive.When the predicate element specifier is D, every eighth bit in the predicate register is copied to the indexed block of VL/64 bits in the destination vector register, where the portion index is in the range 0 to 7, inclusive. The portion index is optional, defaulting to 0 if omitted. When the index is zero, the instruction writes zeroes to the most significant VL-(VL/esize) bits of the destination vector register. When a non-zero index is specified, the packed bitmap is inserted into the destination vector register, and the unindexed blocks remain unchanged. It has encodings from 4 classes:Byte,Doubleword,HalfwordandWord",
    "syntax": [
      "PMOV<Zd>{[<imm>]},<Pn>.S"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand : bits(PL) = P{}(n);varresult : bits(VL);ifimm ==0then\n    result = Zeros{VL};elseresult =Z{VL}(d);\nend;fore =0to elements-1doresult[(elements * imm) + e] =PredicateElement{PL}(operand, e, esize);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "PMUL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PMUL--Polynomial-multiply--unpredicated--?lang=en",
    "description": "Polynomial multiply (unpredicated) This instruction performs a polynomial multiplication over [0, 1] of all elements of the first source vector by the corresponding elements of the second source vector, and places the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "PMUL<Zd>.B,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[e*:esize] =PolynomialMult{esize, esize}(element1, element2)[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "PMULL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PMULL--Multi-vector-polynomial-multiply-long-?lang=en",
    "description": "Multi-vector polynomial multiply long This instruction performs a polynomial multiplication over [0, 1] of the even-numbered elements of the first source vector by the corresponding elements of the second source vector, and places the results in the overlapping double-width elements of the first destination vector. The same operation is performed with odd-numbered elements of the source vectors, writing to the second destination vector. This instruction is unpredicated. This instruction is legal when executed in Streaming SVE mode if both FEAT_SSVE_AES and FEAT_SVE_AES2 are implemented.",
    "syntax": [
      "PMULL {<Zd1>.Q-<Zd2>.Q },<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_SSVE",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SSVE_AES) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult_lo : bits(VL);varresult_hi : bits(VL);fore =0to elements-1doletelement1_lo : bits(esize DIV2) = operand1[(2*e +0)*:(esize DIV2)];letelement2_lo : bits(esize DIV2) = operand2[(2*e +0)*:(esize DIV2)];letelement1_hi : bits(esize DIV2) = operand1[(2*e +1)*:(esize DIV2)];letelement2_hi : bits(esize DIV2) = operand2[(2*e +1)*:(esize DIV2)];\n    result_lo[e*:esize] =PolynomialMult{esize DIV2, esize DIV2}(element1_lo, element2_lo);\n    result_hi[e*:esize] =PolynomialMult{esize DIV2, esize DIV2}(element1_hi, element2_hi);\nend;Z{VL}(d +0) = result_lo;Z{VL}(d +1) = result_hi;",
    "operational_info": ""
  },
  {
    "title": "PMULLB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PMULLB--Polynomial-multiply-long--bottom--?lang=en",
    "description": "Polynomial multiply long (bottom) This instruction performs a polynomial multiplication over [0, 1] of the even-numbered elements of the first source vector by the corresponding elements of the second source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated. ID_AA64ZFR0_EL1.AES indicates whether the 128-bit element variant is implemented. The 128-bit element variant is legal when executed in Streaming SVE mode if one of the following is true: Both FEAT_SSVE_AES and FEAT_SVE_PMULL128 are implemented.FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:16-bit or 64-bit elementsand128-bit element",
    "syntax": [
      "PMULLB<Zd>.Q,<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SSVE",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifesize ==128thenifIsFeatureImplemented(FEAT_SSVE_AES) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\n    end;elseCheckSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize DIV2) = operand1[(2*e +0)*:(esize DIV2)];letelement2 : bits(esize DIV2) = operand2[(2*e +0)*:(esize DIV2)];\n    result[e*:esize] =PolynomialMult{esize DIV2, esize DIV2}(element1, element2);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "PMULLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PMULLT--Polynomial-multiply-long--top--?lang=en",
    "description": "Polynomial multiply long (top) This instruction performs a polynomial multiplication over [0, 1] of the odd-numbered elements of the first source vector by the corresponding elements of the second source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated. ID_AA64ZFR0_EL1.AES indicates whether the 128-bit element variant is implemented. The 128-bit element variant is legal when executed in Streaming SVE mode if one of the following is true: Both FEAT_SSVE_AES and FEAT_SVE_PMULL128 are implemented.FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:16-bit or 64-bit elementsand128-bit element",
    "syntax": [
      "PMULLT<Zd>.Q,<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SSVE",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifesize ==128thenifIsFeatureImplemented(FEAT_SSVE_AES) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\n    end;elseCheckSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize DIV2) = operand1[(2*e +1)*:(esize DIV2)];letelement2 : bits(esize DIV2) = operand2[(2*e +1)*:(esize DIV2)];\n    result[e*:esize] =PolynomialMult{esize DIV2, esize DIV2}(element1, element2);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "PNEXT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PNEXT--Find-next-active-predicate-?lang=en",
    "description": "Find next active predicate This instruction constructs a loop that iterates over all true elements in the vector select predicate register. If all elements in the first source predicate register are false it determines the first true element in the vector select predicate register, otherwise it determines the next true element in the vector select predicate register that follows the last true element in the first source predicate register. All elements of the destination predicate register are set to false, except the element corresponding to the determined vector select element, if any, which is set to true. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "PNEXT<Pdn>.<T>,<Pv>,<Pdn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer{} = VL DIV esize;letmask : bits(PL) = P{}(v);letoperand : bits(PL) = P{}(dn);varresult : bits(PL) = Zeros{PL};letpsize : integer{} = esize DIV8;varnext : integer =LastActiveElement{PL}(operand, esize) +1;whilenext < elements && (!ActivePredicateElement{PL}(mask, next, esize)) looplimit elementsdonext = next +1;\nend;ifnext < elements then\n    result[next*:psize] = ZeroExtend{psize}('1');\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(dn) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "PRFB (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFB--scalar-plus-immediate---Contiguous-prefetch-bytes--immediate-index--?lang=en",
    "description": "Contiguous prefetch bytes (immediate index) This instruction prefetches contiguous byte elements from the memory address generated by a 64-bit scalar base and immediate index in the range -32 to 31 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. NoteArm strongly recommends the following for this instruction:A PE does not perform a prefetch as a result of Inactive elements.Software uses the predicate operand to suppress prefetches from unwanted addresses.",
    "syntax": [
      "PRFB<prfop>,<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(64);ifAnyActiveElement{PL}(mask, esize) thenbase=ifn ==31thenSP{64}()elseX{64}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        let eoff : integer = (offset * elements) + e;letaddr:bits(64)=base+ (eoff << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFB (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFB--scalar-plus-scalar---Contiguous-prefetch-bytes--scalar-index--?lang=en",
    "description": "Contiguous prefetch bytes (scalar index) This instruction prefetches contiguous byte elements from the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element prefetch the index value is incremented, but the index register is not updated. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. NoteArm strongly recommends the following for this instruction:A PE does not perform a prefetch as a result of Inactive elements.Software uses the predicate operand to suppress prefetches from unwanted addresses.",
    "syntax": [
      "PRFB<prfop>,<Pg>, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(64);varoffset :bits(64);ifAnyActiveElement{PL}(mask, esize) thenbase=ifn ==31thenSP{64}()elseX{64}(n);offset =X{64}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        let eoff : integer = UInt(offset) + e;letaddr:bits(64)=base+ (eoff << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFB (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFB--scalar-plus-vector---Gather-prefetch-bytes--scalar-plus-vector--?lang=en",
    "description": "Gather prefetch bytes (scalar plus vector) This instruction performs a gather prefetch of bytes from the active memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally sign-extended or zero-extended from 32 to 64 bits. Inactive addresses are not prefetched from memory. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:32-bit scaled offset,32-bit unpacked scaled offsetand64-bit scaled offset",
    "syntax": [
      "PRFB<prfop>,<Pg>, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(64);varoffset :bits(VL);ifAnyActiveElement{PL}(mask, esize) thenbase=ifn ==31thenSP{64}()elseX{64}(n);offset =Z{VL}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        let offselt :bits(offs_size)= offset[e*:esize][offs_size-1:0];let off : integer =ifoffs_unsigned then UInt(offselt)elseSInt(offselt);letaddr:bits(64)=base+ (off << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFB (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFB--vector-plus-immediate---Gather-prefetch-bytes--vector-plus-immediate--?lang=en",
    "description": "Gather prefetch bytes (vector plus immediate) This instruction performs a gather prefetch of bytes from the active memory addresses generated by a vector base plus immediate index. The index is in the range 0 to 31. Inactive addresses are not prefetched from memory. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "PRFB<prfop>,<Pg>, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letaddr:bits(64)= ZeroExtend{64}(base[e*:esize])+ (offset << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFD (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFD--scalar-plus-immediate---Contiguous-prefetch-doublewords--immediate-index--?lang=en",
    "description": "Contiguous prefetch doublewords (immediate index) This instruction prefetches contiguous doubleword elements from the memory address generated by a 64-bit scalar base and immediate index in the range -32 to 31 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. NoteArm strongly recommends the following for this instruction:A PE does not perform a prefetch as a result of Inactive elements.Software uses the predicate operand to suppress prefetches from unwanted addresses.",
    "syntax": [
      "PRFD<prfop>,<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(64);ifAnyActiveElement{PL}(mask, esize) thenbase=ifn ==31thenSP{64}()elseX{64}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        let eoff : integer = (offset * elements) + e;letaddr:bits(64)=base+ (eoff << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFD (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFD--scalar-plus-scalar---Contiguous-prefetch-doublewords--scalar-index--?lang=en",
    "description": "Contiguous prefetch doublewords (scalar index) This instruction prefetches contiguous doubleword elements from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 8 and added to the base address. After each element prefetch the index value is incremented, but the index register is not updated. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. NoteArm strongly recommends the following for this instruction:A PE does not perform a prefetch as a result of Inactive elements.Software uses the predicate operand to suppress prefetches from unwanted addresses.",
    "syntax": [
      "PRFD<prfop>,<Pg>, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(64);varoffset :bits(64);ifAnyActiveElement{PL}(mask, esize) thenbase=ifn ==31thenSP{64}()elseX{64}(n);offset =X{64}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        let eoff : integer = UInt(offset) + e;letaddr:bits(64)=base+ (eoff << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFD (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFD--scalar-plus-vector---Gather-prefetch-doublewords--scalar-plus-vector--?lang=en",
    "description": "Gather prefetch doublewords (scalar plus vector) This instruction performs a gather prefetch of doublewords from the active memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then multiplied by 8. Inactive addresses are not prefetched from memory. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:32-bit scaled offset,32-bit unpacked scaled offsetand64-bit scaled offset",
    "syntax": [
      "PRFD<prfop>,<Pg>, [<Xn|SP>,<Zm>.D, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(64);varoffset :bits(VL);ifAnyActiveElement{PL}(mask, esize) thenbase=ifn ==31thenSP{64}()elseX{64}(n);offset =Z{VL}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        let offselt :bits(offs_size)= offset[e*:esize][offs_size-1:0];let off : integer =ifoffs_unsigned then UInt(offselt)elseSInt(offselt);letaddr:bits(64)=base+ (off << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFD (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFD--vector-plus-immediate---Gather-prefetch-doublewords--vector-plus-immediate--?lang=en",
    "description": "Gather prefetch doublewords (vector plus immediate) This instruction performs a gather prefetch of doublewords from the active memory addresses generated by a vector base plus immediate index. The index is a multiple of 8 in the range 0 to 248. Inactive addresses are not prefetched from memory. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "PRFD<prfop>,<Pg>, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letaddr:bits(64)= ZeroExtend{64}(base[e*:esize])+ (offset << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFH (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFH--scalar-plus-immediate---Contiguous-prefetch-halfwords--immediate-index--?lang=en",
    "description": "Contiguous prefetch halfwords (immediate index) This instruction prefetches contiguous halfword elements from the memory address generated by a 64-bit scalar base and immediate index in the range -32 to 31 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. NoteArm strongly recommends the following for this instruction:A PE does not perform a prefetch as a result of Inactive elements.Software uses the predicate operand to suppress prefetches from unwanted addresses.",
    "syntax": [
      "PRFH<prfop>,<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(64);ifAnyActiveElement{PL}(mask, esize) thenbase=ifn ==31thenSP{64}()elseX{64}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        let eoff : integer = (offset * elements) + e;letaddr:bits(64)=base+ (eoff << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFH (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFH--scalar-plus-scalar---Contiguous-prefetch-halfwords--scalar-index--?lang=en",
    "description": "Contiguous prefetch halfwords (scalar index) This instruction prefetches contiguous halfword elements from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 2 and added to the base address. After each element prefetch the index value is incremented, but the index register is not updated. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. NoteArm strongly recommends the following for this instruction:A PE does not perform a prefetch as a result of Inactive elements.Software uses the predicate operand to suppress prefetches from unwanted addresses.",
    "syntax": [
      "PRFH<prfop>,<Pg>, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(64);varoffset :bits(64);ifAnyActiveElement{PL}(mask, esize) thenbase=ifn ==31thenSP{64}()elseX{64}(n);offset =X{64}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        let eoff : integer = UInt(offset) + e;letaddr:bits(64)=base+ (eoff << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFH (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFH--scalar-plus-vector---Gather-prefetch-halfwords--scalar-plus-vector--?lang=en",
    "description": "Gather prefetch halfwords (scalar plus vector) This instruction performs a gather prefetch of halfwords from the active memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then multiplied by 2. Inactive addresses are not prefetched from memory. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:32-bit scaled offset,32-bit unpacked scaled offsetand64-bit scaled offset",
    "syntax": [
      "PRFH<prfop>,<Pg>, [<Xn|SP>,<Zm>.D, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(64);varoffset :bits(VL);ifAnyActiveElement{PL}(mask, esize) thenbase=ifn ==31thenSP{64}()elseX{64}(n);offset =Z{VL}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        let offselt :bits(offs_size)= offset[e*:esize][offs_size-1:0];let off : integer =ifoffs_unsigned then UInt(offselt)elseSInt(offselt);letaddr:bits(64)=base+ (off << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFH (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFH--vector-plus-immediate---Gather-prefetch-halfwords--vector-plus-immediate--?lang=en",
    "description": "Gather prefetch halfwords (vector plus immediate) This instruction performs a gather prefetch of halfwords from the active memory addresses generated by a vector base plus immediate index. The index is a multiple of 2 in the range 0 to 62. Inactive addresses are not prefetched from memory. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "PRFH<prfop>,<Pg>, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letaddr:bits(64)= ZeroExtend{64}(base[e*:esize])+ (offset << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFW (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFW--scalar-plus-immediate---Contiguous-prefetch-words--immediate-index--?lang=en",
    "description": "Contiguous prefetch words (immediate index) This instruction prefetches contiguous word elements from the memory address generated by a 64-bit scalar base and immediate index in the range -32 to 31 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. NoteArm strongly recommends the following for this instruction:A PE does not perform a prefetch as a result of Inactive elements.Software uses the predicate operand to suppress prefetches from unwanted addresses.",
    "syntax": [
      "PRFW<prfop>,<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(64);ifAnyActiveElement{PL}(mask, esize) thenbase=ifn ==31thenSP{64}()elseX{64}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        let eoff : integer = (offset * elements) + e;letaddr:bits(64)=base+ (eoff << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFW (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFW--scalar-plus-scalar---Contiguous-prefetch-words--scalar-index--?lang=en",
    "description": "Contiguous prefetch words (scalar index) This instruction prefetches contiguous word elements from the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 4 and added to the base address. After each element prefetch the index value is incremented, but the index register is not updated. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. NoteArm strongly recommends the following for this instruction:A PE does not perform a prefetch as a result of Inactive elements.Software uses the predicate operand to suppress prefetches from unwanted addresses.",
    "syntax": [
      "PRFW<prfop>,<Pg>, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(64);varoffset :bits(64);ifAnyActiveElement{PL}(mask, esize) thenbase=ifn ==31thenSP{64}()elseX{64}(n);offset =X{64}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        let eoff : integer = UInt(offset) + e;letaddr:bits(64)=base+ (eoff << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFW (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFW--scalar-plus-vector---Gather-prefetch-words--scalar-plus-vector--?lang=en",
    "description": "Gather prefetch words (scalar plus vector) This instruction performs a gather prefetch of words from the active memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then multiplied by 4. Inactive addresses are not prefetched from memory. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:32-bit scaled offset,32-bit unpacked scaled offsetand64-bit scaled offset",
    "syntax": [
      "PRFW<prfop>,<Pg>, [<Xn|SP>,<Zm>.D, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(64);varoffset :bits(VL);ifAnyActiveElement{PL}(mask, esize) thenbase=ifn ==31thenSP{64}()elseX{64}(n);offset =Z{VL}(m);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        let offselt :bits(offs_size)= offset[e*:esize][offs_size-1:0];let off : integer =ifoffs_unsigned then UInt(offselt)elseSInt(offselt);letaddr:bits(64)=base+ (off << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PRFW (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PRFW--vector-plus-immediate---Gather-prefetch-words--vector-plus-immediate--?lang=en",
    "description": "Gather prefetch words (vector plus immediate) This instruction performs a gather prefetch of words from the active memory addresses generated by a vector base plus immediate index. The index is a multiple of 4 in the range 0 to 124. Inactive addresses are not prefetched from memory. The <prfop> operand specifies the prefetch hint as follows: Access type:PLD for prefetch for load.PST for prefetch for store.Target cache level:L1 for Level 1 cache.L2 for Level 2 cache.L3 for Level 3 cache.Policy:KEEP for retained or temporal prefetch, allocated in the cache normally.STRM for streaming or non-temporal prefetch, for data that is used only once. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "PRFW<prfop>,<Pg>, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask :bits(PL)= P{}(g);varbase:bits(VL);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        letaddr:bits(64)= ZeroExtend{64}(base[e*:esize])+ (offset << scale);Hint_Prefetch(addr,pref_hint, level,stream);end;end;",
    "operational_info": ""
  },
  {
    "title": "PSEL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PSEL--Predicate-select-between-predicate-register-or-all-false-?lang=en",
    "description": "Predicate select between predicate register or all-false This instruction places the contents of the first source predicate register into the destination predicate register if the indexed element of the second source predicate is true, otherwise it sets the destination predicate to all-false. The indexed element is determined by the sum of a general-purpose index register and an immediate, modulo the number of elements. This instruction does not set the condition flags. For programmer convenience, an assembler must also accept predicate-as-counter register names for the destination predicate register and the first source predicate register.",
    "syntax": [
      "PSEL<Pd>,<Pn>,<Pm>.<T>[<Wv>,<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);letidx : bits(32) = X{}(v);letelement : integer = (UInt(idx) + imm) MOD elements;varresult : bits(PL);ifActivePredicateElement{PL}(operand2, element, esize) then\n    result = operand1;elseresult = Zeros{PL};\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "PTEST",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PTEST--Set-condition-flags-for-predicate-?lang=en",
    "description": "Set condition flags for predicate This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate source register, and sets the V flag to zero.",
    "syntax": [
      "PTEST<Pg>,<Pn>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letmask : bits(PL) = P{}(g);letresult : bits(PL) = P{}(n);\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nIf FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the predicate register written by this instruction might be significantly delayed."
  },
  {
    "title": "PTRUE (predicate as counter)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PTRUE--predicate-as-counter---Initialize-predicate-as-counter-to-all-active-?lang=en",
    "description": "Initialize predicate-as-counter to all active This instruction sets the destination predicate as all Active elements, using the predicate-as-counter encoding.",
    "syntax": [
      "PTRUE<PNd>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letresult : bits(PL) = EncodePredCount{}(esize, elements, elements, FALSE);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "PTRUE (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PTRUE--predicate---Initialize-predicate-from-named-constraint-?lang=en",
    "description": "Initialize predicate from named constraint This instruction sets elements of the destination predicate to true if the element number satisfies the named predicate constraint, or to false otherwise. If the constraint specifies more elements than are available at the current vector length, then all elements of the destination predicate are set to false. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. This instruction does not set the condition flags.",
    "syntax": [
      "PTRUE<Pd>.<T>{,<pattern>}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletpbit : bit =ife < count then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(result, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "PTRUES",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PTRUES--Initialize-predicate-from-named-constraint-and-set-the-condition-flags-?lang=en",
    "description": "Initialize predicate from named constraint and set the condition flags This instruction sets elements of the destination predicate to true if the element number satisfies the named predicate constraint, or to false otherwise. If the constraint specifies more elements than are available at the current vector length, then all elements of the destination predicate are set to false. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "PTRUES<Pd>.<T>{,<pattern>}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletpbit : bit =ife < count then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\nend;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(result, result, esize);\nend;P{PL}(d) = result;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the NZCV condition flags written by this instruction might be significantly delayed."
  },
  {
    "title": "PUNPKHI, PUNPKLO",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/PUNPKHI--PUNPKLO--Unpack-and-widen-half-of-predicate-?lang=en",
    "description": "Unpack and widen half of predicate This instruction unpacks elements from the lowest or highest half of the source predicate and places them in elements of twice their size within the destination predicate. This instruction is unpredicated. It has encodings from 2 classes:High halfandLow half",
    "syntax": [
      "PUNPKLO<Pd>.H,<Pn>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand : bits(PL) = P{}(n);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletpbit : bit =PredicateElement{PL}(operand,ifhi then e + elementselsee, esize DIV2);\n    result[e*:psize] = ZeroExtend{psize}(pbit);\nend;P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "RADDHNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/RADDHNB--Rounding-add-narrow-high-part--bottom--?lang=en",
    "description": "Rounding add narrow high part (bottom) This instruction adds each vector element of the first source vector to the corresponding vector element of the second source vector, and places the most significant rounded half of the result in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero. This instruction is unpredicated.",
    "syntax": [
      "RADDHNB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letres : integer = ((element1 + element2) + (1<< (halfesize -1))) >> halfesize;\n    result[(2*e +0)*:halfesize] = res[halfesize-1:0];\n    result[(2*e +1)*:halfesize] = Zeros{halfesize};\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "RADDHNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/RADDHNT--Rounding-add-narrow-high-part--top--?lang=en",
    "description": "Rounding add narrow high part (top) This instruction adds each vector element of the first source vector to the corresponding vector element of the second source vector, and places the most significant rounded half of the result in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged. This instruction is unpredicated.",
    "syntax": [
      "RADDHNT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(d);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letres : integer = ((element1 + element2) + (1<< (halfesize -1))) >> halfesize;\n    result[(2*e +1)*:halfesize] = res[halfesize-1:0];\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "RAX1",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/RAX1--Bitwise-rotate-left-by-1-and-exclusive-OR-?lang=en",
    "description": "Bitwise rotate left by 1 and exclusive-OR This instruction rotates each 64-bit element of the second source vector left by 1 and performs an exclusive-OR with the corresponding elements of the first source vector. The results are placed in the corresponding elements of the destination vector. This instruction is unpredicated. ID_AA64ZFR0_EL1.SHA3 indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled, or FEAT_SME2p1 is implemented.",
    "syntax": [
      "RAX1<Zd>.D,<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SME2p1) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();letelements : integer = VL DIV64;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(64) = operand1[e*:64];letelement2 : bits(64) = operand2[e*:64];\n    result[e*:64] =element1 XORROL(element2,1);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "RBIT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/RBIT--Reverse-bits--predicated--?lang=en",
    "description": "Reverse bits (predicated) This instruction reverses the bits in each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "RBIT<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =BitReverse{esize}(element);\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "RDFFR (predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/RDFFR--predicated---Return-predicate-of-successfully-loaded-elements-?lang=en",
    "description": "Return predicate of successfully loaded elements This instruction reads the first-fault register (FFR) and places active elements in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction does not set the condition flags. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "RDFFR<Pd>.B,<Pg>/Z"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letmask : bits(PL) = P{}(g);letffr : bits(PL) = FFR{}();letresult : bits(PL) = ffr AND mask;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result,8);\nend;P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "RDFFR (unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/RDFFR--unpredicated---Read-the-first-fault-register-?lang=en",
    "description": "Read the first-fault register This instruction reads the first-fault register (FFR) and places the read values in the destination predicate without predication. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "RDFFR<Pd>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letffr : bits(PL) = FFR{}();P{PL}(d) = ffr;",
    "operational_info": ""
  },
  {
    "title": "RDFFRS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/RDFFRS--Return-predicate-of-successfully-loaded-elements--setting-the-condition-flags-?lang=en",
    "description": "Return predicate of successfully loaded elements, setting the condition flags This instruction reads the first-fault register (FFR) and places active elements in the corresponding elements of the destination predicate. Inactive elements in the destination predicate register are set to zero. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "RDFFRS<Pd>.B,<Pg>/Z"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letmask : bits(PL) = P{}(g);letffr : bits(PL) = FFR{}();letresult : bits(PL) = ffr AND mask;ifsetflags then\n    PSTATE.[N,Z,C,V] =PredTest{PL}(mask, result,8);\nend;P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "RDVL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/RDVL--Read-multiple-of-vector-register-size-to-scalar-register-?lang=en",
    "description": "Read multiple of vector register size to scalar register This instruction multiplies the current vector register size in bytes by an immediate in the range -32 to 31 and places the result in the 64-bit destination general-purpose register.",
    "syntax": [
      "RDVL<Xd>, #<imm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letlen : integer = imm * (VL DIV8);X{64}(d) = len[63:0];",
    "operational_info": ""
  },
  {
    "title": "REV (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/REV--predicate---Reverse-all-elements-in-a-predicate-?lang=en",
    "description": "Reverse all elements in a predicate This instruction reads all elements in the source predicate, reverses the order of all the elements, and places the result in the destination predicate. This instruction is unpredicated.",
    "syntax": [
      "REV<Pd>.<T>,<Pn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letoperand : bits(PL) = P{}(n);letresult : bits(PL) = Reverse{}(operand, esize DIV8);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "REV (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/REV--vector---Reverse-all-elements-in-a-vector--unpredicated--?lang=en",
    "description": "Reverse all elements in a vector (unpredicated) This instruction reads all elements in the source vector, reverses the order of all the elements, and places the result in the destination vector. This instruction is unpredicated.",
    "syntax": [
      "REV<Zd>.<T>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letoperand :bits(VL)= Z{}(n);letresult :bits(VL)=Reverse{}(operand,esize);Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "REVB, REVH, REVW",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/REVB--REVH--REVW--Reverse-bytes---halfwords---words-within-elements--predicated--?lang=en",
    "description": "Reverse bytes / halfwords / words within elements (predicated) This instruction reverses the order of 8-bit bytes, 16-bit halfwords or 32-bit words within each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 6 classes:Byte, merging,Byte, zeroing,Halfword, merging,Halfword, zeroing,Word, mergingandWord, zeroing",
    "syntax": [
      "REVW<Zd>.D,<Pg>/Z,<Zn>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =Reverse{esize}(element, swsize);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "REVD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/REVD--Reverse-64-bit-doublewords-in-elements--predicated--?lang=en",
    "description": "Reverse 64-bit doublewords in elements (predicated) This instruction reverses the order of 64-bit doublewords within each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "REVD<Zd>.Q,<Pg>/Z,<Zn>.Q"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =Reverse{esize}(element, swsize);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "RSHRNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/RSHRNB--Rounding-shift-right-narrow-by-immediate--bottom--?lang=en",
    "description": "Rounding shift right narrow by immediate (bottom) This instruction shifts each unsigned integer value in the source vector elements right by an immediate value, and places the rounded results in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "RSHRNB<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = (UInt(element) + (1<< (shift-1))) >> shift;\n    result[(2*e +0)*:esize] = res[esize-1:0];\n    result[(2*e +1)*:esize] = Zeros{esize};\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "RSHRNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/RSHRNT--Rounding-shift-right-narrow-by-immediate--top--?lang=en",
    "description": "Rounding shift right narrow by immediate (top) This instruction shifts each unsigned integer value in the source vector elements right by an immediate value, and places the rounded results in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "RSHRNT<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = (UInt(element) + (1<< (shift-1))) >> shift;\n    result[(2*e +1)*:esize] = res[esize-1:0];\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "RSUBHNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/RSUBHNB--Rounding-subtract-narrow-high-part--bottom--?lang=en",
    "description": "Rounding subtract narrow high part (bottom) This instruction subtracts each vector element of the second source vector from the corresponding vector element in the first source vector, and places the most significant rounded half of the result in the even-numbered half-width destination elements, while setting the odd-numbered half-width destination elements to zero. This instruction is unpredicated.",
    "syntax": [
      "RSUBHNB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letres : integer = ((element1 - element2) + (1<< (halfesize -1))) >> halfesize;\n    result[(2*e +0)*:halfesize] = res[halfesize-1:0];\n    result[(2*e +1)*:halfesize] = Zeros{halfesize};\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "RSUBHNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/RSUBHNT--Rounding-subtract-narrow-high-part--top--?lang=en",
    "description": "Rounding subtract narrow high part (top) This instruction subtracts each vector element of the second source vector from the corresponding vector element in the first source vector, and places the most significant rounded half of the result in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged. This instruction is unpredicated.",
    "syntax": [
      "RSUBHNT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(d);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letres : integer = ((element1 - element2) + (1<< (halfesize -1))) >> halfesize;\n    result[(2*e +1)*:halfesize] = res[halfesize-1:0];\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SABA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SABA--Signed-absolute-difference-and-accumulate-?lang=en",
    "description": "Signed absolute difference and accumulate This instruction calculates the absolute difference between signed integer values in elements of the first source vector and the corresponding elements of the second source vector, and adds the difference to the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SABA<Zda>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letop2elt : bits(esize) = operand2[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letabsdiff : bits(esize) = Abs(element1 - element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] + absdiff;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SABAL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SABAL--Two-way-signed-absolute-difference-sum-and-accumulate-long-?lang=en",
    "description": "Two-way signed absolute difference sum and accumulate long This instruction calculates the sum of the absolute difference between even-numbered signed elements and the absolute difference between the odd-numbered signed elements of each double-width container of the first and second vectors, and destructively adds the results to the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "SABAL<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelt1_a : integer = SInt(operand1[(2*e +0)*:(esize DIV2)]);letelt1_b : integer = SInt(operand1[(2*e +1)*:(esize DIV2)]);letelt2_a : integer = SInt(operand2[(2*e +0)*:(esize DIV2)]);letelt2_b : integer = SInt(operand2[(2*e +1)*:(esize DIV2)]);letsum = Abs(elt1_a - elt2_a) + Abs(elt1_b - elt2_b);\n    result[e*:esize] = result[e*:esize] + sum[esize-1:0];\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SABALB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SABALB--Signed-absolute-difference-and-accumulate-long--bottom--?lang=en",
    "description": "Signed absolute difference and accumulate long (bottom) This instruction calculates the absolute difference between even-numbered signed integer values in elements of the first source vector and the corresponding elements of the second source vector, and destructively adds the results to the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "SABALB<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2*e +0)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2*e +0)*:(esize DIV2)]);letabsdiff = Abs(element1 - element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] + absdiff;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SABALT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SABALT--Signed-absolute-difference-and-accumulate-long--top--?lang=en",
    "description": "Signed absolute difference and accumulate long (top) This instruction calculates the absolute difference between odd-numbered signed elements of the first source vector and the corresponding elements of the second source vector, and destructively adds the results to the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "SABALT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2*e +1)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2*e +1)*:(esize DIV2)]);letabsdiff = Abs(element1 - element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] + absdiff;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SABD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SABD--Signed-absolute-difference--predicated--?lang=en",
    "description": "Signed absolute difference (predicated) This instruction calculates the absolute difference between signed integer values in active elements of the first source vector and the corresponding elements of the second source vector, and destructively places the difference in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SABD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletabsdiff : integer = Abs(element1 - element2);\n        result[e*:esize] = absdiff[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SABDLB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SABDLB--Signed-absolute-difference-long--bottom--?lang=en",
    "description": "Signed absolute difference long (bottom) This instruction calculates the absolute difference between even-numbered signed integer values in elements of the first source vector and the corresponding elements of the second source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SABDLB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2*e +0)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2*e +0)*:(esize DIV2)]);letres : integer = Abs(element1 - element2);\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SABDLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SABDLT--Signed-absolute-difference-long--top--?lang=en",
    "description": "Signed absolute difference long (top) This instruction calculates the absolute difference between odd-numbered signed integer values in elements of the first source vector and the corresponding elements of the second source vector, and places the results in overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SABDLT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2*e +1)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2*e +1)*:(esize DIV2)]);letres : integer = Abs(element1 - element2);\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SADALP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SADALP--Signed-add-and-accumulate-long-pairwise-?lang=en",
    "description": "Signed add and accumulate long pairwise This instruction adds pairs of adjacent signed integer values and accumulates the results into the overlapping double-width elements of the destination vector.",
    "syntax": [
      "SADALP<Zda>.<T>,<Pg>/M,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand_acc : bits(VL) = Z{}(da);letoperand_src : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doif!ActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = operand_acc[e*:esize];elseletelement1 : integer = SInt(operand_src[(2*e +0)*:(esize DIV2)]);letelement2 : integer = SInt(operand_src[(2*e +1)*:(esize DIV2)]);letsum : bits(esize) = (element1 + element2)[esize-1:0];\n        result[e*:esize] = operand_acc[e*:esize] + sum;\n    end;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SADDLB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SADDLB--Signed-add-long--bottom--?lang=en",
    "description": "Signed add long (bottom) This instruction adds the corresponding even-numbered signed elements of the first and second source vectors, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SADDLB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize DIV2) = operand1[(2*e + sel1)*:(esize DIV2)];letop2elt : bits(esize DIV2) = operand2[(2*e + sel2)*:(esize DIV2)];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letres : integer = element1 + element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SADDLBT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SADDLBT--Signed-add-long--bottom---top--?lang=en",
    "description": "Signed add long (bottom + top) This instruction adds the even-numbered signed elements of the first source vector to the odd-numbered signed elements of the second source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SADDLBT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize DIV2) = operand1[(2*e + sel1)*:(esize DIV2)];letop2elt : bits(esize DIV2) = operand2[(2*e + sel2)*:(esize DIV2)];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letres : integer = element1 + element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SADDLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SADDLT--Signed-add-long--top--?lang=en",
    "description": "Signed add long (top) This instruction adds the corresponding odd-numbered signed elements of the first and second source vectors, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SADDLT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize DIV2) = operand1[(2*e + sel1)*:(esize DIV2)];letop2elt : bits(esize DIV2) = operand2[(2*e + sel2)*:(esize DIV2)];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letres : integer = element1 + element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SADDV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SADDV--Signed-add-reduction-to-scalar-?lang=en",
    "description": "Signed add reduction to scalar This instruction adds all the signed elements of the source vector, and places the result in the SIMD&FP scalar destination register. Narrow elements are first sign-extended to 64 bits. Inactive elements in the source vector are treated as zero.",
    "syntax": [
      "SADDV<Dd>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) = Z{}(n);varsum : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = SInt(operand[e*:esize]);\n        sum = sum + element;\n    end;\nend;V{64}(d) = sum[63:0];",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SADDWB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SADDWB--Signed-add-wide--bottom--?lang=en",
    "description": "Signed add wide (bottom) This instruction adds the even-numbered signed elements of the second source vector to the overlapping double-width elements of the first source vector and places the results in the corresponding double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SADDWB<Zd>.<T>,<Zn>.<T>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[(2*e +0)*:(esize DIV2)]);\n    result[e*:esize] = (element1 + element2)[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SADDWT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SADDWT--Signed-add-wide--top--?lang=en",
    "description": "Signed add wide (top) This instruction adds the odd-numbered signed elements of the second source vector to the overlapping double-width elements of the first source vector and places the results in the corresponding double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SADDWT<Zd>.<T>,<Zn>.<T>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[(2*e +1)*:(esize DIV2)]);\n    result[e*:esize] = (element1 + element2)[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SBCLB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SBCLB--Subtract-with-carry-long--bottom--?lang=en",
    "description": "Subtract with carry long (bottom) This instruction subtracts the even-numbered elements of the first source vector and the inverted 1-bit carry from the least-significant bit of the odd-numbered elements of the second source vector from the even-numbered elements of the destination and accumulator vector. The 1-bit carry output is placed in the corresponding odd-numbered element of the destination vector.",
    "syntax": [
      "SBCLB<Zda>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letpairs : integer =VLDIV(esize *2);letoperand : bits(VL) = Z{}(n);letcarries : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);forp =0to pairs-1doletelement1 : bits(esize) = result[(2*p +0)*:esize];letelement2 : bits(esize) = operand[(2*p +0)*:esize];letcarry_in : bit = carries[(2*p +1)*:esize][0];let(res, nzcv) =AddWithCarry{esize}(element1, NOT(element2), carry_in);letcarry_out : bit = nzcv[1];\n\n    result[(2*p +0)*:esize] = res;\n    result[(2*p +1)*:esize] = ZeroExtend{esize}(carry_out);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SBCLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SBCLT--Subtract-with-carry-long--top--?lang=en",
    "description": "Subtract with carry long (top) This instruction subtracts the odd-numbered elements of the first source vector and the inverted 1-bit carry from the least-significant bit of the odd-numbered elements of the second source vector from the even-numbered elements of the destination and accumulator vector. The 1-bit carry output is placed in the corresponding odd-numbered element of the destination vector.",
    "syntax": [
      "SBCLT<Zda>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letpairs : integer =VLDIV(esize *2);letoperand : bits(VL) = Z{}(n);letcarries : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);forp =0to pairs-1doletelement1 : bits(esize) = result[(2*p +0)*:esize];letelement2 : bits(esize) = operand[(2*p +1)*:esize];letcarry_in : bit = carries[(2*p +1)*:esize][0];let(res, nzcv) =AddWithCarry{esize}(element1, NOT(element2), carry_in);letcarry_out : bit = nzcv[1];\n\n    result[(2*p +0)*:esize] = res;\n    result[(2*p +1)*:esize] = ZeroExtend{esize}(carry_out);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SCLAMP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SCLAMP--Signed-clamp-to-minimum-maximum-?lang=en",
    "description": "Signed clamp to minimum/maximum This instruction clamps each signed element in the destination vector to between the signed minimum value in the corresponding element of the first source vector and the signed maximum value in the corresponding element of the second source vector and destructively writes the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SCLAMP<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(d);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);letelement3 : integer = SInt(operand3[e*:esize]);letres : integer = Min(Max(element1, element3), element2);\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SCVTF (bottom, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SCVTF--bottom--unpredicated---Signed-integer-convert-to-floating-point--bottom--unpredicated--?lang=en",
    "description": "Signed integer convert to floating-point (bottom, unpredicated) This instruction converts each even-numbered signed integer element of the source vector to floating-point, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SCVTF<Zd>.<T>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize DIV2) = operand[(2*e +0)*:(esize DIV2)];\n    result[e*:esize] =FixedToFP{esize, esize DIV2}(element,0, unsigned, FPCR(), rounding);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SCVTF (predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SCVTF--predicated---Signed-integer-convert-to-floating-point--predicated--?lang=en",
    "description": "Signed integer convert to floating-point (predicated) This instruction converts to floating-point from the signed integer in each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. If the input and result types have a different size the smaller type is held unpacked in the least significant bits of elements of the larger size. When the input is the smaller type the upper bits of each source element are ignored. When the result is the smaller type the results are zero-extended to fill each destination element. It has encodings from 14 classes:16-bit to half-precision, merging,16-bit to half-precision, zeroing,32-bit to half-precision, merging,32-bit to half-precision, zeroing,32-bit to single-precision, merging,32-bit to single-precision, zeroing,32-bit to double-precision, merging,32-bit to double-precision, zeroing,64-bit to half-precision, merging,64-bit to half-precision, zeroing,64-bit to single-precision, merging,64-bit to single-precision, zeroing,64-bit to double-precision, mergingand64-bit to double-precision, zeroing",
    "syntax": [
      "SCVTF<Zd>.D,<Pg>/Z,<Zn>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];letfpval : bits(d_esize) =FixedToFP{d_esize, s_esize}(element[s_esize-1:0],0, unsigned,\n                                                                FPCR(), rounding);\n        result[e*:esize] = ZeroExtend{esize}(fpval);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"16-bit to half-precision, merging\" , \"32-bit to half-precision, merging\" , \"32-bit to single-precision, merging\" , \"32-bit to double-precision, merging\" , \"64-bit to half-precision, merging\" , \"64-bit to single-precision, merging\" , \"64-bit to double-precision, merging\" variants:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "SCVTFLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SCVTFLT--Signed-integer-convert-to-floating-point--top--unpredicated--?lang=en",
    "description": "Signed integer convert to floating-point (top, unpredicated) This instruction converts each odd-numbered signed integer element of the source vector to floating-point, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SCVTFLT<Zd>.<T>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize DIV2) = operand[(2*e +1)*:(esize DIV2)];\n    result[e*:esize] =FixedToFP{esize, esize DIV2}(element,0, unsigned, FPCR(), rounding);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SDIV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SDIV--Signed-divide--predicated--?lang=en",
    "description": "Signed divide (predicated) This instruction performs a signed divide on active elements of the first source vector by the corresponding elements of the second source vector and destructively places the quotient in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SDIV<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletdividend : integer = SInt(operand1[e*:esize]);letdivisor : integer = SInt(operand2[e*:esize]);varquotient : integer;ifdivisor ==0then\n            quotient =0;\n        elsif (dividend <0) == (divisor <0) then\n            quotient = Abs(dividend)DIVRMAbs(divisor);// same signs - positive resultelsequotient = -(Abs(dividend)DIVRMAbs(divisor));// different signs - negative resultend;\n        result[e*:esize] = quotient[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SDIVR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SDIVR--Signed-reversed-divide--predicated--?lang=en",
    "description": "Signed reversed divide (predicated) This instruction performs a signed reversed divide on active elements of the second source vector by the corresponding elements of the first source vector, and destructively places the quotient in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SDIVR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletdivisor : integer = SInt(operand1[e*:esize]);letdividend : integer = SInt(operand2[e*:esize]);varquotient : integer;ifdivisor ==0then\n            quotient =0;\n        elsif (dividend <0) == (divisor <0) then\n            quotient = Abs(dividend)DIVRMAbs(divisor);// same signs - positive resultelsequotient = -(Abs(dividend)DIVRMAbs(divisor));// different signs - negative resultend;\n        result[e*:esize] = quotient[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SDOT (2-way, indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SDOT--2-way--indexed---Signed-integer-dot-product-by-indexed-element--two-way--?lang=en",
    "description": "Signed integer dot product by indexed element (two-way) This instruction calculates the dot product of a pair of signed 8-bit or 16-bit integer values held in each 16-bit or 32-bit element of the first source vector multiplied by a pair of signed 8-bit or 16-bit integer values in an indexed 16-bit or 32-bit element of the second source vector, and then destructively adds the widened dot product to the corresponding 16-bit or 32-bit element of the destination vector. The groups within the second source vector are specified using an immediate index that selects the same group position within each 128-bit vector segment. The index range is from 0 to one less than the number of groups per 128-bit segment. This instruction is unpredicated. It has encodings from 2 classes:8-bit to 16-bitand16-bit to 32-bit",
    "syntax": [
      "SDOT<Zda>.S,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;varres : bits(esize) = operand3[e*:esize];fori =0to1doletelement1 : integer = SInt(operand1[(2* e + i)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2* s + i)*:(esize DIV2)]);\n        res = res + element1 * element2;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SDOT (2-way, vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SDOT--2-way--vectors---Signed-integer-dot-product--two-way--?lang=en",
    "description": "Signed integer dot product (two-way) This instruction calculates the dot product of a pair of signed 8-bit or 16-bit integer values held in each 16-bit or 32-bit element of the first source vector multiplied by a pair of signed 8-bit or 16-bit integer values in the corresponding 16-bit or 32-bit element of the second source vector, and then destructively adds the widened dot product to the corresponding 16-bit or 32-bit element of the destination vector. This instruction is unpredicated. It has encodings from 2 classes:8-bit to 16-bitand16-bit to 32-bit",
    "syntax": [
      "SDOT<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1dovarres : bits(esize) = operand3[e*:esize];fori =0to1doletelement1 : integer = SInt(operand1[(2* e + i)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2* e + i)*:(esize DIV2)]);\n        res = res + element1 * element2;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SDOT (4-way, indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SDOT--4-way--indexed---Signed-integer-dot-product-by-indexed-element--four-way--?lang=en",
    "description": "Signed integer dot product by indexed element (four-way) This instruction calculates the dot product of a group of four signed 8-bit or 16-bit integer values held in each 32-bit or 64-bit element of the first source vector multiplied by a group of four signed 8-bit or 16-bit integer values in an indexed 32-bit or 64-bit element of the second source vector, and then destructively adds the widened dot product to the corresponding 32-bit or 64-bit element of the destination vector. The groups within the second source vector are specified using an immediate index that selects the same group position within each 128-bit vector segment. The index range is from 0 to one less than the number of groups per 128-bit segment. This instruction is unpredicated. It has encodings from 2 classes:8-bit to 32-bitand16-bit to 64-bit",
    "syntax": [
      "SDOT<Zda>.D,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;varres : bits(esize) = operand3[e*:esize];fori =0to3doletelement1 : integer = SInt(operand1[(4* e + i)*:(esize DIV4)]);letelement2 : integer = SInt(operand2[(4* s + i)*:(esize DIV4)]);\n        res = res + element1 * element2;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SDOT (4-way, vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SDOT--4-way--vectors---Signed-integer-dot-product--four-way--?lang=en",
    "description": "Signed integer dot product (four-way) This instruction calculates the dot product of a group of four signed 8-bit or 16-bit integer values held in each 32-bit or 64-bit element of the first source vector multiplied by a group of four signed 8-bit or 16-bit integer values in the corresponding 32-bit or 64-bit element of the second source vector, and then destructively adds the widened dot product to the corresponding 32-bit or 64-bit element of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SDOT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1dovarres : bits(esize) = operand3[e*:esize];fori =0to3doletelement1 : integer = SInt(operand1[(4* e + i)*:(esize DIV4)]);letelement2 : integer = SInt(operand2[(4* e + i)*:(esize DIV4)]);\n        res = res + element1 * element2;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SEL (predicates)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SEL--predicates---Conditionally-select-elements-from-two-predicates-?lang=en",
    "description": "Conditionally select elements from two predicates This instruction reads Active elements from the first source predicate and Inactive elements from the second source predicate and places them in the corresponding elements of the destination predicate. This instruction does not set the condition flags. This instruction is used by the aliasMOV (predicate, merging).",
    "syntax": [
      "SEL<Pd>.B,<Pg>,<Pn>.B,<Pm>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letpsize : integer{} = esize DIV8;fore =0to elements-1doletelement1 : bit =PredicateElement{PL}(operand1, e, esize);letelement2 : bit =PredicateElement{PL}(operand2, e, esize);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:psize] = ZeroExtend{psize}(element1);elseresult[e*:psize] = ZeroExtend{psize}(element2);\n    end;\nend;P{PL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SEL (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SEL--vectors---Conditionally-select-elements-from-two-vectors-?lang=en",
    "description": "Conditionally select elements from two vectors This instruction selects elements from the first source vector where the corresponding vector select predicate element is true, and from the second source vector where the predicate element is false, and places them in the corresponding elements of the destination vector. This instruction is used by the aliasMOV (vector, predicated).",
    "syntax": [
      "SEL<Zd>.<T>,<Pv>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(v);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};letoperand2 : bits(VL) =ifAnyActiveElement{PL}(NOT(mask), esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = operand1[e*:esize];elseresult[e*:esize] = operand2[e*:esize];\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SETFFR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SETFFR--Initialize-the-first-fault-register-to-all-true-?lang=en",
    "description": "Initialize the first-fault register to all true This instruction initializes the first-fault register (FFR) to all true prior to a sequence of first-fault or non-fault loads. This instruction is unpredicated. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "SETFFR"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;FFR{PL}() = Ones{PL};",
    "operational_info": ""
  },
  {
    "title": "SHADD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SHADD--Signed-halving-add-?lang=en",
    "description": "Signed halving add This instruction adds active signed elements of the first source vector to the corresponding signed elements of the second source vector, shifts right one bit, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SHADD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = (element1 + element2) >>1;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SHRNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SHRNB--Shift-right-narrow-by-immediate--bottom--?lang=en",
    "description": "Shift right narrow by immediate (bottom) This instruction shifts each unsigned integer value in the source vector elements right by an immediate value, and places the truncated results in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SHRNB<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = UInt(element) >> shift;\n    result[(2*e +0)*:esize] = res[esize-1:0];\n    result[(2*e +1)*:esize] = Zeros{esize};\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SHRNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SHRNT--Shift-right-narrow-by-immediate--top--?lang=en",
    "description": "Shift right narrow by immediate (top) This instruction shifts each unsigned integer value in the source vector elements right by an immediate value, and places the truncated results in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SHRNT<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = UInt(element) >> shift;\n    result[(2*e +1)*:esize] = res[esize-1:0];\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SHSUB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SHSUB--Signed-halving-subtract-?lang=en",
    "description": "Signed halving subtract This instruction subtracts active signed elements of the second source vector from the corresponding signed elements of the first source vector, shifts right one bit, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SHSUB<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = (element1 - element2) >>1;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SHSUBR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SHSUBR--Signed-halving-subtract-reversed-?lang=en",
    "description": "Signed halving subtract reversed This instruction subtracts active signed elements of the first source vector from corresponding signed elements of the second source vector, shifts right one bit, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SHSUBR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = (element2 - element1) >>1;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SLI",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SLI--Shift-left-and-insert--immediate--?lang=en",
    "description": "Shift left and insert (immediate) This instruction shifts each source vector element left by an immediate value, and inserts the result into the corresponding vector element in the destination vector register, merging the shifted bits from each source element with existing bits in each destination vector element. The immediate shift amount is an unsigned value in the range 0 to number of bits per element minus 1. This instruction is unpredicated.",
    "syntax": [
      "SLI<Zd>.<T>,<Zn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement1 : bits(esize) = result[e*:esize];letelement2 : bits(esize) = operand[e*:esize];letmask : bits(esize) = LSL(Ones{esize}, shift);letshiftedval : bits(esize) = LSL(element2, shift);\n    result[e*:esize] = (element1AND(NOT mask)) OR shiftedval;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SM4E",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SM4E--SM4-encryption-and-decryption-?lang=en",
    "description": "SM4 encryption and decryption This instruction reads 16 bytes of input data from each 128-bit segment of the first source vector, together with four iterations of 32-bit round keys from the corresponding 128-bit segments of the second source vector. Each block of data is encrypted by four rounds in accordance with the SM4 standard, and destructively placed in the corresponding segments of the first source vector. This instruction is unpredicated. ID_AA64ZFR0_EL1.SM4 indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "SM4E<Zdn>.S,<Zdn>.S,<Zm>.S"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fors =0to segments-1doletkey : bits(128) = operand2[s*:128];varintval : bits(32);varroundresult : bits(128) = operand1[s*:128];varroundkey : bits(32);forindex =0to3doroundkey = key[index*:32];\n        intval = roundresult[127:96] XOR roundresult[95:64] XOR roundresult[63:32] XOR roundkey;fori =0to3dointval[i*:8]  =Sbox(intval[i*:8]);\n        end;\n\n        intval = (intval XORROL(intval,2) XORROL(intval,10) XORROL(intval,18) XORROL(intval,24));\n        intval = intval XOR roundresult[31:0];\n\n        roundresult[31:0] = roundresult[63:32];\n        roundresult[63:32] = roundresult[95:64];\n        roundresult[95:64] = roundresult[127:96];\n        roundresult[127:96] = intval;\n    end;\n\n    result[s*:128] = roundresult;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SM4EKEY",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SM4EKEY--SM4-key-updates-?lang=en",
    "description": "SM4 key updates This instruction reads four rounds of 32-bit input key values from each 128-bit segment of the first source vector, along with four rounds of 32-bit constants from the corresponding 128-bit segment of the second source vector. The four rounds of output key values are derived in accordance with the SM4 standard, and placed in the corresponding segments of the destination vector. This instruction is unpredicated. ID_AA64ZFR0_EL1.SM4 indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "SM4EKEY<Zd>.S,<Zn>.S,<Zm>.S"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fors =0to segments-1doletsource : bits(128) = operand2[s*:128];varintval : bits(32);varconst: bits(32);varroundresult : bits(128) = operand1[s*:128];forindex =0to3doconst= source[index*:32];\n        intval = roundresult[127:96] XOR roundresult[95:64] XOR roundresult[63:32] XORconst;fori =0to3dointval[i*:8] =Sbox(intval[i*:8]);\n        end;\n\n        intval =intval XORROL(intval,13) XORROL(intval,23);\n        intval = intval XOR roundresult[31:0];\n\n        roundresult[31:0] = roundresult[63:32];\n        roundresult[63:32] = roundresult[95:64];\n        roundresult[95:64] = roundresult[127:96];\n        roundresult[127:96] = intval;\n    end;\n\n    result[s*:128] = roundresult;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SMAX (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMAX--immediate---Signed-maximum-with-immediate--unpredicated--?lang=en",
    "description": "Signed maximum with immediate (unpredicated) This instruction determines the signed maximum of an immediate and each element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate is a signed 8-bit value in the range -128 to +127, inclusive. This instruction is unpredicated.",
    "syntax": [
      "SMAX<Zdn>.<T>,<Zdn>.<T>, #<imm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);\n    result[e*:esize] = Max(element1, imm)[esize-1:0];\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMAX (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMAX--vectors---Signed-maximum--predicated--?lang=en",
    "description": "Signed maximum (predicated) This instruction determines the signed maximum of active elements of the first source vector and the corresponding elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SMAX<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletmaximum : integer = Max(element1, element2);\n        result[e*:esize] = maximum[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMAXP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMAXP--Signed-maximum-pairwise-?lang=en",
    "description": "Signed maximum pairwise This instruction calculates the maximum value of each pair of adjacent signed integer elements within each source vector, and interleaves the results from corresponding lanes. The interleaved result values are destructively placed in the first source vector.",
    "syntax": [
      "SMAXP<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);varelement1 : integer;varelement2 : integer;fore =0to elements-1doif!ActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = operand1[e*:esize];elseifIsEven(e) then\n            element1= SInt(operand1[(e +0)*:esize]);\n            element2 = SInt(operand1[(e +1)*:esize]);elseelement1 = SInt(operand2[(e -1)*:esize]);\n            element2 = SInt(operand2[(e +0)*:esize]);\n        end;letres : integer = Max(element1, element2);\n        result[e*:esize] = res[esize-1:0];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMAXQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMAXQV--Signed-maximum-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Signed maximum reduction of quadword vector segments This instruction determines the signed maximum of the elements in the same position from each 128-bit source vector segment, and places each result into the corresponding element of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as the minimum signed integer for the element size.",
    "syntax": [
      "SMAXQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer = VL DIV128;letelempersegment : integer =128DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(128) = Zeros{};varstmp : bits(128) = Zeros{};vardtmp : integer;fore =0to elempersegment-1dodtmp =ifunsigned then0else-(2^(esize-1));fors =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) then\n            stmp = operand[s*:128];\n            dtmp = Max(dtmp, SInt(stmp[e*:esize]));\n        end;\n    end;\n    result[e*:esize] = dtmp[esize-1:0];\nend;V{128}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SMAXV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMAXV--Signed-maximum-reduction-to-scalar-?lang=en",
    "description": "Signed maximum reduction to scalar This instruction determines the signed maximum of the elements of the source vector, and places the result in the SIMD&FP scalar destination register. Inactive elements in the source vector are treated as the minimum signed integer for the element size.",
    "syntax": [
      "SMAXV<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varmaximum : integer =ifunsigned then0else-(2^(esize-1));fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletopelt : bits(esize) = operand[e*:esize];letelement : integer =ifunsigned thenUInt(opelt) elseSInt(opelt);\n        maximum = Max(maximum, element);\n\n    end;\nend;V{esize}(d) = maximum[esize-1:0];",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SMIN (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMIN--immediate---Signed-minimum-with-immediate--unpredicated--?lang=en",
    "description": "Signed minimum with immediate (unpredicated) This instruction determines the signed minimum of an immediate and each element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate is a signed 8-bit value in the range -128 to +127, inclusive. This instruction is unpredicated.",
    "syntax": [
      "SMIN<Zdn>.<T>,<Zdn>.<T>, #<imm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);\n    result[e*:esize] = Min(element1, imm)[esize-1:0];\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMIN (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMIN--vectors---Signed-minimum--predicated--?lang=en",
    "description": "Signed minimum (predicated) This instruction determines the signed minimum of active elements of the first source vector and the corresponding elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SMIN<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletminimum : integer = Min(element1, element2);\n        result[e*:esize] = minimum[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMINP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMINP--Signed-minimum-pairwise-?lang=en",
    "description": "Signed minimum pairwise This instruction calculates the minimum value of each pair of adjacent signed integer elements within each source vector, and interleaves the results from corresponding lanes. The interleaved result values are destructively placed in the first source vector.",
    "syntax": [
      "SMINP<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);varelement1 : integer;varelement2 : integer;fore =0to elements-1doif!ActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = operand1[e*:esize];elseifIsEven(e) then\n            element1= SInt(operand1[(e +0)*:esize]);\n            element2 = SInt(operand1[(e +1)*:esize]);elseelement1 = SInt(operand2[(e -1)*:esize]);\n            element2 = SInt(operand2[(e +0)*:esize]);\n        end;letres : integer = Min(element1, element2);\n        result[e*:esize] = res[esize-1:0];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMINQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMINQV--Signed-minimum-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Signed minimum reduction of quadword vector segments This instruction determines the signed minimum of the elements in the same position from each 128-bit source vector segment, and places each result into the corresponding element of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as the maximum signed integer for the element size.",
    "syntax": [
      "SMINQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer = VL DIV128;letelempersegment : integer =128DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(128) = Zeros{};varstmp : bits(128) = Zeros{};vardtmp : integer;fore =0to elempersegment-1dodtmp =ifunsignedthen(2^esize -1)else(2^(esize-1) - 1);fors =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) then\n            stmp = operand[s*:128];\n            dtmp = Min(dtmp, SInt(stmp[e*:esize]));\n        end;\n    end;\n    result[e*:esize] = dtmp[esize-1:0];\nend;V{128}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SMINV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMINV--Signed-minimum-reduction-to-scalar-?lang=en",
    "description": "Signed minimum reduction to scalar This instruction determines the signed minimum of the elements of the source vector, and places the result in the SIMD&FP scalar destination register. Inactive elements in the source vector are treated as the maximum signed integer for the element size.",
    "syntax": [
      "SMINV<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varminimum : integer =ifunsignedthen(2^esize -1)else(2^(esize-1) - 1);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletopelt : bits(esize) = operand[e*:esize];letelement : integer =ifunsigned thenUInt(opelt) elseSInt(opelt);\n        minimum = Min(minimum, element);\n\n    end;\nend;V{esize}(d) = minimum[esize-1:0];",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SMLALB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMLALB--indexed---Signed-multiply-add-long-by-indexed-element--bottom--?lang=en",
    "description": "Signed multiply-add long by indexed element (bottom) This instruction multiplies the even-numbered signed elements within each 128-bit segment of the first source vector by the specified signed element in the corresponding second source vector segment, and destructively adds the results to the overlapping double-width elements of the addend vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SMLALB<Zda>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = SInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = SInt(operand2[(2* s + index)*:esize]);letproduct : bits(2*esize) = (element1 * element2)[2*esize-1:0];\n    result[e*:(2*esize)] = result[e*:(2*esize)] + product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMLALB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMLALB--vectors---Signed-multiply-add-long--bottom--?lang=en",
    "description": "Signed multiply-add long (bottom) This instruction multiplies the even-numbered signed elements of the first source vector by the corresponding elements of the second source vector, and destructively adds the results to the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "SMLALB<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2*e +0)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2*e +0)*:(esize DIV2)]);letproduct = (element1 * element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] + product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMLALT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMLALT--indexed---Signed-multiply-add-long-by-indexed-element--top--?lang=en",
    "description": "Signed multiply-add long by indexed element (top) This instruction multiplies the odd-numbered signed elements within each 128-bit segment of the first source vector by the specified signed element in the corresponding second source vector segment, and destructively adds the results to the overlapping double-width elements of the addend vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SMLALT<Zda>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = SInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = SInt(operand2[(2* s + index)*:esize]);letproduct : bits(2*esize) = (element1 * element2)[2*esize-1:0];\n    result[e*:(2*esize)] = result[e*:(2*esize)] + product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMLALT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMLALT--vectors---Signed-multiply-add-long--top--?lang=en",
    "description": "Signed multiply-add long (top) This instruction multiplies the odd-numbered signed elements of the first source vector by the corresponding elements of the second source vector, and destructively adds the results to the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "SMLALT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2*e +1)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2*e +1)*:(esize DIV2)]);letproduct = (element1 * element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] + product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMLSLB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMLSLB--indexed---Signed-multiply-subtract-long-by-indexed-element--bottom--?lang=en",
    "description": "Signed multiply-subtract long by indexed element (bottom) This instruction multiplies the even-numbered signed elements within each 128-bit segment of the first source vector by the specified signed element in the corresponding second source vector segment, and destructively subtracts the results from the overlapping double-width elements of the addend vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SMLSLB<Zda>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = SInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = SInt(operand2[(2* s + index)*:esize]);letproduct : bits(2*esize) = (element1 * element2)[2*esize-1:0];\n    result[e*:(2*esize)] = result[e*:(2*esize)] - product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMLSLB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMLSLB--vectors---Signed-multiply-subtract-long--bottom--?lang=en",
    "description": "Signed multiply-subtract long (bottom) This instruction multiplies the even-numbered signed elements of the first source vector by the corresponding elements of the second source vector, and destructively subtracts the results from the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "SMLSLB<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2*e +0)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2*e +0)*:(esize DIV2)]);letproduct = (element1 * element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] - product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMLSLT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMLSLT--indexed---Signed-multiply-subtract-long-by-indexed-element--top--?lang=en",
    "description": "Signed multiply-subtract long by indexed element (top) This instruction multiplies the odd-numbered signed elements within each 128-bit segment of the first source vector by the specified signed element in the corresponding second source vector segment, and destructively subtracts the results from the overlapping double-width elements of the addend vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SMLSLT<Zda>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = SInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = SInt(operand2[(2* s + index)*:esize]);letproduct : bits(2*esize) = (element1 * element2)[2*esize-1:0];\n    result[e*:(2*esize)] = result[e*:(2*esize)] - product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMLSLT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMLSLT--vectors---Signed-multiply-subtract-long--top--?lang=en",
    "description": "Signed multiply-subtract long (top) This instruction multiplies the odd-numbered signed elements of the first source vector by the corresponding elements of the second source vector, and destructively subtracts the results from the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "SMLSLT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2*e +1)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2*e +1)*:(esize DIV2)]);letproduct = (element1 * element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] - product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMMLA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMMLA--Signed-8-bit-integer-matrix-multiply-accumulate-to-32-bit-integer-?lang=en",
    "description": "Signed 8-bit integer matrix multiply-accumulate to 32-bit integer This instruction multiplies the 2×8 matrix of signed 8-bit integer values held in each 128-bit segment of the first source vector by the 8×2 matrix of signed 8-bit integer values in the corresponding segment of the second source vector. The resulting 2×2 widened 32-bit integer matrix product is then destructively added to the 32-bit integer matrix accumulator held in the corresponding segment of the addend and destination vector. This is equivalent to performing an 8-way dot product per destination element. This instruction is unpredicated. ID_AA64ZFR0_EL1.I8MM indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "SMMLA<Zda>.S,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_I8MM",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letoperand1 :bits(VL)= Z{}(n);letoperand2 :bits(VL)= Z{}(m);letoperand3 :bits(VL)= Z{}(da);varresult :bits(VL)= Zeros{};varop1, op2 :bits(128);varres,addend:bits(128);fors =0to segments-1do\n    op1    = operand1[s*:(128)];op2    = operand2[s*:(128)];addend= operand3[s*:(128)];res    =MatMulAdd(addend,op1, op2, op1_unsigned, op2_unsigned);result[s*:(128)] = res;end;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMULH (predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMULH--predicated---Signed-multiply-returning-high-half--predicated--?lang=en",
    "description": "Signed multiply returning high half (predicated) This instruction performs a widening multiplication of signed integer values in active elements of the first source vector by the corresponding elements of the second source vector and destructively places the high half of the result in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SMULH<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletproduct : integer = (element1 * element2) >> esize;\n        result[e*:esize] = product[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SMULH (unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMULH--unpredicated---Signed-multiply-returning-high-half--unpredicated--?lang=en",
    "description": "Signed multiply returning high half (unpredicated) This instruction performs a widening multiplication of signed integer values of all elements of the first source vector by the corresponding elements of the second source vector and places the high half of the result in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SMULH<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letop2elt : bits(esize) = operand2[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letproduct : integer = (element1 * element2) >> esize;\n    result[e*:esize] = product[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SMULLB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMULLB--indexed---Signed-multiply-long-by-indexed-element--bottom--?lang=en",
    "description": "Signed multiply long by indexed element (bottom) This instruction multiplies the even-numbered signed elements within each 128-bit segment of the first source vector by the specified signed element in the corresponding second source vector segment, and places the results in the overlapping double-width elements of the destination vector register. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SMULLB<Zd>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = SInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = SInt(operand2[(2* s + index)*:esize]);letres : integer = element1 * element2;\n    result[e*:(2*esize)] = res[2*esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SMULLB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMULLB--vectors---Signed-multiply-long--bottom--?lang=en",
    "description": "Signed multiply long (bottom) This instruction multiplies the even-numbered signed elements of the first source vector by the corresponding elements of the second source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SMULLB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2*e +0)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2*e +0)*:(esize DIV2)]);letres : integer = element1 * element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SMULLT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMULLT--indexed---Signed-multiply-long-by-indexed-element--top--?lang=en",
    "description": "Signed multiply long by indexed element (top) This instruction multiplies the odd-numbered signed elements within each 128-bit segment of the first source vector by the specified element in the corresponding second source vector segment, and places the results in the overlapping double-width elements of the destination vector register. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SMULLT<Zd>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = SInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = SInt(operand2[(2* s + index)*:esize]);letres : integer = element1 * element2;\n    result[e*:(2*esize)] = res[2*esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SMULLT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SMULLT--vectors---Signed-multiply-long--top--?lang=en",
    "description": "Signed multiply long (top) This instruction multiplies the odd-numbered signed elements of the first source vector by the corresponding elements of the second source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SMULLT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2*e +1)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2*e +1)*:(esize DIV2)]);letres : integer = element1 * element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SPLICE",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SPLICE--Splice-two-vectors-under-predicate-control-?lang=en",
    "description": "Splice two vectors under predicate control This instruction selects a region from the first source vector and copies it to the lowest-numbered elements of the result. Any remaining elements of the result are set to a copy of the lowest-numbered elements from the second source vector. The region is selected using the first and last true elements in the vector select predicate register. The result is destructively placed in the destination and first source vector, or constructively placed in the destination vector. It has encodings from 2 classes:ConstructiveandDestructive",
    "syntax": [
      "SPLICE<Zdn>.<T>,<Pv>,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(v);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(s1)elseZeros{VL};letoperand2 : bits(VL) = Z{}(s2);varresult : bits(VL);varx : integer =0;varactive : boolean = FALSE;letlastnum : integer =LastActiveElement{PL}(mask, esize);iflastnum >=0thenfore =0to lastnumdoactive = active ||ActivePredicateElement{PL}(mask, e, esize);ifactive then\n            result[x*:esize] = operand1[e*:esize];\n            x = x +1;\n        end;\n    end;\nend;letnelements : integer = (elements - x) -1;fore =0to nelementsdoresult[x*:esize] = operand2[e*:esize];\n    x = x +1;\nend;Z{VL}(dst) = result;",
    "operational_info": "For the \"Destructive\" variant:\nThe destructive variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the destructive variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQABS",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQABS--Signed-saturating-absolute-value-?lang=en",
    "description": "Signed saturating absolute value This instruction calculates the absolute value of the signed integer in each Active element of the source vector, and places the results in the corresponding elements of the destination vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "SQABS<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenvarelement : integer = SInt(operand[e*:esize]);\n        element = Abs(element);\n        result[e*:esize] =SignedSat{esize}(element);\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "SQADD (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQADD--immediate---Signed-saturating-add-immediate--unpredicated--?lang=en",
    "description": "Signed saturating add immediate (unpredicated) This instruction performs a signed saturating addition of an unsigned immediate to each element of the source vector, and destructively places the results in the corresponding elements of the source vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated. The immediate is an unsigned value in the range 0 to 255, and for element widths of 16 bits or higher it may also be a positive multiple of 256 in the range 256 to 65280. The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\".",
    "syntax": [
      "SQADD<Zdn>.<T>,<Zdn>.<T>, #<imm>{,<shift>}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);\n    (result[e*:esize], -) =SatQ{esize}(element1 + imm, unsigned);\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQADD (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQADD--vectors--predicated---Signed-saturating-add--predicated--?lang=en",
    "description": "Signed saturating add (predicated) This instruction adds active signed elements of the first source vector to corresponding signed elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SQADD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =SignedSat{esize}(element1 + element2);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQADD (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQADD--vectors--unpredicated---Signed-saturating-add--unpredicated--?lang=en",
    "description": "Signed saturating add (unpredicated) This instruction performs a signed saturating addition of all elements of the first source vector to the corresponding elements of the second source vector, and places the results in the corresponding elements of the destination vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQADD<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letop2elt : bits(esize) = operand2[e*:esize];ifunsigned thenletelement1 : integer = UInt(op1elt);letelement2 : integer = UInt(op2elt);\n        (result[e*:esize], -) =UnsignedSatQ{esize}(element1 + element2);elseletelement1 : integer = SInt(op1elt);letelement2 : integer = SInt(op2elt);\n        (result[e*:esize], -) =SignedSatQ{esize}(element1 + element2);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQCADD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQCADD--Saturating-complex-integer-add-?lang=en",
    "description": "Saturating complex integer add This instruction adds the real and imaginary components of the integral complex numbers from the first source vector to the complex numbers from the second source vector that have first been rotated by 90 or 270 degrees in the direction from the positive real axis towards the positive imaginary axis, when considered in polar representation, equivalent to multiplying the complex numbers in the second source vector by ±j beforehand. The results are destructively placed in the corresponding elements of the first source vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated. Each complex number is represented in a vector register as an even/odd pair of elements with the real part in the even-numbered element and the imaginary part in the odd-numbered element.",
    "syntax": [
      "SQCADD<Zdn>.<T>,<Zdn>.<T>,<Zm>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letpairs : integer =VLDIV(2* esize);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);forp =0to pairs-1dovaracc_r : integer  = SInt(operand1[(2* p +0)*:esize]);varacc_i : integer  = SInt(operand1[(2* p +1)*:esize]);letelt2_r : integer = SInt(operand2[(2* p +0)*:esize]);letelt2_i : integer = SInt(operand2[(2* p +1)*:esize]);ifsub_i then\n        acc_r = acc_r - elt2_i;\n        acc_i = acc_i + elt2_r;\n    end;ifsub_r then\n        acc_r = acc_r + elt2_i;\n        acc_i = acc_i - elt2_r;\n    end;\n\n    result[(2* p +0)*:esize] =SignedSat{esize}(acc_r);\n    result[(2* p +1)*:esize] =SignedSat{esize}(acc_i);\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQCVTN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQCVTN--Signed-32-bit-integer-saturating-extract-narrow-to-interleaved-16-bit-integer-?lang=en",
    "description": "Signed 32-bit integer saturating extract narrow to interleaved 16-bit integer This instruction saturates the signed integer value in each element of the pair of source vectors to half the original source element width, and places the two-way interleaved results in the half-width destination elements. This instruction is unpredicated.",
    "syntax": [
      "SQCVTN<Zd>.H, {<Zn1>.S-<Zn2>.S }"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);varresult : bits(VL);fore =0to elements-1dofori =0to1doletoperand : bits(VL) = Z{}(n+i);letelement : integer = SInt(operand[e*:(2* esize)]);\n        result[(2*e + i)*:esize] =SignedSat{esize}(element);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQCVTUN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQCVTUN--Signed-32-bit-integer-saturating-extract-narrow-to-interleaved-unsigned-16-bit-integer-?lang=en",
    "description": "Signed 32-bit integer saturating extract narrow to interleaved unsigned 16-bit integer This instruction saturates the signed integer value in each element of the pair of source vectors to unsigned integer value that is half the original source element width, and places the two-way interleaved results in the half-width destination elements. This instruction is unpredicated.",
    "syntax": [
      "SQCVTUN<Zd>.H, {<Zn1>.S-<Zn2>.S }"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);varresult : bits(VL);fore =0to elements-1dofori =0to1doletoperand : bits(VL) = Z{}(n+i);letelement : integer = SInt(operand[e*:(2* esize)]);\n        result[(2*e + i)*:esize] =UnsignedSat{esize}(element);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQDECB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDECB--Signed-saturating-decrement-scalar-by-multiple-of-8-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating decrement scalar by multiple of 8-bit predicate constraint element count This instruction determines the number of active 8-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement the scalar destination. The result is saturated to the source general-purpose register's signed integer range. A 32-bit saturated result is then sign-extended to 64 bits. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQDECB<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) - (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) - (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "SQDECD (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDECD--scalar---Signed-saturating-decrement-scalar-by-multiple-of-64-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating decrement scalar by multiple of 64-bit predicate constraint element count This instruction determines the number of active 64-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement the scalar destination. The result is saturated to the source general-purpose register's signed integer range. A 32-bit saturated result is then sign-extended to 64 bits. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQDECD<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) - (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) - (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "SQDECD (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDECD--vector---Signed-saturating-decrement-vector-by-multiple-of-64-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating decrement vector by multiple of 64-bit predicate constraint element count This instruction determines the number of active 64-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement all destination vector elements. The results are saturated to the 64-bit signed integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception.",
    "syntax": [
      "SQDECD<Zdn>.D{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(op1elt) - (count * imm));else(result[e*:esize], -) =SignedSatQ{esize}(SInt(op1elt) - (count * imm));\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDECH (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDECH--scalar---Signed-saturating-decrement-scalar-by-multiple-of-16-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating decrement scalar by multiple of 16-bit predicate constraint element count This instruction determines the number of active 16-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement the scalar destination. The result is saturated to the source general-purpose register's signed integer range. A 32-bit saturated result is then sign-extended to 64 bits. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQDECH<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) - (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) - (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "SQDECH (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDECH--vector---Signed-saturating-decrement-vector-by-multiple-of-16-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating decrement vector by multiple of 16-bit predicate constraint element count This instruction determines the number of active 16-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement all destination vector elements. The results are saturated to the 16-bit signed integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception.",
    "syntax": [
      "SQDECH<Zdn>.H{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(op1elt) - (count * imm));else(result[e*:esize], -) =SignedSatQ{esize}(SInt(op1elt) - (count * imm));\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDECP (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDECP--scalar---Signed-saturating-decrement-scalar-by-count-of-true-predicate-elements-?lang=en",
    "description": "Signed saturating decrement scalar by count of true predicate elements This instruction determines the number of true elements in the source predicate and then uses the result to decrement the scalar destination. The result is saturated to the source general-purpose register's signed integer range. A 32-bit saturated result is then sign-extended to 64 bits. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQDECP<Xdn>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(ssize) = X{}(dn);letoperand2 : bits(PL) = P{}(m);varresult : bits(ssize);varcount : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(operand2, e, esize) then\n        count = count +1;\n    end;\nend;ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) - count);X{64}(dn) = ZeroExtend{64}(result);else(result, -) =SignedSatQ{ssize}(SInt(operand1) - count);X{64}(dn) = SignExtend{64}(result);\nend;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "SQDECP (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDECP--vector---Signed-saturating-decrement-vector-by-count-of-true-predicate-elements-?lang=en",
    "description": "Signed saturating decrement vector by count of true predicate elements This instruction determines the number of true elements in the source predicate and then uses the result to decrement all destination vector elements. The results are saturated to the element signed integer range. The predicate size specifier may be omitted in assembler source code, but this is deprecated and will be prohibited in a future release of the architecture.",
    "syntax": [
      "SQDECP<Zdn>.<T>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(PL) = P{}(m);varresult : bits(VL);varcount : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(operand2, e, esize) then\n        count = count +1;\n    end;\nend;fore =0to elements-1doletopelt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(opelt) - count);else(result[e*:esize], -) =SignedSatQ{esize}(SInt(opelt) - count);\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDECW (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDECW--scalar---Signed-saturating-decrement-scalar-by-multiple-of-32-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating decrement scalar by multiple of 32-bit predicate constraint element count This instruction determines the number of active 32-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement the scalar destination. The result is saturated to the source general-purpose register's signed integer range. A 32-bit saturated result is then sign-extended to 64 bits. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQDECW<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) - (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) - (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "SQDECW (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDECW--vector---Signed-saturating-decrement-vector-by-multiple-of-32-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating decrement vector by multiple of 32-bit predicate constraint element count This instruction determines the number of active 32-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement all destination vector elements. The results are saturated to the 32-bit signed integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception.",
    "syntax": [
      "SQDECW<Zdn>.S{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(op1elt) - (count * imm));else(result[e*:esize], -) =SignedSatQ{esize}(SInt(op1elt) - (count * imm));\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDMLALB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMLALB--indexed---Signed-saturating-doubling-multiply-add-by-indexed-element--bottom--?lang=en",
    "description": "Signed saturating doubling multiply-add by indexed element (bottom) This instruction multiples the even-numbered signed elements within each 128-bit segment of the first source vector by the specified signed element in the corresponding second source vector segment and then doubles the result of the multiplication. Each intermediate value is saturated to the double-width N-bit value's signed integer range -2(N-1)to (2(N-1))-1 and is then destructively added to the overlapping double-width elements of the addend and destination vector. Each destination element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQDMLALB<Zda>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = SInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = SInt(operand2[(2* s + index)*:esize]);letelement3 : integer = SInt(result[e*:(2*esize)]);letproduct : integer = SInt(SignedSat{2*esize}(2* element1 * element2));letres : integer = element3 + product;\n    result[e*:(2*esize)] =SignedSat{2*esize}(res);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDMLALB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMLALB--vectors---Signed-saturating-doubling-multiply-add--bottom--?lang=en",
    "description": "Signed saturating doubling multiply-add (bottom) This instruction multiples the even-numbered signed elements of the first source vector by the corresponding elements of the second source vector, and then doubles the result of the multiplication. Each intermediate value is saturated to the double-width N-bit value's signed integer range -2(N-1)to (2(N-1))-1 and is then destructively added to the overlapping double-width elements of the addend and destination vector. Each destination element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQDMLALB<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2* e + sel1)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2* e + sel2)*:(esize DIV2)]);letelement3 : integer = SInt(result[e*:esize]);letproduct : integer = SInt(SignedSat{esize}(2* element1 * element2));\n    result[e*:esize] =SignedSat{esize}(element3 + product);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDMLALBT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMLALBT--Signed-saturating-doubling-multiply-add--bottom---top--?lang=en",
    "description": "Signed saturating doubling multiply-add (bottom × top) This instruction multiplies the even-numbered signed elements of the first source vector by the corresponding odd-numbered signed elements of the second source vector, and then doubles the result of the multiplication. Each intermediate value is saturated to the double-width N-bit value's signed integer range -2(N-1)to (2(N-1))-1 and is then destructively added to the overlapping double-width elements of the addend and destination vector. Each destination element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQDMLALBT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2* e + sel1)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2* e + sel2)*:(esize DIV2)]);letelement3 : integer = SInt(result[e*:esize]);letproduct : integer = SInt(SignedSat{esize}(2* element1 * element2));\n    result[e*:esize] =SignedSat{esize}(element3 + product);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDMLALT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMLALT--indexed---Signed-saturating-doubling-multiply-add-by-indexed-element--top--?lang=en",
    "description": "Signed saturating doubling multiply-add by indexed element (top) This instruction multiplies the odd-numbered signed elements within each 128-bit segment of the first source vector by the specified signed element in the corresponding second source vector segment, and then doubles the result. Each intermediate value is saturated to the double-width N-bit value's signed integer range -2(N-1)to (2(N-1))-1 and is then destructively added to the overlapping double-width elements of the addend and destination vector. Each destination element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQDMLALT<Zda>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = SInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = SInt(operand2[(2* s + index)*:esize]);letelement3 : integer = SInt(result[e*:(2*esize)]);letproduct : integer = SInt(SignedSat{2*esize}(2* element1 * element2));letres : integer = element3 + product;\n    result[e*:(2*esize)] =SignedSat{2*esize}(res);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDMLALT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMLALT--vectors---Signed-saturating-doubling-multiply-add--top--?lang=en",
    "description": "Signed saturating doubling multiply-add (top) This instruction multiplies the odd-numbered signed elements of the first source vector by the corresponding elements of the second source vector, and then doubles the result. Each intermediate value is saturated to the double-width N-bit value's signed integer range -2(N-1)to (2(N-1))-1 and is then destructively added to the overlapping double-width elements of the addend and destination vector. Each destination element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQDMLALT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2* e + sel1)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2* e + sel2)*:(esize DIV2)]);letelement3 : integer = SInt(result[e*:esize]);letproduct : integer = SInt(SignedSat{esize}(2* element1 * element2));\n    result[e*:esize] =SignedSat{esize}(element3 + product);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDMLSLB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMLSLB--indexed---Signed-saturating-doubling-multiply-subtract-by-indexed-element--bottom--?lang=en",
    "description": "Signed saturating doubling multiply-subtract by indexed element (bottom) This instruction multiplies the even-numbered signed elements within each 128-bit segment of the first source vector by the specified signed element in the corresponding second source vector segment, and then doubles the result. Each intermediate value is saturated to the double-width N-bit value's signed integer range -2(N-1)to (2(N-1))-1 and is then destructively subtracted from the overlapping double-width elements of the addend and destination vector. Each destination element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQDMLSLB<Zda>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = SInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = SInt(operand2[(2* s + index)*:esize]);letelement3 : integer = SInt(result[e*:(2*esize)]);letproduct : integer = SInt(SignedSat{2*esize}(2* element1 * element2));letres : integer = element3 - product;\n    result[e*:(2*esize)] =SignedSat{2*esize}(res);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDMLSLB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMLSLB--vectors---Signed-saturating-doubling-multiply-subtract--bottom--?lang=en",
    "description": "Signed saturating doubling multiply-subtract (bottom) This instruction multiplies the even-numbered signed elements of the first source vector by the corresponding elements of the second source vector, and then doubles the result. Each intermediate value is saturated to the double-width N-bit value's signed integer range -2(N-1)to (2(N-1))-1 and is then destructively subtracted from the overlapping double-width elements of the addend and destination vector. Each destination element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQDMLSLB<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2* e + sel1)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2* e + sel2)*:(esize DIV2)]);letelement3 : integer = SInt(result[e*:esize]);letproduct : integer = SInt(SignedSat{esize}(2* element1 * element2));\n    result[e*:esize] =SignedSat{esize}(element3 - product);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDMLSLBT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMLSLBT--Signed-saturating-doubling-multiply-subtract--bottom---top--?lang=en",
    "description": "Signed saturating doubling multiply-subtract (bottom × top) This instruction multiplies the even-numbered signed elements of the first source vector by the corresponding odd-numbered signed elements of the second source vector, and then doubles the result. Each intermediate value is saturated to the double-width N-bit value's signed integer range -2(N-1)to (2(N-1))-1 and is then destructively subtracted from the overlapping double-width elements of the addend and destination vector. Each destination element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQDMLSLBT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2* e + sel1)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2* e + sel2)*:(esize DIV2)]);letelement3 : integer = SInt(result[e*:esize]);letproduct : integer = SInt(SignedSat{esize}(2* element1 * element2));\n    result[e*:esize] =SignedSat{esize}(element3 - product);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDMLSLT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMLSLT--indexed---Signed-saturating-doubling-multiply-subtract-by-indexed-element--top--?lang=en",
    "description": "Signed saturating doubling multiply-subtract by indexed element (top) This instruction multiplies the odd-numbered signed elements within each 128-bit segment of the first source vector by the specified signed element in the corresponding second source vector segment, and then doubles the result. Each intermediate value is saturated to the double-width N-bit value's signed integer range -2(N-1)to (2(N-1))-1 and is then destructively subtracted from the overlapping double-width elements of the addend and destination vector. Each destination element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQDMLSLT<Zda>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = SInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = SInt(operand2[(2* s + index)*:esize]);letelement3 : integer = SInt(result[e*:(2*esize)]);letproduct : integer = SInt(SignedSat{2*esize}(2* element1 * element2));letres : integer = element3 - product;\n    result[e*:(2*esize)] =SignedSat{2*esize}(res);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDMLSLT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMLSLT--vectors---Signed-saturating-doubling-multiply-subtract--top--?lang=en",
    "description": "Signed saturating doubling multiply-subtract (top) This instruction multiplies the odd-numbered signed elements of the first source vector by the corresponding elements of the second source vector, and then doubles the result. Each intermediate value is saturated to the double-width N-bit value's signed integer range -2(N-1)to (2(N-1))-1 and is then destructively subtracted from the overlapping double-width elements of the addend and destination vector. Each destination element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQDMLSLT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2* e + sel1)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2* e + sel2)*:(esize DIV2)]);letelement3 : integer = SInt(result[e*:esize]);letproduct : integer = SInt(SignedSat{esize}(2* element1 * element2));\n    result[e*:esize] =SignedSat{esize}(element3 - product);\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQDMULH (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMULH--indexed---Signed-saturating-doubling-multiply-high-by-indexed-element-?lang=en",
    "description": "Signed saturating doubling multiply high by indexed element This instruction multiplies all signed elements within each 128-bit segment of the first source vector by the specified signed element in the corresponding second source vector segment, then doubles and places the most significant half of the result in the corresponding elements of the destination vector register. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 3 classes:16-bit,32-bitand64-bit",
    "syntax": [
      "SQDMULH<Zd>.D,<Zn>.D,<Zm>.D[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[s*:esize]);letres : integer =2* element1 * element2;\n    result[e*:esize] =SignedSat{esize}(res >> esize);\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SQDMULH (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMULH--vectors---Signed-saturating-doubling-multiply-high--unpredicated--?lang=en",
    "description": "Signed saturating doubling multiply high (unpredicated) This instruction multiplies the signed elements of the first source vector by the corresponding elements of the second source vector, and then doubles the result and places the most significant half of the results in the corresponding elements of the destination vector register. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQDMULH<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);letres : integer =2* element1 * element2;\n    result[e*:esize] =SignedSat{esize}(res >> esize);\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SQDMULLB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMULLB--indexed---Signed-saturating-doubling-multiply-by-indexed-element--bottom--?lang=en",
    "description": "Signed saturating doubling multiply by indexed element (bottom) This instruction multiplies the even-numbered signed elements within each 128-bit segment of the first source vector by the specified element in the corresponding second source vector segment, and then doubles and places the results in overlapping double-width elements of the destination vector register. Each result element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQDMULLB<Zd>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = SInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = SInt(operand2[(2* s + index)*:esize]);letres : integer =2* element1 * element2;\n    result[e*:(2*esize)] =SignedSat{2*esize}(res);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQDMULLB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMULLB--vectors---Signed-saturating-doubling-multiply--bottom--?lang=en",
    "description": "Signed saturating doubling multiply (bottom) This instruction multiplies the even-numbered signed elements of the first source vector by the corresponding elements of the second source vector, and then doubles the result and places the results in the overlapping double-width elements of the destination vector. Each result element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQDMULLB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2*e +0)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2*e +0)*:(esize DIV2)]);letres : integer =2* element1 * element2;\n    result[e*:esize] =SignedSat{esize}(res);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQDMULLT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMULLT--indexed---Signed-saturating-doubling-multiply-by-indexed-element--top--?lang=en",
    "description": "Signed saturating doubling multiply by indexed element (top) This instruction multiplies the odd-numbered signed elements within each 128-bit segment of the first source vector by the specified element in the corresponding second source vector segment, and then doubles and places the results in overlapping double-width elements of the destination vector register. Each result element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQDMULLT<Zd>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = SInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = SInt(operand2[(2* s + index)*:esize]);letres : integer =2* element1 * element2;\n    result[e*:(2*esize)] =SignedSat{2*esize}(res);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQDMULLT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQDMULLT--vectors---Signed-saturating-doubling-multiply--top--?lang=en",
    "description": "Signed saturating doubling multiply (top) This instruction multiplies the odd-numbered signed elements of the first source vector by the corresponding elements of the second source vector, and then doubles and places the results in the overlapping double-width elements of the destination vector. Each result element is saturated to the double-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQDMULLT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[(2*e +1)*:(esize DIV2)]);letelement2 : integer = SInt(operand2[(2*e +1)*:(esize DIV2)]);letres : integer =2* element1 * element2;\n    result[e*:esize] =SignedSat{esize}(res);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQINCB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQINCB--Signed-saturating-increment-scalar-by-multiple-of-8-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating increment scalar by multiple of 8-bit predicate constraint element count This instruction determines the number of active 8-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment the scalar destination. The result is saturated to the source general-purpose register's signed integer range. A 32-bit saturated result is then sign-extended to 64 bits. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQINCB<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) + (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) + (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "SQINCD (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQINCD--scalar---Signed-saturating-increment-scalar-by-multiple-of-64-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating increment scalar by multiple of 64-bit predicate constraint element count This instruction determines the number of active 64-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment the scalar destination. The result is saturated to the source general-purpose register's signed integer range. A 32-bit saturated result is then sign-extended to 64 bits. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQINCD<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) + (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) + (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "SQINCD (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQINCD--vector---Signed-saturating-increment-vector-by-multiple-of-64-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating increment vector by multiple of 64-bit predicate constraint element count This instruction determines the number of active 64-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment all destination vector elements. The results are saturated to the 64-bit signed integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception.",
    "syntax": [
      "SQINCD<Zdn>.D{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(op1elt) + (count * imm));else(result[e*:esize], -) =SignedSatQ{esize}(SInt(op1elt) + (count * imm));\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQINCH (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQINCH--scalar---Signed-saturating-increment-scalar-by-multiple-of-16-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating increment scalar by multiple of 16-bit predicate constraint element count This instruction determines the number of active 16-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment the scalar destination. The result is saturated to the source general-purpose register's signed integer range. A 32-bit saturated result is then sign-extended to 64 bits. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQINCH<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) + (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) + (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "SQINCH (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQINCH--vector---Signed-saturating-increment-vector-by-multiple-of-16-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating increment vector by multiple of 16-bit predicate constraint element count This instruction determines the number of active 16-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment all destination vector elements. The results are saturated to the 16-bit signed integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception.",
    "syntax": [
      "SQINCH<Zdn>.H{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(op1elt) + (count * imm));else(result[e*:esize], -) =SignedSatQ{esize}(SInt(op1elt) + (count * imm));\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQINCP (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQINCP--scalar---Signed-saturating-increment-scalar-by-count-of-true-predicate-elements-?lang=en",
    "description": "Signed saturating increment scalar by count of true predicate elements This instruction determines the number of true elements in the source predicate and then uses the result to increment the scalar destination. The result is saturated to the source general-purpose register's signed integer range. A 32-bit saturated result is then sign-extended to 64 bits. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQINCP<Xdn>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(ssize) = X{}(dn);letoperand2 : bits(PL) = P{}(m);varresult : bits(ssize);varcount : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(operand2, e, esize) then\n        count = count +1;\n    end;\nend;ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) + count);X{64}(dn) = ZeroExtend{64}(result);else(result, -) =SignedSatQ{ssize}(SInt(operand1) + count);X{64}(dn) = SignExtend{64}(result);\nend;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "SQINCP (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQINCP--vector---Signed-saturating-increment-vector-by-count-of-true-predicate-elements-?lang=en",
    "description": "Signed saturating increment vector by count of true predicate elements This instruction determines the number of true elements in the source predicate and then uses the result to increment all destination vector elements. The results are saturated to the element signed integer range. The predicate size specifier may be omitted in assembler source code, but this is deprecated and will be prohibited in a future release of the architecture.",
    "syntax": [
      "SQINCP<Zdn>.<T>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(PL) = P{}(m);varresult : bits(VL);varcount : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(operand2, e, esize) then\n        count = count +1;\n    end;\nend;fore =0to elements-1doletopelt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(opelt) + count);else(result[e*:esize], -) =SignedSatQ{esize}(SInt(opelt) + count);\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQINCW (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQINCW--scalar---Signed-saturating-increment-scalar-by-multiple-of-32-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating increment scalar by multiple of 32-bit predicate constraint element count This instruction determines the number of active 32-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment the scalar destination. The result is saturated to the source general-purpose register's signed integer range. A 32-bit saturated result is then sign-extended to 64 bits. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "SQINCW<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) + (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) + (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "SQINCW (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQINCW--vector---Signed-saturating-increment-vector-by-multiple-of-32-bit-predicate-constraint-element-count-?lang=en",
    "description": "Signed saturating increment vector by multiple of 32-bit predicate constraint element count This instruction determines the number of active 32-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment all destination vector elements. The results are saturated to the 32-bit signed integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception.",
    "syntax": [
      "SQINCW<Zdn>.S{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(op1elt) + (count * imm));else(result[e*:esize], -) =SignedSatQ{esize}(SInt(op1elt) + (count * imm));\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQNEG",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQNEG--Signed-saturating-negate-?lang=en",
    "description": "Signed saturating negate This instruction negates the signed integer value in each Active element of the source vector, and places the results in the corresponding elements of the destination vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "SQNEG<Zd>.<T>,<Pg>/Z,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenvarelement : integer = SInt(operand[e*:esize]);\n        element = -element;\n        result[e*:esize] =SignedSat{esize}(element);\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "SQRDCMLAH (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRDCMLAH--indexed---Saturating-rounding-doubling-complex-integer-multiply-add-high-by-indexed-element-?lang=en",
    "description": "Saturating rounding doubling complex integer multiply-add high by indexed element This instruction multiplies without saturation the duplicated real components for rotations 0 and 180, or imaginary components for rotations 90 and 270, of the integral numbers in each 128-bit segment of the first source vector by the specified complex number in the corresponding the second source vector segment rotated by 0, 90, 180 or 270 degrees in the direction from the positive real axis towards the positive imaginary axis, when considered in polar representation. This instruction then doubles and adds the products to the corresponding components of the complex numbers in the addend vector, and destructively places the most significant rounded half of the results in the corresponding elements of the addend vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated. These transformations permit the creation of a variety of multiply-add and multiply-subtract operations on complex numbers by combining two of these instructions with the same vector operands but with rotations that are 90 degrees apart. Each complex number is represented in a vector register as an even/odd pair of elements with the real part in the even-numbered element and the imaginary part in the odd-numbered element. It has encodings from 2 classes:16-bitand32-bit",
    "syntax": [
      "SQRDCMLAH<Zda>.S,<Zn>.S,<Zm>.S[<imm>],<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letpairs : integer =VLDIV(2* esize);letpairspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);varres_r, res_i : integer;forp =0to pairs-1doletsegmentbase : integer = p - (p MOD pairspersegment);lets : integer = segmentbase + index;letelt1_a : integer = SInt(operand1[(2* p + sel_a)*:esize]);letelt2_a : integer = SInt(operand2[(2* s + sel_a)*:esize]);letelt2_b : integer = SInt(operand2[(2* s + sel_b)*:esize]);letelt3_r : bits(esize) = operand3[(2* p +0)*:esize];letelt3_i : bits(esize) = operand3[(2* p +1)*:esize];letproduct_r : integer =  elt1_a * elt2_a;letproduct_i : integer =  elt1_a * elt2_b;ifsub_r then\n        res_r = (SInt(elt3_r) << esize) -2* product_r;elseres_r = (SInt(elt3_r) << esize) +2* product_r;\n    end;\n    res_r = (res_r + (1<< (esize-1))) >> esize;\n    result[(2* p +0)*:esize] =SignedSat{esize}(res_r);ifsub_i then\n        res_i = (SInt(elt3_i) << esize) -2* product_i;elseres_i = (SInt(elt3_i) << esize) +2* product_i;\n    end;\n    res_i = (res_i + (1<< (esize-1))) >> esize;\n    result[(2* p +1)*:esize] =SignedSat{esize}(res_i);\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQRDCMLAH (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRDCMLAH--vectors---Saturating-rounding-doubling-complex-integer-multiply-add-high-?lang=en",
    "description": "Saturating rounding doubling complex integer multiply-add high This instruction multiplies without saturation the duplicated real components for rotations 0 and 180, or imaginary components for rotations 90 and 270, of the integral numbers in the first source vector by the corresponding complex number in the second source vector rotated by 0, 90, 180 or 270 degrees in the direction from the positive real axis towards the positive imaginary axis, when considered in polar representation. This instruction then doubles and adds the products to the corresponding components of the complex numbers in the addend vector, and destructively places the most significant rounded half of the results in the corresponding elements of the addend vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated. These transformations permit the creation of a variety of multiply-add and multiply-subtract operations on complex numbers by combining two of these instructions with the same vector operands but with rotations that are 90 degrees apart. Each complex number is represented in a vector register as an even/odd pair of elements with the real part in the even-numbered element and the imaginary part in the odd-numbered element.",
    "syntax": [
      "SQRDCMLAH<Zda>.<T>,<Zn>.<T>,<Zm>.<T>,<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letpairs : integer =VLDIV(2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);varres_r, res_i : integer;forp =0to pairs-1doletelt1_a : integer = SInt(operand1[(2* p + sel_a)*:esize]);letelt2_a : integer = SInt(operand2[(2* p + sel_a)*:esize]);letelt2_b : integer = SInt(operand2[(2* p + sel_b)*:esize]);letelt3_r : bits(esize) = operand3[(2* p +0)*:esize];letelt3_i : bits(esize) = operand3[(2* p +1)*:esize];letproduct_r : integer =  elt1_a * elt2_a;letproduct_i : integer =  elt1_a * elt2_b;ifsub_r then\n        res_r = (SInt(elt3_r) << esize) -2* product_r;elseres_r = (SInt(elt3_r) << esize) +2* product_r;\n    end;\n    res_r = (res_r + (1<< (esize-1))) >> esize;\n    result[(2* p +0)*:esize] =SignedSat{esize}(res_r);ifsub_i then\n        res_i = (SInt(elt3_i) << esize) -2* product_i;elseres_i = (SInt(elt3_i) << esize) +2* product_i;\n    end;\n    res_i = (res_i + (1<< (esize-1))) >> esize;\n    result[(2* p +1)*:esize] =SignedSat{esize}(res_i);\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQRDMLAH (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRDMLAH--indexed---Signed-saturating-rounding-doubling-multiply-add-high-by-indexed-element-?lang=en",
    "description": "Signed saturating rounding doubling multiply-add high by indexed element This instruction multiplies all signed elements within each 128-bit segment of the first source vector by the specified signed element of the corresponding second source vector segment, and then doubles the result and destructively adds the rounded high half of each result to the corresponding elements of the addend and destination vector. Each destination element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 3 classes:16-bit,32-bitand64-bit",
    "syntax": [
      "SQRDMLAH<Zda>.D,<Zn>.D,<Zm>.D[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[s*:esize]);letelement3 : integer = SInt(operand3[e*:esize]);letres : integer = (element3 << esize) + (2* element1 * element2);\n    result[e*:esize] =SignedSat{esize}((res + (1<< (esize -1))) >> esize);\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQRDMLAH (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRDMLAH--vectors---Signed-saturating-rounding-doubling-multiply-add-high--unpredicated--?lang=en",
    "description": "Signed saturating rounding doubling multiply-add high (unpredicated) This instruction multiplies the signed elements of the first source vector by the corresponding elements of the second source vector, and then doubles the result and destructively adds the rounded high half of each result to the corresponding elements of the addend and destination vector. Each destination element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQRDMLAH<Zda>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);letelement3 : integer = SInt(operand3[e*:esize]);letres : integer = (element3 << esize) + (2* element1 * element2);\n    result[e*:esize] =SignedSat{esize}((res + (1<< (esize -1))) >> esize);\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQRDMLSH (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRDMLSH--indexed---Signed-saturating-rounding-doubling-multiply-subtract-high-by-indexed-element-?lang=en",
    "description": "Signed saturating rounding doubling multiply-subtract high by indexed element This instruction multiplies all signed elements within each 128-bit segment of the first source vector by the specified signed element of the corresponding second source vector segment, and then doubles the result and destructively subtracts the rounded high half of each result to the corresponding elements of the addend and destination vector. Each destination element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 3 classes:16-bit,32-bitand64-bit",
    "syntax": [
      "SQRDMLSH<Zda>.D,<Zn>.D,<Zm>.D[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[s*:esize]);letelement3 : integer = SInt(operand3[e*:esize]);letres : integer = (element3 << esize) - (2* element1 * element2);\n    result[e*:esize] =SignedSat{esize}((res + (1<< (esize -1))) >> esize);\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQRDMLSH (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRDMLSH--vectors---Signed-saturating-rounding-doubling-multiply-subtract-high--unpredicated--?lang=en",
    "description": "Signed saturating rounding doubling multiply-subtract high (unpredicated) This instruction multiplies the signed elements of the first source vector by the corresponding elements of the second source vector, and then doubles the result and destructively subtracts the rounded high half of each result from the corresponding elements of the addend and destination vector. Each destination element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQRDMLSH<Zda>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);letelement3 : integer = SInt(operand3[e*:esize]);letres : integer = (element3 << esize) - (2* element1 * element2);\n    result[e*:esize] =SignedSat{esize}((res + (1<< (esize -1))) >> esize);\n\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQRDMULH (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRDMULH--indexed---Signed-saturating-rounding-doubling-multiply-high-by-indexed-element-?lang=en",
    "description": "Signed saturating rounding doubling multiply high by indexed element This instruction multiplies all signed elements within each 128-bit segment of the first source vector by the specified signed element in the corresponding second source vector segment, then doubles and places the most significant rounded half of the result in the corresponding elements of the destination vector register. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 3 classes:16-bit,32-bitand64-bit",
    "syntax": [
      "SQRDMULH<Zd>.D,<Zn>.D,<Zm>.D[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;letelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[s*:esize]);letres : integer =2* element1 * element2;\n    result[e*:esize] =SignedSat{esize}((res + (1<< (esize -1))) >> esize);\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SQRDMULH (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRDMULH--vectors---Signed-saturating-rounding-doubling-multiply-high--unpredicated--?lang=en",
    "description": "Signed saturating rounding doubling multiply high (unpredicated) This instruction multiplies the signed elements of the first source vector by the corresponding elements of the second source vector, and then doubles the result and places the most significant rounded half of the result in the corresponding elements of the destination vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQRDMULH<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);letres : integer =2* element1 * element2;\n    result[e*:esize] =SignedSat{esize}((res + (1<< (esize -1))) >> esize);\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SQRSHL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRSHL--Signed-saturating-rounding-shift-left--predicated--?lang=en",
    "description": "Signed saturating rounding shift left (predicated) This instruction shifts active signed elements of the first source vector by corresponding elements of the second source vector, and destructively places the rounded results in the corresponding elements of the first source vector. A positive shift amount performs a left shift, otherwise a right shift by the negated shift amount is performed. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SQRSHL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = SInt(operand1[e*:esize]);varshift : integer =ShiftSat(SInt(operand2[e*:esize]), esize);varres : integer;ifshift >=0then\n            res = element << shift;elseshift = -shift;\n            res = (element + (1<< (shift -1))) >> shift;\n        end;\n        result[e*:esize] =SignedSat{esize}(res);elseresult[e*:esize] = operand1[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQRSHLR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRSHLR--Signed-saturating-rounding-shift-left-reversed--predicated--?lang=en",
    "description": "Signed saturating rounding shift left reversed (predicated) This instruction shifts active signed elements of the second source vector by corresponding elements of the first source vector and destructively places the rounded results in the corresponding elements of the first source vector. A positive shift amount performs a left shift, otherwise a right shift by the negated shift amount is performed. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SQRSHLR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letoperand2 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = SInt(operand1[e*:esize]);varshift : integer =ShiftSat(SInt(operand2[e*:esize]), esize);varres : integer;ifshift >=0then\n            res = element << shift;elseshift = -shift;\n            res = (element + (1<< (shift -1))) >> shift;\n        end;\n        result[e*:esize] =SignedSat{esize}(res);elseresult[e*:esize] = operand2[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQRSHRN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRSHRN--Signed-saturating-rounding-shift-right-narrow-by-immediate-to-interleaved-integer-?lang=en",
    "description": "Signed saturating rounding shift right narrow by immediate to interleaved integer This instruction shifts right by an immediate value the signed integer value in each element of the two source vectors, and places the two-way interleaved rounded results in the half-width destination elements. Each result element is saturated to the half-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per destination element. This instruction is unpredicated. It has encodings from 2 classes:8-bitand16-bit",
    "syntax": [
      "SQRSHRN<Zd>.H, {<Zn1>.S-<Zn2>.S }, #<const>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);varresult : bits(VL);fore =0to elements-1dofori =0to1doletoperand : bits(VL) = Z{}(n+i);letelement : bits(2* esize) = operand[e*:(2* esize)];letres : integer = (SInt(element) + (1<< (shift-1))) >> shift;\n        result[(2*e + i)*:esize] =SignedSat{esize}(res);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQRSHRNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRSHRNB--Signed-saturating-rounding-shift-right-narrow-by-immediate--bottom--?lang=en",
    "description": "Signed saturating rounding shift right narrow by immediate (bottom) This instruction shifts each signed integer value in the source vector elements right by an immediate value, and places the rounded results in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero. Each result element is saturated to the half-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SQRSHRNB<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = (SInt(element) + (1<< (shift-1))) >> shift;\n    result[(2*e +0)*:esize] =SignedSat{esize}(res);\n    result[(2*e +1)*:esize] = Zeros{esize};\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQRSHRNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRSHRNT--Signed-saturating-rounding-shift-right-narrow-by-immediate--top--?lang=en",
    "description": "Signed saturating rounding shift right narrow by immediate (top) This instruction shifts each signed integer value in the source vector elements right by an immediate value, and places the rounded results in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged. Each result element is saturated to the half-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SQRSHRNT<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = (SInt(element) + (1<< (shift-1))) >> shift;\n    result[(2*e +1)*:esize] =SignedSat{esize}(res);\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQRSHRUN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRSHRUN--Signed-saturating-rounding-shift-right-unsigned-narrow-by-immediate-and-interleave-?lang=en",
    "description": "Signed saturating rounding shift right unsigned narrow by immediate and interleave This instruction shifts right by an immediate value the signed integer value in each element of the two source vectors, and places the two-way interleaved rounded results in the half-width destination elements. Each result element is saturated to the half-width N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per destination element. This instruction is unpredicated. It has encodings from 2 classes:8-bitand16-bit",
    "syntax": [
      "SQRSHRUN<Zd>.H, {<Zn1>.S-<Zn2>.S }, #<const>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);varresult : bits(VL);fore =0to elements-1dofori =0to1doletoperand : bits(VL) = Z{}(n+i);letelement : bits(2* esize) = operand[e*:(2* esize)];letres : integer = (SInt(element) + (1<< (shift-1))) >> shift;\n        result[(2*e + i)*:esize] =UnsignedSat{esize}(res);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQRSHRUNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRSHRUNB--Signed-saturating-rounding-shift-right-narrow-by-immediate-to-unsigned-integer--bottom--?lang=en",
    "description": "Signed saturating rounding shift right narrow by immediate to unsigned integer (bottom) This instruction shifts each signed integer value in the source vector elements right by an immediate value, and places the rounded results in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero. Each result element is saturated to the half-width N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SQRSHRUNB<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = (SInt(element) + (1<< (shift-1))) >> shift;\n    result[(2*e +0)*:esize] =UnsignedSat{esize}(res);\n    result[(2*e +1)*:esize] = Zeros{esize};\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQRSHRUNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQRSHRUNT--Signed-saturating-rounding-shift-right-narrow-by-immediate-to-unsigned-integer--top--?lang=en",
    "description": "Signed saturating rounding shift right narrow by immediate to unsigned integer (top) This instruction shifts each signed integer value in the source vector elements right by an immediate value, and places the rounded results in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged. Each result element is saturated to the half-width N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SQRSHRUNT<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = (SInt(element) + (1<< (shift-1))) >> shift;\n    result[(2*e +1)*:esize] =UnsignedSat{esize}(res);\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQSHL (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSHL--immediate---Signed-saturating-shift-left-by-immediate-?lang=en",
    "description": "Signed saturating shift left by immediate This instruction shifts left by immediate each active signed element of the source vector, and destructively places the results in the corresponding elements of the source vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The immediate shift amount is an unsigned value in the range 0 to number of bits per element minus 1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SQSHL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);letmask : bits(PL) = P{}(g);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = element1 << shift;\n        result[e*:esize] =SignedSat{esize}(res);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQSHL (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSHL--vectors---Signed-saturating-shift-left--predicated--?lang=en",
    "description": "Signed saturating shift left (predicated) This instruction shifts active signed elements of the first source vector by corresponding elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. A positive shift amount performs a left shift, otherwise a right shift by the negated shift amount is performed. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SQSHL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = SInt(operand1[e*:esize]);varshift : integer =ShiftSat(SInt(operand2[e*:esize]), esize);varres : integer;ifshift >=0then\n            res = element << shift;elseshift = -shift;\n            res = element >> shift;\n        end;\n        result[e*:esize] =SignedSat{esize}(res);elseresult[e*:esize] = operand1[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQSHLR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSHLR--Signed-saturating-shift-left-reversed--predicated--?lang=en",
    "description": "Signed saturating shift left reversed (predicated) This instruction shifts active signed elements of the second source vector by the corresponding elements of the first source vector, and destructively places the results in the corresponding elements of the first source vector. A positive shift amount performs a left shift, otherwise a right shift by the negated shift amount is performed. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SQSHLR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letoperand2 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = SInt(operand1[e*:esize]);varshift : integer =ShiftSat(SInt(operand2[e*:esize]), esize);varres : integer;ifshift >=0then\n            res = element << shift;elseshift = -shift;\n            res = element >> shift;\n        end;\n        result[e*:esize] =SignedSat{esize}(res);elseresult[e*:esize] = operand2[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQSHLU",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSHLU--Signed-saturating-shift-left-unsigned-by-immediate-?lang=en",
    "description": "Signed saturating shift left unsigned by immediate This instruction shifts left by immediate each active signed element of the source vector, and destructively places the results in the corresponding elements of the source vector. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 0 to number of bits per element minus 1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SQSHLU<Zdn>.<T>,<Pg>/M,<Zdn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);letmask : bits(PL) = P{}(g);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = element1 << shift;\n        result[e*:esize] =UnsignedSat{esize}(res);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQSHRN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSHRN--Signed-saturating-shift-right-narrow-by-immediate-to-interleaved-integer-?lang=en",
    "description": "Signed saturating shift right narrow by immediate to interleaved integer This instruction shifts right by an immediate value the signed integer value in each element of the two source vectors, and places the two-way interleaved truncated results in the half-width destination elements. Each result element is saturated to the half-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per destination element. This instruction is unpredicated.",
    "syntax": [
      "SQSHRN<Zd>.<T>, {<Zn1>.<Tb>-<Zn2>.<Tb>}, #<const>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);varresult : bits(VL);fore =0to elements-1dofori =0to1doletoperand : bits(VL) = Z{}(n+i);letelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = SInt(element) >> shift;\n        result[(2*e + i)*:esize] =SignedSat{esize}(res);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQSHRNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSHRNB--Signed-saturating-shift-right-narrow-by-immediate--bottom--?lang=en",
    "description": "Signed saturating shift right narrow by immediate (bottom) This instruction shifts each signed integer value in the source vector elements right by an immediate value, and places the truncated results in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero. Each result element is saturated to the half-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SQSHRNB<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = SInt(element) >> shift;\n    result[(2*e +0)*:esize] =SignedSat{esize}(res);\n    result[(2*e +1)*:esize] = Zeros{esize};\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQSHRNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSHRNT--Signed-saturating-shift-right-narrow-by-immediate--top--?lang=en",
    "description": "Signed saturating shift right narrow by immediate (top) This instruction shifts each signed integer value in the source vector elements right by an immediate value, and places the truncated results in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged. Each result element is saturated to the half-width N-bit element's signed integer range -2(N-1)to (2(N-1))-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SQSHRNT<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = SInt(element) >> shift;\n    result[(2*e +1)*:esize] =SignedSat{esize}(res);\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQSHRUN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSHRUN--Signed-saturating-shift-right-narrow-by-immediate-to-interleaved-unsigned-integer-?lang=en",
    "description": "Signed saturating shift right narrow by immediate to interleaved unsigned integer This instruction shifts right by an immediate value the signed integer value in each element of the two source vectors, and places the two-way interleaved truncated results in the half-width destination elements. Each result element is saturated to the half-width N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per destination element. This instruction is unpredicated.",
    "syntax": [
      "SQSHRUN<Zd>.<T>, {<Zn1>.<Tb>-<Zn2>.<Tb>}, #<const>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);varresult : bits(VL);fore =0to elements-1dofori =0to1doletoperand : bits(VL) = Z{}(n+i);letelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = SInt(element) >> shift;\n        result[(2*e + i)*:esize] =UnsignedSat{esize}(res);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQSHRUNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSHRUNB--Signed-saturating-shift-right-narrow-by-immediate-to-unsigned-integer--bottom--?lang=en",
    "description": "Signed saturating shift right narrow by immediate to unsigned integer (bottom) This instruction shifts each signed integer value in the source vector elements right by an immediate value, and places the truncated results in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero. Each result element is saturated to the half-width N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SQSHRUNB<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = SInt(element) >> shift;\n    result[(2*e +0)*:esize] =UnsignedSat{esize}(res);\n    result[(2*e +1)*:esize] = Zeros{esize};\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQSHRUNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSHRUNT--Signed-saturating-shift-right-narrow-by-immediate-to-unsigned-integer--top--?lang=en",
    "description": "Signed saturating shift right narrow by immediate to unsigned integer (top) This instruction shifts each signed integer value in the source vector elements right by an immediate value, and places the truncated results in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged. Each result element is saturated to the half-width N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SQSHRUNT<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = SInt(element) >> shift;\n    result[(2*e +1)*:esize] =UnsignedSat{esize}(res);\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQSUB (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSUB--immediate---Signed-saturating-subtract-immediate--unpredicated--?lang=en",
    "description": "Signed saturating subtract immediate (unpredicated) This instruction performs a signed saturating subtraction of an unsigned immediate from each element of the source vector, and destructively places the results in the corresponding elements of the source vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated. The immediate is an unsigned value in the range 0 to 255, and for element widths of 16 bits or higher it may also be a positive multiple of 256 in the range 256 to 65280. The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\".",
    "syntax": [
      "SQSUB<Zdn>.<T>,<Zdn>.<T>, #<imm>{,<shift>}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);\n    (result[e*:esize], -) =SatQ{esize}(element1 - imm, unsigned);\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQSUB (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSUB--vectors--predicated---Signed-saturating-subtract--predicated--?lang=en",
    "description": "Signed saturating subtract (predicated) This instruction subtracts active signed elements of the second source vector from corresponding signed elements of the first source vector and destructively places the results in the corresponding elements of the first source vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SQSUB<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =SignedSat{esize}(element1 - element2);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQSUB (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSUB--vectors--unpredicated---Signed-saturating-subtract--unpredicated--?lang=en",
    "description": "Signed saturating subtract (unpredicated) This instruction performs a signed saturating subtraction of all elements of the second source vector from the corresponding elements of the first source vector, and places the results in the corresponding elements of the destination vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. This instruction is unpredicated.",
    "syntax": [
      "SQSUB<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letop2elt : bits(esize) = operand2[e*:esize];ifunsigned thenletelement1 : integer = UInt(op1elt);letelement2 : integer = UInt(op2elt);\n        (result[e*:esize], -) =UnsignedSatQ{esize}(element1 - element2);elseletelement1 : integer = SInt(op1elt);letelement2 : integer = SInt(op2elt);\n        (result[e*:esize], -) =SignedSatQ{esize}(element1 - element2);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQSUBR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQSUBR--Signed-saturating-subtract-reversed--predicated--?lang=en",
    "description": "Signed saturating subtract reversed (predicated) This instruction subtracts active signed elements of the first source vector from corresponding signed elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SQSUBR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =SignedSat{esize}(element2 - element1);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SQXTNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQXTNB--Signed-saturating-extract-narrow--bottom--?lang=en",
    "description": "Signed saturating extract narrow (bottom) This instruction saturates the signed integer value in each source element to half the original source element width, and places the results in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero.",
    "syntax": [
      "SQXTNB<Zd>.<T>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);varresult : bits(VL);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letres : bits(halfesize) =SignedSat{halfesize}(element1);\n    result[(2*e +0)*:halfesize] = res;\n    result[(2*e +1)*:halfesize] = Zeros{halfesize};\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQXTNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQXTNT--Signed-saturating-extract-narrow--top--?lang=en",
    "description": "Signed saturating extract narrow (top) This instruction saturates the signed integer value in each source element to half the original source element width, and places the results in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged.",
    "syntax": [
      "SQXTNT<Zd>.<T>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letres : bits(halfesize) =SignedSat{halfesize}(element1);\n    result[(2*e +1)*:halfesize] = res;\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQXTUNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQXTUNB--Signed-saturating-extract-narrow-to-unsigned-integer--bottom--?lang=en",
    "description": "Signed saturating extract narrow to unsigned integer (bottom) This instruction saturates the signed integer value in each source element to an unsigned integer value that is half the original source element width, and places the results in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero.",
    "syntax": [
      "SQXTUNB<Zd>.<T>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);varresult : bits(VL);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letres : bits(halfesize) =UnsignedSat{halfesize}(element1);\n    result[(2*e +0)*:halfesize] = res;\n    result[(2*e +1)*:halfesize] = Zeros{halfesize};\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SQXTUNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SQXTUNT--Signed-saturating-extract-narrow-to-unsigned-integer--top--?lang=en",
    "description": "Signed saturating extract narrow to unsigned integer (top) This instruction saturates the signed integer value in each source element to an unsigned integer value that is half the original source element width, and places the results in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged.",
    "syntax": [
      "SQXTUNT<Zd>.<T>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letres : bits(halfesize) =UnsignedSat{halfesize}(element1);\n    result[(2*e +1)*:halfesize] = res;\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SRHADD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SRHADD--Signed-rounding-halving-add-?lang=en",
    "description": "Signed rounding halving add This instruction adds active signed elements of the first source vector to corresponding signed elements of the second source vector, shifts right one bit, and destructively places the rounded results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SRHADD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = (element1 + element2 +1) >>1;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SRI",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SRI--Shift-right-and-insert--immediate--?lang=en",
    "description": "Shift right and insert (immediate) This instruction shifts each source vector element right by an immediate value, and inserts the result into the corresponding vector element in the destination vector register, merging the shifted bits from each source element with existing bits in each destination vector element. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SRI<Zd>.<T>,<Zn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement1 : bits(esize) = result[e*:esize];letelement2 : bits(esize) = operand[e*:esize];letmask : bits(esize) = LSR(Ones{esize}, shift);letshiftedval : bits(esize) = LSR(element2, shift);\n    result[e*:esize] = (element1AND(NOT mask)) OR shiftedval;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SRSHL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SRSHL--Signed-rounding-shift-left--predicated--?lang=en",
    "description": "Signed rounding shift left (predicated) This instruction shifts active signed elements of the first source vector by corresponding elements of the second source vector, and destructively places the rounded results in the corresponding elements of the first source vector. A positive shift amount performs a left shift, otherwise a right shift by the negated shift amount is performed. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SRSHL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = SInt(operand1[e*:esize]);varshift : integer =ShiftSat(SInt(operand2[e*:esize]), esize);varres : integer;ifshift >=0then\n            res = element << shift;elseshift = -shift;\n            res = (element + (1<< (shift -1))) >> shift;\n        end;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SRSHLR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SRSHLR--Signed-rounding-shift-left-reversed--predicated--?lang=en",
    "description": "Signed rounding shift left reversed (predicated) This instruction shifts active signed elements of the second source vector by corresponding elements of the first source vector and destructively places the rounded results in the corresponding elements of the first source vector. A positive shift amount performs a left shift, otherwise a right shift by the negated shift amount is performed. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SRSHLR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letoperand2 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = SInt(operand1[e*:esize]);varshift : integer =ShiftSat(SInt(operand2[e*:esize]), esize);varres : integer;ifshift >=0then\n            res = element << shift;elseshift = -shift;\n            res = (element + (1<< (shift -1))) >> shift;\n        end;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand2[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SRSHR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SRSHR--Signed-rounding-shift-right-by-immediate-?lang=en",
    "description": "Signed rounding shift right by immediate This instruction shifts right by immediate each active signed element of the source vector, and destructively places the rounded results in the corresponding elements of the source vector. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SRSHR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);letmask : bits(PL) = P{}(g);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = (element1 + (1<< (shift -1))) >> shift;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SRSRA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SRSRA--Signed-rounding-shift-right-and-accumulate--immediate--?lang=en",
    "description": "Signed rounding shift right and accumulate (immediate) This instruction shifts right by immediate each signed element of the source vector, preserving the sign bit, and destructively adds the rounded intermediate result to the corresponding elements of the addend vector. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SRSRA<Zda>.<T>,<Zn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement : integer = (SInt(operand1[e*:esize]) + (1<< (shift -1))) >> shift;\n    result[e*:esize] = operand2[e*:esize] + element[esize-1:0];\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SSHLLB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SSHLLB--Signed-shift-left-long-by-immediate--bottom--?lang=en",
    "description": "Signed shift left long by immediate (bottom) This instruction shifts left by immediate each even-numbered signed element of the source vector, and places the results in the overlapping double-width elements of the destination vector. The immediate shift amount is an unsigned value in the range 0 to number of bits per element minus 1. This instruction is unpredicated.",
    "syntax": [
      "SSHLLB<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize) = operand[(2*e +0)*:esize];letshifted_value : integer = SInt(element) << shift;\n    result[e*:(2*esize)] = shifted_value[2*esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SSHLLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SSHLLT--Signed-shift-left-long-by-immediate--top--?lang=en",
    "description": "Signed shift left long by immediate (top) This instruction shifts left by immediate each odd-numbered signed element of the source vector, and places the results in the overlapping double-width elements of the destination vector. The immediate shift amount is an unsigned value in the range 0 to number of bits per element minus 1. This instruction is unpredicated.",
    "syntax": [
      "SSHLLT<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize) = operand[(2*e +1)*:esize];letshifted_value : integer = SInt(element) << shift;\n    result[e*:(2*esize)] = shifted_value[2*esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SSRA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SSRA--Signed-shift-right-and-accumulate--immediate--?lang=en",
    "description": "Signed shift right and accumulate (immediate) This instruction shifts right by immediate each signed element of the source vector, preserving the sign bit, and destructively adds the truncated intermediate result to the corresponding elements of the addend vector. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "SSRA<Zda>.<T>,<Zn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement : integer = SInt(operand1[e*:esize]) >> shift;\n    result[e*:esize] = operand2[e*:esize] + element[esize-1:0];\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SSUBLB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SSUBLB--Signed-subtract-long--bottom--?lang=en",
    "description": "Signed subtract long (bottom) This instruction subtracts the even-numbered signed elements of the second source vector from the corresponding signed elements of the first source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SSUBLB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize DIV2) = operand1[(2*e + sel1)*:(esize DIV2)];letop2elt : bits(esize DIV2) = operand2[(2*e + sel2)*:(esize DIV2)];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letres : integer = element1 - element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SSUBLBT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SSUBLBT--Signed-subtract-long--bottom---top--?lang=en",
    "description": "Signed subtract long (bottom - top) This instruction subtracts the odd-numbered signed elements of the second source vector from the even-numbered signed elements of the first source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SSUBLBT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize DIV2) = operand1[(2*e + sel1)*:(esize DIV2)];letop2elt : bits(esize DIV2) = operand2[(2*e + sel2)*:(esize DIV2)];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letres : integer = element1 - element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SSUBLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SSUBLT--Signed-subtract-long--top--?lang=en",
    "description": "Signed subtract long (top) This instruction subtracts the odd-numbered signed elements of the second source vector from the corresponding signed elements of the first source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SSUBLT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize DIV2) = operand1[(2*e + sel1)*:(esize DIV2)];letop2elt : bits(esize DIV2) = operand2[(2*e + sel2)*:(esize DIV2)];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letres : integer = element1 - element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SSUBLTB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SSUBLTB--Signed-subtract-long--top---bottom--?lang=en",
    "description": "Signed subtract long (top - bottom) This instruction subtracts the even-numbered signed elements of the second source vector from the odd-numbered signed elements of the first source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SSUBLTB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize DIV2) = operand1[(2*e + sel1)*:(esize DIV2)];letop2elt : bits(esize DIV2) = operand2[(2*e + sel2)*:(esize DIV2)];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letres : integer = element1 - element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SSUBWB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SSUBWB--Signed-subtract-wide--bottom--?lang=en",
    "description": "Signed subtract wide (bottom) This instruction subtracts the even-numbered signed elements of the second source vector from the overlapping double-width elements of the first source vector and places the results in the corresponding double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SSUBWB<Zd>.<T>,<Zn>.<T>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[(2*e +0)*:(esize DIV2)]);\n    result[e*:esize] = (element1 - element2)[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SSUBWT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SSUBWT--Signed-subtract-wide--top--?lang=en",
    "description": "Signed subtract wide (top) This instruction subtracts the even-numbered signed elements of the second source vector from the overlapping double-width elements of the first source vector and places the results in the corresponding double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SSUBWT<Zd>.<T>,<Zn>.<T>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = SInt(operand1[e*:esize]);letelement2 : integer = SInt(operand2[(2*e +1)*:(esize DIV2)]);\n    result[e*:esize] = (element1 - element2)[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1B (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1B--scalar-plus-immediate--consecutive-registers---Contiguous-store-of-bytes-from-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous store of bytes from multiple consecutive vectors (immediate index) This instruction performs a contiguous store of bytes from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "ST1B {<Zt1>.B-<Zt4>.B },<PNg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1B (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1B--scalar-plus-immediate--single-register---Contiguous-store-bytes-from-vector--immediate-index--?lang=en",
    "description": "Contiguous store bytes from vector (immediate index) This instruction performs a contiguous store of bytes from elements of a vector register to the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory.",
    "syntax": [
      "ST1B {<Zt>.<T>},<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1B (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1B--scalar-plus-scalar--consecutive-registers---Contiguous-store-of-bytes-from-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous store of bytes from multiple consecutive vectors (scalar index) This instruction performs a contiguous store of bytes from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "ST1B {<Zt1>.B-<Zt4>.B },<PNg>, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1B (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1B--scalar-plus-scalar--single-register---Contiguous-store-bytes-from-vector--scalar-index--?lang=en",
    "description": "Contiguous store bytes from vector (scalar index) This instruction performs a contiguous store of bytes from elements of a vector register to the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory.",
    "syntax": [
      "ST1B {<Zt>.<T>},<Pg>, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1B (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1B--scalar-plus-vector---Scatter-store-bytes-from-a-vector--vector-index--?lang=en",
    "description": "Scatter store bytes from a vector (vector index) This instruction performs a scatter store of bytes from the Active elements of a vector register to the memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally sign-extended or zero-extended from 32 to 64 bits. Inactive elements are not written to memory. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 3 classes:32-bit unpacked unscaled offset,32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "ST1B {<Zt>.D },<Pg>, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(VL);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\n    src =Z{VL}(t);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);Mem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1B (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1B--vector-plus-immediate---Scatter-store-bytes-from-a-vector--immediate-index--?lang=en",
    "description": "Scatter store bytes from a vector (immediate index) This instruction performs a scatter store of bytes from the Active elements of a vector register to the memory addresses generated by a vector base plus immediate index. The index is in the range 0 to 31. Inactive elements are not written to memory. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "ST1B {<Zt>.D },<Pg>, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\n    src =Z{VL}(t);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset * mbytes, accdesc);Mem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1D (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1D--scalar-plus-immediate--consecutive-registers---Contiguous-store-of-doublewords-from-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous store of doublewords from multiple consecutive vectors (immediate index) This instruction performs a contiguous store of doublewords from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "ST1D {<Zt1>.D-<Zt4>.D },<PNg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1D (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1D--scalar-plus-immediate--single-register---Contiguous-store-doublewords-from-vector--immediate-index--?lang=en",
    "description": "Contiguous store doublewords from vector (immediate index) This instruction performs a contiguous store of doublewords from elements of a vector register to the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. The 128-bit element variant is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:64-bit elementand128-bit element",
    "syntax": [
      "ST1D {<Zt>.Q },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifesize <128thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1D (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1D--scalar-plus-scalar--consecutive-registers---Contiguous-store-of-doublewords-from-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous store of doublewords from multiple consecutive vectors (scalar index) This instruction performs a contiguous store of doublewords from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "ST1D {<Zt1>.D-<Zt4>.D },<PNg>, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1D (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1D--scalar-plus-scalar--single-register---Contiguous-store-doublewords-from-vector--scalar-index--?lang=en",
    "description": "Contiguous store doublewords from vector (scalar index) This instruction performs a contiguous store of doublewords from elements of a vector register to the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 8 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. The 128-bit element variant is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:64-bit elementand128-bit element",
    "syntax": [
      "ST1D {<Zt>.Q },<Pg>, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifesize <128thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1D (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1D--scalar-plus-vector---Scatter-store-doublewords-from-a-vector--vector-index--?lang=en",
    "description": "Scatter store doublewords from a vector (vector index) This instruction performs a scatter store of doublewords from the Active elements of a vector register to the memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 8. Inactive elements are not written to memory. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 4 classes:32-bit unpacked scaled offset,32-bit unpacked unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "ST1D {<Zt>.D },<Pg>, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(VL);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\n    src =Z{VL}(t);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);Mem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1D (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1D--vector-plus-immediate---Scatter-store-doublewords-from-a-vector--immediate-index--?lang=en",
    "description": "Scatter store doublewords from a vector (immediate index) This instruction performs a scatter store of doublewords from the Active elements of a vector register to the memory addresses generated by a vector base plus immediate index. The index is a multiple of 8 in the range 0 to 248. Inactive elements are not written to memory. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "ST1D {<Zt>.D },<Pg>, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\n    src =Z{VL}(t);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset * mbytes, accdesc);Mem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1H (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1H--scalar-plus-immediate--consecutive-registers---Contiguous-store-of-halfwords-from-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous store of halfwords from multiple consecutive vectors (immediate index) This instruction performs a contiguous store of halfwords from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "ST1H {<Zt1>.H-<Zt4>.H },<PNg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1H (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1H--scalar-plus-immediate--single-register---Contiguous-store-halfwords-from-vector--immediate-index--?lang=en",
    "description": "Contiguous store halfwords from vector (immediate index) This instruction performs a contiguous store of halfwords from elements of a vector register to the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory.",
    "syntax": [
      "ST1H {<Zt>.<T>},<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1H (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1H--scalar-plus-scalar--consecutive-registers---Contiguous-store-of-halfwords-from-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous store of halfwords from multiple consecutive vectors (scalar index) This instruction performs a contiguous store of halfwords from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "ST1H {<Zt1>.H-<Zt4>.H },<PNg>, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1H (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1H--scalar-plus-scalar--single-register---Contiguous-store-halfwords-from-vector--scalar-index--?lang=en",
    "description": "Contiguous store halfwords from vector (scalar index) This instruction performs a contiguous store of halfwords from elements of a vector register to the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 2 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory.",
    "syntax": [
      "ST1H {<Zt>.<T>},<Pg>, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1H (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1H--scalar-plus-vector---Scatter-store-halfwords-from-a-vector--vector-index--?lang=en",
    "description": "Scatter store halfwords from a vector (vector index) This instruction performs a scatter store of halfwords from the Active elements of a vector register to the memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 2. Inactive elements are not written to memory. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 6 classes:32-bit scaled offset,32-bit unpacked scaled offset,32-bit unpacked unscaled offset,32-bit unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "ST1H {<Zt>.D },<Pg>, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(VL);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\n    src =Z{VL}(t);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);Mem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1H (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1H--vector-plus-immediate---Scatter-store-halfwords-from-a-vector--immediate-index--?lang=en",
    "description": "Scatter store halfwords from a vector (immediate index) This instruction performs a scatter store of halfwords from the Active elements of a vector register to the memory addresses generated by a vector base plus immediate index. The index is a multiple of 2 in the range 0 to 62. Inactive elements are not written to memory. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "ST1H {<Zt>.D },<Pg>, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\n    src =Z{VL}(t);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset * mbytes, accdesc);Mem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1Q",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1Q--Scatter-store-quadwords-?lang=en",
    "description": "Scatter store quadwords This instruction performs a scatter store of quadwords from the Active elements of a vector register to the memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements are not written to memory. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "ST1Q {<Zt>.Q },<Pg>, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;CheckNonStreamingSVEEnabled();letelements : integer = VL DIV128;letmask :bits(PL)= P{}(g);varbase:bits(VL);varoffset :bits(64);varsrc :bits(VL);letcontiguous :boolean= FALSE;letnontemporal :boolean= FALSE;letpredicated :boolean= TRUE;lettagchecked :boolean= TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);ifAnyActiveElement{PL}(mask,128) thenbase=Z{VL}(n);offset =X{64}(m);src =Z{VL}(t);end;fore =0to elements-1doifActivePredicateElement{PL}(mask, e,128) then\n        letbaddr:bits(64)=base[(2*e)*:64];letaddr:bits(64)=AddressAdd(baddr,offset, accdesc);Mem{128}(addr,accdesc) = src[e*:128];end;end;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1W (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1W--scalar-plus-immediate--consecutive-registers---Contiguous-store-of-words-from-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous store of words from multiple consecutive vectors (immediate index) This instruction performs a contiguous store of words from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "ST1W {<Zt1>.S-<Zt4>.S },<PNg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1W (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1W--scalar-plus-immediate--single-register---Contiguous-store-words-from-vector--immediate-index--?lang=en",
    "description": "Contiguous store words from vector (immediate index) This instruction performs a contiguous store of words from elements of a vector register to the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. The 128-bit element variant is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit and 64-bit elementsand128-bit element",
    "syntax": [
      "ST1W {<Zt>.Q },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifesize <128thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1W (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1W--scalar-plus-scalar--consecutive-registers---Contiguous-store-of-words-from-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous store of words from multiple consecutive vectors (scalar index) This instruction performs a contiguous store of words from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "ST1W {<Zt1>.S-<Zt4>.S },<PNg>, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1W (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1W--scalar-plus-scalar--single-register---Contiguous-store-words-from-vector--scalar-index--?lang=en",
    "description": "Contiguous store words from vector (scalar index) This instruction performs a contiguous store of words from elements of a vector register to the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 4 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. The 128-bit element variant is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit and 64-bit elementsand128-bit element",
    "syntax": [
      "ST1W {<Zt>.Q },<Pg>, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifesize <128thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1W (scalar plus vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1W--scalar-plus-vector---Scatter-store-words-from-a-vector--vector-index--?lang=en",
    "description": "Scatter store words from a vector (vector index) This instruction performs a scatter store of words from the Active elements of a vector register to the memory addresses generated by a 64-bit scalar base plus vector index. The index values are optionally first sign-extended or zero-extended from 32 to 64 bits and then optionally multiplied by 4. Inactive elements are not written to memory. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 6 classes:32-bit scaled offset,32-bit unpacked scaled offset,32-bit unpacked unscaled offset,32-bit unscaled offset,64-bit scaled offsetand64-bit unscaled offset",
    "syntax": [
      "ST1W {<Zt>.D },<Pg>, [<Xn|SP>,<Zm>.D]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(VL);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;base=ifn ==31thenSP{64}()elseX{64}(n);\n    offset =Z{VL}(m);\n    src =Z{VL}(t);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletoffselt : bits(offs_size) = offset[e*:esize][offs_size-1:0];letoff : integer =ifoffs_unsigned thenUInt(offselt) elseSInt(offselt);letaddr : bits(64) =AddressAdd(base, off << scale, accdesc);Mem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST1W (vector plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST1W--vector-plus-immediate---Scatter-store-words-from-a-vector--immediate-index--?lang=en",
    "description": "Scatter store words from a vector (immediate index) This instruction performs a scatter store of words from the Active elements of a vector register to the memory addresses generated by a vector base plus immediate index. The index is a multiple of 4 in the range 0 to 124. Inactive elements are not written to memory. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit elementand64-bit element",
    "syntax": [
      "ST1W {<Zt>.D },<Pg>, [<Zn>.D{, #<imm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\n    src =Z{VL}(t);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset * mbytes, accdesc);Mem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST2B (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST2B--scalar-plus-immediate---Contiguous-store-two-byte-structures-from-two-vectors--immediate-index--?lang=en",
    "description": "Contiguous store two-byte structures from two vectors (immediate index) This instruction performs a contiguous store of two-byte structures, each from the same element number in two vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 2 in the range -16 to 14 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive bytes in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST2B {<Zt1>.B,<Zt2>.B },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST2B (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST2B--scalar-plus-scalar---Contiguous-store-two-byte-structures-from-two-vectors--scalar-index--?lang=en",
    "description": "Contiguous store two-byte structures from two vectors (scalar index) This instruction performs a contiguous store of two-byte structures, each from the same element number in two vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register and added to the base address. After each structure access the index value is incremented by two. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive bytes in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST2B {<Zt1>.B,<Zt2>.B },<Pg>, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST2D (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST2D--scalar-plus-immediate---Contiguous-store-two-doubleword-structures-from-two-vectors--immediate-index--?lang=en",
    "description": "Contiguous store two-doubleword structures from two vectors (immediate index) This instruction performs a contiguous store of two-doubleword structures, each from the same element number in two vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 2 in the range -16 to 14 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive doublewords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST2D {<Zt1>.D,<Zt2>.D },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST2D (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST2D--scalar-plus-scalar---Contiguous-store-two-doubleword-structures-from-two-vectors--scalar-index--?lang=en",
    "description": "Contiguous store two-doubleword structures from two vectors (scalar index) This instruction performs a contiguous store of two-doubleword structures, each from the same element number in two vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by two. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive doublewords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST2D {<Zt1>.D,<Zt2>.D },<Pg>, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST2H (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST2H--scalar-plus-immediate---Contiguous-store-two-halfword-structures-from-two-vectors--immediate-index--?lang=en",
    "description": "Contiguous store two-halfword structures from two vectors (immediate index) This instruction performs a contiguous store of two-halfword structures, each from the same element number in two vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 2 in the range -16 to 14 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive halfwords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST2H {<Zt1>.H,<Zt2>.H },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST2H (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST2H--scalar-plus-scalar---Contiguous-store-two-halfword-structures-from-two-vectors--scalar-index--?lang=en",
    "description": "Contiguous store two-halfword structures from two vectors (scalar index) This instruction performs a contiguous store of two-halfword structures, each from the same element number in two vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by two. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive halfwords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST2H {<Zt1>.H,<Zt2>.H },<Pg>, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST2Q (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST2Q--scalar-plus-immediate---Contiguous-store-two-quadword-structures-from-two-vectors--immediate-index--?lang=en",
    "description": "Contiguous store two-quadword structures from two vectors (immediate index) This instruction performs a contiguous store of two-quadword structures, each from the same element number in two vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 2 in the range -16 to 14 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive quadwords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST2Q {<Zt1>.Q,<Zt2>.Q },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST2Q (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST2Q--scalar-plus-scalar---Contiguous-store-two-quadword-structures-from-two-vectors--scalar-index--?lang=en",
    "description": "Contiguous store two-quadword structures from two vectors (scalar index) This instruction performs a contiguous store of two-quadword structures, each from the same element number in two vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by two. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive quadwords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST2Q {<Zt1>.Q,<Zt2>.Q },<Pg>, [<Xn|SP>,<Xm>, LSL #4]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST2W (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST2W--scalar-plus-immediate---Contiguous-store-two-word-structures-from-two-vectors--immediate-index--?lang=en",
    "description": "Contiguous store two-word structures from two vectors (immediate index) This instruction performs a contiguous store of two-word structures, each from the same element number in two vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 2 in the range -16 to 14 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive words in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST2W {<Zt1>.S,<Zt2>.S },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST2W (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST2W--scalar-plus-scalar---Contiguous-store-two-word-structures-from-two-vectors--scalar-index--?lang=en",
    "description": "Contiguous store two-word structures from two vectors (scalar index) This instruction performs a contiguous store of two-word structures, each from the same element number in two vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by two. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the two vector registers, or equivalently to the two consecutive words in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST2W {<Zt1>.S,<Zt2>.S },<Pg>, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[2]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST3B (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST3B--scalar-plus-immediate---Contiguous-store-three-byte-structures-from-three-vectors--immediate-index--?lang=en",
    "description": "Contiguous store three-byte structures from three vectors (immediate index) This instruction performs a contiguous store of three-byte structures, each from the same element number in three vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 3 in the range -24 to 21 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive bytes in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST3B {<Zt1>.B,<Zt2>.B,<Zt3>.B },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST3B (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST3B--scalar-plus-scalar---Contiguous-store-three-byte-structures-from-three-vectors--scalar-index--?lang=en",
    "description": "Contiguous store three-byte structures from three vectors (scalar index) This instruction performs a contiguous store of three-byte structures, each from the same element number in three vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register and added to the base address. After each structure access the index value is incremented by three. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive bytes in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST3B {<Zt1>.B,<Zt2>.B,<Zt3>.B },<Pg>, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST3D (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST3D--scalar-plus-scalar---Contiguous-store-three-doubleword-structures-from-three-vectors--scalar-index--?lang=en",
    "description": "Contiguous store three-doubleword structures from three vectors (scalar index) This instruction performs a contiguous store of three-doubleword structures, each from the same element number in three vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by three. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive doublewords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST3D {<Zt1>.D,<Zt2>.D,<Zt3>.D },<Pg>, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST3H (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST3H--scalar-plus-immediate---Contiguous-store-three-halfword-structures-from-three-vectors--immediate-index--?lang=en",
    "description": "Contiguous store three-halfword structures from three vectors (immediate index) This instruction performs a contiguous store of three-halfword structures, each from the same element number in three vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 3 in the range -24 to 21 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive halfwords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST3H {<Zt1>.H,<Zt2>.H,<Zt3>.H },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST3H (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST3H--scalar-plus-scalar---Contiguous-store-three-halfword-structures-from-three-vectors--scalar-index--?lang=en",
    "description": "Contiguous store three-halfword structures from three vectors (scalar index) This instruction performs a contiguous store of three-halfword structures, each from the same element number in three vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by three. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive halfwords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST3H {<Zt1>.H,<Zt2>.H,<Zt3>.H },<Pg>, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST3Q (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST3Q--scalar-plus-immediate---Contiguous-store-three-quadword-structures-from-three-vectors--immediate-index--?lang=en",
    "description": "Contiguous store three-quadword structures from three vectors (immediate index) This instruction performs a contiguous store of three-quadword structures, each from the same element number in three vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 3 in the range -24 to 21 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive quadwords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST3Q {<Zt1>.Q,<Zt2>.Q,<Zt3>.Q },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST3Q (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST3Q--scalar-plus-scalar---Contiguous-store-three-quadword-structures-from-three-vectors--scalar-index--?lang=en",
    "description": "Contiguous store three-quadword structures from three vectors (scalar index) This instruction performs a contiguous store of three-quadword structures, each from the same element number in three vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by three. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive quadwords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST3Q {<Zt1>.Q,<Zt2>.Q,<Zt3>.Q },<Pg>, [<Xn|SP>,<Xm>, LSL #4]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST3W (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST3W--scalar-plus-immediate---Contiguous-store-three-word-structures-from-three-vectors--immediate-index--?lang=en",
    "description": "Contiguous store three-word structures from three vectors (immediate index) This instruction performs a contiguous store of three-word structures, each from the same element number in three vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 3 in the range -24 to 21 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive words in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST3W {<Zt1>.S,<Zt2>.S,<Zt3>.S },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST3W (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST3W--scalar-plus-scalar---Contiguous-store-three-word-structures-from-three-vectors--scalar-index--?lang=en",
    "description": "Contiguous store three-word structures from three vectors (scalar index) This instruction performs a contiguous store of three-word structures, each from the same element number in three vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by three. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the three vector registers, or equivalently to the three consecutive words in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST3W {<Zt1>.S,<Zt2>.S,<Zt3>.S },<Pg>, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[3]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST4B (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST4B--scalar-plus-immediate---Contiguous-store-four-byte-structures-from-four-vectors--immediate-index--?lang=en",
    "description": "Contiguous store four-byte structures from four vectors (immediate index) This instruction performs a contiguous store of four-byte structures, each from the same element number in four vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 4 in the range -32 to 28 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive bytes in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST4B {<Zt1>.B,<Zt2>.B,<Zt3>.B,<Zt4>.B },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST4B (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST4B--scalar-plus-scalar---Contiguous-store-four-byte-structures-from-four-vectors--scalar-index--?lang=en",
    "description": "Contiguous store four-byte structures from four vectors (scalar index) This instruction performs a contiguous store of four-byte structures, each from the same element number in four vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register and added to the base address. After each structure access the index value is incremented by four. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive bytes in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST4B {<Zt1>.B,<Zt2>.B,<Zt3>.B,<Zt4>.B },<Pg>, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST4D (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST4D--scalar-plus-immediate---Contiguous-store-four-doubleword-structures-from-four-vectors--immediate-index--?lang=en",
    "description": "Contiguous store four-doubleword structures from four vectors (immediate index) This instruction performs a contiguous store of four-doubleword structures, each from the same element number in four vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 4 in the range -32 to 28 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive doublewords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST4D {<Zt1>.D,<Zt2>.D,<Zt3>.D,<Zt4>.D },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST4D (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST4D--scalar-plus-scalar---Contiguous-store-four-doubleword-structures-from-four-vectors--scalar-index--?lang=en",
    "description": "Contiguous store four-doubleword structures from four vectors (scalar index) This instruction performs a contiguous store of four-doubleword structures, each from the same element number in four vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by four. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive doublewords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST4D {<Zt1>.D,<Zt2>.D,<Zt3>.D,<Zt4>.D },<Pg>, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST4H (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST4H--scalar-plus-immediate---Contiguous-store-four-halfword-structures-from-four-vectors--immediate-index--?lang=en",
    "description": "Contiguous store four-halfword structures from four vectors (immediate index) This instruction performs a contiguous store of four-halfword structures, each from the same element number in four vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 4 in the range -32 to 28 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive halfwords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST4H {<Zt1>.H,<Zt2>.H,<Zt3>.H,<Zt4>.H },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST4H (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST4H--scalar-plus-scalar---Contiguous-store-four-halfword-structures-from-four-vectors--scalar-index--?lang=en",
    "description": "Contiguous store four-halfword structures from four vectors (scalar index) This instruction performs a contiguous store of four-halfword structures, each from the same element number in four vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by four. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive halfwords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST4H {<Zt1>.H,<Zt2>.H,<Zt3>.H,<Zt4>.H },<Pg>, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST4Q (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST4Q--scalar-plus-immediate---Contiguous-store-four-quadword-structures-from-four-vectors--immediate-index--?lang=en",
    "description": "Contiguous store four-quadword structures from four vectors (immediate index) This instruction performs a contiguous store of four-quadword structures, each from the same element number in four vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 4 in the range -32 to 28 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive quadwords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST4Q {<Zt1>.Q,<Zt2>.Q,<Zt3>.Q,<Zt4>.Q },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST4Q (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST4Q--scalar-plus-scalar---Contiguous-store-four-quadword-structures-from-four-vectors--scalar-index--?lang=en",
    "description": "Contiguous store four-quadword structures from four vectors (scalar index) This instruction performs a contiguous store of four-quadword structures, each from the same element number in four vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by four. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive quadwords in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST4Q {<Zt1>.Q,<Zt2>.Q,<Zt3>.Q,<Zt4>.Q },<Pg>, [<Xn|SP>,<Xm>, LSL #4]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST4W (scalar plus immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST4W--scalar-plus-immediate---Contiguous-store-four-word-structures-from-four-vectors--immediate-index--?lang=en",
    "description": "Contiguous store four-word structures from four vectors (immediate index) This instruction performs a contiguous store of four-word structures, each from the same element number in four vector registers to the memory address generated by a 64-bit scalar base and an immediate index that is a multiple of 4 in the range -32 to 28 that is multiplied by the vector's in-memory size, irrespective of predication. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive words in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST4W {<Zt1>.S,<Zt2>.S,<Zt3>.S,<Zt4>.S },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * nreg * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ST4W (scalar plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ST4W--scalar-plus-scalar---Contiguous-store-four-word-structures-from-four-vectors--scalar-index--?lang=en",
    "description": "Contiguous store four-word structures from four vectors (scalar index) This instruction performs a contiguous store of four-word structures, each from the same element number in four vector registers to the memory address generated by a 64-bit scalar base and a 64-bit scalar index register scaled by the element size (LSL option) and added to the base address. After each structure access the index value is incremented by four. The index register is not updated by the instruction. Each predicate element applies to the same element number in each of the four vector registers, or equivalently to the four consecutive words in memory that make up each structure. Inactive structures are not written to memory.",
    "syntax": [
      "ST4W {<Zt1>.S,<Zt2>.S,<Zt3>.S,<Zt4>.S },<Pg>, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);letmask : bits(PL) = P{}(g);varoffset : bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varvalues : array [[4]]ofbits(VL);letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dovalues[[r]] =Z{VL}((t+r) MOD32);\nend;fore =0to elements-1doforr =0to nreg-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = values[[r]][e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1B (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1B--scalar-plus-immediate--consecutive-registers---Contiguous-store-non-temporal-of-bytes-from-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous store non-temporal of bytes from multiple consecutive vectors (immediate index) This instruction performs a contiguous non-temporal store of bytes from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "STNT1B {<Zt1>.B-<Zt4>.B },<PNg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1B (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1B--scalar-plus-immediate--single-register---Contiguous-store-non-temporal-bytes-from-vector--immediate-index--?lang=en",
    "description": "Contiguous store non-temporal bytes from vector (immediate index) This instruction performs a contiguous non-temporal store of bytes from elements of a vector register to the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "STNT1B {<Zt>.B },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varsrc : bits(VL);letmask : bits(PL) = P{}(g);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1B (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1B--scalar-plus-scalar--consecutive-registers---Contiguous-store-non-temporal-of-bytes-from-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous store non-temporal of bytes from multiple consecutive vectors (scalar index) This instruction performs a contiguous non-temporal store of bytes from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "STNT1B {<Zt1>.B-<Zt4>.B },<PNg>, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1B (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1B--scalar-plus-scalar--single-register---Contiguous-store-non-temporal-bytes-from-vector--scalar-index--?lang=en",
    "description": "Contiguous store non-temporal bytes from vector (scalar index) This instruction performs a contiguous non-temporal store of bytes from elements of a vector register to the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "STNT1B {<Zt>.B },<Pg>, [<Xn|SP>,<Xm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);varoffset : bits(64);varaddr : bits(64);varsrc : bits(VL);letmask : bits(PL) = P{}(g);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated  : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1B (vector plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1B--vector-plus-scalar---Scatter-store-non-temporal-bytes-?lang=en",
    "description": "Scatter store non-temporal bytes This instruction performs a non-temporal scatter store of bytes from the active elements of a vector register to the memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "STNT1B {<Zt>.D },<Pg>, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varoffset : bits(64);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\n    offset =X{64}(m);\n    src =Z{VL}(t);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset, accdesc);Mem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1D (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1D--scalar-plus-immediate--consecutive-registers---Contiguous-store-non-temporal-of-doublewords-from-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous store non-temporal of doublewords from multiple consecutive vectors (immediate index) This instruction performs a contiguous non-temporal store of doublewords from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "STNT1D {<Zt1>.D-<Zt4>.D },<PNg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1D (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1D--scalar-plus-immediate--single-register---Contiguous-store-non-temporal-doublewords-from-vector--immediate-index--?lang=en",
    "description": "Contiguous store non-temporal doublewords from vector (immediate index) This instruction performs a contiguous non-temporal store of doublewords from elements of a vector register to the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "STNT1D {<Zt>.D },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varsrc : bits(VL);letmask : bits(PL) = P{}(g);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1D (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1D--scalar-plus-scalar--consecutive-registers---Contiguous-store-non-temporal-of-doublewords-from-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous store non-temporal of doublewords from multiple consecutive vectors (scalar index) This instruction performs a contiguous non-temporal store of doublewords from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "STNT1D {<Zt1>.D-<Zt4>.D },<PNg>, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1D (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1D--scalar-plus-scalar--single-register---Contiguous-store-non-temporal-doublewords-from-vector--scalar-index--?lang=en",
    "description": "Contiguous store non-temporal doublewords from vector (scalar index) This instruction performs a contiguous non-temporal store of doublewords from elements of a vector register to the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 8 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "STNT1D {<Zt>.D },<Pg>, [<Xn|SP>,<Xm>, LSL #3]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);varoffset : bits(64);varaddr : bits(64);varsrc : bits(VL);letmask : bits(PL) = P{}(g);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated  : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1D (vector plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1D--vector-plus-scalar---Scatter-store-non-temporal-doublewords-?lang=en",
    "description": "Scatter store non-temporal doublewords This instruction performs a non-temporal scatter store of doublewords from the active elements of a vector register to the memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "STNT1D {<Zt>.D },<Pg>, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varoffset : bits(64);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\n    offset =X{64}(m);\n    src =Z{VL}(t);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset, accdesc);Mem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1H (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1H--scalar-plus-immediate--consecutive-registers---Contiguous-store-non-temporal-of-halfwords-from-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous store non-temporal of halfwords from multiple consecutive vectors (immediate index) This instruction performs a contiguous non-temporal store of halfwords from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "STNT1H {<Zt1>.H-<Zt4>.H },<PNg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1H (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1H--scalar-plus-immediate--single-register---Contiguous-store-non-temporal-halfwords-from-vector--immediate-index--?lang=en",
    "description": "Contiguous store non-temporal halfwords from vector (immediate index) This instruction performs a contiguous non-temporal store of halfwords from elements of a vector register to the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "STNT1H {<Zt>.H },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varsrc : bits(VL);letmask : bits(PL) = P{}(g);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1H (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1H--scalar-plus-scalar--consecutive-registers---Contiguous-store-non-temporal-of-halfwords-from-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous store non-temporal of halfwords from multiple consecutive vectors (scalar index) This instruction performs a contiguous non-temporal store of halfwords from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "STNT1H {<Zt1>.H-<Zt4>.H },<PNg>, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1H (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1H--scalar-plus-scalar--single-register---Contiguous-store-non-temporal-halfwords-from-vector--scalar-index--?lang=en",
    "description": "Contiguous store non-temporal halfwords from vector (scalar index) This instruction performs a contiguous non-temporal store of halfwords from elements of a vector register to the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 2 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "STNT1H {<Zt>.H },<Pg>, [<Xn|SP>,<Xm>, LSL #1]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);varoffset : bits(64);varaddr : bits(64);varsrc : bits(VL);letmask : bits(PL) = P{}(g);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated  : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1H (vector plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1H--vector-plus-scalar---Scatter-store-non-temporal-halfwords-?lang=en",
    "description": "Scatter store non-temporal halfwords This instruction performs a non-temporal scatter store of halfwords from the active elements of a vector register to the memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "STNT1H {<Zt>.D },<Pg>, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varoffset : bits(64);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\n    offset =X{64}(m);\n    src =Z{VL}(t);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset, accdesc);Mem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1W (scalar plus immediate, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1W--scalar-plus-immediate--consecutive-registers---Contiguous-store-non-temporal-of-words-from-multiple-consecutive-vectors--immediate-index--?lang=en",
    "description": "Contiguous store non-temporal of words from multiple consecutive vectors (immediate index) This instruction performs a contiguous non-temporal store of words from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and immediate index that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "STNT1W {<Zt1>.S-<Zt4>.S },<PNg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * nreg * elements * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1W (scalar plus immediate, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1W--scalar-plus-immediate--single-register---Contiguous-store-non-temporal-words-from-vector--immediate-index--?lang=en",
    "description": "Contiguous store non-temporal words from vector (immediate index) This instruction performs a contiguous non-temporal store of words from elements of a vector register to the memory address generated by a 64-bit scalar base and immediate index in the range -8 to 7 that is multiplied by the vector's in-memory size, irrespective of predication, and added to the base address. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "STNT1W {<Zt>.S },<Pg>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);varaddr : bits(64);letmbytes : integer{} = esize DIV8;varsrc : bits(VL);letmask : bits(PL) = P{}(g);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\naddr =AddressAdd(base, offset * elements * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1W (scalar plus scalar, consecutive registers)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1W--scalar-plus-scalar--consecutive-registers---Contiguous-store-non-temporal-of-words-from-multiple-consecutive-vectors--scalar-index--?lang=en",
    "description": "Contiguous store non-temporal of words from multiple consecutive vectors (scalar index) This instruction performs a contiguous non-temporal store of words from elements of two or four consecutive vector registers to the memory address generated by a 64-bit scalar base and scalar index that is added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon. It has encodings from 2 classes:Two registersandFour registers",
    "syntax": [
      "STNT1W {<Zt1>.S-<Zt4>.S },<PNg>, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmbytes : integer{} = esize DIV8;varoffset : bits(64);varbase: bits(64);varaddr : bits(64);varsrc : bits(VL);letpred : bits(PL) = P{}(g);letmask : bits(PL * nreg) = CounterToPredicate{}(pred[15:0]);letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;lettransfer : integer = t;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     tagchecked);if!AnyActiveElement{PL*nreg}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);forr =0to nreg-1dosrc =Z{VL}(transfer+r);fore =0to elements-1doifActivePredicateElement{PL*nreg}(mask, r * elements + e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n        end;\n        addr =AddressIncrement(addr, mbytes, accdesc);\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1W (scalar plus scalar, single register)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1W--scalar-plus-scalar--single-register---Contiguous-store-non-temporal-words-from-vector--scalar-index--?lang=en",
    "description": "Contiguous store non-temporal words from vector (scalar index) This instruction performs a contiguous non-temporal store of words from elements of a vector register to the memory address generated by a 64-bit scalar base and scalar index that is multiplied by 4 and added to the base address. After each element access the index value is incremented, but the index register is not updated. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon.",
    "syntax": [
      "STNT1W {<Zt>.S },<Pg>, [<Xn|SP>,<Xm>, LSL #2]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;varbase: bits(64);varoffset : bits(64);varaddr : bits(64);varsrc : bits(VL);letmask : bits(PL) = P{}(g);letmbytes : integer{} = esize DIV8;letcontiguous : boolean = TRUE;letnontemporal : boolean = TRUE;letpredicated  : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);if!AnyActiveElement{PL}(mask, esize) thenifn ==31&&ConstrainUnpredictableBool(Unpredictable_CHECKSPNONEACTIVE)thenCheckSPAlignment();\n    end;elseifn ==31thenCheckSPAlignment(); end;\n    src =Z{VL}(t);\nend;base=ifn ==31thenSP{64}()elseX{64}(n);\noffset =X{64}(m);\naddr =AddressAdd(base, UInt(offset) * mbytes, accdesc);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenMem{esize}(addr, accdesc) = src[e*:esize];\n    end;\n    addr =AddressIncrement(addr, mbytes, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STNT1W (vector plus scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STNT1W--vector-plus-scalar---Scatter-store-non-temporal-words-?lang=en",
    "description": "Scatter store non-temporal words This instruction performs a non-temporal scatter store of words from the active elements of a vector register to the memory addresses generated by a vector base plus a 64-bit unscaled scalar register offset. Inactive elements are not written to memory. A non-temporal store is a hint to the system that this data is unlikely to be referenced again soon. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 2 classes:32-bit unscaled offsetand64-bit unscaled offset",
    "syntax": [
      "STNT1W {<Zt>.D },<Pg>, [<Zn>.D{,<Xm>}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);varbase: bits(VL);varoffset : bits(64);varsrc : bits(VL);letmbytes : integer{} = msize DIV8;letcontiguous : boolean = FALSE;letnontemporal : boolean = TRUE;letpredicated : boolean = TRUE;lettagchecked : boolean = TRUE;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                     predicated, tagchecked);ifAnyActiveElement{PL}(mask, esize) thenbase=Z{VL}(n);\n    offset =X{64}(m);\n    src =Z{VL}(t);\nend;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletbaddr : bits(64) = ZeroExtend{}(base[e*:esize]);letaddr : bits(64) =AddressAdd(baddr, offset, accdesc);Mem{msize}(addr, accdesc) = src[e*:esize][msize-1:0];\n    end;\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STR (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STR--predicate---Store-predicate-register-?lang=en",
    "description": "Store predicate register This instruction stores a predicate register to a memory address generated by a 64-bit scalar base, plus an immediate offset in the range -256 to 255 that is multiplied by the current predicate register size in bytes. This instruction is unpredicated. The store is performed as contiguous byte accesses, each containing 8 consecutive predicate bits in ascending element order, with no endian conversion and no guarantee of single-copy atomicity larger than a byte. However, if alignment is checked, then a general-purpose base register must be aligned to 2 bytes. For programmer convenience, an assembler must also accept a predicate-as-counter register name for the source predicate register.",
    "syntax": [
      "STR<Pt>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letPL : integer{} =CurrentVL() DIV8;letelements : integer = PL DIV8;varsrc : bits(PL);varbase: bits(64);letoffset : integer = imm * elements;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = FALSE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);ifn ==31thenCheckSPAlignment();base=SP{64}();elsebase=X{64}(n);\nend;\n\nsrc =P{PL}(t);varaddr : bits(64) =AddressAdd(base, offset, accdesc);letaligned : boolean = IsAlignedSize(base,2);if!aligned &&AlignmentEnforced() thenletfault :FaultRecord=AlignmentFault(accdesc, addr);AArch64_Abort(fault);\nend;fore =0to elements-1doAArch64_MemSingle{8}(addr, accdesc, aligned) = src[e*:8];\n    addr =AddressIncrement(addr,1, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "STR (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/STR--vector---Store-vector-register-?lang=en",
    "description": "Store vector register This instruction stores a vector register to a memory address generated by a 64-bit scalar base, plus an immediate offset in the range -256 to 255 that is multiplied by the current vector register size in bytes. This instruction is unpredicated. The store is performed as contiguous byte accesses, with no endian conversion and no guarantee of single-copy atomicity larger than a byte. However, if alignment is checked, then the base register must be aligned to 16 bytes.",
    "syntax": [
      "STR<Zt>, [<Xn|SP>{, #<imm>, MUL VL}]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV8;varsrc : bits(VL);varbase: bits(64);letoffset : integer = imm * elements;letcontiguous : boolean = TRUE;letnontemporal : boolean = FALSE;letpredicated : boolean = FALSE;lettagchecked : boolean = n !=31;letaccdesc :AccessDescriptor=CreateAccDescSVE(MemOp_STORE, nontemporal, contiguous,\n                                                  predicated, tagchecked);ifn ==31thenCheckSPAlignment();base=SP{64}();elsebase=X{64}(n);\nend;\n\nsrc =Z{VL}(t);varaddr : bits(64) =AddressAdd(base, offset, accdesc);letaligned : boolean = IsAlignedSize(base,16);if!aligned &&AlignmentEnforced() thenletfault :FaultRecord=AlignmentFault(accdesc, addr);AArch64_Abort(fault);\nend;fore =0to elements-1doAArch64_MemSingle{8}(addr, accdesc, aligned) = src[e*:8];\n    addr =AddressIncrement(addr,1, accdesc);\nend;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SUB (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUB--immediate---Subtract-immediate--unpredicated--?lang=en",
    "description": "Subtract immediate (unpredicated) This instruction subtracts an unsigned immediate from each element of the source vector, and destructively places the results in the corresponding elements of the source vector. This instruction is unpredicated. The immediate is an unsigned value in the range 0 to 255, and for element widths of 16 bits or higher it may also be a positive multiple of 256 in the range 256 to 65280. The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\".",
    "syntax": [
      "SUB<Zdn>.<T>,<Zdn>.<T>, #<imm>{,<shift>}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];\n    result[e*:esize] = element1 - imm;\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SUB (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUB--vectors--predicated---Subtract--predicated--?lang=en",
    "description": "Subtract (predicated) This instruction subtracts active elements of the second source vector from the corresponding elements of the first source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SUB<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = element1 - element2;elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SUB (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUB--vectors--unpredicated---Subtract--unpredicated--?lang=en",
    "description": "Subtract (unpredicated) This instruction subtracts elements of the second source vector from the corresponding elements of the first source vector, and places the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "SUB<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];\n    result[e*:esize] = element1 - element2;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SUBHNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUBHNB--Subtract-narrow-high-part--bottom--?lang=en",
    "description": "Subtract narrow high part (bottom) This instruction subtracts each vector element of the second source vector from the corresponding vector element in the first source vector, and places the most significant half of the result in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero. This instruction is unpredicated.",
    "syntax": [
      "SUBHNB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letres : integer = (element1 - element2) >> halfesize;\n    result[(2*e +0)*:halfesize] = res[halfesize-1:0];\n    result[(2*e +1)*:halfesize] = Zeros{halfesize};\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SUBHNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUBHNT--Subtract-narrow-high-part--top--?lang=en",
    "description": "Subtract narrow high part (top) This instruction subtracts each vector element of the second source vector from the corresponding vector element in the first source vector, and places the most significant half of the result in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged. This instruction is unpredicated.",
    "syntax": [
      "SUBHNT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(d);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letres : integer = (element1 - element2) >> halfesize;\n    result[(2*e +1)*:halfesize] = res[halfesize-1:0];\n\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SUBP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUBP--Subtract-pairwise-?lang=en",
    "description": "Subtract pairwise This instruction subtracts pairs of adjacent elements within each source vector, and destructively places the interleaved results in the first source vector.",
    "syntax": [
      "SUBP<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);varelement1 : integer;varelement2 : integer;fore =0to elements-1doif!ActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = operand1[e*:esize];elseifIsEven(e) then\n            element1= UInt(operand1[(e +0)*:esize]);\n            element2 = UInt(operand1[(e +1)*:esize]);elseelement1 = UInt(operand2[(e -1)*:esize]);\n            element2 = UInt(operand2[(e +0)*:esize]);\n        end;letres : integer = element1 - element2;\n        result[e*:esize] = res[esize-1:0];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SUBPT (predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUBPT--predicated---Subtract-checked-pointer-vectors--predicated--?lang=en",
    "description": "Subtract checked pointer vectors (predicated) This instruction subtracts, with pointer check, Active elements of the second source vector from corresponding elements of the first source vector and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "SUBPT<Zdn>.D,<Pg>/M,<Zdn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_CPA",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV64;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask,64) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(64) = operand1[e*:64];letelement2 : bits(64) = operand2[e*:64];ifActivePredicateElement{PL}(mask, e,64) thenletres : bits(64) = element1 - element2;\n        result[e*:64] =PointerAddCheck(res, element1);elseresult[e*:64] = operand1[e*:64];\n    end;Z{VL}(dn) = result;\nend;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SUBPT (unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUBPT--unpredicated---Subtract-checked-pointer-vectors--unpredicated--?lang=en",
    "description": "Subtract checked pointer vectors (unpredicated) This instruction subtracts, with pointer check, all elements of the second source vector from corresponding elements of the first source vector and places the results in the corresponding elements of the destination vector. This instruction is unpredicated. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "SUBPT<Zd>.D,<Zn>.D,<Zm>.D"
    ],
    "features": [
      "FEAT_CPA",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV64;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : bits(64) = operand1[e*:64];letelement2 : bits(64) = operand2[e*:64];letres : bits(64) = element1 - element2;\n    result[e*:64] =PointerAddCheck(res, element1);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "SUBR (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUBR--immediate---Reversed-subtract-from-immediate--unpredicated--?lang=en",
    "description": "Reversed subtract from immediate (unpredicated) This instruction performs a reversed subtraction from an unsigned immediate each element of the source vector, and destructively places the results in the corresponding elements of the source vector. This instruction is unpredicated. The immediate is an unsigned value in the range 0 to 255, and for element widths of 16 bits or higher it may also be a positive multiple of 256 in the range 256 to 65280. The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\".",
    "syntax": [
      "SUBR<Zdn>.<T>,<Zdn>.<T>, #<imm>{,<shift>}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);\n    result[e*:esize] = (imm - element1)[esize-1:0];\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SUBR (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUBR--vectors---Reversed-subtract--predicated--?lang=en",
    "description": "Reversed subtract (predicated) This instruction performs a reversed subtraction on active elements of the first source vector from the corresponding elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SUBR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = element2 - element1;elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SUDOT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUDOT--Signed-by-unsigned-8-bit-integer-dot-product-by-indexed-element-to-32-bit-integer-?lang=en",
    "description": "Signed by unsigned 8-bit integer dot product by indexed element to 32-bit integer This instruction calculates the dot product of a group of four signed 8-bit integer values held in each 32-bit element of the first source vector multiplied by a group of four unsigned 8-bit integer values in an indexed 32-bit element of the second source vector, and then destructively adds the widened dot product to the corresponding 32-bit element of the destination vector. The groups within the second source vector are specified using an immediate index that selects the same group position within each 128-bit vector segment. The index range is from 0 to 3. This instruction is unpredicated. ID_AA64ZFR0_EL1.I8MM indicates whether this instruction is implemented.",
    "syntax": [
      "SUDOT<Zda>.S,<Zn>.B,<Zm>.B[<imm>]"
    ],
    "features": [
      "FEAT_I8MM",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;varres : bits(esize) = operand3[e*:esize];fori =0to3doletelement1 : integer = SInt(operand1[(4* e + i)*:(esize DIV4)]);letelement2 : integer = UInt(operand2[(4* s + i)*:(esize DIV4)]);\n        res = res + element1 * element2;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SUNPKHI, SUNPKLO",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUNPKHI--SUNPKLO--Signed-unpack-and-extend-half-of-vector-?lang=en",
    "description": "Signed unpack and extend half of vector This instruction unpacks and sign-extends elements from the lowest or highest half of the source vector, and writes the sign-extended values into double-width elements of the destination vector. This instruction is unpredicated. It has encodings from 2 classes:High halfandLow half",
    "syntax": [
      "SUNPKLO<Zd>.<T>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;lethsize : integer{} = esize DIV2;letoffset : integer =ifhi then elementselse0;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(hsize) = operand[(e + offset)*:hsize];\n    result[e*:esize] = Extend{esize}(element, unsigned);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "SUQADD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SUQADD--Signed-saturating-unsigned-add-?lang=en",
    "description": "Signed saturating unsigned add This instruction adds active unsigned elements of the source vector to the corresponding signed elements of the addend vector, and destructively places the results in the corresponding elements of the addend vector. Each result element is saturated to the N-bit element's signed integer range -2(N-1)to (2(N-1))-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "SUQADD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =SignedSat{esize}(SInt(element1) + UInt(element2));elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "SXTB, SXTH, SXTW",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/SXTB--SXTH--SXTW--Signed-byte---halfword---word-extend--predicated--?lang=en",
    "description": "Signed byte / halfword / word extend (predicated) This instruction sign-extends the least-significant sub-element of each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 6 classes:Byte, merging,Byte, zeroing,Halfword, merging,Halfword, zeroing,Word, mergingandWord, zeroing",
    "syntax": [
      "SXTW<Zd>.D,<Pg>/Z,<Zn>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] = Extend{esize}(element[s_esize-1:0], unsigned);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "TBL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/TBL--Programmable-table-lookup-in-one-or-two-vector-table--zeroing--?lang=en",
    "description": "Programmable table lookup in one or two vector table (zeroing) This instruction reads each element of the second source (index) vector and uses its value to select an indexed element from a table of elements consisting of one or two consecutive vector registers, where the first or only vector holds the lower numbered elements, and places the indexed table element in the destination vector element corresponding to the index vector element. If an index value is greater than or equal to the number of vector elements, then it places zero in the corresponding destination vector element. Since the index values can select any element in a vector this operation is not naturally vector length agnostic. It has encodings from 2 classes:Single register tableandTwo register table",
    "syntax": [
      "TBL<Zd>.<T>, {<Zn1>.<T>,<Zn2>.<T>},<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letindexes :bits(VL)= Z{}(m);varresult :bits(VL);lettable_size : integer{} =ifdouble_table then VL*2elseVL;lettable_elems : integer = table_size DIV esize;vartable :bits(table_size);ifdouble_table then\n    let top :bits(VL)= Z{}((n +1) MOD32);letbottom:bits(VL)= Z{}(n);table = (top::bottom)[table_size-1:0];elsetable =Z{table_size}(n);end;fore =0to elements-1do\n    let idx : integer = UInt(indexes[e*:esize]);result[e*:esize] =ifidx < table_elems then table[idx*:esize]elseZeros{esize};end;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "TBLQ",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/TBLQ--Programmable-table-lookup-within-each-quadword-vector-segment--zeroing--?lang=en",
    "description": "Programmable table lookup within each quadword vector segment (zeroing) This instruction reads, for each 128-bit destination vector segment, each element of the corresponding second source (index) vector segment and uses its value to select an indexed element from the corresponding first source (table) vector segment. The indexed table element is placed in the element of the destination vector that corresponds to the index vector element. If an index value is greater than or equal to the number of elements in a 128-bit vector segment then it places zero in the corresponding destination vector element. This instruction is unpredicated.",
    "syntax": [
      "TBLQ<Zd>.<T>, {<Zn>.<T>},<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letelements : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fors =0to segments-1dofore =0to elements-1doletidx : integer = UInt(operand2[(s * elements + e)*:esize]);ifidx < elements then\n            result[(s * elements + e)*:esize] = operand1[(s * elements + idx)*:esize];elseresult[(s * elements + e)*:esize] = Zeros{esize};\n        end;\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "TBX",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/TBX--Programmable-table-lookup-in-single-vector-table--merging--?lang=en",
    "description": "Programmable table lookup in single vector table (merging) This instruction reads each element of the second source (index) vector and uses its value to select an indexed element from a table of elements in the first source vector, and places the indexed element in the destination vector element corresponding to the index vector element. If an index value is greater than or equal to the number of vector elements, then the corresponding destination vector element is left unchanged. Since the index values can select any element in a vector this operation is not naturally vector length agnostic.",
    "syntax": [
      "TBX<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement2 : integer = UInt(operand2[e*:esize]);ifelement2 < elements then\n        result[e*:esize] = operand1[element2*:esize];\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "TBXQ",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/TBXQ--Programmable-table-lookup-within-each-quadword-vector-segment--merging--?lang=en",
    "description": "Programmable table lookup within each quadword vector segment (merging) This instruction reads, for each 128-bit destination vector segment, each element of the corresponding second source (index) vector segment and uses its value to select an indexed element from the corresponding first source (table) vector segment. The indexed table element is placed in the element of the destination vector that corresponds to the index vector element. If an index value is greater than or equal to the number of elements in a 128-bit vector segment then the corresponding destination vector element is left unchanged. This instruction is unpredicated.",
    "syntax": [
      "TBXQ<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letelements : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(d);fors =0to segments-1dofore =0to elements-1doletidx : integer = UInt(operand2[(s * elements + e)*:esize]);ifidx < elements then\n            result[(s * elements + e)*:esize] = operand1[(s * elements + idx)*:esize];\n        end;\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "TRN1, TRN2 (predicates)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/TRN1--TRN2--predicates---Interleave-even-or-odd-elements-from-two-predicates-?lang=en",
    "description": "Interleave even or odd elements from two predicates This instruction interleaves alternating even or odd-numbered elements from the first and second source predicates, and places the values in the destination predicate. This instruction is unpredicated. It has encodings from 2 classes:EvenandOdd",
    "syntax": [
      "TRN2<Pd>.<T>,<Pn>.<T>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letpairs : integer =VLDIV(esize *2);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);forp =0to pairs-1doresult[(2*p+0)*:(esize DIV8)] = operand1[(2*p+part)*:(esize DIV8)];\n    result[(2*p+1)*:(esize DIV8)] = operand2[(2*p+part)*:(esize DIV8)];\nend;P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "TRN1, TRN2 (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/TRN1--TRN2--vectors---Interleave-even-or-odd-elements-from-two-vectors-?lang=en",
    "description": "Interleave even or odd elements from two vectors This instruction interleaves alternating even or odd-numbered elements from the first and second source vectors, and places the values in the destination vector. This instruction is unpredicated. The 128-bit element variant requires that the Effective SVE vector length is at least 256 bits. ID_AA64ZFR0_EL1.F64MM indicates whether the 128-bit element variant is implemented. The 128-bit element variant is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 4 classes:Even,Even (quadwords),OddandOdd (quadwords)",
    "syntax": [
      "TRN2<Zd>.Q,<Zn>.Q,<Zm>.Q"
    ],
    "features": [
      "FEAT_F64MM",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "ifesize <128thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();ifVL < esize *2thenEndOfDecode(Decode_UNDEF); end;letpairs : integer =VLDIV(esize *2);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Zeros{};forp =0to pairs-1doresult[(2*p+0)*:esize] = operand1[(2*p+part)*:esize];\n    result[(2*p+1)*:esize] = operand2[(2*p+part)*:esize];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UABA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UABA--Unsigned-absolute-difference-and-accumulate-?lang=en",
    "description": "Unsigned absolute difference and accumulate This instruction calculates the absolute difference between unsigned integer values in elements of the first source vector and the corresponding elements of the second source vector, and adds the difference to the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UABA<Zda>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letop2elt : bits(esize) = operand2[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letabsdiff : bits(esize) = Abs(element1 - element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] + absdiff;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UABAL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UABAL--Two-way-unsigned-absolute-difference-sum-and-accumulate-long-?lang=en",
    "description": "Two-way unsigned absolute difference sum and accumulate long This instruction calculates the sum of the absolute difference between even-numbered unsigned elements and the absolute difference between the odd-numbered unsigned elements of each double-width container of the first and second vectors, and destructively adds the results to the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "UABAL<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelt1_a : integer = UInt(operand1[(2*e +0)*:(esize DIV2)]);letelt1_b : integer = UInt(operand1[(2*e +1)*:(esize DIV2)]);letelt2_a : integer = UInt(operand2[(2*e +0)*:(esize DIV2)]);letelt2_b : integer = UInt(operand2[(2*e +1)*:(esize DIV2)]);letsum = Abs(elt1_a - elt2_a) + Abs(elt1_b - elt2_b);\n    result[e*:esize] = result[e*:esize] + sum[esize-1:0];\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UABALB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UABALB--Unsigned-absolute-difference-and-accumulate-long--bottom--?lang=en",
    "description": "Unsigned absolute difference and accumulate long (bottom) This instruction calculates the absolute difference between even-numbered unsigned elements of the first source vector and the corresponding elements of the second source vector, and destructively adds the results to the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "UABALB<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = UInt(operand1[(2*e +0)*:(esize DIV2)]);letelement2 : integer = UInt(operand2[(2*e +0)*:(esize DIV2)]);letabsdiff = Abs(element1 - element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] + absdiff;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UABALT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UABALT--Unsigned-absolute-difference-and-accumulate-long--top--?lang=en",
    "description": "Unsigned absolute difference and accumulate long (top) This instruction calculates the absolute difference between odd-numbered unsigned elements of the first source vector and the corresponding elements of the second source vector, and destructively adds the results to the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "UABALT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = UInt(operand1[(2*e +1)*:(esize DIV2)]);letelement2 : integer = UInt(operand2[(2*e +1)*:(esize DIV2)]);letabsdiff = Abs(element1 - element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] + absdiff;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UABD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UABD--Unsigned-absolute-difference--predicated--?lang=en",
    "description": "Unsigned absolute difference (predicated) This instruction calculates the absolute difference between unsigned integer values in active elements of the first source vector and the corresponding elements of the second source vector, and destructively places the difference in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UABD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletabsdiff : integer = Abs(element1 - element2);\n        result[e*:esize] = absdiff[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UABDLB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UABDLB--Unsigned-absolute-difference-long--bottom--?lang=en",
    "description": "Unsigned absolute difference long (bottom) This instruction calculates the absolute difference between the even-numbered unsigned integer values in elements of the first source vector and the corresponding elements of the second source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UABDLB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[(2*e +0)*:(esize DIV2)]);letelement2 : integer = UInt(operand2[(2*e +0)*:(esize DIV2)]);letres : integer = Abs(element1 - element2);\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UABDLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UABDLT--Unsigned-absolute-difference-long--top--?lang=en",
    "description": "Unsigned absolute difference long (top) This instruction calculates the absolute difference between the odd-numbered unsigned integer values in elements of the first source vector and the corresponding elements of the second source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UABDLT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[(2*e +1)*:(esize DIV2)]);letelement2 : integer = UInt(operand2[(2*e +1)*:(esize DIV2)]);letres : integer = Abs(element1 - element2);\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UADALP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UADALP--Unsigned-add-and-accumulate-long-pairwise-?lang=en",
    "description": "Unsigned add and accumulate long pairwise This instruction adds pairs of adjacent unsigned integer values and accumulates the results into the overlapping double-width elements of the destination vector.",
    "syntax": [
      "UADALP<Zda>.<T>,<Pg>/M,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand_acc : bits(VL) = Z{}(da);letoperand_src : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doif!ActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = operand_acc[e*:esize];elseletelement1 : integer = UInt(operand_src[(2*e +0)*:(esize DIV2)]);letelement2 : integer = UInt(operand_src[(2*e +1)*:(esize DIV2)]);letsum : bits(esize) = (element1 + element2)[esize-1:0];\n        result[e*:esize] = operand_acc[e*:esize] + sum;\n    end;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UADDLB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UADDLB--Unsigned-add-long--bottom--?lang=en",
    "description": "Unsigned add long (bottom) This instruction adds the corresponding even-numbered unsigned elements of the first and second source vectors, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UADDLB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize DIV2) = operand1[(2*e + sel1)*:(esize DIV2)];letop2elt : bits(esize DIV2) = operand2[(2*e + sel2)*:(esize DIV2)];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letres : integer = element1 + element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UADDLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UADDLT--Unsigned-add-long--top--?lang=en",
    "description": "Unsigned add long (top) This instruction adds the corresponding odd-numbered unsigned elements of the first and second source vectors, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UADDLT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize DIV2) = operand1[(2*e + sel1)*:(esize DIV2)];letop2elt : bits(esize DIV2) = operand2[(2*e + sel2)*:(esize DIV2)];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letres : integer = element1 + element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UADDV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UADDV--Unsigned-add-reduction-to-scalar-?lang=en",
    "description": "Unsigned add reduction to scalar This instruction adds all the unsigned elements of the source vector, and places the result in the SIMD&FP scalar destination register. Narrow elements are first zero-extended to 64 bits. Inactive elements in the source vector are treated as zero.",
    "syntax": [
      "UADDV<Dd>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varsum : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = UInt(operand[e*:esize]);\n        sum = sum + element;\n\n    end;\nend;V{64}(d) = sum[63:0];",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UADDWB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UADDWB--Unsigned-add-wide--bottom--?lang=en",
    "description": "Unsigned add wide (bottom) This instruction adds the even-numbered unsigned elements of the second source vector to the overlapping double-width elements of the first source vector and places the results in the corresponding double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UADDWB<Zd>.<T>,<Zn>.<T>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[(2*e +0)*:(esize DIV2)]);\n    result[e*:esize] = (element1 + element2)[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UADDWT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UADDWT--Unsigned-add-wide--top--?lang=en",
    "description": "Unsigned add wide (top) This instruction adds the odd-numbered unsigned elements of the second source vector to the overlapping double-width elements of the first source vector and places the results in the corresponding double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UADDWT<Zd>.<T>,<Zn>.<T>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[(2*e +1)*:(esize DIV2)]);\n    result[e*:esize] = (element1 + element2)[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UCLAMP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UCLAMP--Unsigned-clamp-to-minimum-maximum-?lang=en",
    "description": "Unsigned clamp to minimum/maximum This instruction clamps each unsigned element in the destination vector to between the unsigned minimum value in the corresponding element of the first source vector and the unsigned maximum value in the corresponding element of the second source vector and destructively writes the results in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UCLAMP<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(d);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);letelement3 : integer = UInt(operand3[e*:esize]);letres : integer = Min(Max(element1, element3), element2);\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UCVTF (bottom, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UCVTF--bottom--unpredicated---Unsigned-integer-convert-to-floating-point--bottom--unpredicated--?lang=en",
    "description": "Unsigned integer convert to floating-point (bottom, unpredicated) This instruction converts each even-numbered unsigned integer element of the source vector to floating-point, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UCVTF<Zd>.<T>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize DIV2) = operand[(2*e +0)*:(esize DIV2)];\n    result[e*:esize] =FixedToFP{esize, esize DIV2}(element,0, unsigned, FPCR(), rounding);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UCVTF (predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UCVTF--predicated---Unsigned-integer-convert-to-floating-point--predicated--?lang=en",
    "description": "Unsigned integer convert to floating-point (predicated) This instruction converts to floating-point from the unsigned integer in each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. If the input and result types have a different size the smaller type is held unpacked in the least significant bits of elements of the larger size. When the input is the smaller type the upper bits of each source element are ignored. When the result is the smaller type the results are zero-extended to fill each destination element. It has encodings from 14 classes:16-bit to half-precision, merging,16-bit to half-precision, zeroing,32-bit to half-precision, merging,32-bit to half-precision, zeroing,32-bit to single-precision, merging,32-bit to single-precision, zeroing,32-bit to double-precision, merging,32-bit to double-precision, zeroing,64-bit to half-precision, merging,64-bit to half-precision, zeroing,64-bit to single-precision, merging,64-bit to single-precision, zeroing,64-bit to double-precision, mergingand64-bit to double-precision, zeroing",
    "syntax": [
      "UCVTF<Zd>.D,<Pg>/Z,<Zn>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];letfpval : bits(d_esize) =FixedToFP{d_esize, s_esize}(element[s_esize-1:0],0, unsigned,\n                                                                FPCR(), rounding);\n        result[e*:esize] = ZeroExtend{esize}(fpval);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"16-bit to half-precision, merging\" , \"32-bit to half-precision, merging\" , \"32-bit to single-precision, merging\" , \"32-bit to double-precision, merging\" , \"64-bit to half-precision, merging\" , \"64-bit to single-precision, merging\" , \"64-bit to double-precision, merging\" variants:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "UCVTFLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UCVTFLT--Unsigned-integer-convert-to-floating-point--top--unpredicated--?lang=en",
    "description": "Unsigned integer convert to floating-point (top, unpredicated) This instruction converts each odd-numbered unsigned integer element of the source vector to floating-point, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UCVTFLT<Zd>.<T>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize DIV2) = operand[(2*e +1)*:(esize DIV2)];\n    result[e*:esize] =FixedToFP{esize, esize DIV2}(element,0, unsigned, FPCR(), rounding);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UDIV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UDIV--Unsigned-divide--predicated--?lang=en",
    "description": "Unsigned divide (predicated) This instruction performs an unsigned division on active elements of the first source vector by the corresponding elements of the second source vector and destructively places the quotient in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UDIV<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletdividend : integer = UInt(operand1[e*:esize]);letdivisor : integer = UInt(operand2[e*:esize]);varquotient : integer;ifdivisor ==0then\n            quotient =0;elsequotient = dividend DIVRM divisor;\n        end;\n        result[e*:esize] = quotient[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UDIVR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UDIVR--Unsigned-reversed-divide--predicated--?lang=en",
    "description": "Unsigned reversed divide (predicated) This instruction performs an unsigned reversed division on active elements of the second source vector by the corresponding elements of the first source vector, and destructively places the quotient in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UDIVR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletdivisor : integer = UInt(operand1[e*:esize]);letdividend : integer = UInt(operand2[e*:esize]);varquotient : integer;ifdivisor ==0then\n            quotient =0;elsequotient = dividend DIVRM divisor;\n        end;\n        result[e*:esize] = quotient[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UDOT (2-way, indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UDOT--2-way--indexed---Unsigned-integer-dot-product-by-indexed-element--two-way--?lang=en",
    "description": "Unsigned integer dot product by indexed element (two-way) This instruction calculates the dot product of a pair of unsigned 8-bit or 16-bit integer values held in each 16-bit or 32-bit element of the first source vector multiplied by a pair of unsigned 8-bit or 16-bit integer values in an indexed 16-bit or 32-bit element of the second source vector, and then destructively adds the widened dot product to the corresponding 16-bit or 32-bit element of the destination vector. The groups within the second source vector are specified using an immediate index that selects the same group position within each 128-bit vector segment. The index range is from 0 to one less than the number of groups per 128-bit segment. This instruction is unpredicated. It has encodings from 2 classes:8-bit to 16-bitand16-bit to 32-bit",
    "syntax": [
      "UDOT<Zda>.S,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;varres : bits(esize) = operand3[e*:esize];fori =0to1doletelement1 : integer = UInt(operand1[(2* e + i)*:(esize DIV2)]);letelement2 : integer = UInt(operand2[(2* s + i)*:(esize DIV2)]);\n        res = res + element1 * element2;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UDOT (2-way, vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UDOT--2-way--vectors---Unsigned-integer-dot-product--two-way--?lang=en",
    "description": "Unsigned integer dot product (two-way) This instruction calculates the dot product of a pair of unsigned 8-bit or 16-bit integer values held in each 16-bit or 32-bit element of the first source vector multiplied by a pair of unsigned 8-bit or 16-bit integer values in the corresponding 16-bit or 32-bit element of the second source vector, and then destructively adds the widened dot product to the corresponding 16-bit or 32-bit element of the destination vector. This instruction is unpredicated. It has encodings from 2 classes:8-bit to 16-bitand16-bit to 32-bit",
    "syntax": [
      "UDOT<Zda>.S,<Zn>.H,<Zm>.H"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1dovarres : bits(esize) = operand3[e*:esize];fori =0to1doletelement1 : integer = UInt(operand1[(2* e + i)*:(esize DIV2)]);letelement2 : integer = UInt(operand2[(2* e + i)*:(esize DIV2)]);\n        res = res + element1 * element2;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UDOT (4-way, indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UDOT--4-way--indexed---Unsigned-integer-dot-product-by-indexed-element--four-way--?lang=en",
    "description": "Unsigned integer dot product by indexed element (four-way) This instruction calculates the dot product of a group of four unsigned 8-bit or 16-bit integer values held in each 32-bit or 64-bit element of the first source vector multiplied by a group of four unsigned 8-bit or 16-bit integer values in an indexed 32-bit or 64-bit element of the second source vector, and then destructively adds the widened dot product to the corresponding 32-bit or 64-bit element of the destination vector. The groups within the second source vector are specified using an immediate index that selects the same group position within each 128-bit vector segment. The index range is from 0 to one less than the number of groups per 128-bit segment. This instruction is unpredicated. It has encodings from 2 classes:8-bit to 32-bitand16-bit to 64-bit",
    "syntax": [
      "UDOT<Zda>.D,<Zn>.H,<Zm>.H[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;varres : bits(esize) = operand3[e*:esize];fori =0to3doletelement1 : integer = UInt(operand1[(4* e + i)*:(esize DIV4)]);letelement2 : integer = UInt(operand2[(4* s + i)*:(esize DIV4)]);\n        res = res + element1 * element2;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UDOT (4-way, vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UDOT--4-way--vectors---Unsigned-integer-dot-product--four-way--?lang=en",
    "description": "Unsigned integer dot product (four-way) This instruction calculates the dot product of a group of four unsigned 8-bit or 16-bit integer values held in each 32-bit or 64-bit element of the first source vector multiplied by a group of four unsigned 8-bit or 16-bit integer values in the corresponding 32-bit or 64-bit element of the second source vector, and then destructively adds the widened dot product to the corresponding 32-bit or 64-bit element of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UDOT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1dovarres : bits(esize) = operand3[e*:esize];fori =0to3doletelement1 : integer = UInt(operand1[(4* e + i)*:(esize DIV4)]);letelement2 : integer = UInt(operand2[(4* e + i)*:(esize DIV4)]);\n        res = res + element1 * element2;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UHADD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UHADD--Unsigned-halving-add-?lang=en",
    "description": "Unsigned halving add This instruction adds active unsigned elements of the first source vector to corresponding unsigned elements of the second source vector, shifts right one bit, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UHADD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = (element1 + element2) >>1;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UHSUB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UHSUB--Unsigned-halving-subtract-?lang=en",
    "description": "Unsigned halving subtract This instruction subtracts active unsigned elements of the second source vector from corresponding unsigned elements of the first source vector, shifts right one bit, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UHSUB<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = (element1 - element2) >>1;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UHSUBR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UHSUBR--Unsigned-halving-subtract-reversed-?lang=en",
    "description": "Unsigned halving subtract reversed This instruction subtracts active unsigned elements of the first source vector from corresponding unsigned elements of the second source vector, shifts right one bit, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UHSUBR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = (element2 - element1) >>1;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMAX (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMAX--immediate---Unsigned-maximum-with-immediate--unpredicated--?lang=en",
    "description": "Unsigned maximum with immediate (unpredicated) This instruction determines the unsigned maximum of an immediate and each element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate is an unsigned 8-bit value in the range 0 to 255, inclusive. This instruction is unpredicated.",
    "syntax": [
      "UMAX<Zdn>.<T>,<Zdn>.<T>, #<imm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);\n    result[e*:esize] = Max(element1, imm)[esize-1:0];\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMAX (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMAX--vectors---Unsigned-maximum--predicated--?lang=en",
    "description": "Unsigned maximum (predicated) This instruction determines the unsigned maximum of active elements of the first source vector and the corresponding elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UMAX<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletmaximum : integer = Max(element1, element2);\n        result[e*:esize] = maximum[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMAXP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMAXP--Unsigned-maximum-pairwise-?lang=en",
    "description": "Unsigned maximum pairwise This instruction calculates the maximum value of each pair of adjacent unsigned integer elements within each source vector, and interleaves the results from corresponding lanes. The interleaved result values are destructively placed in the first source vector.",
    "syntax": [
      "UMAXP<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);varelement1 : integer;varelement2 : integer;fore =0to elements-1doif!ActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = operand1[e*:esize];elseifIsEven(e) then\n            element1= UInt(operand1[(e +0)*:esize]);\n            element2 = UInt(operand1[(e +1)*:esize]);elseelement1 = UInt(operand2[(e -1)*:esize]);\n            element2 = UInt(operand2[(e +0)*:esize]);\n        end;letres : integer = Max(element1, element2);\n        result[e*:esize] = res[esize-1:0];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMAXQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMAXQV--Unsigned-maximum-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Unsigned maximum reduction of quadword vector segments This instruction determines the unsigned maximum of the elements in the same position from each 128-bit source vector segment, and places each result into the corresponding element of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as zero.",
    "syntax": [
      "UMAXQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer = VL DIV128;letelempersegment : integer =128DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(128) = Zeros{};varstmp : bits(128) = Zeros{};vardtmp : integer;fore =0to elempersegment-1dodtmp =ifunsigned then0else-(2^(esize-1));fors =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) then\n            stmp = operand[s*:128];\n            dtmp = Max(dtmp, UInt(stmp[e*:esize]));\n        end;\n    end;\n    result[e*:esize] = dtmp[esize-1:0];\nend;V{128}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UMAXV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMAXV--Unsigned-maximum-reduction-to-scalar-?lang=en",
    "description": "Unsigned maximum reduction to scalar This instruction determines the unsigned maximum of the elements of the source vector, and places the result in the SIMD&FP scalar destination register. Inactive elements in the source vector are treated as zero.",
    "syntax": [
      "UMAXV<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varmaximum : integer =ifunsigned then0else-(2^(esize-1));fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletopelt : bits(esize) = operand[e*:esize];letelement : integer =ifunsigned thenUInt(opelt) elseSInt(opelt);\n        maximum = Max(maximum, element);\n\n    end;\nend;V{esize}(d) = maximum[esize-1:0];",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UMIN (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMIN--immediate---Unsigned-minimum-with-immediate--unpredicated--?lang=en",
    "description": "Unsigned minimum with immediate (unpredicated) This instruction determines the unsigned minimum of an immediate and each element of the source vector, and destructively places the results in the corresponding elements of the source vector. The immediate is an unsigned 8-bit value in the range 0 to 255, inclusive. This instruction is unpredicated.",
    "syntax": [
      "UMIN<Zdn>.<T>,<Zdn>.<T>, #<imm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);\n    result[e*:esize] = Min(element1, imm)[esize-1:0];\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMIN (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMIN--vectors---Unsigned-minimum--predicated--?lang=en",
    "description": "Unsigned minimum (predicated) This instruction determines the unsigned minimum of active elements of the first source vector and the corresponding elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UMIN<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletminimum : integer = Min(element1, element2);\n        result[e*:esize] = minimum[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMINP",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMINP--Unsigned-minimum-pairwise-?lang=en",
    "description": "Unsigned minimum pairwise This instruction calculates the minimum value of each pair of adjacent unsigned integer elements within each source vector, and interleaves the results from corresponding lanes. The interleaved result values are destructively placed in the first source vector.",
    "syntax": [
      "UMINP<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);varelement1 : integer;varelement2 : integer;fore =0to elements-1doif!ActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] = operand1[e*:esize];elseifIsEven(e) then\n            element1= UInt(operand1[(e +0)*:esize]);\n            element2 = UInt(operand1[(e +1)*:esize]);elseelement1 = UInt(operand2[(e -1)*:esize]);\n            element2 = UInt(operand2[(e +0)*:esize]);\n        end;letres : integer = Min(element1, element2);\n        result[e*:esize] = res[esize-1:0];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMINQV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMINQV--Unsigned-minimum-reduction-of-quadword-vector-segments-?lang=en",
    "description": "Unsigned minimum reduction of quadword vector segments This instruction determines the unsigned minimum of the elements in the same position from each 128-bit source vector segment, and places each result into the corresponding element of the 128-bit SIMD&FP destination register. Inactive elements in the source vector are treated as the maximum unsigned integer for the element size.",
    "syntax": [
      "UMINQV<Vd>.<T>,<Pg>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letsegments : integer = VL DIV128;letelempersegment : integer =128DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(128) = Zeros{};varstmp : bits(128) = Zeros{};vardtmp : integer;fore =0to elempersegment-1dodtmp =ifunsignedthen(2^esize -1)else(2^(esize-1) - 1);fors =0to segments-1doifActivePredicateElement{PL}(mask, s * elempersegment + e, esize) then\n            stmp = operand[s*:128];\n            dtmp = Min(dtmp, UInt(stmp[e*:esize]));\n        end;\n    end;\n    result[e*:esize] = dtmp[esize-1:0];\nend;V{128}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UMINV",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMINV--Unsigned-minimum-reduction-to-scalar-?lang=en",
    "description": "Unsigned minimum reduction to scalar This instruction determines the unsigned minimum of the elements of the source vector, and places the result in the SIMD&FP scalar destination register. Inactive elements in the source vector are treated as the maximum unsigned integer for the element size.",
    "syntax": [
      "UMINV<V><d>,<Pg>,<Zn>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varminimum : integer =ifunsignedthen(2^esize -1)else(2^(esize-1) - 1);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletopelt : bits(esize) = operand[e*:esize];letelement : integer =ifunsigned thenUInt(opelt) elseSInt(opelt);\n        minimum = Min(minimum, element);\n\n    end;\nend;V{esize}(d) = minimum[esize-1:0];",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UMLALB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMLALB--indexed---Unsigned-multiply-add-long-by-indexed-element--bottom--?lang=en",
    "description": "Unsigned multiply-add long by indexed element (bottom) This instruction multiplies the even-numbered unsigned elements within each 128-bit segment of the first source vector by the specified unsigned element in the corresponding second source vector segment, and destructively adds the results to the overlapping double-width elements of the addend vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UMLALB<Zda>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = UInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = UInt(operand2[(2* s + index)*:esize]);letproduct : bits(2*esize) = (element1 * element2)[2*esize-1:0];\n    result[e*:(2*esize)] = result[e*:(2*esize)] + product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMLALB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMLALB--vectors---Unsigned-multiply-add-long--bottom--?lang=en",
    "description": "Unsigned multiply-add long (bottom) This instruction multiplies the even-numbered unsigned elements of the first source vector by the corresponding elements of the second source vector, and destructively adds the results to the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "UMLALB<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = UInt(operand1[(2*e +0)*:(esize DIV2)]);letelement2 : integer = UInt(operand2[(2*e +0)*:(esize DIV2)]);letproduct = (element1 * element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] + product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMLALT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMLALT--indexed---Unsigned-multiply-add-long-by-indexed-element--top--?lang=en",
    "description": "Unsigned multiply-add long by indexed element (top) This instruction multiplies the odd-numbered unsigned elements within each 128-bit segment of the first source vector by the specified unsigned element in the corresponding second source vector segment, and destructively adds the results to the overlapping double-width elements of the addend vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UMLALT<Zda>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = UInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = UInt(operand2[(2* s + index)*:esize]);letproduct : bits(2*esize) = (element1 * element2)[2*esize-1:0];\n    result[e*:(2*esize)] = result[e*:(2*esize)] + product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMLALT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMLALT--vectors---Unsigned-multiply-add-long--top--?lang=en",
    "description": "Unsigned multiply-add long (top) This instruction multiplies the odd-numbered unsigned elements of the first source vector by the corresponding elements of the second source vector, and destructively adds the results to the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "UMLALT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = UInt(operand1[(2*e +1)*:(esize DIV2)]);letelement2 : integer = UInt(operand2[(2*e +1)*:(esize DIV2)]);letproduct = (element1 * element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] + product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMLSLB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMLSLB--indexed---Unsigned-multiply-subtract-long-by-indexed-element--bottom--?lang=en",
    "description": "Unsigned multiply-subtract long by indexed element (bottom) This instruction multiplies the even-numbered unsigned elements within each 128-bit segment of the first source vector by the specified unsigned element in the corresponding second source vector segment, and destructively subtracts the results from the overlapping double-width elements of the addend vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UMLSLB<Zda>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = UInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = UInt(operand2[(2* s + index)*:esize]);letproduct : bits(2*esize) = (element1 * element2)[2*esize-1:0];\n    result[e*:(2*esize)] = result[e*:(2*esize)] - product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMLSLB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMLSLB--vectors---Unsigned-multiply-subtract-long--bottom--?lang=en",
    "description": "Unsigned multiply-subtract long (bottom) This instruction multiplies the even-numbered unsigned elements of the first source vector by the corresponding elements of the second source vector, and destructively subtracts the results from the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "UMLSLB<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = UInt(operand1[(2*e +0)*:(esize DIV2)]);letelement2 : integer = UInt(operand2[(2*e +0)*:(esize DIV2)]);letproduct = (element1 * element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] - product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMLSLT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMLSLT--indexed---Unsigned-multiply-subtract-long-by-indexed-element--top--?lang=en",
    "description": "Unsigned multiply-subtract long by indexed element (top) This instruction multiplies the odd-numbered unsigned elements within each 128-bit segment of the first source vector by the specified unsigned element in the corresponding second source vector segment, and destructively subtracts the results from the overlapping double-width elements of the addend vector. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UMLSLT<Zda>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = UInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = UInt(operand2[(2* s + index)*:esize]);letproduct : bits(2*esize) = (element1 * element2)[2*esize-1:0];\n    result[e*:(2*esize)] = result[e*:(2*esize)] - product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMLSLT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMLSLT--vectors---Unsigned-multiply-subtract-long--top--?lang=en",
    "description": "Unsigned multiply-subtract long (top) This instruction multiplies the odd-numbered unsigned elements of the first source vector by the corresponding elements of the second source vector, and destructively subtracts the results from the overlapping double-width elements of the addend vector. This instruction is unpredicated.",
    "syntax": [
      "UMLSLT<Zda>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Z{}(da);fore =0to elements-1doletelement1 : integer = UInt(operand1[(2*e +1)*:(esize DIV2)]);letelement2 : integer = UInt(operand2[(2*e +1)*:(esize DIV2)]);letproduct = (element1 * element2)[esize-1:0];\n    result[e*:esize] = result[e*:esize] - product;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMMLA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMMLA--Unsigned-8-bit-integer-matrix-multiply-accumulate-to-32-bit-integer-?lang=en",
    "description": "Unsigned 8-bit integer matrix multiply-accumulate to 32-bit integer This instruction multiplies the 2×8 matrix of unsigned 8-bit integer values held in each 128-bit segment of the first source vector by the 8×2 matrix of unsigned 8-bit integer values in the corresponding segment of the second source vector. The resulting 2×2 widened 32-bit integer matrix product is then destructively added to the 32-bit integer matrix accumulator held in the corresponding segment of the addend and destination vector. This is equivalent to performing an 8-way dot product per destination element. This instruction is unpredicated. ID_AA64ZFR0_EL1.I8MM indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "UMMLA<Zda>.S,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_I8MM",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letoperand1 :bits(VL)= Z{}(n);letoperand2 :bits(VL)= Z{}(m);letoperand3 :bits(VL)= Z{}(da);varresult :bits(VL)= Zeros{};varop1, op2 :bits(128);varres,addend:bits(128);fors =0to segments-1do\n    op1    = operand1[s*:(128)];op2    = operand2[s*:(128)];addend= operand3[s*:(128)];res    =MatMulAdd(addend,op1, op2, op1_unsigned, op2_unsigned);result[s*:(128)] = res;end;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMULH (predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMULH--predicated---Unsigned-multiply-returning-high-half--predicated--?lang=en",
    "description": "Unsigned multiply returning high half (predicated) This instruction performs a widening multiplication of unsigned integer values in active elements of the first source vector by the corresponding elements of the second source vector and destructively places the high half of the result in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UMULH<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletproduct : integer = (element1 * element2) >> esize;\n        result[e*:esize] = product[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UMULH (unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMULH--unpredicated---Unsigned-multiply-returning-high-half--unpredicated--?lang=en",
    "description": "Unsigned multiply returning high half (unpredicated) This instruction performs a widening multiplication of unsigned integer values of all elements of the first source vector by the corresponding elements of the second source vector and places the high half of the result in the corresponding elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UMULH<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letop2elt : bits(esize) = operand2[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letproduct : integer = (element1 * element2) >> esize;\n    result[e*:esize] = product[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UMULLB (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMULLB--indexed---Unsigned-multiply-long-by-indexed-element--bottom--?lang=en",
    "description": "Unsigned multiply long by indexed element (bottom) This instruction multiplies the even-numbered unsigned elements within each 128-bit segment of the first source vector by the specified unsigned element in the corresponding second source vector segment, and places the results in the overlapping double-width elements of the destination vector register. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UMULLB<Zd>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = UInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = UInt(operand2[(2* s + index)*:esize]);letres : integer = element1 * element2;\n    result[e*:(2*esize)] = res[2*esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UMULLB (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMULLB--vectors---Unsigned-multiply-long--bottom--?lang=en",
    "description": "Unsigned multiply long (bottom) This instruction multiplies the even-numbered unsigned elements of the first source vector by the corresponding elements of the second source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UMULLB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[(2*e +0)*:(esize DIV2)]);letelement2 : integer = UInt(operand2[(2*e +0)*:(esize DIV2)]);letres : integer = element1 * element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UMULLT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMULLT--indexed---Unsigned-multiply-long-by-indexed-element--top--?lang=en",
    "description": "Unsigned multiply long by indexed element (top) This instruction multiplies the odd-numbered unsigned elements within each 128-bit segment of the first source vector by the specified unsigned element in the corresponding second source vector segment, and places the results in the overlapping double-width elements of the destination vector register. The elements within the second source vector are specified using an immediate index that selects the same element position within each 128-bit vector segment. The index range is from 0 to one less than the number of elements per 128-bit segment. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UMULLT<Zd>.D,<Zn>.S,<Zm>.S[<imm>]"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);leteltspersegment : integer =128DIV (2* esize);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1dolets : integer = e - (e MOD eltspersegment);letelement1 : integer = UInt(operand1[(2* e + sel)*:esize]);letelement2 : integer = UInt(operand2[(2* s + index)*:esize]);letres : integer = element1 * element2;\n    result[e*:(2*esize)] = res[2*esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UMULLT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UMULLT--vectors---Unsigned-multiply-long--top--?lang=en",
    "description": "Unsigned multiply long (top) This instruction multiplies the odd-numbered unsigned elements of the first source vector by the corresponding elements of the second source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "UMULLT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[(2*e +1)*:(esize DIV2)]);letelement2 : integer = UInt(operand2[(2*e +1)*:(esize DIV2)]);letres : integer = element1 * element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UQADD (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQADD--immediate---Unsigned-saturating-add-immediate--unpredicated--?lang=en",
    "description": "Unsigned saturating add immediate (unpredicated) This instruction performs an unsigned saturating addition of an unsigned immediate to each element of the source vector, and destructively places the results in the corresponding elements of the source vector. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. This instruction is unpredicated. The immediate is an unsigned value in the range 0 to 255, and for element widths of 16 bits or higher it may also be a positive multiple of 256 in the range 256 to 65280. The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\".",
    "syntax": [
      "UQADD<Zdn>.<T>,<Zdn>.<T>, #<imm>{,<shift>}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);\n    (result[e*:esize], -) =SatQ{esize}(element1 + imm, unsigned);\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQADD (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQADD--vectors--predicated---Unsigned-saturating-add--predicated--?lang=en",
    "description": "Unsigned saturating add (predicated) This instruction adds active unsigned elements of the first source vector to corresponding unsigned elements of the second source vector, and destructively places the results in the corresponding elements of the first source vector. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UQADD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =UnsignedSat{esize}(element1 + element2);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQADD (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQADD--vectors--unpredicated---Unsigned-saturating-add--unpredicated--?lang=en",
    "description": "Unsigned saturating add (unpredicated) This instruction performs an unsigned saturating addition of all elements of the first source vector to the corresponding elements of the second source vector, and places the results in the corresponding elements of the destination vector. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. This instruction is unpredicated.",
    "syntax": [
      "UQADD<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letop2elt : bits(esize) = operand2[e*:esize];ifunsigned thenletelement1 : integer = UInt(op1elt);letelement2 : integer = UInt(op2elt);\n        (result[e*:esize], -) =UnsignedSatQ{esize}(element1 + element2);elseletelement1 : integer = SInt(op1elt);letelement2 : integer = SInt(op2elt);\n        (result[e*:esize], -) =SignedSatQ{esize}(element1 + element2);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UQCVTN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQCVTN--Unsigned-32-bit-integer-saturating-extract-narrow-to-interleaved-16-bit-integer-?lang=en",
    "description": "Unsigned 32-bit integer saturating extract narrow to interleaved 16-bit integer This instruction saturates the unsigned integer value in each element of the pair of source vectors to half the original source element width, and places the two-way interleaved results in the half-width destination elements. This instruction is unpredicated.",
    "syntax": [
      "UQCVTN<Zd>.H, {<Zn1>.S-<Zn2>.S }"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);varresult : bits(VL);fore =0to elements-1dofori =0to1doletoperand : bits(VL) = Z{}(n+i);letelement : integer = UInt(operand[e*:(2* esize)]);\n        result[(2*e + i)*:esize] =UnsignedSat{esize}(element);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UQDECB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQDECB--Unsigned-saturating-decrement-scalar-by-multiple-of-8-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating decrement scalar by multiple of 8-bit predicate constraint element count This instruction determines the number of active 8-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement the scalar destination. The result is saturated to the general-purpose register's unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UQDECB<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) - (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) - (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "UQDECD (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQDECD--scalar---Unsigned-saturating-decrement-scalar-by-multiple-of-64-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating decrement scalar by multiple of 64-bit predicate constraint element count This instruction determines the number of active 64-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement the scalar destination. The result is saturated to the general-purpose register's unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UQDECD<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) - (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) - (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "UQDECD (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQDECD--vector---Unsigned-saturating-decrement-vector-by-multiple-of-64-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating decrement vector by multiple of 64-bit predicate constraint element count This instruction determines the number of active 64-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement all destination vector elements. The results are saturated to the 64-bit unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception.",
    "syntax": [
      "UQDECD<Zdn>.D{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(op1elt) - (count * imm));else(result[e*:esize], -) =SignedSatQ{esize}(SInt(op1elt) - (count * imm));\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQDECH (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQDECH--scalar---Unsigned-saturating-decrement-scalar-by-multiple-of-16-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating decrement scalar by multiple of 16-bit predicate constraint element count This instruction determines the number of active 16-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement the scalar destination. The result is saturated to the general-purpose register's unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UQDECH<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) - (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) - (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "UQDECH (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQDECH--vector---Unsigned-saturating-decrement-vector-by-multiple-of-16-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating decrement vector by multiple of 16-bit predicate constraint element count This instruction determines the number of active 16-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement all destination vector elements. The results are saturated to the 16-bit unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception.",
    "syntax": [
      "UQDECH<Zdn>.H{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(op1elt) - (count * imm));else(result[e*:esize], -) =SignedSatQ{esize}(SInt(op1elt) - (count * imm));\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQDECP (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQDECP--scalar---Unsigned-saturating-decrement-scalar-by-count-of-true-predicate-elements-?lang=en",
    "description": "Unsigned saturating decrement scalar by count of true predicate elements This instruction determines the number of true elements in the source predicate and then uses the result to decrement the scalar destination. The result is saturated to the general-purpose register's unsigned integer range. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UQDECP<Xdn>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(ssize) = X{}(dn);letoperand2 : bits(PL) = P{}(m);varresult : bits(ssize);varcount : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(operand2, e, esize) then\n        count = count +1;\n    end;\nend;ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) - count);X{64}(dn) = ZeroExtend{64}(result);else(result, -) =SignedSatQ{ssize}(SInt(operand1) - count);X{64}(dn) = SignExtend{64}(result);\nend;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "UQDECP (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQDECP--vector---Unsigned-saturating-decrement-vector-by-count-of-true-predicate-elements-?lang=en",
    "description": "Unsigned saturating decrement vector by count of true predicate elements This instruction determines the number of true elements in the source predicate and then uses the result to decrement all destination vector elements. The results are saturated to the element unsigned integer range. The predicate size specifier may be omitted in assembler source code, but this is deprecated and will be prohibited in a future release of the architecture.",
    "syntax": [
      "UQDECP<Zdn>.<T>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(PL) = P{}(m);varresult : bits(VL);varcount : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(operand2, e, esize) then\n        count = count +1;\n    end;\nend;fore =0to elements-1doletopelt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(opelt) - count);else(result[e*:esize], -) =SignedSatQ{esize}(SInt(opelt) - count);\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQDECW (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQDECW--scalar---Unsigned-saturating-decrement-scalar-by-multiple-of-32-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating decrement scalar by multiple of 32-bit predicate constraint element count This instruction determines the number of active 32-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement the scalar destination. The result is saturated to the general-purpose register's unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UQDECW<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) - (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) - (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "UQDECW (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQDECW--vector---Unsigned-saturating-decrement-vector-by-multiple-of-32-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating decrement vector by multiple of 32-bit predicate constraint element count This instruction determines the number of active 32-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to decrement all destination vector elements. The results are saturated to the 32-bit unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception.",
    "syntax": [
      "UQDECW<Zdn>.S{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(op1elt) - (count * imm));else(result[e*:esize], -) =SignedSatQ{esize}(SInt(op1elt) - (count * imm));\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQINCB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQINCB--Unsigned-saturating-increment-scalar-by-multiple-of-8-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating increment scalar by multiple of 8-bit predicate constraint element count This instruction determines the number of active 8-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment the scalar destination. The result is saturated to the general-purpose register's unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UQINCB<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) + (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) + (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "UQINCD (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQINCD--scalar---Unsigned-saturating-increment-scalar-by-multiple-of-64-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating increment scalar by multiple of 64-bit predicate constraint element count This instruction determines the number of active 64-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment the scalar destination. The result is saturated to the general-purpose register's unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UQINCD<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) + (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) + (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "UQINCD (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQINCD--vector---Unsigned-saturating-increment-vector-by-multiple-of-64-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating increment vector by multiple of 64-bit predicate constraint element count This instruction determines the number of active 64-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment all destination vector elements. The results are saturated to the 64-bit unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception.",
    "syntax": [
      "UQINCD<Zdn>.D{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(op1elt) + (count * imm));else(result[e*:esize], -) =SignedSatQ{esize}(SInt(op1elt) + (count * imm));\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQINCH (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQINCH--scalar---Unsigned-saturating-increment-scalar-by-multiple-of-16-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating increment scalar by multiple of 16-bit predicate constraint element count This instruction determines the number of active 16-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment the scalar destination. The result is saturated to the general-purpose register's unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UQINCH<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) + (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) + (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "UQINCH (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQINCH--vector---Unsigned-saturating-increment-vector-by-multiple-of-16-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating increment vector by multiple of 16-bit predicate constraint element count This instruction determines the number of active 16-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment all destination vector elements. The results are saturated to the 16-bit unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception.",
    "syntax": [
      "UQINCH<Zdn>.H{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(op1elt) + (count * imm));else(result[e*:esize], -) =SignedSatQ{esize}(SInt(op1elt) + (count * imm));\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQINCP (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQINCP--scalar---Unsigned-saturating-increment-scalar-by-count-of-true-predicate-elements-?lang=en",
    "description": "Unsigned saturating increment scalar by count of true predicate elements This instruction determines the number of true elements in the source predicate and then uses the result to increment the scalar destination. The result is saturated to the general-purpose register's unsigned integer range. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UQINCP<Xdn>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(ssize) = X{}(dn);letoperand2 : bits(PL) = P{}(m);varresult : bits(ssize);varcount : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(operand2, e, esize) then\n        count = count +1;\n    end;\nend;ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) + count);X{64}(dn) = ZeroExtend{64}(result);else(result, -) =SignedSatQ{ssize}(SInt(operand1) + count);X{64}(dn) = SignExtend{64}(result);\nend;",
    "operational_info": "If FEAT_SME is implemented and the PE is in Streaming SVE mode, then any subsequent instruction which is dependent on the general-purpose register written by this instruction might be significantly delayed."
  },
  {
    "title": "UQINCP (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQINCP--vector---Unsigned-saturating-increment-vector-by-count-of-true-predicate-elements-?lang=en",
    "description": "Unsigned saturating increment vector by count of true predicate elements This instruction determines the number of true elements in the source predicate and then uses the result to increment all destination vector elements. The results are saturated to the element unsigned integer range. The predicate size specifier may be omitted in assembler source code, but this is deprecated and will be prohibited in a future release of the architecture.",
    "syntax": [
      "UQINCP<Zdn>.<T>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(PL) = P{}(m);varresult : bits(VL);varcount : integer =0;fore =0to elements-1doifActivePredicateElement{PL}(operand2, e, esize) then\n        count = count +1;\n    end;\nend;fore =0to elements-1doletopelt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(opelt) + count);else(result[e*:esize], -) =SignedSatQ{esize}(SInt(opelt) + count);\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQINCW (scalar)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQINCW--scalar---Unsigned-saturating-increment-scalar-by-multiple-of-32-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating increment scalar by multiple of 32-bit predicate constraint element count This instruction determines the number of active 32-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment the scalar destination. The result is saturated to the general-purpose register's unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception. It has encodings from 2 classes:32-bitand64-bit",
    "syntax": [
      "UQINCW<Xdn>{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(ssize) = X{}(dn);varresult : bits(ssize);ifunsignedthen(result, -)=UnsignedSatQ{ssize}(UInt(operand1) + (count * imm));else(result, -) =SignedSatQ{ssize}(SInt(operand1) + (count * imm));\nend;X{64}(dn) = Extend{64}(result, unsigned);",
    "operational_info": ""
  },
  {
    "title": "UQINCW (vector)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQINCW--vector---Unsigned-saturating-increment-vector-by-multiple-of-32-bit-predicate-constraint-element-count-?lang=en",
    "description": "Unsigned saturating increment vector by multiple of 32-bit predicate constraint element count This instruction determines the number of active 32-bit elements implied by the named predicate constraint, multiplies that by an immediate in the range 1 to 16 inclusive, and then uses the result to increment all destination vector elements. The results are saturated to the 32-bit unsigned integer range. The named predicate constraint limits the number of active elements in a single predicate to: A fixed number (VL1 to VL256)The largest power of two (POW2)The largest multiple of three or four (MUL3 or MUL4)All available, implicitly a multiple of two (ALL). Unspecified or out of range constraint encodings generate an empty predicate or zero element count rather than Undefined Instruction exception.",
    "syntax": [
      "UQINCW<Zdn>.S{,<pattern>{, MUL #<imm>}}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letcount : integer =DecodePredCount(pat, esize);letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];ifunsignedthen(result[e*:esize], -)=UnsignedSatQ{esize}(UInt(op1elt) + (count * imm));else(result[e*:esize], -) =SignedSatQ{esize}(SInt(op1elt) + (count * imm));\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQRSHL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQRSHL--Unsigned-saturating-rounding-shift-left--predicated--?lang=en",
    "description": "Unsigned saturating rounding shift left (predicated) This instruction shifts active unsigned elements of the first source vector by corresponding elements of the second source vector and destructively places the rounded results in the corresponding elements of the first source vector. A positive shift amount performs a left shift, otherwise a right shift by the negated shift amount is performed. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UQRSHL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = UInt(operand1[e*:esize]);varshift : integer =ShiftSat(SInt(operand2[e*:esize]), esize);varres : integer;ifshift >=0then\n            res = element << shift;elseshift = -shift;\n            res = (element + (1<< (shift -1))) >> shift;\n        end;\n        result[e*:esize] =UnsignedSat{esize}(res);elseresult[e*:esize] = operand1[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQRSHLR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQRSHLR--Unsigned-saturating-rounding-shift-left-reversed--predicated--?lang=en",
    "description": "Unsigned saturating rounding shift left reversed (predicated) This instruction shifts active unsigned elements of the second source vector by corresponding elements of the first source vector and destructively places the rounded results in the corresponding elements of the first source vector. A positive shift amount performs a left shift, otherwise a right shift by the negated shift amount is performed. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UQRSHLR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letoperand2 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = UInt(operand1[e*:esize]);varshift : integer =ShiftSat(SInt(operand2[e*:esize]), esize);varres : integer;ifshift >=0then\n            res = element << shift;elseshift = -shift;\n            res = (element + (1<< (shift -1))) >> shift;\n        end;\n        result[e*:esize] =UnsignedSat{esize}(res);elseresult[e*:esize] = operand2[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQRSHRN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQRSHRN--Unsigned-saturating-rounding-shift-right-narrow-by-immediate-to-interleaved-integer-?lang=en",
    "description": "Unsigned saturating rounding shift right narrow by immediate to interleaved integer This instruction shifts right by an immediate value the unsigned integer value in each element of the two source vectors, and places the two-way interleaved rounded results in the half-width destination elements. Each result element is saturated to the half-width N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per destination element. This instruction is unpredicated. It has encodings from 2 classes:8-bitand16-bit",
    "syntax": [
      "UQRSHRN<Zd>.H, {<Zn1>.S-<Zn2>.S }, #<const>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);varresult : bits(VL);fore =0to elements-1dofori =0to1doletoperand : bits(VL) = Z{}(n+i);letelement : bits(2* esize) = operand[e*:(2* esize)];letres : integer = (UInt(element) + (1<< (shift-1))) >> shift;\n        result[(2*e + i)*:esize] =UnsignedSat{esize}(res);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UQRSHRNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQRSHRNB--Unsigned-saturating-rounding-shift-right-narrow-by-immediate--bottom--?lang=en",
    "description": "Unsigned saturating rounding shift right narrow by immediate (bottom) This instruction shifts each unsigned integer value in the source vector elements right by an immediate value, and places the rounded results in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero. Each result element is saturated to the half-width N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "UQRSHRNB<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = (UInt(element) + (1<< (shift-1))) >> shift;\n    result[(2*e +0)*:esize] =UnsignedSat{esize}(res);\n    result[(2*e +1)*:esize] = Zeros{esize};\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UQRSHRNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQRSHRNT--Unsigned-saturating-rounding-shift-right-narrow-by-immediate--top--?lang=en",
    "description": "Unsigned saturating rounding shift right narrow by immediate (top) This instruction shifts each unsigned integer value in the source vector elements by an immediate value, and places the rounded results in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged. Each result element is saturated to the half-width N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "UQRSHRNT<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = (UInt(element) + (1<< (shift-1))) >> shift;\n    result[(2*e +1)*:esize] =UnsignedSat{esize}(res);\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UQSHL (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQSHL--immediate---Unsigned-saturating-shift-left-by-immediate-?lang=en",
    "description": "Unsigned saturating shift left by immediate This instruction shifts left by immediate each active unsigned element of the source vector, and destructively places the results in the corresponding elements of the source vector. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 0 to number of bits per element minus 1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UQSHL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);letmask : bits(PL) = P{}(g);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = element1 << shift;\n        result[e*:esize] =UnsignedSat{esize}(res);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQSHL (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQSHL--vectors---Unsigned-saturating-shift-left--predicated--?lang=en",
    "description": "Unsigned saturating shift left (predicated) This instruction shifts active unsigned elements of the first source vector by corresponding elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. A positive shift amount performs a left shift, otherwise a right shift by the negated shift amount is performed. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UQSHL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = UInt(operand1[e*:esize]);varshift : integer =ShiftSat(SInt(operand2[e*:esize]), esize);varres : integer;ifshift >=0then\n            res = element << shift;elseshift = -shift;\n            res = element >> shift;\n        end;\n        result[e*:esize] =UnsignedSat{esize}(res);elseresult[e*:esize] = operand1[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQSHLR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQSHLR--Unsigned-saturating-shift-left-reversed--predicated--?lang=en",
    "description": "Unsigned saturating shift left reversed (predicated) This instruction shifts active unsigned elements of the second source vector by corresponding elements of the first source vector and destructively places the results in the corresponding elements of the first source vector. A positive shift amount performs a left shift, otherwise a right shift by the negated shift amount is performed. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UQSHLR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letoperand2 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = UInt(operand1[e*:esize]);varshift : integer =ShiftSat(SInt(operand2[e*:esize]), esize);varres : integer;ifshift >=0then\n            res = element << shift;elseshift = -shift;\n            res = element >> shift;\n        end;\n        result[e*:esize] =UnsignedSat{esize}(res);elseresult[e*:esize] = operand2[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQSHRN",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQSHRN--Unsigned-saturating-shift-right-narrow-by-immediate-to-interleaved-integer-?lang=en",
    "description": "Unsigned saturating shift right narrow by immediate to interleaved integer This instruction shifts right by an immediate value the unsigned integer value in each element of the two source vectors, and places the two-way interleaved truncated results in the half-width destination elements. Each result element is saturated to the half-width N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per destination element. This instruction is unpredicated.",
    "syntax": [
      "UQSHRN<Zd>.<T>, {<Zn1>.<Tb>-<Zn2>.<Tb>}, #<const>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);varresult : bits(VL);fore =0to elements-1dofori =0to1doletoperand : bits(VL) = Z{}(n+i);letelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = UInt(element) >> shift;\n        result[(2*e + i)*:esize] =UnsignedSat{esize}(res);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UQSHRNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQSHRNB--Unsigned-saturating-shift-right-narrow-by-immediate--bottom--?lang=en",
    "description": "Unsigned saturating shift right narrow by immediate (bottom) This instruction shifts each unsigned integer value in the source vector elements right by an immediate value, and places the truncated results in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero. Each result element is saturated to the half-width N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "UQSHRNB<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = UInt(element) >> shift;\n    result[(2*e +0)*:esize] =UnsignedSat{esize}(res);\n    result[(2*e +1)*:esize] = Zeros{esize};\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UQSHRNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQSHRNT--Unsigned-saturating-shift-right-narrow-by-immediate--top--?lang=en",
    "description": "Unsigned saturating shift right narrow by immediate (top) This instruction shifts each unsigned integer value in the source vector elements right by an immediate value, and places the truncated results in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged. Each result element is saturated to the half-width N-bit element's unsigned integer range 0 to (2N)-1. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "UQSHRNT<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);fore =0to elements-1doletelement : bits(2*esize) = operand[e*:(2*esize)];letres : integer = UInt(element) >> shift;\n    result[(2*e +1)*:esize] =UnsignedSat{esize}(res);\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UQSUB (immediate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQSUB--immediate---Unsigned-saturating-subtract-immediate--unpredicated--?lang=en",
    "description": "Unsigned saturating subtract immediate (unpredicated) This instruction performs an unsigned saturating subtraction of an unsigned immediate from each element of the source vector, and destructively places the results in the corresponding elements of the source vector. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. This instruction is unpredicated. The immediate is an unsigned value in the range 0 to 255, and for element widths of 16 bits or higher it may also be a positive multiple of 256 in the range 256 to 65280. The immediate is encoded in 8 bits with an optional left shift by 8. The preferred disassembly when the shift option is specified is \"#<imm>, LSL #8\". However an assembler and disassembler may also allow use of the shifted 16-bit value unless the immediate is 0 and the shift amount is 8, which must be unambiguously described as \"#0, LSL #8\".",
    "syntax": [
      "UQSUB<Zdn>.<T>,<Zdn>.<T>, #<imm>{,<shift>}"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);\n    (result[e*:esize], -) =SatQ{esize}(element1 - imm, unsigned);\n\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQSUB (vectors, predicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQSUB--vectors--predicated---Unsigned-saturating-subtract--predicated--?lang=en",
    "description": "Unsigned saturating subtract (predicated) This instruction subtracts active unsigned elements of the second source vector from corresponding unsigned elements of the first source vector and destructively places the results in the corresponding elements of the first source vector. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UQSUB<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =UnsignedSat{esize}(element1 - element2);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQSUB (vectors, unpredicated)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQSUB--vectors--unpredicated---Unsigned-saturating-subtract--unpredicated--?lang=en",
    "description": "Unsigned saturating subtract (unpredicated) This instruction performs an unsigned saturating subtraction of all elements of the second source vector from the corresponding elements of the first source vector, and places the results in the corresponding elements of the destination vector. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. This instruction is unpredicated.",
    "syntax": [
      "UQSUB<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize) = operand1[e*:esize];letop2elt : bits(esize) = operand2[e*:esize];ifunsigned thenletelement1 : integer = UInt(op1elt);letelement2 : integer = UInt(op2elt);\n        (result[e*:esize], -) =UnsignedSatQ{esize}(element1 - element2);elseletelement1 : integer = SInt(op1elt);letelement2 : integer = SInt(op2elt);\n        (result[e*:esize], -) =SignedSatQ{esize}(element1 - element2);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UQSUBR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQSUBR--Unsigned-saturating-subtract-reversed--predicated--?lang=en",
    "description": "Unsigned saturating subtract reversed (predicated) This instruction subtracts active unsigned elements of the first source vector from corresponding unsigned elements of the second source vector and destructively places the results in the corresponding elements of the first source vector. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "UQSUBR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =UnsignedSat{esize}(element2 - element1);elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "UQXTNB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQXTNB--Unsigned-saturating-extract-narrow--bottom--?lang=en",
    "description": "Unsigned saturating extract narrow (bottom) This instruction saturates the unsigned integer value in each source element to half the original source element width, and places the results in the even-numbered half-width destination elements, while setting the odd-numbered elements to zero.",
    "syntax": [
      "UQXTNB<Zd>.<T>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);varresult : bits(VL);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letres : bits(halfesize) =UnsignedSat{halfesize}(element1);\n    result[(2*e +0)*:halfesize] = res;\n    result[(2*e +1)*:halfesize] = Zeros{halfesize};\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UQXTNT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UQXTNT--Unsigned-saturating-extract-narrow--top--?lang=en",
    "description": "Unsigned saturating extract narrow (top) This instruction saturates the unsigned integer value in each source element to half the original source element width, and places the results in the odd-numbered half-width destination elements, leaving the even-numbered elements unchanged.",
    "syntax": [
      "UQXTNT<Zd>.<T>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);varresult : bits(VL) = Z{}(d);lethalfesize : integer{} = esize DIV2;fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letres : bits(halfesize) =UnsignedSat{halfesize}(element1);\n    result[(2*e +1)*:halfesize] = res;\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "URECPE",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/URECPE--Unsigned-reciprocal-estimate--predicated--?lang=en",
    "description": "Unsigned reciprocal estimate (predicated) This instruction finds the approximate reciprocal of each active unsigned element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "URECPE<Zd>.S,<Pg>/Z,<Zn>.S"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =UnsignedRecipEstimate{esize}(element);\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "URHADD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/URHADD--Unsigned-rounding-halving-add-?lang=en",
    "description": "Unsigned rounding halving add This instruction adds active unsigned elements of the first source vector to corresponding unsigned elements of the second source vector, shifts right one bit, and destructively places the rounded results in the corresponding elements of the first source vector. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "URHADD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = (element1 + element2 +1) >>1;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "URSHL",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/URSHL--Unsigned-rounding-shift-left--predicated--?lang=en",
    "description": "Unsigned rounding shift left (predicated) This instruction shifts active unsigned elements of the first source vector by corresponding elements of the second source vector and destructively places the rounded results in the corresponding elements of the first source vector. A positive shift amount performs a left shift, otherwise a right shift by the negated shift amount is performed. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "URSHL<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = UInt(operand1[e*:esize]);varshift : integer =ShiftSat(SInt(operand2[e*:esize]), esize);varres : integer;ifshift >=0then\n            res = element << shift;elseshift = -shift;\n            res = (element + (1<< (shift -1))) >> shift;\n        end;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "URSHLR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/URSHLR--Unsigned-rounding-shift-left-reversed--predicated--?lang=en",
    "description": "Unsigned rounding shift left reversed (predicated) This instruction shifts active unsigned elements of the second source vector by corresponding elements of the first source vector and destructively places the rounded results in the corresponding elements of the first source vector. A positive shift amount performs a left shift, otherwise a right shift by the negated shift amount is performed. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "URSHLR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};letoperand2 : bits(VL) = Z{}(dn);varresult : bits(VL);fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : integer = UInt(operand1[e*:esize]);varshift : integer =ShiftSat(SInt(operand2[e*:esize]), esize);varres : integer;ifshift >=0then\n            res = element << shift;elseshift = -shift;\n            res = (element + (1<< (shift -1))) >> shift;\n        end;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand2[e*:esize];\n\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "URSHR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/URSHR--Unsigned-rounding-shift-right-by-immediate-?lang=en",
    "description": "Unsigned rounding shift right by immediate This instruction shifts right by immediate each active unsigned element of the source vector, and destructively places the rounded results in the corresponding elements of the source vector. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "URSHR<Zdn>.<T>,<Pg>/M,<Zdn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(dn);letmask : bits(PL) = P{}(g);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);ifActivePredicateElement{PL}(mask, e, esize) thenletres : integer = (element1 + (1<< (shift -1))) >> shift;\n        result[e*:esize] = res[esize-1:0];elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "URSQRTE",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/URSQRTE--Unsigned-reciprocal-square-root-estimate--predicated--?lang=en",
    "description": "Unsigned reciprocal square root estimate (predicated) This instruction finds the approximate reciprocal square root of each active unsigned element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 2 classes:MergingandZeroing",
    "syntax": [
      "URSQRTE<Zd>.S,<Pg>/Z,<Zn>.S"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] =UnsignedRSqrtEstimate{esize}(element);\n\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "For the \"Merging\" variant:\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "URSRA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/URSRA--Unsigned-rounding-shift-right-and-accumulate--immediate--?lang=en",
    "description": "Unsigned rounding shift right and accumulate (immediate) This instruction shifts right by immediate each unsigned element of the source vector, inserting zeroes, and destructively adds the rounded intermediate result to the corresponding elements of the addend vector. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "URSRA<Zda>.<T>,<Zn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement : integer = (UInt(operand1[e*:esize]) + (1<< (shift -1))) >> shift;\n    result[e*:esize] = operand2[e*:esize] + element[esize-1:0];\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "USDOT (indexed)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/USDOT--indexed---Unsigned-by-signed-8-bit-integer-dot-product-by-indexed-element-to-32-bit-integer-?lang=en",
    "description": "Unsigned by signed 8-bit integer dot product by indexed element to 32-bit integer This instruction calculates the dot product of a group of four unsigned 8-bit integer values held in each 32-bit element of the first source vector multiplied by a group of four signed 8-bit integer values in an indexed 32-bit element of the second source vector, and then destructively adds the widened dot product to the corresponding 32-bit element of the destination vector. The groups within the second source vector are specified using an immediate index that selects the same group position within each 128-bit vector segment. The index range is from 0 to 3. This instruction is unpredicated. ID_AA64ZFR0_EL1.I8MM indicates whether this instruction is implemented.",
    "syntax": [
      "USDOT<Zda>.S,<Zn>.B,<Zm>.B[<imm>]"
    ],
    "features": [
      "FEAT_I8MM",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;leteltspersegment : integer =128DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletsegmentbase : integer = e - (e MOD eltspersegment);lets : integer = segmentbase + index;varres : bits(esize) = operand3[e*:esize];fori =0to3doletelement1 : integer = UInt(operand1[(4* e + i)*:(esize DIV4)]);letelement2 : integer = SInt(operand2[(4* s + i)*:(esize DIV4)]);\n        res = res + element1 * element2;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "USDOT (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/USDOT--vectors---Unsigned-by-signed-8-bit-integer-dot-product-to-32-bit-integer-?lang=en",
    "description": "Unsigned by signed 8-bit integer dot product to 32-bit integer This instruction calculates the dot product of a group of four unsigned 8-bit integer values held in each 32-bit element of the first source vector multiplied by a group of four signed 8-bit integer values in the corresponding 32-bit element of the second source vector, and then destructively adds the widened dot product to the corresponding 32-bit element of the destination vector. This instruction is unpredicated. ID_AA64ZFR0_EL1.I8MM indicates whether this instruction is implemented.",
    "syntax": [
      "USDOT<Zda>.S,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_I8MM",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);letoperand3 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1dovarres : bits(esize) = operand3[e*:esize];fori =0to3doletelement1 : integer = UInt(operand1[(4* e + i)*:(esize DIV4)]);letelement2 : integer = SInt(operand2[(4* e + i)*:(esize DIV4)]);\n        res = res + element1 * element2;\n    end;\n    result[e*:esize] = res;\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "USHLLB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/USHLLB--Unsigned-shift-left-long-by-immediate--bottom--?lang=en",
    "description": "Unsigned shift left long by immediate (bottom) This instruction shifts left by immediate each even-numbered unsigned element of the source vector, and places the results in the overlapping double-width elements of the destination vector. The immediate shift amount is an unsigned value in the range 0 to number of bits per element minus 1. This instruction is unpredicated.",
    "syntax": [
      "USHLLB<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize) = operand[(2*e +0)*:esize];letshifted_value : integer = UInt(element) << shift;\n    result[e*:(2*esize)] = shifted_value[2*esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "USHLLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/USHLLT--Unsigned-shift-left-long-by-immediate--top--?lang=en",
    "description": "Unsigned shift left long by immediate (top) This instruction shifts left by immediate each odd-numbered unsigned element of the source vector, and places the results in the overlapping double-width elements of the destination vector. The immediate shift amount is an unsigned value in the range 0 to number of bits per element minus 1. This instruction is unpredicated.",
    "syntax": [
      "USHLLT<Zd>.<T>,<Zn>.<Tb>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer =VLDIV(2* esize);letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize) = operand[(2*e +1)*:esize];letshifted_value : integer = UInt(element) << shift;\n    result[e*:(2*esize)] = shifted_value[2*esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "USMMLA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/USMMLA--Unsigned-by-signed-8-bit-integer-matrix-multiply-accumulate-to-32-bit-integer-?lang=en",
    "description": "Unsigned by signed 8-bit integer matrix multiply-accumulate to 32-bit integer This instruction multiplies the 2×8 matrix of unsigned 8-bit integer values held in each 128-bit segment of the first source vector by the 8×2 matrix of signed 8-bit integer values in the corresponding segment of the second source vector. The resulting 2×2 widened 32-bit integer matrix product is then destructively added to the 32-bit integer matrix accumulator held in the corresponding segment of the addend and destination vector. This is equivalent to performing an 8-way dot product per destination element. This instruction is unpredicated. ID_AA64ZFR0_EL1.I8MM indicates whether this instruction is implemented. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "USMMLA<Zda>.S,<Zn>.B,<Zm>.B"
    ],
    "features": [
      "FEAT_I8MM",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letoperand1 :bits(VL)= Z{}(n);letoperand2 :bits(VL)= Z{}(m);letoperand3 :bits(VL)= Z{}(da);varresult :bits(VL)= Zeros{};varop1, op2 :bits(128);varres,addend:bits(128);fors =0to segments-1do\n    op1    = operand1[s*:(128)];op2    = operand2[s*:(128)];addend= operand3[s*:(128)];res    =MatMulAdd(addend,op1, op2, op1_unsigned, op2_unsigned);result[s*:(128)] = res;end;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "USQADD",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/USQADD--Unsigned-saturating-signed-add-?lang=en",
    "description": "Unsigned saturating signed add This instruction adds active signed elements of the source vector to the corresponding unsigned elements of the addend vector, and destructively places the results in the corresponding elements of the addend vector. Each result element is saturated to the N-bit element's unsigned integer range 0 to (2N)-1. Inactive elements in the destination vector register remain unmodified.",
    "syntax": [
      "USQADD<Zdn>.<T>,<Pg>/M,<Zdn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand1 : bits(VL) = Z{}(dn);letoperand2 : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(m)elseZeros{VL};varresult : bits(VL);fore =0to elements-1doletelement1 : bits(esize) = operand1[e*:esize];letelement2 : bits(esize) = operand2[e*:esize];ifActivePredicateElement{PL}(mask, e, esize) then\n        result[e*:esize] =UnsignedSat{esize}(UInt(element1) + SInt(element2));elseresult[e*:esize] = operand1[e*:esize];\n    end;\nend;Z{VL}(dn) = result;",
    "operational_info": "This instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of this instruction.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "USRA",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/USRA--Unsigned-shift-right-and-accumulate--immediate--?lang=en",
    "description": "Unsigned shift right and accumulate (immediate) This instruction shifts right by immediate each unsigned element of the source vector, inserting zeroes, and destructively adds the truncated intermediate result to the corresponding elements of the addend vector. The immediate shift amount is an unsigned value in the range 1 to number of bits per element. This instruction is unpredicated.",
    "syntax": [
      "USRA<Zda>.<T>,<Zn>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(da);varresult : bits(VL);fore =0to elements-1doletelement : integer = UInt(operand1[e*:esize]) >> shift;\n    result[e*:esize] = operand2[e*:esize] + element[esize-1:0];\nend;Z{VL}(da) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "USUBLB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/USUBLB--Unsigned-subtract-long--bottom--?lang=en",
    "description": "Unsigned subtract long (bottom) This instruction subtracts the even-numbered unsigned elements of the second source vector from the corresponding unsigned elements of the first source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "USUBLB<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize DIV2) = operand1[(2*e + sel1)*:(esize DIV2)];letop2elt : bits(esize DIV2) = operand2[(2*e + sel2)*:(esize DIV2)];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letres : integer = element1 - element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "USUBLT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/USUBLT--Unsigned-subtract-long--top--?lang=en",
    "description": "Unsigned subtract long (top) This instruction subtracts the odd-numbered unsigned elements of the second source vector from the corresponding unsigned elements of the first source vector, and places the results in the overlapping double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "USUBLT<Zd>.<T>,<Zn>.<Tb>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletop1elt : bits(esize DIV2) = operand1[(2*e + sel1)*:(esize DIV2)];letop2elt : bits(esize DIV2) = operand2[(2*e + sel2)*:(esize DIV2)];letelement1 : integer =ifunsigned thenUInt(op1elt) elseSInt(op1elt);letelement2 : integer =ifunsigned thenUInt(op2elt) elseSInt(op2elt);letres : integer = element1 - element2;\n    result[e*:esize] = res[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "USUBWB",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/USUBWB--Unsigned-subtract-wide--bottom--?lang=en",
    "description": "Unsigned subtract wide (bottom) This instruction subtracts the even-numbered unsigned elements of the second source vector from the overlapping double-width elements of the first source vector and places the results in the corresponding double-width elements of the destination vector. This instruction is unpredicated.",
    "syntax": [
      "USUBWB<Zd>.<T>,<Zn>.<T>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[(2*e +0)*:(esize DIV2)]);\n    result[e*:esize] = (element1 - element2)[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "USUBWT",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/USUBWT--Unsigned-subtract-wide--top--?lang=en",
    "description": "Unsigned subtract wide (top) This instruction subtracts the odd-numbered unsigned elements of the second source vector from the overlapping double-width elements of the first source vector and places the results in the corresponding double-width elements of the destination vector. This instruction is unpredicated. This instruction is unpredicated.",
    "syntax": [
      "USUBWT<Zd>.<T>,<Zn>.<T>,<Zm>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fore =0to elements-1doletelement1 : integer = UInt(operand1[e*:esize]);letelement2 : integer = UInt(operand2[(2*e +1)*:(esize DIV2)]);\n    result[e*:esize] = (element1 - element2)[esize-1:0];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UUNPKHI, UUNPKLO",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UUNPKHI--UUNPKLO--Unsigned-unpack-and-extend-half-of-vector-?lang=en",
    "description": "Unsigned unpack and extend half of vector This instruction unpacks and zero-extends elements from the lowest or highest half of the source vector, and writes the zero-extended values into double-width elements of the destination vector. This instruction is unpredicated. It has encodings from 2 classes:High halfandLow half",
    "syntax": [
      "UUNPKLO<Zd>.<T>,<Zn>.<Tb>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;lethsize : integer{} = esize DIV2;letoffset : integer =ifhi then elementselse0;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(hsize) = operand[(e + offset)*:hsize];\n    result[e*:esize] = Extend{esize}(element, unsigned);\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UXTB, UXTH, UXTW",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UXTB--UXTH--UXTW--Unsigned-byte---halfword---word-extend--predicated--?lang=en",
    "description": "Unsigned byte / halfword / word extend (predicated) This instruction zero-extends the least-significant sub-element of each active element of the source vector, and places the results in the corresponding elements of the destination vector. Inactive elements in the destination vector register remain unmodified or are set to zero, depending on whether merging or zeroing predication is selected. It has encodings from 6 classes:Byte, merging,Byte, zeroing,Halfword, merging,Halfword, zeroing,Word, mergingandWord, zeroing",
    "syntax": [
      "UXTW<Zd>.D,<Pg>/Z,<Zn>.D"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SME2",
      "FEAT_SVE",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = P{}(g);letoperand : bits(VL) =ifAnyActiveElement{PL}(mask, esize) thenZ{VL}(n)elseZeros{VL};varresult : bits(VL) =ifmerging thenZ{VL}(d)elseZeros{VL};fore =0to elements-1doifActivePredicateElement{PL}(mask, e, esize) thenletelement : bits(esize) = operand[e*:esize];\n        result[e*:esize] = Extend{esize}(element[s_esize-1:0], unsigned);\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThe merging variant of this instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand the merging variant of this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXcan be predicated or unpredicated.A predicatedMOVPRFXmust use the same governing predicate register as the merging variant this instruction.A predicatedMOVPRFXmust use the larger of the destination element size and first source element size in the preferred disassembly of the merging variant of this instruction.TheMOVPRFXmust specify the same destination register as the merging variant of this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of the merging variant of this instruction."
  },
  {
    "title": "UZP1, UZP2 (predicates)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UZP1--UZP2--predicates---Concatenate-even-or-odd-elements-from-two-predicates-?lang=en",
    "description": "Concatenate even or odd elements from two predicates This instruction concatenates adjacent even or odd-numbered elements from the first and second source predicates, and places the values in the destination predicate. This instruction is unpredicated. It has encodings from 2 classes:EvenandOdd",
    "syntax": [
      "UZP2<Pd>.<T>,<Pn>.<T>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letpairs : integer =VLDIV(esize *2);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);forp =0to pairs -1doresult[p*:(esize DIV8)] = operand1[(2*p+part)*:(esize DIV8)];\nend;forp =0to pairs -1doresult[(pairs+p)*:(esize DIV8)] = operand2[(2*p+part)*:(esize DIV8)];\nend;P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "UZP1, UZP2 (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UZP1--UZP2--vectors---Concatenate-even-or-odd-elements-from-two-vectors-?lang=en",
    "description": "Concatenate even or odd elements from two vectors This instruction concatenates adjacent even or odd-numbered elements from the first and second source vectors, and places the values in the destination vector. This instruction is unpredicated. Note: UZP1 is equivalent to truncating and packing each element from two source vectors into a single destination vector with elements of half the size. The 128-bit element variant requires that the Effective SVE vector length is at least 256 bits. ID_AA64ZFR0_EL1.F64MM indicates whether the 128-bit element variant is implemented. The 128-bit element variant is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 4 classes:Even,Even (quadwords),OddandOdd (quadwords)",
    "syntax": [
      "UZP2<Zd>.Q,<Zn>.Q,<Zm>.Q"
    ],
    "features": [
      "FEAT_F64MM",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "ifesize <128thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();ifVL < esize *2thenEndOfDecode(Decode_UNDEF); end;letpairs : integer =VLDIV(esize *2);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Zeros{};forp =0to pairs -1doresult[p*:esize] = operand1[(2*p+part)*:esize];\nend;forp =0to pairs -1doresult[(pairs+p)*:esize] = operand2[(2*p+part)*:esize];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UZPQ1",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UZPQ1--Concatenate-even-elements-within-each-pair-of-quadword-vector-segments-?lang=en",
    "description": "Concatenate even elements within each pair of quadword vector segments This instruction concatenates adjacent even-numbered elements from the corresponding 128-bit vector segments of the first and second source vectors, and places them in the corresponding elements of the destination vector segment. This instruction is unpredicated.",
    "syntax": [
      "UZPQ1<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letelements : integer =128DIV esize;letpairs : integer = elements DIV2;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fors =0to segments-1doletsoff : integer = s * elements;forp =0to pairs-1doresult[(soff + p)*:esize] = operand1[(soff +2* p + part)*:esize];\n    end;forp =0to pairs-1doresult[(soff + pairs + p)*:esize] = operand2[(soff +2* p + part)*:esize];\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "UZPQ2",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/UZPQ2--Concatenate-odd-elements-within-each-pair-of-quadword-vector-segments-?lang=en",
    "description": "Concatenate odd elements within each pair of quadword vector segments This instruction concatenates adjacent odd-numbered elements from the corresponding 128-bit vector segments of the first and second source vectors, and places them in the corresponding elements of the destination vector segment. This instruction is unpredicated.",
    "syntax": [
      "UZPQ2<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letelements : integer =128DIV esize;letpairs : integer = elements DIV2;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fors =0to segments-1doletsoff : integer = s * elements;forp =0to pairs-1doresult[(soff + p)*:esize] = operand1[(soff +2* p + part)*:esize];\n    end;forp =0to pairs-1doresult[(soff + pairs + p)*:esize] = operand2[(soff +2* p + part)*:esize];\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "WHILEGE (predicate as counter)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEGE--predicate-as-counter---While-decrementing-signed-scalar-greater-than-or-equal-to-scalar--predicate-as-counter--?lang=en",
    "description": "While decrementing signed scalar greater than or equal to scalar (predicate-as-counter) This instruction generates a predicate for a group of two or four vectors that starting from the highest numbered element of the group is true while the decrementing value of the first, signed scalar operand is greater than or equal to the second scalar operand and false thereafter down to the lowest numbered element of the group. If the second scalar operand is equal to the minimum signed integer value, then a condition that includes an equality test can never fail and the result will be an all-true predicate. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is decremented by one for each destination predicate element, irrespective of the predicate result element size. The predicate result is placed in the predicate destination register using the predicate-as-counter encoding. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEGE<PNd>.<T>,<Xn>,<Xm>,<vl>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = width * (VL DIV esize);varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;varcount : integer =0;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore = elements-1downto0dovarcond : boolean;letelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);caseop ofwhenCmp_GT=> cond = (element1 >  element2);whenCmp_GE=> cond = (element1 >= element2);\n    end;\n\n    last = last && cond;iflast then count = count +1; end;\n    operand1 = operand1 -1;\nend;\n\nresult =EncodePredCount{PL}(esize, elements, count, invert);\nPSTATE.[N,Z,C,V] =PredCountTest(elements, count, invert);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILEGE (predicate pair)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEGE--predicate-pair---While-decrementing-signed-scalar-greater-than-or-equal-to-scalar--pair-of-predicates--?lang=en",
    "description": "While decrementing signed scalar greater than or equal to scalar (pair of predicates) This instruction generates a pair of predicates that starting from the highest numbered element of the pair is true while the decrementing value of the first, signed scalar operand is greater than or equal to the second scalar operand and false thereafter down to the lowest numbered element of the pair. If the second scalar operand is equal to the minimum signed integer value, then a condition that includes an equality test can never fail and the result will be an all-true predicate. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is decremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The lower-numbered elements are placed in the first predicate destination register, and the higher-numbered elements in the second predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEGE {<Pd1>.<T>,<Pd2>.<T>},<Xn>,<Xm>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL*2) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL*2);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore = (elements*2)-1downto0doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_GT=> cond = (element1 >  element2);whenCmp_GE=> cond = (element1 >= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 -1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL*2}(mask, result, esize);P{PL}(d0) = result[PL-1:0];P{PL}(d1) = result[PL*2-1:PL];",
    "operational_info": ""
  },
  {
    "title": "WHILEGE (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEGE--predicate---While-decrementing-signed-scalar-greater-than-or-equal-to-scalar-?lang=en",
    "description": "While decrementing signed scalar greater than or equal to scalar This instruction generates a predicate that starting from the highest numbered element is true while the decrementing value of the first, signed scalar operand is greater than or equal to the second scalar operand and false thereafter down to the lowest numbered element. If the second scalar operand is equal to the minimum signed integer value, then a condition that includes an equality test can never fail and the result will be an all-true predicate. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is decremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The predicate result is placed in the predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEGE<Pd>.<T>,<R><n>,<R><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore = elements-1downto0doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_GT=> cond = (element1 >  element2);whenCmp_GE=> cond = (element1 >= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 -1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILEGT (predicate as counter)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEGT--predicate-as-counter---While-decrementing-signed-scalar-greater-than-scalar--predicate-as-counter--?lang=en",
    "description": "While decrementing signed scalar greater than scalar (predicate-as-counter) This instruction generates a predicate for a group of two or four vectors that starting from the highest numbered element of the group is true while the decrementing value of the first, signed scalar operand is greater than the second scalar operand and false thereafter down to the lowest numbered element of the group. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is decremented by one for each destination predicate element, irrespective of the predicate result element size. The predicate result is placed in the predicate destination register using the predicate-as-counter encoding. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEGT<PNd>.<T>,<Xn>,<Xm>,<vl>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = width * (VL DIV esize);varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;varcount : integer =0;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore = elements-1downto0dovarcond : boolean;letelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);caseop ofwhenCmp_GT=> cond = (element1 >  element2);whenCmp_GE=> cond = (element1 >= element2);\n    end;\n\n    last = last && cond;iflast then count = count +1; end;\n    operand1 = operand1 -1;\nend;\n\nresult =EncodePredCount{PL}(esize, elements, count, invert);\nPSTATE.[N,Z,C,V] =PredCountTest(elements, count, invert);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILEGT (predicate pair)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEGT--predicate-pair---While-decrementing-signed-scalar-greater-than-scalar--pair-of-predicates--?lang=en",
    "description": "While decrementing signed scalar greater than scalar (pair of predicates) This instruction generates a pair of predicates that starting from the highest numbered element of the pair is true while the decrementing value of the first, signed scalar operand is greater than the second scalar operand and false thereafter down to the lowest numbered element of the pair. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is decremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The lower-numbered elements are placed in the first predicate destination register, and the higher-numbered elements in the second predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEGT {<Pd1>.<T>,<Pd2>.<T>},<Xn>,<Xm>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL*2) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL*2);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore = (elements*2)-1downto0doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_GT=> cond = (element1 >  element2);whenCmp_GE=> cond = (element1 >= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 -1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL*2}(mask, result, esize);P{PL}(d0) = result[PL-1:0];P{PL}(d1) = result[PL*2-1:PL];",
    "operational_info": ""
  },
  {
    "title": "WHILEGT (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEGT--predicate---While-decrementing-signed-scalar-greater-than-scalar-?lang=en",
    "description": "While decrementing signed scalar greater than scalar This instruction generates a predicate that starting from the highest numbered element is true while the decrementing value of the first, signed scalar operand is greater than the second scalar operand and false thereafter down to the lowest numbered element. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is decremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The predicate result is placed in the predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEGT<Pd>.<T>,<R><n>,<R><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore = elements-1downto0doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_GT=> cond = (element1 >  element2);whenCmp_GE=> cond = (element1 >= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 -1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILEHI (predicate as counter)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEHI--predicate-as-counter---While-decrementing-unsigned-scalar-higher-than-scalar--predicate-as-counter--?lang=en",
    "description": "While decrementing unsigned scalar higher than scalar (predicate-as-counter) This instruction generates a predicate for a group of two or four vectors that starting from the highest numbered element of the group is true while the decrementing value of the first, unsigned scalar operand is higher than the second scalar operand and false thereafter down to the lowest numbered element of the group. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is decremented by one for each destination predicate element, irrespective of the predicate result element size. The predicate result is placed in the predicate destination register using the predicate-as-counter encoding. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEHI<PNd>.<T>,<Xn>,<Xm>,<vl>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = width * (VL DIV esize);varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;varcount : integer =0;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore = elements-1downto0dovarcond : boolean;letelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);caseop ofwhenCmp_GT=> cond = (element1 >  element2);whenCmp_GE=> cond = (element1 >= element2);\n    end;\n\n    last = last && cond;iflast then count = count +1; end;\n    operand1 = operand1 -1;\nend;\n\nresult =EncodePredCount{PL}(esize, elements, count, invert);\nPSTATE.[N,Z,C,V] =PredCountTest(elements, count, invert);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILEHI (predicate pair)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEHI--predicate-pair---While-decrementing-unsigned-scalar-higher-than-scalar--pair-of-predicates--?lang=en",
    "description": "While decrementing unsigned scalar higher than scalar (pair of predicates) This instruction generates a pair of predicates that starting from the highest numbered element of the pair is true while the decrementing value of the first, unsigned scalar operand is higher than the second scalar operand and false thereafter down to the lowest numbered element of the pair. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is decremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The lower-numbered elements are placed in the first predicate destination register, and the higher-numbered elements in the second predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEHI {<Pd1>.<T>,<Pd2>.<T>},<Xn>,<Xm>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL*2) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL*2);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore = (elements*2)-1downto0doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_GT=> cond = (element1 >  element2);whenCmp_GE=> cond = (element1 >= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 -1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL*2}(mask, result, esize);P{PL}(d0) = result[PL-1:0];P{PL}(d1) = result[PL*2-1:PL];",
    "operational_info": ""
  },
  {
    "title": "WHILEHI (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEHI--predicate---While-decrementing-unsigned-scalar-higher-than-scalar-?lang=en",
    "description": "While decrementing unsigned scalar higher than scalar This instruction generates a predicate that starting from the highest numbered element is true while the decrementing value of the first, unsigned scalar operand is higher than the second scalar operand and false thereafter down to the lowest numbered element. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is decremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The predicate result is placed in the predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEHI<Pd>.<T>,<R><n>,<R><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore = elements-1downto0doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_GT=> cond = (element1 >  element2);whenCmp_GE=> cond = (element1 >= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 -1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILEHS (predicate as counter)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEHS--predicate-as-counter---While-decrementing-unsigned-scalar-higher-than-or-same-as-scalar--predicate-as-counter--?lang=en",
    "description": "While decrementing unsigned scalar higher than or same as scalar (predicate-as-counter) This instruction generates a predicate for a group of two or four vectors that starting from the highest numbered element of the group is true while the decrementing value of the first, unsigned scalar operand is higher than or the same as the second scalar operand and false thereafter down to the lowest numbered element of the group. If the second scalar operand is equal to the minimum unsigned integer value, then a condition that includes an equality test can never fail and the result will be an all-true predicate. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is decremented by one for each destination predicate element, irrespective of the predicate result element size. The predicate result is placed in the predicate destination register using the predicate-as-counter encoding. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEHS<PNd>.<T>,<Xn>,<Xm>,<vl>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = width * (VL DIV esize);varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;varcount : integer =0;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore = elements-1downto0dovarcond : boolean;letelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);caseop ofwhenCmp_GT=> cond = (element1 >  element2);whenCmp_GE=> cond = (element1 >= element2);\n    end;\n\n    last = last && cond;iflast then count = count +1; end;\n    operand1 = operand1 -1;\nend;\n\nresult =EncodePredCount{PL}(esize, elements, count, invert);\nPSTATE.[N,Z,C,V] =PredCountTest(elements, count, invert);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILEHS (predicate pair)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEHS--predicate-pair---While-decrementing-unsigned-scalar-higher-than-or-same-as-scalar--pair-of-predicates--?lang=en",
    "description": "While decrementing unsigned scalar higher than or same as scalar (pair of predicates) This instruction generates a pair of predicates that starting from the highest numbered element of the pair is true while the decrementing value of the first, unsigned scalar operand is higher than or the same as the second scalar operand and false thereafter down to the lowest numbered element of the pair. If the second scalar operand is equal to the minimum unsigned integer value, then a condition that includes an equality test can never fail and the result will be an all-true predicate. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is decremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The lower-numbered elements are placed in the first predicate destination register, and the higher-numbered elements in the second predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEHS {<Pd1>.<T>,<Pd2>.<T>},<Xn>,<Xm>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL*2) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL*2);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore = (elements*2)-1downto0doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_GT=> cond = (element1 >  element2);whenCmp_GE=> cond = (element1 >= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 -1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL*2}(mask, result, esize);P{PL}(d0) = result[PL-1:0];P{PL}(d1) = result[PL*2-1:PL];",
    "operational_info": ""
  },
  {
    "title": "WHILEHS (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEHS--predicate---While-decrementing-unsigned-scalar-higher-than-or-same-as-scalar-?lang=en",
    "description": "While decrementing unsigned scalar higher than or same as scalar This instruction generates a predicate that starting from the highest numbered element is true while the decrementing value of the first, unsigned scalar operand is higher than or the same as the second scalar operand and false thereafter down to the lowest numbered element. If the second scalar operand is equal to the minimum unsigned integer value, then a condition that includes an equality test can never fail and the result will be an all-true predicate. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is decremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The predicate result is placed in the predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEHS<Pd>.<T>,<R><n>,<R><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore = elements-1downto0doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_GT=> cond = (element1 >  element2);whenCmp_GE=> cond = (element1 >= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 -1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILELE (predicate as counter)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILELE--predicate-as-counter---While-incrementing-signed-scalar-less-than-or-equal-to-scalar--predicate-as-counter--?lang=en",
    "description": "While incrementing signed scalar less than or equal to scalar (predicate-as-counter) This instruction generates a predicate for a group of two or four vectors that starting from the lowest numbered element of the group is true while the incrementing value of the first, signed scalar operand is less than or equal to the second scalar operand and false thereafter up to the highest numbered element of the group. If the second scalar operand is equal to the maximum signed integer value, then a condition that includes an equality test can never fail and the result will be an all-true predicate. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is incremented by one for each destination predicate element, irrespective of the predicate result element size. The predicate result is placed in the predicate destination register using the predicate-as-counter encoding. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILELE<PNd>.<T>,<Xn>,<Xm>,<vl>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = width * (VL DIV esize);varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;varcount : integer =0;letop2val : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore =0to elements-1doletop1val : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_LT=> cond = (op1val <  op2val);whenCmp_LE=> cond = (op1val <= op2val);\n    end;\n\n    last = last && cond;iflast then count = count +1; end;\n    operand1 = operand1 +1;\nend;\n\nresult =EncodePredCount{PL}(esize, elements, count, invert);\nPSTATE.[N,Z,C,V] =PredCountTest(elements, count, invert);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILELE (predicate pair)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILELE--predicate-pair---While-incrementing-signed-scalar-less-than-or-equal-to-scalar--pair-of-predicates--?lang=en",
    "description": "While incrementing signed scalar less than or equal to scalar (pair of predicates) This instruction generates a pair of predicates that starting from the lowest numbered element of the pair is true while the incrementing value of the first, signed scalar operand is less than or equal to the second scalar operand and false thereafter up to the highest numbered element of the pair. If the second scalar operand is equal to the maximum signed integer value, then a condition that includes an equality test can never fail and the result will be an all-true predicate. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is incremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The lower-numbered elements are placed in the first predicate destination register, and the higher-numbered elements in the second predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILELE {<Pd1>.<T>,<Pd2>.<T>},<Xn>,<Xm>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL*2) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL*2);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore =0to (elements*2)-1doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_LT=> cond = (element1 <  element2);whenCmp_LE=> cond = (element1 <= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 +1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL*2}(mask, result, esize);P{PL}(d0) = result[PL-1:0];P{PL}(d1) = result[PL*2-1:PL];",
    "operational_info": ""
  },
  {
    "title": "WHILELE (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILELE--predicate---While-incrementing-signed-scalar-less-than-or-equal-to-scalar-?lang=en",
    "description": "While incrementing signed scalar less than or equal to scalar This instruction generates a predicate that starting from the lowest numbered element is true while the incrementing value of the first, signed scalar operand is less than or equal to the second scalar operand and false thereafter up to the highest numbered element. If the second scalar operand is equal to the maximum signed integer value, then a condition that includes an equality test can never fail and the result will be an all-true predicate. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is incremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The predicate result is placed in the predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILELE<Pd>.<T>,<R><n>,<R><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore =0to elements-1doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_LT=> cond = (element1 <  element2);whenCmp_LE=> cond = (element1 <= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 +1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILELO (predicate as counter)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILELO--predicate-as-counter---While-incrementing-unsigned-scalar-lower-than-scalar--predicate-as-counter--?lang=en",
    "description": "While incrementing unsigned scalar lower than scalar (predicate-as-counter) This instruction generates a predicate for a group of two or four vectors that starting from the lowest numbered element of the group is true while the incrementing value of the first, unsigned scalar operand is lower than the second scalar operand and false thereafter up to the highest numbered element of the group. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is incremented by one for each destination predicate element, irrespective of the predicate result element size. The predicate result is placed in the predicate destination register using the predicate-as-counter encoding. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILELO<PNd>.<T>,<Xn>,<Xm>,<vl>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = width * (VL DIV esize);varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;varcount : integer =0;letop2val : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore =0to elements-1doletop1val : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_LT=> cond = (op1val <  op2val);whenCmp_LE=> cond = (op1val <= op2val);\n    end;\n\n    last = last && cond;iflast then count = count +1; end;\n    operand1 = operand1 +1;\nend;\n\nresult =EncodePredCount{PL}(esize, elements, count, invert);\nPSTATE.[N,Z,C,V] =PredCountTest(elements, count, invert);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILELO (predicate pair)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILELO--predicate-pair---While-incrementing-unsigned-scalar-lower-than-scalar--pair-of-predicates--?lang=en",
    "description": "While incrementing unsigned scalar lower than scalar (pair of predicates) This instruction generates a pair of predicates that starting from the lowest numbered element of the pair is true while the incrementing value of the first, unsigned scalar operand is lower than the second scalar operand and false thereafter up to the highest numbered element of the pair. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is incremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The lower-numbered elements are placed in the first predicate destination register, and the higher-numbered elements in the second predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILELO {<Pd1>.<T>,<Pd2>.<T>},<Xn>,<Xm>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL*2) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL*2);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore =0to (elements*2)-1doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_LT=> cond = (element1 <  element2);whenCmp_LE=> cond = (element1 <= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 +1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL*2}(mask, result, esize);P{PL}(d0) = result[PL-1:0];P{PL}(d1) = result[PL*2-1:PL];",
    "operational_info": ""
  },
  {
    "title": "WHILELO (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILELO--predicate---While-incrementing-unsigned-scalar-lower-than-scalar-?lang=en",
    "description": "While incrementing unsigned scalar lower than scalar This instruction generates a predicate that starting from the lowest numbered element is true while the incrementing value of the first, unsigned scalar operand is lower than the second scalar operand and false thereafter up to the highest numbered element. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is incremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The predicate result is placed in the predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILELO<Pd>.<T>,<R><n>,<R><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore =0to elements-1doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_LT=> cond = (element1 <  element2);whenCmp_LE=> cond = (element1 <= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 +1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILELS (predicate as counter)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILELS--predicate-as-counter---While-incrementing-unsigned-scalar-lower-than-or-same-as-scalar--predicate-as-counter--?lang=en",
    "description": "While incrementing unsigned scalar lower than or same as scalar (predicate-as-counter) This instruction generates a predicate for a group of two or four vectors that starting from the lowest numbered element of the group is true while the incrementing value of the first, unsigned scalar operand is lower than or the same as the second scalar operand and false thereafter up to the highest numbered element of the group. If the second scalar operand is equal to the maximum unsigned integer value, then a condition that includes an equality test can never fail and the result will be an all-true predicate. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is incremented by one for each destination predicate element, irrespective of the predicate result element size. The predicate result is placed in the predicate destination register using the predicate-as-counter encoding. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILELS<PNd>.<T>,<Xn>,<Xm>,<vl>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = width * (VL DIV esize);varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;varcount : integer =0;letop2val : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore =0to elements-1doletop1val : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_LT=> cond = (op1val <  op2val);whenCmp_LE=> cond = (op1val <= op2val);\n    end;\n\n    last = last && cond;iflast then count = count +1; end;\n    operand1 = operand1 +1;\nend;\n\nresult =EncodePredCount{PL}(esize, elements, count, invert);\nPSTATE.[N,Z,C,V] =PredCountTest(elements, count, invert);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILELS (predicate pair)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILELS--predicate-pair---While-incrementing-unsigned-scalar-lower-than-or-same-as-scalar--pair-of-predicates--?lang=en",
    "description": "While incrementing unsigned scalar lower than or same as scalar (pair of predicates) This instruction generates a pair of predicates that starting from the lowest numbered element of the pair is true while the incrementing value of the first, unsigned scalar operand is lower than or the same as the second scalar operand and false thereafter up to the highest numbered element of the pair. If the second scalar operand is equal to the maximum unsigned integer value, then a condition that includes an equality test can never fail and the result will be an all-true predicate. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is incremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The lower-numbered elements are placed in the first predicate destination register, and the higher-numbered elements in the second predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILELS {<Pd1>.<T>,<Pd2>.<T>},<Xn>,<Xm>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL*2) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL*2);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore =0to (elements*2)-1doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_LT=> cond = (element1 <  element2);whenCmp_LE=> cond = (element1 <= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 +1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL*2}(mask, result, esize);P{PL}(d0) = result[PL-1:0];P{PL}(d1) = result[PL*2-1:PL];",
    "operational_info": ""
  },
  {
    "title": "WHILELS (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILELS--predicate---While-incrementing-unsigned-scalar-lower-than-or-same-as-scalar-?lang=en",
    "description": "While incrementing unsigned scalar lower than or same as scalar This instruction generates a predicate that starting from the lowest numbered element is true while the incrementing value of the first, unsigned scalar operand is lower than or the same as the second scalar operand and false thereafter up to the highest numbered element. If the second scalar operand is equal to the maximum unsigned integer value, then a condition that includes an equality test can never fail and the result will be an all-true predicate. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is incremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The predicate result is placed in the predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILELS<Pd>.<T>,<R><n>,<R><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore =0to elements-1doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_LT=> cond = (element1 <  element2);whenCmp_LE=> cond = (element1 <= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 +1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILELT (predicate as counter)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILELT--predicate-as-counter---While-incrementing-signed-scalar-less-than-scalar--predicate-as-counter--?lang=en",
    "description": "While incrementing signed scalar less than scalar (predicate-as-counter) This instruction generates a predicate for a group of two or four vectors that starting from the lowest numbered element of the group is true while the incrementing value of the first, signed scalar operand is less than the second scalar operand and false thereafter up to the highest numbered element of the group. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is incremented by one for each destination predicate element, irrespective of the predicate result element size. The predicate result is placed in the predicate destination register using the predicate-as-counter encoding. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILELT<PNd>.<T>,<Xn>,<Xm>,<vl>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "ifIsFeatureImplemented(FEAT_SVE2p1) thenCheckSVEEnabled();elseCheckStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = width * (VL DIV esize);varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;varcount : integer =0;letop2val : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore =0to elements-1doletop1val : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_LT=> cond = (op1val <  op2val);whenCmp_LE=> cond = (op1val <= op2val);\n    end;\n\n    last = last && cond;iflast then count = count +1; end;\n    operand1 = operand1 +1;\nend;\n\nresult =EncodePredCount{PL}(esize, elements, count, invert);\nPSTATE.[N,Z,C,V] =PredCountTest(elements, count, invert);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILELT (predicate pair)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILELT--predicate-pair---While-incrementing-signed-scalar-less-than-scalar--pair-of-predicates--?lang=en",
    "description": "While incrementing signed scalar less than scalar (pair of predicates) This instruction generates a pair of predicates that starting from the lowest numbered element of the pair is true while the incrementing value of the first, signed scalar operand is less than the second scalar operand and false thereafter up to the highest numbered element of the pair. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is incremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The lower-numbered elements are placed in the first predicate destination register, and the higher-numbered elements in the second predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILELT {<Pd1>.<T>,<Pd2>.<T>},<Xn>,<Xm>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL*2) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL*2);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore =0to (elements*2)-1doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_LT=> cond = (element1 <  element2);whenCmp_LE=> cond = (element1 <= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 +1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL*2}(mask, result, esize);P{PL}(d0) = result[PL-1:0];P{PL}(d1) = result[PL*2-1:PL];",
    "operational_info": ""
  },
  {
    "title": "WHILELT (predicate)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILELT--predicate---While-incrementing-signed-scalar-less-than-scalar-?lang=en",
    "description": "While incrementing signed scalar less than scalar This instruction generates a predicate that starting from the lowest numbered element is true while the incrementing value of the first, signed scalar operand is less than the second scalar operand and false thereafter up to the highest numbered element. The full width of the scalar operands is significant for the purposes of comparison, and the full width first operand is incremented by one for each destination predicate element, irrespective of the predicate result element size. The first general-purpose source register is not itself updated. The predicate result is placed in the predicate destination register. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILELT<Pd>.<T>,<R><n>,<R><m>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = Ones{};varoperand1 : bits(rsize) = X{}(n);letoperand2 : bits(rsize) = X{}(m);varresult : bits(PL);varlast : boolean = TRUE;letpsize : integer{} = esize DIV8;letelement2 : integer =ifunsigned thenUInt(operand2) elseSInt(operand2);fore =0to elements-1doletelement1 : integer =ifunsigned thenUInt(operand1) elseSInt(operand1);varcond : boolean;caseop ofwhenCmp_LT=> cond = (element1 <  element2);whenCmp_LE=> cond = (element1 <= element2);\n    end;\n\n    last = last && cond;letpbit : bit =iflast then'1'else'0';\n    result[e*:psize] = ZeroExtend{psize}(pbit);\n    operand1 = operand1 +1;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILERW",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILERW--While-free-of-read-after-write-conflicts-?lang=en",
    "description": "While free of read-after-write conflicts This instruction checks two addresses for a conflict or overlap between address ranges of the form [addr,addr+VL÷8), where VL is the accessible vector length in bits, that could result in a loop-carried dependency through memory due to the use of these addresses by contiguous load and store instructions within the same iteration of a loop. Generate a predicate whose elements are true while the addresses cannot conflict within the same iteration, and false thereafter. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILERW<Pd>.<T>,<Xn>,<Xm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = Ones{};letsrc1 : bits(64) = X{}(n);letsrc2 : bits(64) = X{}(m);letoperand1 : integer = UInt(src1);letoperand2 : integer = UInt(src2);varresult : bits(PL);letpsize : integer{} = esize DIV8;letdiff : integer = Abs(operand2 - operand1) DIVRM (esize DIV8);fore =0to elements-1doifdiff ==0|| e < diff then\n        result[e*:psize] = ZeroExtend{psize}('1');elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WHILEWR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WHILEWR--While-free-of-write-after-read-write-conflicts-?lang=en",
    "description": "While free of write-after-read/write conflicts This instruction checks two addresses for a conflict or overlap between address ranges of the form [addr,addr+VL÷8), where VL is the accessible vector length in bits, that could result in a loop-carried dependency through memory due to the use of these addresses by contiguous load and store instructions within the same iteration of a loop. Generate a predicate whose elements are true while the addresses cannot conflict within the same iteration, and false thereafter. This instruction sets the First (N), None (Z), and !Last (C) condition flags based on the predicate result, and sets the V flag to zero.",
    "syntax": [
      "WHILEWR<Pd>.<T>,<Xn>,<Xm>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letelements : integer = VL DIV esize;letmask : bits(PL) = Ones{};letsrc1 : bits(64) = X{}(n);letsrc2 : bits(64) = X{}(m);letoperand1 : integer = UInt(src1);letoperand2 : integer = UInt(src2);varresult : bits(PL);letpsize : integer{} = esize DIV8;letdiff : integer = (operand2 - operand1) DIVRM (esize DIV8);fore =0to elements-1doifdiff <=0|| e < diff then\n        result[e*:psize] = ZeroExtend{psize}('1');elseresult[e*:psize] = ZeroExtend{psize}('0');\n    end;\nend;\n\nPSTATE.[N,Z,C,V] =PredTest{PL}(mask, result, esize);P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "WRFFR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/WRFFR--Write-the-first-fault-register-?lang=en",
    "description": "Write the first-fault register This instruction reads the source predicate register, and writes the value in the register to the first-fault register (FFR). This instruction is intended to restore a saved FFR and is not recommended for general use by applications. This instruction requires that the source predicate contains a monotonic predicate value, in which starting from bit 0 there are zero or more 1 bits, followed only by 0 bits in any remaining bit positions. If the source is not a monotonic predicate value, then the resulting value in the FFR will beUNPREDICTABLE. It is not possible to generate a non-monotonic value in FFR when usingSETFFRfollowed by first-fault or non-fault loads. This instruction is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled.",
    "syntax": [
      "WRFFR<Pn>.B"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckNonStreamingSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letoperand : bits(PL) = P{}(n);lethsb : integer{} = HighestSetBit(operand);ifhsb <0|| IsOnes(operand[hsb:0]) thenFFR{PL}() = operand;else// not a monotonic predicateFFR{PL}() = ARBITRARY : bits(PL);\nend;",
    "operational_info": ""
  },
  {
    "title": "XAR",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/XAR--Bitwise-exclusive-OR-and-rotate-right-by-immediate-?lang=en",
    "description": "Bitwise exclusive-OR and rotate right by immediate This instruction performs a bitwise exclusive-OR between the corresponding elements of the first source vector and the second source vector, and then rotates each result element right by an immediate amount. The final results are destructively placed in the corresponding elements of the destination and first source vector. This instruction is unpredicated.",
    "syntax": [
      "XAR<Zdn>.<T>,<Zdn>.<T>,<Zm>.<T>, #<const>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand1 :bits(VL)= Z{}(dn);letoperand2 :bits(VL)= Z{}(m);varresult :bits(VL);fore =0to elements-1do\n    let element1 :bits(esize)= operand1[e*:esize];let element2 :bits(esize)= operand2[e*:esize];result[e*:esize] =ROR(element1XOR element2, rot);end;Z{VL}(dn) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT.\nThis instruction might be immediately preceded in program order by aMOVPRFXinstruction. TheMOVPRFXmust conform to all of the following requirements, otherwise the behavior of theMOVPRFXand this instruction isCONSTRAINED UNPREDICTABLE:\nTheMOVPRFXmust be unpredicated.TheMOVPRFXmust specify the same destination register as this instruction.The destination register must not refer to architectural register state referenced by any other source operand register of this instruction."
  },
  {
    "title": "ZIP1, ZIP2 (predicates)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ZIP1--ZIP2--predicates---Interleave-elements-from-two-half-predicates-?lang=en",
    "description": "Interleave elements from two half predicates This instruction interleaves alternating elements from the lowest or highest halves of the first and second source predicates, and places the values in the destination predicate. This instruction is unpredicated. It has encodings from 2 classes:High halvesandLow halves",
    "syntax": [
      "ZIP1<Pd>.<T>,<Pn>.<T>,<Pm>.<T>"
    ],
    "features": [
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letPL : integer{} = VL DIV8;letpairs : integer =VLDIV(esize *2);letoperand1 : bits(PL) = P{}(n);letoperand2 : bits(PL) = P{}(m);varresult : bits(PL);letbase: integer = part * pairs;forp =0to pairs-1doresult[(2*p+0)*:(esize DIV8)] = operand1[(base+p)*:(esize DIV8)];\n    result[(2*p+1)*:(esize DIV8)] = operand2[(base+p)*:(esize DIV8)];\nend;P{PL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "ZIP1, ZIP2 (vectors)",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ZIP1--ZIP2--vectors---Interleave-elements-from-two-half-vectors-?lang=en",
    "description": "Interleave elements from two half vectors This instruction interleaves alternating elements from the lowest or highest halves of the first and second source vectors, and places the values in the destination vector. This instruction is unpredicated. The 128-bit element variant requires that the Effective SVE vector length is at least 256 bits. ID_AA64ZFR0_EL1.F64MM indicates whether the 128-bit element variant is implemented. The 128-bit element variant is illegal when executed in Streaming SVE mode, unless FEAT_SME_FA64 is implemented and enabled. It has encodings from 4 classes:High halves,High halves (quadwords),Low halvesandLow halves (quadwords)",
    "syntax": [
      "ZIP1<Zd>.Q,<Zn>.Q,<Zm>.Q"
    ],
    "features": [
      "FEAT_F64MM",
      "FEAT_SME",
      "FEAT_SVE"
    ],
    "pseudocode": "ifesize <128thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled();\nend;letVL : integer{} =CurrentVL();ifVL < esize *2thenEndOfDecode(Decode_UNDEF); end;letpairs : integer =VLDIV(esize *2);letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL) = Zeros{};letbase: integer = part * pairs;forp =0to pairs-1doresult[(2*p+0)*:esize] = operand1[(base+p)*:esize];\n    result[(2*p+1)*:esize] = operand2[(base+p)*:esize];\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ZIPQ1",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ZIPQ1--Interleave-elements-from-low-halves-of-each-pair-of-quadword-vector-segments-?lang=en",
    "description": "Interleave elements from low halves of each pair of quadword vector segments This instruction interleaves alternating elements from low halves of the corresponding 128-bit vector segments of the first and second source vectors, and places them in the corresponding elements of the destination vector segment. This instruction is unpredicated.",
    "syntax": [
      "ZIPQ1<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letelements : integer =128DIV esize;letpairs : integer = elements DIV2;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fors =0to segments-1doletbase: integer = s * elements + part * pairs;forp =0to pairs-1doresult[(s * elements +2* p +0)*:esize] = operand1[(base+ p)*:esize];\n        result[(s * elements +2* p +1)*:esize] = operand2[(base+ p)*:esize];\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "ZIPQ2",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/ZIPQ2--Interleave-elements-from-high-halves-of-each-pair-of-quadword-vector-segments-?lang=en",
    "description": "Interleave elements from high halves of each pair of quadword vector segments This instruction interleaves alternating elements from high halves of the corresponding 128-bit vector segments of the first and second source vectors, and places them in the corresponding elements of the destination vector segment. This instruction is unpredicated.",
    "syntax": [
      "ZIPQ2<Zd>.<T>,<Zn>.<T>,<Zm>.<T>"
    ],
    "features": [
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckSVEEnabled();letVL : integer{} =CurrentVL();letsegments : integer = VL DIV128;letelements : integer =128DIV esize;letpairs : integer = elements DIV2;letoperand1 : bits(VL) = Z{}(n);letoperand2 : bits(VL) = Z{}(m);varresult : bits(VL);fors =0to segments-1doletbase: integer = s * elements + part * pairs;forp =0to pairs-1doresult[(s * elements +2* p +0)*:esize] = operand1[(base+ p)*:esize];\n        result[(s * elements +2* p +1)*:esize] = operand2[(base+ p)*:esize];\n    end;\nend;Z{VL}(d) = result;",
    "operational_info": "This instruction is a data-independent-time instruction as described inAbout PSTATE.DIT."
  },
  {
    "title": "",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/F1CVT--F2CVT--8-bit-floating-point-convert-to-half-precision-?lang=en",
    "description": "",
    "syntax": [
      "F2CVT<Zd>.H,<Zn>.B"
    ],
    "features": [
      "FEAT_FP8",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize) = operand[e*:esize];letres : bits(d_esize) =FP8ConvertFP(element[s_esize-1:0], issrc2, FPCR(), FPMR());\n    result[e*:esize] = ZeroExtend{esize}(res);\nend;Z{VL}(d) = result;",
    "operational_info": ""
  },
  {
    "title": "",
    "url": "https://developer.arm.com/documentation/ddi0602/2026-03/SVE-Instructions/F1CVTLT--F2CVTLT--8-bit-floating-point-convert-to-half-precision--top--?lang=en",
    "description": "",
    "syntax": [
      "F2CVTLT<Zd>.H,<Zn>.B"
    ],
    "features": [
      "FEAT_FP8",
      "FEAT_SME2",
      "FEAT_SVE2"
    ],
    "pseudocode": "CheckFPMREnabled();ifIsFeatureImplemented(FEAT_SME2) thenCheckSVEEnabled();elseCheckNonStreamingSVEEnabled(); end;letVL : integer{} =CurrentVL();letelements : integer = VL DIV esize;letoperand : bits(VL) = Z{}(n);varresult : bits(VL);fore =0to elements-1doletelement : bits(esize DIV2) = operand[(2*e +1)*:(esize DIV2)];\n    result[e*:esize] =FP8ConvertFP(element, issrc2, FPCR(), FPMR());\n\nend;Z{VL}(d) = result;",
    "operational_info": ""
  }
]